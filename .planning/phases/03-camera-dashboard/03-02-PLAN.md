---
phase: 03-camera-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "HA dashboard shows live camera feeds from all 3 active Frigate cameras (driveway, front_door, garage)"
    - "Recent detection events are displayed with snapshot thumbnails showing what was detected and when"
    - "Detection can be enabled or disabled per camera directly from the HA dashboard"
    - "Detection event history is browsable with the ability to filter by camera AND object type (person, car, package)"
    - "Camera card shows Detection Off badge overlay when detection is disabled for that camera"
    - "Dashboard works on mobile (HA Companion app) with single-column stacked layout"
  artifacts: []
  key_links:
    - from: "HA Cameras dashboard"
      to: "Frigate camera entities"
      via: "frigate-hass-card camera_entity references"
      pattern: "camera\\.(driveway|front_door|garage)"
    - from: "HA Cameras dashboard"
      to: "Frigate detect/motion switches"
      via: "entities card with switch toggles and conditional badge overlays"
      pattern: "switch\\..*(detect|motion)"
---

<objective>
Verify the camera dashboard end-to-end: live feeds, event browsing with filtering, detection controls, Detection Off badge overlays, and mobile layout. Fix any rendering issues discovered during verification.

Purpose: Confirm all dashboard requirements are met from both desktop and mobile perspectives.

Output: Verified working dashboard with any fixes applied. Phase 3 complete.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@modules/automation/home-assistant.nix

# Prior plan summary for what was deployed
@.planning/phases/03-camera-dashboard/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify dashboard rendering and fix issues</name>
  <files>modules/automation/home-assistant.nix</files>
  <action>
SSH to ser8 and verify the dashboard is working correctly. Check each requirement:

**Live camera feeds:**
- Access https://hass.shad-bangus.ts.net and navigate to the Cameras dashboard
- Verify all 3 cameras show live video streams (not still snapshots)
- If the card shows "Unknown card type", check HA logs (`journalctl -u home-assistant -n 50 --no-pager` on ser8) for resource loading errors. The card type may need to be `custom:advanced-camera-card` instead of `custom:frigate-card` -- update the `cameraCard` and event gallery card `type` fields in the Nix dashboard config if so, rebuild, and redeploy.

**Detection Off badge overlay:**
- Disable detection on one camera via the Detection Controls section
- Verify a red badge overlay appears on that camera's live feed card
- Re-enable detection and verify the badge disappears
- If the badge does not render, the `elements` configuration may need adjustment for the card version. Check advanced-camera-card docs for the correct `elements`/`conditions` syntax and update accordingly.

**Event filtering by camera and object type:**
- Click Events tab
- Verify the gallery card shows a filter bar with camera and object type (label) dropdowns
- Filter by a specific camera -- verify only that camera's events appear
- Filter by object type (e.g., "person") -- verify only person detection events appear
- If the filter bar does not appear, the `media_gallery.controls.filter` config may need adjustment. Check if `event_gallery` has its own filter config option and update.
- If Frigate has no recent events, trigger a test detection or check: `ssh bdhill@ser8 'curl -s http://127.0.0.1:5000/api/events?limit=5 | python3 -m json.tool | head -20'`

**Detection controls:**
- Verify the controls section shows toggle switches for each camera
- Toggle driveway detection off and back on to confirm it works
- Verify both object detection and motion detection switches are present per camera

**Layout:**
- If the 3-column grid does not display correctly, adjust the grid config in the dashboard Nix expression
- If cards are too small/large, adjust dimensions

For any issues found, fix the dashboard config in `modules/automation/home-assistant.nix`, rebuild with `make check`, and redeploy with `make switch-ser8`. Iterate until all requirements render correctly on desktop.
  </action>
  <verify>
1. 3 cameras streaming live video on Live Cameras tab
2. Detection Off badge overlay appears/disappears when toggling detection switch
3. Events tab filter bar allows filtering by camera and object type
4. Detection toggle switches functional for detect + motion per camera
  </verify>
  <done>Dashboard renders correctly on desktop with all requirements met: live feeds, Detection Off badge, event filtering, detection controls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of dashboard on desktop and mobile</name>
  <files></files>
  <action>
Present the completed dashboard for user verification on both desktop and mobile.

What was built:
- Live Cameras tab: 3-camera grid (driveway, front_door, garage) using frigate-hass-card with live video streams
- Detection Off badge: Red overlay badge appears on camera card when detection is disabled for that camera
- Events tab: Detection event gallery with filter bar supporting camera AND object type (person, car, package) filtering
- Detection Controls: Object detection + motion detection toggles per camera with master toggle
- Tabbed navigation between Live and Events views

Verification steps for user:

Desktop verification:
1. Open https://hass.shad-bangus.ts.net in browser
2. Click "Cameras" in the sidebar -- should open the camera dashboard
3. Verify Live Cameras tab shows 3 live video feeds in a grid layout
4. Verify cameras are in order: driveway, front_door, garage
5. Click/tap a camera feed -- should expand to fullscreen
6. Verify no bounding box overlays on live feeds (clean camera view)
7. Disable detection for one camera via Detection Controls -- verify red Detection Off badge appears on that camera card
8. Re-enable detection -- verify badge disappears
9. Scroll down to Detection Controls section -- verify 6 toggles (detect + motion x 3 cameras)
10. Click Events tab -- verify detection event thumbnails are visible
11. Use the filter bar to filter by a specific camera -- only that camera's events shown
12. Use the filter bar to filter by object type (person/car/package) -- only matching events shown
13. Click an event thumbnail -- should expand inline with larger snapshot and details

Mobile verification (HA Companion app):
14. Open HA Companion app on phone
15. Navigate to Cameras dashboard
16. Verify cameras stack vertically in single column (not squeezed 3-across)
17. Verify live streams load on mobile
18. Verify Events tab works on mobile

Type "approved" to complete Phase 3, or describe issues to fix.
  </action>
  <verify>User confirms all verification steps pass on both desktop and mobile.</verify>
  <done>User approves dashboard layout and functionality. Phase 3 complete.</done>
</task>

</tasks>

<verification>
All Phase 3 success criteria:
1. HA dashboard shows live camera feeds from all 3 active Frigate cameras (driveway, front_door, garage)
2. Recent detection events are displayed with snapshot thumbnails showing what was detected and when
3. Detection can be enabled or disabled per camera directly from the HA dashboard
4. Camera card shows Detection Off badge overlay when detection is disabled
5. Detection event history is browsable with the ability to filter by camera AND object type (person, car, package)
6. Dashboard works on mobile (HA Companion app) with single-column stacked layout
</verification>

<success_criteria>
- All 3 camera live feeds render correctly on both desktop and mobile
- Detection Off badge overlay visible when detection is disabled per camera
- Event gallery shows detection history with thumbnails, filterable by camera and object type
- Detection toggles are functional from the dashboard
- User has approved the dashboard layout and functionality
- Phase 3 and the entire Frigate-HA milestone is complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-camera-dashboard/03-02-SUMMARY.md`
</output>
