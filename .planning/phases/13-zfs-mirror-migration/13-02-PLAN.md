---
phase: 13-zfs-mirror-migration
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - hosts/ser8/disko-config.nix
  - hosts/ser8/configuration.nix
  - hosts/ser8/impermanence.nix
  - scripts/smoketests/media/test-zfs-media.sh
  - scripts/smoketests/media/all.sh
autonomous: false
requirements: [ZFS-02, ZFS-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "hosts/ser8/disko-config.nix declares zpool `media` as a two-disk mirror using exactly wwn-0x5000c500b56ea81a and wwn-0x5000c500b3733a87, with dataset `data` mounted at /mnt/media (D-01/D-02 Desired ZFS Configuration)."
    - "hosts/ser8/configuration.nix no longer declares fileSystems.\"/mnt/media\" as fuse.mergerfs, and boot.zfs.extraPools includes \"media\" alongside \"backup\"."
    - "mergerfs and mergerfs-tools are removed from environment.systemPackages."
    - "scripts/smoketests/media/test-zfs-media.sh exists and is dispatched from scripts/smoketests/media/all.sh, so `make smoketests-ser8` reaches it once the mirror is live."
    - "`make build-ser8` succeeds against the new declaration without activating it (Step 1.2 exit criteria: no live generation changed)."
  artifacts:
    - path: "hosts/ser8/disko-config.nix"
      provides: "media zpool mirror declaration + media-disk1/2 ZFS members replacing the ext4 declarations"
    - path: "hosts/ser8/configuration.nix"
      provides: "MergerFS fileSystems entry removed; boot.zfs.extraPools includes media; mergerfs packages removed"
    - path: "scripts/smoketests/media/test-zfs-media.sh"
      provides: "pool health, mirror-by-WWN, canonical directories, service access, import-write smoketest (D-19)"
  key_links:
    - from: "scripts/smoketests/media/all.sh"
      to: "scripts/smoketests/media/test-zfs-media.sh"
      via: "direct script invocation, same pattern as other media/all.sh checks"
      pattern: "test-zfs-media\\.sh"
    - from: "hosts/ser8/configuration.nix#boot.zfs.extraPools"
      to: "hosts/ser8/disko-config.nix#zpool.media"
      via: "extraPools must list every non-root pool disko declares, or the pool will not auto-import on boot"
      pattern: "extraPools.*media"
  prohibitions: []
---

<objective>
Implement the repository-side ZFS storage declaration on a feature branch (D-15), write the new pool-health smoketest (D-19), and validate the whole change builds and evaluates without activating it.

Purpose: The `media` zpool does not exist on disk until Plan 13-05's Step 4.4 — until then, `main` must keep describing a bootable ser8, so this work lives on `zfs-media-mirror` and merges only after the cutover switch succeeds (D-15). This plan is purely repository-writing; nothing on live ser8 changes.
Output: A feature branch with the ZFS pool/dataset declaration, MergerFS removal, and a new smoketest, validated by `make build-ser8` with zero warnings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-zfs-mirror-migration/13-CONTEXT.md
@.planning/phases/13-zfs-mirror-migration/13-PATTERNS.md
@.planning/phases/13-zfs-mirror-migration/13-01-SUMMARY.md
@.planning/SER8-ZFS-MIRROR-MIGRATION.md
@hosts/ser8/disko-config.nix
@hosts/ser8/configuration.nix
@hosts/ser8/impermanence.nix
@scripts/smoketests/ser8/test-zfs-health.sh
@scripts/smoketests/media/all.sh
@scripts/smoketests/lib/services.sh
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 1.1: Create the feature branch and implement the ZFS storage declaration</name>
  <read_first>hosts/ser8/disko-config.nix, hosts/ser8/configuration.nix, hosts/ser8/impermanence.nix, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Desired-ZFS-Configuration, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Repository-Changes</read_first>
  <reversibility rating="reversible">A repository declaration with no matching live pool yet; fully reversible by editing the branch further or deleting it before merge.</reversibility>
  <decision>Approve creating branch `zfs-media-mirror` and writing the disko/configuration/impermanence changes on it.</decision>
  <context>
    Mutation class: repository-writing only (D-15's branch-through-cutover strategy). Do not activate — the media pool does not exist on disk yet.
    Preserve the two approved WWNs exactly; do not touch `main`, `backup-disk*`, `rpool`, or `backup` beyond incidental nixfmt reformatting.
  </context>
  <options>
    <option id="approve"><name>Create branch and write the declaration</name><pros>Unblocks Step 1.2 validation and every subsequent plan.</pros><cons>None — no live system touched.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks the rest of the phase.</cons></option>
  </options>
  <action>
    After approval: `git checkout -b zfs-media-mirror` from `main`. In `hosts/ser8/disko-config.nix`: replace the `media-disk1`/`media-disk2` entries (currently ext4 partitions mounted at `/mnt/disk1`/`/mnt/disk2`) with ZFS-content partitions assigned to pool `media`, keeping `device = "/dev/disk/by-id/wwn-0x5000c500b56ea81a"` and `wwn-0x5000c500b3733a87"` exactly unchanged. Add a new `zpool.media` block modeled on the existing `zpool.backup` block (mode `"mirror"` instead of `"raidz2"`), with `options = { ashift = "12"; autotrim = "on"; }`, `rootFsOptions = { acltype = "posixacl"; compression = "lz4"; recordsize = "1M"; atime = "off"; xattr = "sa"; normalization = "formD"; dedup = "off"; mountpoint = "none"; canmount = "off"; }`, and one dataset `"data" = { type = "zfs_fs"; options = { mountpoint = "/mnt/media"; "com.sun:auto-snapshot" = "false"; }; }`. Add a D-23-style plain-language comment above the `media` block explaining: two-disk mirror for media libraries, single dataset for simplicity (not hardlink necessity — torrents are retired), no auto-snapshots (operator controls backup lifecycle), downloads now stage separately on NVMe before import.
    In `hosts/ser8/configuration.nix`: remove the `fileSystems."/mnt/media"` block (the `fuse.mergerfs` declaration, currently around lines 154-168) entirely. Change `extraPools = [ "backup" ];` to `extraPools = [ "backup" "media" ];`. Remove `mergerfs` and `mergerfs-tools` from `environment.systemPackages` (currently around lines 250-252), keeping `zfs`, `zfstools`, `sanoid`, and the other packages unchanged.
    In `hosts/ser8/impermanence.nix`: review the existing `/mnt/media` tmpfiles rules (lines 160-178) and confirm they do not conflict with a native ZFS mount (they should not — ZFS creates the mount automatically via `mountpoint`, and the `d /mnt/media/... media media -` rules only fix ownership/permissions after mount). Leave them in place unless evaluation surfaces a conflict; if so, document the conflict and resolution in the SUMMARY.md rather than silently deleting rules.
    Run `nixfmt-rfc-style` (via `make fmt`) on all three files before committing.
  </action>
  <verify>
    <automated>grep -A2 'zpool = {' hosts/ser8/disko-config.nix | grep -q media || grep -c '"media"' hosts/ser8/disko-config.nix</automated>
  </verify>
  <acceptance_criteria>
    - `hosts/ser8/disko-config.nix` contains a `media` zpool block with `mode = "mirror"` and both approved WWNs under `disk.media-disk1`/`disk.media-disk2` with `content.type = "zfs"` and `pool = "media"`.
    - `hosts/ser8/configuration.nix` no longer contains `fuse.mergerfs` or `mergerfs`/`mergerfs-tools` anywhere.
    - `hosts/ser8/configuration.nix` contains `extraPools = [ "backup" "media" ];` (or equivalent list containing both).
    - `git branch --show-current` on the working tree reports `zfs-media-mirror`.
  </acceptance_criteria>
  <resume-signal>Type "approved" to create the branch and write the storage declaration.</resume-signal>
  <done>The zfs-media-mirror branch declares the media pool/dataset and removes MergerFS, uncommitted changes are formatted cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Write scripts/smoketests/media/test-zfs-media.sh and wire it into media/all.sh (D-19)</name>
  <files>scripts/smoketests/media/test-zfs-media.sh, scripts/smoketests/media/all.sh</files>
  <read_first>scripts/smoketests/ser8/test-zfs-health.sh, scripts/smoketests/media/all.sh, scripts/smoketests/lib/services.sh, scripts/lib/all.sh</read_first>
  <behavior>
    - Test 1 (mount type): `mount | grep "/mnt/media.*type zfs"` matches -> pass; matches `fuse.mergerfs` or nothing -> fail.
    - Test 2 (pool online): `zpool status -x media` reports "pool 'media' is healthy" -> pass; anything else -> fail.
    - Test 3 (mirror membership): `zpool status media` output contains both `wwn-0x5000c500b56ea81a` and `wwn-0x5000c500b3733a87` -> pass; missing either -> fail.
    - Test 4 (canonical directories): `movies`, `tv`, `books`, `music` all exist under `/mnt/media` -> pass; any missing -> fail.
    - Test 5 (service access): the `media` group can read `/mnt/media/movies` and `/mnt/media/tv` -> pass; permission denied -> fail.
    - Test 6 (import-write, D-22): a test file written under `/mnt/media` lands with uid 1100 (media user) and is removable -> pass; wrong ownership or write failure -> fail.
  </behavior>
  <action>
    Create `scripts/smoketests/media/test-zfs-media.sh` following the `test-zfs-health.sh` structure exactly: shebang + SPDX header, `set -euo pipefail`, source `./scripts/lib/all.sh`, `title "$0"`, usage check (`$1` = host), resolve `ipaddr`/`user` via `get_ip`/`get_user`, define `run_test`/`remote` helpers matching `test-zfs-health.sh:43-66` verbatim, then implement the six tests from the behavior spec above as `test_*` functions using `remote zpool status -x media`, `remote zpool status media`, `remote mount`, `remote test -d "$MEDIA_MOUNT/$dir"`, and a remote-executed write/stat/rm sequence for the import-write test (write via `remote sh -c 'echo test > /mnt/media/downloads/... && stat -c%u ... && rm ...'` or equivalent single remote invocation since `remote()` only returns stdout of one command). Declare `APPROVED_WWN_1="wwn-0x5000c500b56ea81a"` and `APPROVED_WWN_2="wwn-0x5000c500b3733a87"` as constants at the top. End with the standard `tests_run`/`tests_passed` summary block matching `test-zfs-health.sh:164-192`, exiting non-zero if any test failed. Run `shellcheck` and `shfmt -d` on the new file and fix every warning.
    Then edit `scripts/smoketests/media/all.sh`: add a dispatch block near the end (after the existing Bazarr-access check, before the final `pass "All media services smoketests passed"` line) that runs `./scripts/smoketests/media/test-zfs-media.sh "$host"` and treats a non-zero exit as a suite failure (`fail "ZFS media storage smoketest failed"; exit 1`), matching the file's existing inline `info`/`pass`/`fail`/`exit 1` idiom (no `run_suite` fanout — this file does not use it).
  </action>
  <verify>
    <automated>shellcheck scripts/smoketests/media/test-zfs-media.sh && shfmt -d scripts/smoketests/media/test-zfs-media.sh scripts/smoketests/media/all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoketests/media/test-zfs-media.sh` exists, is executable, and `shellcheck`/`shfmt -d` report zero issues.
    - `scripts/smoketests/media/all.sh` contains a call to `./scripts/smoketests/media/test-zfs-media.sh "$host"` that exits the suite non-zero on failure.
    - The script's WWN constants exactly match `wwn-0x5000c500b56ea81a` and `wwn-0x5000c500b3733a87`.
    - No `hardlink` test remains anywhere in the new script (D-22 replaces it with the import-write test).
  </acceptance_criteria>
  <done>test-zfs-media.sh implements all six checks from the behavior spec, passes shellcheck/shfmt, and is reachable from `make smoketests-ser8` via media/all.sh.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 1.2: Validate the repository change without activation</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-1.2, Makefile</read_first>
  <decision>Approve running formatting, static analysis, and a focused ser8 build to validate the branch — never `make test-ser8` or `make switch-ser8` at this stage.</decision>
  <context>
    Mutation class: build and evaluation only. The media pool does not exist on disk; activating now would try to import a nonexistent pool. This step proves the declaration is syntactically and structurally sound before any live work in Plan 13-03 onward.
  </context>
  <options>
    <option id="approve"><name>Run validation</name><pros>Catches evaluation/build errors on the cheapest possible loop, before days of staging work.</pros><cons>None.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks Plan 13-03.</cons></option>
  </options>
  <action>
    After approval, inside `make dev`: `make fmt`; `statix check`; `make build-ser8`; `shellcheck scripts/smoketests/media/*.sh`. Treat every warning from every tool as a failure to resolve before proceeding, per repository convention. Do not run `make test-ser8`, `make dry-activate-ser8`, or `make switch-ser8` — those activate the configuration, which is out of scope until Plan 13-05's Step 4.5. Commit the validated branch (`git add hosts/ser8/disko-config.nix hosts/ser8/configuration.nix hosts/ser8/impermanence.nix scripts/smoketests/media/test-zfs-media.sh scripts/smoketests/media/all.sh && git commit`).
  </action>
  <verify>
    <automated>make build-ser8</automated>
  </verify>
  <acceptance_criteria>
    - `make fmt` produces no diff.
    - `statix check` reports zero warnings.
    - `make build-ser8` succeeds (exit 0) and does not touch the live generation.
    - `shellcheck` on the new smoketest reports zero issues.
    - The branch is committed with all five modified/new files.
  </acceptance_criteria>
  <resume-signal>Type "approved" to run Step 1.2 validation.</resume-signal>
  <done>The zfs-media-mirror branch builds cleanly with zero warnings; no live ser8 generation has changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Feature branch -> main | Main must always describe a bootable ser8; the branch is the only place a not-yet-real pool can be declared |
| disko declaration -> live disk erase (Plan 13-05) | This plan's declared WWNs are the exact targets Plan 13-05 will erase — a typo here propagates to a destructive command |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | disko-config.nix WWN transcription error | critical | mitigate | Acceptance criteria requires exact WWN string match; Plan 13-05's Step 4.1 re-resolves and re-confirms both WWNs live before any mutation, independent of this declaration |
| T-13-06 | Elevation of Privilege | `make test-ser8`/`make switch-ser8` run prematurely against a nonexistent pool | high | mitigate | Task 3 action explicitly forbids running either command in this plan; only `make build-ser8` (evaluation only) runs here |
| T-13-07 | Repudiation | Feature branch accidentally merged or pushed to main before cutover | high | mitigate | D-15 discipline: merge only happens in Plan 13-06 after Step 4.5's switch succeeds; this plan does not merge |
</threat_model>

<verification>
`make build-ser8` exits 0 on the branch. `git log` shows the branch's commits are NOT present on `main` (no premature merge). The new smoketest passes `shellcheck`/`shfmt -d`.
</verification>

<success_criteria>
The `zfs-media-mirror` branch declares the media pool/dataset, removes MergerFS, and ships a new pool-health smoketest — all validated by a clean `make build-ser8`, with zero live ser8 state changed.
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback)

ZFS-02 and ZFS-04 were classified `unclassified` by the automated edge-case probe. Both are fully pinned by CONTEXT.md's Desired ZFS Configuration table and D-19/D-22 (smoketest scope, import-write test replacing the hardlink test) — implemented as explicit acceptance criteria above rather than derived edge cases. Reviewed and accepted as covered.
</flagged_assumptions>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification as pre-existing):

- `zfs-media-mirror` (new git branch)
- `scripts/smoketests/media/test-zfs-media.sh` (new file)
- `zpool.media` / `media/data` (new disko declaration; not yet live until Plan 13-05)

<output>
Create `.planning/phases/13-zfs-mirror-migration/13-02-SUMMARY.md` when done, documenting this plan's rollback-matrix row (unchanged live state — Rollback Matrix's "Before Step 3.1" row still applies) and the branch name for later plans to reference.
</output>
