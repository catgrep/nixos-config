---
phase: 10-household-foundation-and-mealie
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/google-tasks-takeout.md
autonomous: false
requirements: [IMP-01]
user_setup:
  - service: google-takeout
    why: "Phase 13's Google Tasks import (IMP-02, IMP-03) depends on the real archive; the export is asynchronous and takes hours to days, so it must be requested at the very start of Phase 10"
    dashboard_config:
      - task: "Deselect all products, select Tasks only, choose a one-time export delivered as a download link, then request it"
        location: "takeout.google.com, signed in as the household Google account that owns the Tasks lists"

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Google Takeout export limited to the Tasks product has been requested from the household account that owns the task lists, and the request date is recorded (IMP-01)."
    - ".planning/research/google-tasks-takeout.md exists and carries the eight-point inspection checklist that plan 10-08 fills in against the delivered archive (IMP-01)."
    - "No import code is written in this phase — the artifact records structure and counts only (IMP-01)."
  artifacts:
    - .planning/research/google-tasks-takeout.md
  key_links:
    - ".planning/research/google-tasks-takeout.md -> plan 10-08 inspection -> Phase 13 IMP-02/IMP-03 importer design"
  prohibitions:
    - statement: "Must NOT commit the Google Takeout archive, or any household member's personal task titles, notes, or contents, into the repository or into .planning/ artifacts."
      status: active
      verification: flagged-unverified
---

<objective>
Get the Google Takeout Tasks export requested on day one of the phase, and stand up the artifact that plan 10-08 will fill in when the archive arrives.

Purpose: IMP-01 exists because the Takeout envelope is undocumented and Phase 13's importer cannot be designed against a guess. The export is asynchronous — Google takes hours to days — so the request has to go out before any of the Mealie work starts, or the archive misses the phase window and Phase 13 blocks on it.

Output: a requested export, and `.planning/research/google-tasks-takeout.md` holding the request metadata plus the inspection checklist, with every finding column still empty.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-household-foundation-and-mealie/10-CONTEXT.md
@.planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md
</context>

<flagged_assumptions>
| Requirement | Probe | Why unresolved |
|---|---|---|
| IMP-01 | unclassified — review manually | The requirement is pure fact-finding: its whole purpose is to replace assumptions with observed structure. There is no input-domain edge to resolve at request time, and every structural edge (empty lists, missing recurrence keys, parent-before-child ordering) becomes answerable only once the real archive is in hand — which is plan 10-08's job. |

Research assumptions this plan deliberately does not resolve (RESEARCH.md Assumptions Log A1, A2): the nested `{ items: [ { ..., items: [task] } ] }` envelope shape, and the claim that Takeout exports no usable recurrence data. Both are hypotheses to falsify in plan 10-08, not facts to build on. A2 is the single highest-value finding for Phase 13 scoping — if recurrence data *is* present, Phase 13's "hand-author recurring chores" work is unnecessary.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Request the Google Takeout Tasks export</name>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-CONTEXT.md (the Specific Ideas note that the export request happens at phase start because it is asynchronous and takes hours to days)
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (the "Google Takeout Tasks Archive (IMP-01)" section and the environment note that a Google account with Tasks data is a user action with no fallback)
    - .planning/REQUIREMENTS.md (IMP-01, and IMP-02/IMP-03 which consume the result in Phase 13)
  </read_first>
  <action>
Halt and hand control to the human: this is a blocking checkpoint. The full directive is in <instructions> and the operator steps are in <how-to-verify>. Run no command against Google and capture no credential.
  </action>
  <verify>
    <human-check>The human reports the export submitted with its date and the visible task-list count. See <verification> for why no automated check exists.</human-check>
  </verify>
  <instructions>
Halt and hand control to the human. Present the steps in the how-to-verify block below, wait for the resume signal, and record the submission date, the account name used, and the visible task-list count into the plan working notes so Task 2 can write them into the artifact. Record no credential, cookie, or token of any kind.
  </instructions>
  <verification>
    <human-check>The human confirms the export was submitted and reports the submission date and the visible task-list count. There is no automated check: requesting a Takeout export needs an authenticated browser session against a personal Google account, and no CLI, API, or service-account path reaches another person's Tasks data. This is one of the two manual-only verifications 10-VALIDATION.md records for this phase.</human-check>
  </verification>
  <acceptance_criteria>
    - the human has reported the export as submitted, with an explicit submission date
    - the Google account name used is recorded (name only, never a credential)
    - the count of task lists visible in Google Tasks at request time is recorded, so plan 10-08 can detect a partial export
    - the export was scoped to the Tasks product only, not a full Takeout
    - no credential, cookie, or OAuth token was captured, scripted, or written anywhere
  </acceptance_criteria>
  <what-built>Nothing automated. Requesting a Google Takeout export requires an authenticated session in a browser against the household's own Google account. There is no CLI, no API, and no service-account path that reaches another person's Tasks data, which is precisely why this is a human action rather than an unwritten script.</what-built>
  <how-to-verify>
    1. Sign in at `takeout.google.com` as the household Google account that owns the Tasks lists (the account whose chores Phase 13 will import — not a secondary account).
    2. Click "Deselect all", then select **Tasks** only. Do not export other products: a full Takeout is large, slow, and drags unrelated personal data into the household's download folder.
    3. Choose a **one-time** export, delivery method "Send download link by email", file type `.zip`, and the default size split.
    4. Click "Create export" and record the exact date and time the request was submitted.
    5. Note the account used and the number of task lists visible in Google Tasks at request time — plan 10-08 compares that count against the archive to catch a partial export.
    6. Do NOT wait for delivery. The export takes hours to days; the rest of Phase 10 proceeds in parallel and plan 10-08 picks the archive up at the end of the phase.
  </how-to-verify>
  <resume-signal>Type "requested" plus the submission date, or describe what blocked the request</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Stand up the Takeout inspection artifact</name>
  <files>.planning/research/google-tasks-takeout.md</files>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (the "Google Takeout Tasks Archive (IMP-01)" section: the expected-shape hypothesis, the recurrence-gap claim, and the eight-point inspection checklist that this artifact must carry verbatim)
    - .planning/research/SUMMARY.md (for the surrounding document conventions in `.planning/research/`)
    - CLAUDE.md (Secrets and Safety: never commit personal or credential material)
  </read_first>
  <action>
Create `.planning/research/google-tasks-takeout.md` as the durable home for the IMP-01 finding, structured so plan 10-08 fills it in rather than inventing a format under time pressure.

Open with a status block recording: the request date captured in Task 1, the Google account identifier used (account *name* only, never a password or token), the count of task lists visible at request time, the delivery method, and a `Status: requested — archive not yet delivered` line that plan 10-08 flips.

Carry a "Hypothesis to falsify" section stating plainly that everything below the status block is currently unverified: the expected nested envelope of an array of lists each holding a nested array of tasks, the expected task fields mirroring the Google Tasks API `Task` resource (`kind`, `id`, `etag`, `title`, `updated`, `selfLink`, `parent`, `position`, `notes`, `status`, `due`, `completed`, `deleted`, `hidden`, `links`), and the claim that Takeout carries no usable recurrence information. Label the section explicitly as assumption, not fact, so a Phase 13 reader cannot mistake it for a finding.

Carry the eight-point inspection checklist as an unchecked task list, one item per line, quoting RESEARCH.md's wording: exact top-level key names and nesting quoted from the real file; the full field list on a single task quoted verbatim; whether any recurrence key appears on a task known to repeat; how many entries carry a completed status and how far back completion timestamps go; whether deleted or hidden entries are present; whether parent/position ordering ever places children before parents; whether `due` is date-only anchored at UTC midnight (and therefore shifts back a day when rendered in America/Los_Angeles); and the archive's SHA-256 plus where the archive is stored.

Close with a "Data handling" section stating the rule this artifact must obey: record structure, field names, and counts only. The archive itself is household personal data and is never committed to this repository; no task title, note body, or list name from the real archive appears in this file.

Add a "Phase 13 handoff" section naming which of IMP-02 and IMP-03 each checklist item feeds, so the findings arrive with their consumers attached.

Every sentence on its own physical line, per the repository's Markdown convention. No em dashes.
  </action>
  <verify>
    <automated>test -f .planning/research/google-tasks-takeout.md && grep -c '^- \[ \]' .planning/research/google-tasks-takeout.md && grep -q 'Status: requested' .planning/research/google-tasks-takeout.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/google-tasks-takeout.md` exists
    - `grep -c '^- \[ \]' .planning/research/google-tasks-takeout.md` returns at least 8 (one unchecked item per inspection point)
    - `grep -c 'Status: requested' .planning/research/google-tasks-takeout.md` returns 1
    - the file contains a heading matching `Hypothesis` and a heading matching `Data handling`
    - `grep -ci 'recurrence' .planning/research/google-tasks-takeout.md` returns at least 1
    - `grep -c 'SHA-256\|sha256' .planning/research/google-tasks-takeout.md` returns at least 1
    - `rg -c '\.zip$|Takeout.*\.zip' --glob '!.planning/**' .` finds no archive committed anywhere in the repository
    - `git status --porcelain` shows no file larger than 64 KiB staged by this task
    - no em dash character appears in the file
  </acceptance_criteria>
  <done>The artifact exists with all eight inspection points unchecked, the hypothesis clearly labelled as unverified, the request metadata recorded, and the data-handling rule stated. Plan 10-08 has a form to fill rather than a format to invent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google account -> Takeout archive -> local disk | Household personal data leaves Google's custody and lands on a developer machine |
| local archive -> repository / .planning artifacts | The point at which personal data could accidentally become version-controlled and permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Information Disclosure | Google Takeout archive contents | high | mitigate | The artifact records structure, field names, and counts only. The archive is stored outside the repository, its location and SHA-256 are noted rather than its contents, and Task 2's acceptance criteria grep for a committed archive and for oversized staged files |
| T-10-11 | Information Disclosure | Google account credentials during the export request | medium | mitigate | The request is a human browser action; no credential, cookie, or OAuth token is captured, scripted, or recorded. Only the account name is written down |
| T-10-12 | Tampering | A partial or wrong-account export silently accepted | medium | mitigate | Task 1 records the visible task-list count at request time so plan 10-08 can detect an archive that is missing lists or came from the wrong account |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan (RESEARCH.md Package Legitimacy Audit: not applicable to this phase, zero `[SLOP]`, zero `[SUS]`) |
</threat_model>

<artifacts_this_phase_produces>
**Files:** `.planning/research/google-tasks-takeout.md`

**External state created outside this repository:** one pending Google Takeout export job on the household Google account, delivered as a download link by email. This job is runtime state in Google's systems, not repository state; if the phase is abandoned, the delivered archive should be deleted from local disk rather than left lying around.

No code, no NixOS options, no systemd units, no CLI flags. IMP-01 is deliberately fact-finding only: no import code is written in this phase.
</artifacts_this_phase_produces>

<verification>
- The export request has been submitted and its date recorded
- `.planning/research/google-tasks-takeout.md` exists with eight unchecked inspection points
- The hypothesis section is explicitly labelled as unverified assumption
- No archive, task title, note body, or list name from real household data appears anywhere in the repository
</verification>

<success_criteria>
The long-lead dependency is in flight on day one, and the artifact that receives its findings exists with a fixed shape. Phase 13 will inherit observed structure rather than the community-tooling guess RESEARCH.md currently carries.
</success_criteria>

<output>
Create `.planning/phases/10-household-foundation-and-mealie/10-02-SUMMARY.md` when done.
</output>
