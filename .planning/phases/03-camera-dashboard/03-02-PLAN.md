---
phase: 03-camera-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "HA dashboard shows live camera feeds from all 3 active Frigate cameras (driveway, front_door, garage)"
    - "Recent detection events are displayed with snapshot thumbnails showing what was detected and when"
    - "Detection can be enabled or disabled per camera directly from the HA dashboard"
    - "Detection event history is browsable with the ability to filter by camera or object type"
    - "Dashboard works on mobile (HA Companion app) with single-column stacked layout"
  artifacts: []
  key_links:
    - from: "HA Cameras dashboard"
      to: "Frigate camera entities"
      via: "advanced-camera-card camera_entity references"
      pattern: "camera\\.(driveway|front_door|garage)"
    - from: "HA Cameras dashboard"
      to: "Frigate detect/motion switches"
      via: "entities card with switch toggles"
      pattern: "switch\\..*(detect|motion)"
---

<objective>
Verify the camera dashboard end-to-end: live feeds, event browsing, detection controls, and mobile layout. Fix any rendering issues discovered during verification.

Purpose: Confirm all 4 dashboard requirements (DASH-01 through DASH-04) are met from both desktop and mobile perspectives.

Output: Verified working dashboard with any fixes applied. Phase 3 complete.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@modules/automation/home-assistant.nix

# Prior plan summary for what was deployed
@.planning/phases/03-camera-dashboard/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify dashboard rendering and fix issues</name>
  <files>modules/automation/home-assistant.nix</files>
  <action>
SSH to ser8 and verify the dashboard is working correctly. Check each requirement:

**DASH-01: Live camera feeds**
- Access https://hass.shad-bangus.ts.net and navigate to the Cameras dashboard
- Verify all 3 cameras show live video streams (not still snapshots)
- If cameras show "Unknown card type: custom:advanced-camera-card", the resource is not registered:
  1. Check `ls -la /var/lib/hass/www/` for the card JS file
  2. Check HA logs: `ssh bdhill@ser8 'journalctl -u home-assistant -n 50 --no-pager'`
  3. If needed, add resource manually via HA UI: Settings -> Dashboards -> Resources -> `/local/nixos-lovelace-modules/advanced-camera-card.js` type Module
  4. Or update the dashboard YAML with correct resource path

**DASH-02: Detection events with thumbnails**
- Click Events tab
- Verify event galleries show recent detection snapshots with camera name and timestamp
- If galleries are empty, check that Frigate has recent events: `ssh bdhill@ser8 'curl -s http://127.0.0.1:5000/api/events?limit=5 | python3 -m json.tool | head -20'`

**DASH-03: Detection controls**
- Verify the controls section shows toggle switches for each camera
- Toggle driveway detection off -- verify the switch state changes
- Toggle it back on
- Verify both object detection and motion detection switches are present per camera
- If the "master toggle" (show_header_toggle) does not work as expected, consider replacing with a script entity or template switch that controls all cameras at once

**DASH-04: Event history browsing**
- Click through event galleries to verify thumbnails are viewable
- Verify clicking a thumbnail expands it inline (advanced-camera-card's gallery viewer)
- Verify events from multiple cameras are browsable

**Layout fixes:**
- If the 3-column grid does not display correctly, adjust the grid config
- If `panel` view type only shows one card, switch to default view type in the dashboard YAML
- If cards are too small/large, adjust advanced-camera-card dimensions

For any issues found, fix the dashboard YAML in `modules/automation/home-assistant.nix`, rebuild with `make check`, and redeploy with `make switch-ser8`.

Iterate until all 4 dashboard requirements render correctly on desktop.
  </action>
  <verify>
All 4 requirements verified via browser:
1. Live feeds: 3 cameras streaming video
2. Events: Thumbnail grid with detection snapshots
3. Controls: Toggle switches functional for detect + motion per camera
4. History: Event galleries browsable with inline expand
  </verify>
  <done>Dashboard renders correctly on desktop with all requirements met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of dashboard on desktop and mobile</name>
  <files></files>
  <action>
Present the completed dashboard for user verification on both desktop and mobile.

What was built:
- Live Cameras tab: 3-camera grid (driveway, front_door, garage) using advanced-camera-card with live video streams
- Events tab: Detection event galleries with snapshot thumbnails per camera
- Detection Controls: Object detection + motion detection toggles per camera with master toggle
- Tabbed navigation between Live and Events views

Verification steps for user:

Desktop verification:
1. Open https://hass.shad-bangus.ts.net in browser
2. Click "Cameras" in the sidebar -- should open the camera dashboard
3. Verify Live Cameras tab shows 3 live video feeds in a grid layout
4. Verify cameras are in order: driveway, front_door, garage
5. Click/tap a camera feed -- should expand to fullscreen
6. Verify no bounding box overlays on live feeds (clean camera view)
7. Scroll down to Detection Controls section -- verify 6 toggles (detect + motion x 3 cameras)
8. Toggle one switch off and back on to confirm it works
9. Click Events tab -- verify detection event thumbnails are visible
10. Click an event thumbnail -- should expand inline with larger snapshot and details

Mobile verification (HA Companion app):
11. Open HA Companion app on phone
12. Navigate to Cameras dashboard
13. Verify cameras stack vertically in single column (not squeezed 3-across)
14. Verify live streams load on mobile
15. Verify Events tab works on mobile

Type "approved" to complete Phase 3, or describe issues to fix.
  </action>
  <verify>User confirms all verification steps pass on both desktop and mobile.</verify>
  <done>User approves dashboard layout and functionality. Phase 3 complete.</done>
</task>

</tasks>

<verification>
All Phase 3 success criteria from ROADMAP.md:
1. HA dashboard shows live camera feeds from all 3 active Frigate cameras (driveway, front_door, garage) -- DASH-01
2. Recent detection events are displayed with snapshot thumbnails showing what was detected and when -- DASH-02
3. Detection can be enabled or disabled per camera directly from the HA dashboard -- DASH-03
4. Detection event history is browsable with the ability to filter by camera or object type -- DASH-04
</verification>

<success_criteria>
- All 3 camera live feeds render correctly on both desktop and mobile
- Event galleries show detection history with thumbnails
- Detection toggles are functional from the dashboard
- User has approved the dashboard layout and functionality
- Phase 3 and the entire Frigate-HA milestone is complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-camera-dashboard/03-02-SUMMARY.md`
</output>
