---
phase: 12-fleet-repair
plan: 05
type: execute
wave: 3
depends_on: ["12-04"]
files_modified:
  - .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json
  - .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json
  - .planning/phases/12-fleet-repair/evidence/radarr-root-folder-cleanup.md
  - .planning/phases/12-fleet-repair/evidence/download-client-deregistration.md
autonomous: true
requirements: [FLEET-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full movie + moviefile Radarr API snapshot is exported before any root-folder change and diffed after; the diff asserts zero lost movie-file records (D-14)."
  artifacts:
    - .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json
    - .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json
  key_links:
    - "Radarr's three bogus root folders under /mnt/media/downloads/usenet/complete/movies -> individually re-homed movie records -> /mnt/media/movies canonical root -> zero-diff verification against the before-snapshot"
    - "plan 12-04's orchestration.nix/switch-ser8 removal of setup_qbittorrent_client -> Radarr/Sonarr/Prowlarr download-client de-registration in this plan (D-16), so the next activation cannot silently re-add the entry this plan removes"
  prohibitions:
    - statement: "Must never call Radarr's root-folder or movie-file removal/move API with deleteFiles=true when removing bogus root folders or re-homing movie records — zero files may be deleted (D-15)."
      status: unverified
      flagged: true
    - statement: "Must not remove a bogus root folder via the API before every movie record registered under it has been investigated and re-homed — removing the root first would orphan those records."
      status: unverified
      flagged: true
---

<objective>
Clean Radarr's root folders down to the single canonical /mnt/media/movies via API with zero media
loss (D-14/D-15), and remove the now-dead "qBittorrent" download-client registrations from Radarr,
Sonarr, and Prowlarr (D-16) now that plan 12-04 has switched the code that would otherwise silently
re-add them.

Purpose: FLEET-04 requires Radarr to report exactly one root folder with every previously registered
movie file still present; D-16 keeps the arr stack API-clean as a direct consequence of the FLEET-01
retirement.
Output: Radarr with a single /mnt/media/movies root folder; before/after movie snapshots proving zero
loss; Radarr, Sonarr, and Prowlarr with no qBittorrent download-client entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-fleet-repair/12-CONTEXT.md
@.planning/SER8-ZFS-MIRROR-MIGRATION.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Snapshot, investigate bogus root folders, and re-home affected movie records</name>
  <files>.planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json, .planning/phases/12-fleet-repair/evidence/radarr-root-folder-cleanup.md</files>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md</read_first>
  <action>
    Resolve Radarr's API key path without decrypting it locally: `nix eval .#nixosConfigurations.ser8.config.sops.secrets.radarr_api_key.path --raw`, then fetch the live value over SSH only when needed for a curl call (never echo/print the key itself in any output written to a file).
    Export the full movie list and movie-file list from Radarr's API (`GET /api/v3/movie` and, if Radarr's version exposes it separately, `GET /api/v3/moviefile`) and save the raw JSON response to .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json — this is the "before" snapshot D-14 requires.
    List Radarr's current root folders (`GET /api/v3/rootfolder`) and identify the three bogus roots under `/mnt/media/downloads/usenet/complete/movies` (per .planning/SER8-ZFS-MIRROR-MIGRATION.md's Known Blockers section) alongside the correct `/mnt/media/movies` root.
    For every movie record in the before-snapshot whose path falls under one of the three bogus roots, investigate individually (per D-15): if the corresponding file already exists at the canonical `/mnt/media/movies` path, re-point that movie's `path`/`rootFolderPath` via `PUT /api/v3/movie/{id}` to the canonical location without moving the file; otherwise, trigger a Radarr-managed file move to `/mnt/media/movies` (e.g. via the movie editor's move-files option or `POST /api/v3/command` with a rename/move command) BEFORE removing the bogus root. Never pass `deleteFiles=true` to any Radarr API call in this task.
    Once every record under a bogus root is confirmed re-homed, remove that root folder via `DELETE /api/v3/rootfolder/{id}` for all three bogus roots — the correct `/mnt/media/movies` root is left untouched.
    Record every investigated record, its resolution (re-pointed vs moved), and the root-folder removal calls in .planning/phases/12-fleet-repair/evidence/radarr-root-folder-cleanup.md.
  </action>
  <verify>
    <automated>test -s .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json && test -f .planning/phases/12-fleet-repair/evidence/radarr-root-folder-cleanup.md</automated>
  </verify>
  <done>The before-snapshot is saved; every movie record under a bogus root is re-homed to /mnt/media/movies with zero deletion; all three bogus root folders are removed via API; only /mnt/media/movies remains.</done>
  <acceptance_criteria>
    - .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json is non-empty valid JSON.
    - .planning/phases/12-fleet-repair/evidence/radarr-root-folder-cleanup.md lists every record found under a bogus root and its re-homing resolution.
    - A live `GET /api/v3/rootfolder` call against Radarr returns exactly one root folder, `/mnt/media/movies`.
    - No command run in this task included `deleteFiles=true` or `deleteFiles: true` (grep the evidence file for the literal to confirm none were logged).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Post-cleanup snapshot and zero-loss diff verification (D-14)</name>
  <precondition>Task 1's bogus-root removal is complete and /mnt/media/movies is the only remaining root folder.</precondition>
  <files>.planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json</files>
  <read_first>.planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json</read_first>
  <action>
    Export the same movie + moviefile snapshot from Radarr's API again and save it to .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json.
    Diff the before and after snapshots by movie ID (or TMDB ID, whichever is stable across the re-home) and assert: the total movie count is unchanged, every movie ID present before is still present after, and every movie's `hasFile`/file-presence flag that was `true` before is still `true` after (paths may differ for the re-homed records, but no record may have flipped from having a file to not having one).
    Write the diff result (counts before/after, any discrepancies found, and the explicit zero-loss conclusion) into .planning/phases/12-fleet-repair/evidence/radarr-root-folder-cleanup.md as a closing section.
  </action>
  <verify>
    <automated>test -s .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json && python3 -c "
import json
b=json.load(open('.planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json'))
a=json.load(open('.planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json'))
bl = b if isinstance(b, list) else b.get('records', b)
al = a if isinstance(a, list) else a.get('records', a)
assert len(bl) == len(al), f'count mismatch: {len(bl)} vs {len(al)}'
print('OK', len(bl), len(al))
"</automated>
  </verify>
  <done>The after-snapshot is saved, the diff confirms zero lost movie/moviefile records against the before-snapshot, and the conclusion is recorded in the evidence file.</done>
  <acceptance_criteria>
    - .planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json is non-empty valid JSON.
    - The movie count in the after-snapshot equals the movie count in the before-snapshot.
    - Every movie ID/TMDB ID present in the before-snapshot is present in the after-snapshot.
    - radarr-root-folder-cleanup.md's closing section states the zero-loss conclusion explicitly with the before/after counts.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove dead qBittorrent download-client entries from Radarr, Sonarr, and Prowlarr (D-16)</name>
  <precondition>plan 12-04's orchestration.nix removal is switched live on ser8 (setup_qbittorrent_client no longer runs on activation), so this removal will not be silently re-added on the next boot.</precondition>
  <files>.planning/phases/12-fleet-repair/evidence/download-client-deregistration.md</files>
  <read_first>.planning/phases/12-fleet-repair/12-CONTEXT.md</read_first>
  <action>
    Resolve each app's API key path the same way as Task 1 (`nix eval .#nixosConfigurations.ser8.config.sops.secrets.&lt;app&gt;_api_key.path --raw`), never printing decrypted key values in any output.
    For Radarr: `GET /api/v3/downloadclient`, identify the entry named "qBittorrent" (`implementation: "QBittorrent"`), and `DELETE /api/v3/downloadclient/{id}`.
    For Sonarr: same pattern against its `/api/v3/downloadclient` endpoint.
    For Prowlarr: Prowlarr does not register qBittorrent as a download client directly (only Radarr/Sonarr do), but confirm via `GET /api/v1/applications` (or the appropriate Prowlarr endpoint) whether any stale application/indexer-proxy entry references the removed qBittorrent stack, and remove it if found; if none exists, record that explicitly as a valid "nothing to remove" outcome rather than forcing an unnecessary API call.
    After each deletion, re-query the respective app's download-client (or application) list and confirm no entry named "qBittorrent" remains.
    Record every app checked, what was found, and what was removed (or the explicit "not present" finding) in .planning/phases/12-fleet-repair/evidence/download-client-deregistration.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-fleet-repair/evidence/download-client-deregistration.md && grep -qi "radarr" .planning/phases/12-fleet-repair/evidence/download-client-deregistration.md && grep -qi "sonarr" .planning/phases/12-fleet-repair/evidence/download-client-deregistration.md && grep -qi "prowlarr" .planning/phases/12-fleet-repair/evidence/download-client-deregistration.md</automated>
  </verify>
  <done>Radarr and Sonarr no longer list a qBittorrent download client; Prowlarr is confirmed to have no stale reference; the outcome for all three apps is recorded.</done>
  <acceptance_criteria>
    - A live `GET /api/v3/downloadclient` call against Radarr returns no entry with `implementation: "QBittorrent"`.
    - A live `GET /api/v3/downloadclient` call against Sonarr returns no entry with `implementation: "QBittorrent"`.
    - .planning/phases/12-fleet-repair/evidence/download-client-deregistration.md documents the finding for all three apps (Radarr, Sonarr, Prowlarr), including a valid "not present" outcome for Prowlarr if that is what was found.
    - No decrypted API key value appears in the evidence file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This plan's API calls -> live Radarr/Sonarr/Prowlarr state | Root-folder removal and movie re-homing operate directly on production media metadata and, indirectly, on-disk files via Radarr-managed moves. |
| API key resolution -> evidence files | Resolving and using API keys must never leak the decrypted value into a committed evidence file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-13 | Tampering / Data Loss | `deleteFiles=true` passed to a Radarr API call | critical | mitigate | Explicit prohibition in this plan's frontmatter; every API call in Task 1 is reviewed against this before execution; verified by the zero-loss diff in Task 2. |
| T-12-14 | Tampering / Data Loss | Removing a bogus root folder before its movie records are re-homed, orphaning them | high | mitigate | Task 1's action explicitly sequences re-home-then-remove-root; Task 2's diff is the independent check. |
| T-12-15 | Information Disclosure | Decrypted Radarr/Sonarr/Prowlarr API keys or their values written into evidence files | medium | mitigate | API keys resolved via `nix eval ... .path` (path only) and used transiently in curl calls; evidence files record findings, never key material. |
</threat_model>

<verification>
- Before/after Radarr movie snapshots are saved and diffed with zero record loss.
- Radarr reports exactly one root folder (`/mnt/media/movies`).
- No qBittorrent download-client entry remains in Radarr or Sonarr; Prowlarr's status is confirmed either way.
</verification>

<success_criteria>
Radarr reports `/mnt/media/movies` as its only root folder with every previously registered movie
file still present, and the arr stack carries no dead qBittorrent download-client registration —
satisfying ROADMAP Phase 12 success criterion 4.
</success_criteria>

## Artifacts this phase produces (this plan)

- New: `.planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-before.json`.
- New: `.planning/phases/12-fleet-repair/evidence/radarr-movie-snapshot-after.json`.
- New: `.planning/phases/12-fleet-repair/evidence/radarr-root-folder-cleanup.md`.
- New: `.planning/phases/12-fleet-repair/evidence/download-client-deregistration.md`.
- Live-only (Radarr): three bogus root folders removed via API; movie records re-homed to `/mnt/media/movies`.
- Live-only (Radarr, Sonarr): qBittorrent download-client registration removed via API.

<output>
Create `.planning/phases/12-fleet-repair/12-05-SUMMARY.md` when done
</output>
