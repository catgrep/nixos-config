---
phase: 12-fleet-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/common/users.nix
  - .planning/PROJECT.md
  - .planning/phases/12-fleet-repair/evidence/identity-audit.md
  - .planning/phases/12-fleet-repair/evidence/ownership-audit.md
autonomous: true
requirements: [FLEET-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "uid 1002 and gid 992 are exclusive to the media user/group: no other user or group declared in modules/common/users.nix, and no other account in live ser8 /etc/passwd or /etc/group, claims uid 1002 or gid 992 (D-07)."
    - "A zero-mismatch scoped-audit result is a valid, successful outcome: if the targeted find under /mnt/media and media-service state directories returns no paths owned outside 1002:992, no chown runs and the empty result is recorded as evidence, not treated as a failure to fix (D-09)."
    - "The scoped ownership audit (D-09) runs only after modules/common/users.nix's uid 1002 / gid 992 declaration has been activated on ser8 via `make switch-ser8`, so the audit compares live ownership against the declared 1002/992 target, never against the stale 1100/1100 declaration."
  artifacts:
    - modules/common/users.nix
    - .planning/phases/12-fleet-repair/evidence/identity-audit.md
    - .planning/phases/12-fleet-repair/evidence/ownership-audit.md
  key_links:
    - "modules/common/users.nix groups.media.gid and users.users.media.uid -> every modules/media/*.nix service that forces group = \"media\" (sonarr, radarr, bazarr, sabnzbd, nzbget, jellyfin) -> live ser8 /etc/passwd + /etc/group via mutableUsers = false activation"
  prohibitions:
    - statement: "Must not perform a blind recursive re-chown of the media tree (~7.8 TB) or any full-tree census — only individually reviewed, mismatched paths surfaced by the scoped find in D-09 may be chowned."
      status: unverified
      flagged: true
    - statement: "The D-10 identity census across jellyfin, sonarr, radarr, bazarr, sabnzbd, and nzbget is discovery-only in this phase — must not change any of those services' declared uid/gid values here (sabnzbd's static uid/gid pin is explicitly authorized separately by D-12 in plan 12-02, not by this plan); census findings are recorded for Phase 15's Nixflix adapter, not acted on now."
      status: unverified
      flagged: true
---

<objective>
Reconcile the repo's declared media user/group identity (uid 1100 / gid 1100) to ser8's actual
live identity (uid 1002 / gid 992) per D-07/D-08, so the ~7.8 TB media tree never needs a mass
re-chown, then run the extended identity census (D-10) and the scoped ownership audit (D-09),
recording the reconciliation decision in PROJECT.md Key Decisions per D-06.

Purpose: FLEET-03 requires the repo's media identity declaration to match live ser8 reality with
no blind recursive re-chown, and this decision blocks the Phase 13 storage freeze plus feeds
Phase 15's Nixflix adapter (which must force live identities).
Output: modules/common/users.nix declaring uid 1002 / gid 992; an identity census evidence file;
a scoped ownership audit evidence file; a new PROJECT.md Key Decisions row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fleet-repair/12-CONTEXT.md
@modules/common/users.nix
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Declare live media identity (uid 1002 / gid 992) and activate on ser8</name>
  <reversibility rating="costly">D-07: moving to any other uid/gid later would require exactly the mass re-chown across ~7.8 TB this decision is chosen to avoid.</reversibility>
  <files>modules/common/users.nix</files>
  <read_first>modules/common/users.nix, .planning/phases/12-fleet-repair/12-PATTERNS.md</read_first>
  <action>
    Before editing, run a collision pre-flight: `ssh bdhill@192.168.68.65 'getent passwd 1002; getent group 992'` and confirm the only match for each is the media account/group (or that they are currently unclaimed by any *other* live account), and `grep -rn "uid = 1002\|gid = 992" modules/ hosts/ users/` in the repo to confirm no other declared user or group already claims either id.
    In modules/common/users.nix, change `groups.media.gid` from 1100 to 992, and change `users.users.media.uid` from 1100 to 1002 (per D-07). Do not touch `groups.jellyfin.gid` (1101) or any other declaration — this task is scoped to the media user/group only.
    Run `make dry-activate-ser8` and confirm it exits 0.
    Run `make test-ser8` (temporary activation, safe rollback on reboot per the safety ladder) and confirm via `ssh bdhill@192.168.68.65 'id media'` that uid is now 1002 and gid is now 992.
    Spot-check that existing media files remain readable under the new mapping: `ssh bdhill@192.168.68.65 'stat -c "%u:%g" /mnt/media/movies'` should already show `1002:992` (this is the pre-existing live drift D-07 adopts, not a change this task causes).
    Run `make switch-ser8` to persist the change as the boot default.
  </action>
  <verify>
    <automated>make dry-activate-ser8 exits 0; ssh bdhill@192.168.68.65 'id media' reports uid=1002 and gid=992 after switch-ser8</automated>
  </verify>
  <done>modules/common/users.nix declares uid 1002 / gid 992 for the media user/group, ser8 is switched onto the new generation, and live `id media` confirms the change with no other account colliding on either id.</done>
  <acceptance_criteria>
    - modules/common/users.nix contains `gid = 992;` under `groups.media` and `uid = 1002;` under `users.users.media`.
    - `groups.jellyfin.gid` is unchanged at 1101.
    - `ssh bdhill@192.168.68.65 'getent passwd 1002'` returns exactly one line and it is the media account.
    - `ssh bdhill@192.168.68.65 'getent group 992'` returns exactly one line and it is the media group.
    - `make dry-activate-ser8` exits 0 both before and after the switch.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extended identity census across media-adjacent service accounts (D-10)</name>
  <files>.planning/phases/12-fleet-repair/evidence/identity-audit.md</files>
  <read_first>modules/media/sonarr.nix, modules/media/radarr.nix, modules/media/bazarr.nix, modules/media/sabnzbd.nix, modules/media/nzbget.nix, modules/media/jellyfin.nix</read_first>
  <action>
    Over SSH to ser8 (bdhill@192.168.68.65), for each of jellyfin, sonarr, radarr, bazarr, sabnzbd, and nzbget: run `getent passwd &lt;name&gt;` and `getent group &lt;name&gt;` (and `getent group media` for the shared group) to record each account's live uid, primary group, and gid.
    Cross-reference each live value against what modules/media/*.nix and modules/common/users.nix declare for that account today (most declare `isSystemUser = true` with no explicit uid, meaning NixOS auto-allocates the uid — record the current allocation as observed).
    Write the findings to .planning/phases/12-fleet-repair/evidence/identity-audit.md as a table: service | declared uid/gid (or "auto-allocated") | live uid/gid | drift observed (yes/no) | note.
    This task is discovery-only: per the prohibition recorded in this plan's frontmatter, do not change any of these services' declared identity in this task — sabnzbd's own static uid/gid pin is implemented separately in plan 12-02 (D-12), and any other drift found here is recorded for Phase 15's Nixflix adapter, not acted on now.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-fleet-repair/evidence/identity-audit.md && grep -c '|' .planning/phases/12-fleet-repair/evidence/identity-audit.md | grep -qv '^0$'</automated>
  </verify>
  <done>identity-audit.md exists with one row per censused service (jellyfin, sonarr, radarr, bazarr, sabnzbd, nzbget) recording declared-vs-live uid/gid and whether drift was observed.</done>
  <acceptance_criteria>
    - .planning/phases/12-fleet-repair/evidence/identity-audit.md exists and contains exactly 6 service rows (jellyfin, sonarr, radarr, bazarr, sabnzbd, nzbget).
    - No file under modules/media/ or modules/common/users.nix was modified by this task (`git diff --stat` shows no changes to those paths for this task's commit).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scoped ownership audit, targeted chown, and PROJECT.md decision record (D-09/D-06)</name>
  <files>.planning/phases/12-fleet-repair/evidence/ownership-audit.md, .planning/PROJECT.md</files>
  <read_first>.planning/PROJECT.md, .planning/phases/12-fleet-repair/12-CONTEXT.md</read_first>
  <action>
    Run a scoped, non-recursive-blind find over ser8 for paths NOT owned by uid 1002 / gid 992: `ssh bdhill@192.168.68.65 'find /mnt/media /var/lib/jellyfin /var/lib/sonarr /var/lib/radarr /var/lib/bazarr /var/lib/sabnzbd /var/lib/nzbget -xdev \( ! -uid 1002 -o ! -gid 992 \) -print'` (note: some of these dirs use their own service-specific uid rather than media's, so treat this as a starting candidate list, not an unconditional violation list — cross-reference each hit against the service's own declared identity from Task 2's census before deciding it is a real mismatch).
    Write every candidate path and its resolution (chown / not-a-mismatch / deferred) to .planning/phases/12-fleet-repair/evidence/ownership-audit.md.
    For paths confirmed as genuine mismatches (owned by neither 1002:992 nor the owning service's own legitimate identity), chown only those specific reviewed paths: `ssh bdhill@192.168.68.65 'sudo chown 1002:992 &lt;exact-path&gt;'` — never a blanket `chown -R` across /mnt/media or any of the state directories.
    If the audit finds zero mismatches, record that explicitly in ownership-audit.md as the successful outcome (per this plan's must_haves.truths) — do not force a chown run to "do something."
    Add a new row to .planning/PROJECT.md's Key Decisions table (after the last existing row) recording: repo now declares uid 1002 / gid 992 for the media user/group (replacing 1100/1100), the ~7.8 TB media tree was never re-chowned, and the identity audit extended to jellyfin/sonarr/radarr/bazarr/sabnzbd/nzbget with findings feeding Phase 15's Nixflix adapter (D-06/D-07/D-08/D-09/D-10).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-fleet-repair/evidence/ownership-audit.md && grep -c "FLEET-03" .planning/PROJECT.md</automated>
  </verify>
  <done>ownership-audit.md records every candidate path and its disposition (including a valid empty/zero-mismatch outcome); any confirmed mismatches were chowned individually; PROJECT.md's Key Decisions table has a new row for the FLEET-03 identity reconciliation.</done>
  <acceptance_criteria>
    - .planning/phases/12-fleet-repair/evidence/ownership-audit.md exists and either lists zero mismatches explicitly or lists each mismatched path with its chown disposition.
    - No chown command in this task's history used a `-R` flag or a wildcard/glob path — every chown target is an exact, individually-reviewed path from the audit.
    - .planning/PROJECT.md contains a new Key Decisions row referencing FLEET-03 and citing D-07 (uid 1002 / gid 992).
    - `ssh bdhill@192.168.68.65 'find /mnt/media -xdev ! -uid 1002 -o ! -gid 992 -print -quit'` (re-run after any chowns) returns no unresolved path that wasn't already accounted for in ownership-audit.md.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> ser8 (SSH, root-equivalent via sudo) | Identity declaration changes and chown commands execute with root privileges on a live production host over SSH. |
| Nix declaration -> live /etc/passwd,/etc/group | `mutableUsers = false` means activation reconciles live accounts to the Nix declaration automatically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | modules/common/users.nix uid/gid reassignment | high | mitigate | Pre-flight `getent passwd 1002`/`getent group 992` collision check before editing (Task 1); confirmed exclusive to media before switch-ser8. |
| T-12-02 | Elevation of Privilege | Scoped chown (D-09) | medium | mitigate | Chown restricted to individually reviewed exact paths from the audit list only; no `-R`, no glob, no full-tree pass (Task 3). |
| T-12-03 | Repudiation | Ownership/identity changes with no audit trail | low | mitigate | Every finding and every chown decision recorded in evidence files (identity-audit.md, ownership-audit.md) and the PROJECT.md decision row. |
</threat_model>

<verification>
- `make dry-activate-ser8` exits 0 after the users.nix edit.
- `ssh bdhill@192.168.68.65 'id media'` reports uid=1002, gid=992 after `make switch-ser8`.
- identity-audit.md and ownership-audit.md both exist with complete findings.
- PROJECT.md Key Decisions table has the new FLEET-03 row.
</verification>

<success_criteria>
Live ser8 media user/group identities match the repo's declaration (uid 1002 / gid 992) with no blind
recursive re-chown performed, the extended identity census is recorded, and the reconciliation approach
is recorded in PROJECT.md Key Decisions — satisfying ROADMAP Phase 12 success criterion 3.
</success_criteria>

## Artifacts this phase produces (this plan)

- Modified: `modules/common/users.nix` (uid 1100->1002, gid 1100->992 for the media user/group; `groups.jellyfin.gid` unchanged).
- New: `.planning/phases/12-fleet-repair/evidence/identity-audit.md` (D-10 census).
- New: `.planning/phases/12-fleet-repair/evidence/ownership-audit.md` (D-09 audit).
- New: one row in `.planning/PROJECT.md`'s Key Decisions table (FLEET-03 / D-06/D-07/D-08/D-09/D-10).

<output>
Create `.planning/phases/12-fleet-repair/12-01-SUMMARY.md` when done
</output>
