---
phase: 11-homebox-actual-budget-and-donetick
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - scripts/smoketests/household/all.sh
  - scripts/smoketests/household/test-actual-service.sh
  - scripts/smoketests/household/test-actual-endpoint.sh
autonomous: false
requirements: [ACT-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Exactly one non-deleted row exists in account.sqlite's files table, and its encrypt_keyid is NULL -- one budget file created, end-to-end encryption explicitly declined."
    - "If a second budget-creation attempt races the first (adjacency/concurrency edge), the acceptance check requires exactly one non-deleted row, not tolerating a duplicate as a pass."
    - "If no budget file was created at all (empty-input edge), the check fails rather than treating absence as a pass."
  artifacts:
    - scripts/smoketests/household/test-actual-service.sh
    - scripts/smoketests/household/test-actual-endpoint.sh
  key_links:
    - "account.sqlite files.encrypt_keyid -> ACT-02's 'e2e encryption explicitly declined' proof"
  prohibitions:
    - statement: "Must NOT create the initial budget file with end-to-end encryption enabled, which would silently reverse the REQUIREMENTS.md Out-of-Scope decision ('Actual end-to-end encryption... conflicts with restore requirement')."
      status: active
      verification: flagged-unverified
    - statement: "Must NOT script or reverse-engineer Actual's binary sync/protobuf protocol to fabricate a budget file programmatically -- the human-action checkpoint exists precisely because this decision belongs to a household member, not to the agent."
      status: active
      verification: flagged-unverified
---

<objective>
Close ACT-02: a household member creates Actual's one budget file through the web UI (the only practical path -- the sync protocol has no simple scriptable equivalent), explicitly declining end-to-end encryption, and this plan proves it happened by querying `account.sqlite` directly rather than trusting the report alone.

Purpose: this is the second half of ACT-02, split from 11-02 specifically because a `checkpoint:human-action` task must never share a plan with autonomous implementation tasks (Split Signals: "Checkpoint + implementation in same plan").

Output: one household decision recorded in `account.sqlite`, verified by SQL, plus two new household smoketest scripts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/11-homebox-actual-budget-and-donetick/11-02-SUMMARY.md
</context>

<flagged_assumptions>
| Requirement | Probe | Why unresolved |
|---|---|---|
| ACT-02 | ordering -- "When elements compare equal, is output order specified and stable?" | Does not meaningfully apply to a single budget-file existence check; the `files` table has exactly one non-deleted row and ordering across rows is never asserted. Flagged rather than silently dropped. |
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create the one budget file and decline end-to-end encryption</name>
  <action>Create Actual's first budget file through the web UI and decline end-to-end encryption when prompted.</action>
  <instructions>
    Actual is reachable at https://actual.shad-bangus.ts.net with the server password set in plan 11-02. Creating a budget file and choosing whether to encrypt it happens entirely client-side through Actual's sync protocol (packages/sync-server/src/app-sync), which has no simple scriptable equivalent without implementing that binary protocol -- the supported path is the web UI's "Start Fresh" first-budget wizard, which is also the right place for a household member to make this financial-data decision themselves rather than have an agent make it silently on their behalf.

    1. Open `https://actual.shad-bangus.ts.net` in a browser and log in with the password from plan 11-02.
    2. Choose "Start Fresh" (or equivalent "create a new budget") from the first-run screen -- do not import a demo/sample budget.
    3. When prompted about end-to-end encryption, decline it (leave it unencrypted) -- per REQUIREMENTS.md Out of Scope: "Actual end-to-end encryption... conflicts with restore requirement."
    4. Confirm the budget loads and the household name/title looks correct (household naming is fully up to you; nothing here is Nix-managed).
  </instructions>
  <verification>Task 2 queries account.sqlite directly and requires exactly one non-deleted row with a NULL encrypt_keyid.</verification>
  <resume-signal>Type "created" once the budget file exists and you declined encryption, or describe what happened if the wizard behaved differently.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify the budget file by direct SQL query, add household smoketests</name>
  <files>scripts/smoketests/household/all.sh, scripts/smoketests/household/test-actual-service.sh, scripts/smoketests/household/test-actual-endpoint.sh</files>
  <read_first>
    - scripts/smoketests/household/test-mealie-service.sh (state-directory and persist-dir checks to mirror)
    - scripts/smoketests/household/test-mealie-endpoint.sh (local-endpoint and tsnet checks to mirror)
    - scripts/smoketests/household/all.sh (the `TESTS` array to extend)
  </read_first>
  <action>
On ser8, query `account.sqlite` directly to prove ACT-02 rather than trusting Task 1's report alone: `sudo sqlite3 /var/lib/actual/server-files/account.sqlite "SELECT count(*), group_concat(encrypt_keyid) FROM files WHERE deleted=0"`. This must return exactly one row with a NULL/empty `encrypt_keyid` -- one budget file, unencrypted. If the count is 0, the checkpoint's report was wrong or the wizard did not complete; do not treat that as a pass. If the count is more than 1, a duplicate was created; do not treat that as a pass either.

Create `scripts/smoketests/household/test-actual-service.sh` on `test-mealie-service.sh`'s shape: unit-active check (`actual.service`), port-listening check (3000), no-startup-error journal check, state-directory-shape check asserting `/var/lib/actual` is a real directory owned `actual:actual` mode 0700 plus its `/persist` counterpart, and the `files` table count/encrypt_keyid check above (guarded the same way Mealie's `MEALIE_ALLOW_UNSEEDED` guards a pre-bootstrap run -- name the env var `ACTUAL_ALLOW_UNSEEDED`, unset in every committed file, so a future re-run of this smoketest before a budget exists reports informationally rather than failing the whole suite).

Create `scripts/smoketests/household/test-actual-endpoint.sh` on `test-mealie-endpoint.sh`'s shape: local `/account/needs-bootstrap` check returning `bootstrapped:true`, a tsnet-DNS check for `actual.shad-bangus.ts.net`, and a tsnet-HTTPS check.

Add both new scripts to the `TESTS` array in `scripts/smoketests/household/all.sh`, after the Homebox entries.
  </action>
  <verify>
    <automated>./scripts/smoketests/household/test-actual-service.sh ser8 && ./scripts/smoketests/household/test-actual-endpoint.sh ser8</automated>
  </verify>
  <acceptance_criteria>
    - `sudo sqlite3 /var/lib/actual/server-files/account.sqlite "SELECT count(*) FROM files WHERE deleted=0"` on ser8 returns `1`, not `0` and not `>1`
    - `sudo sqlite3 /var/lib/actual/server-files/account.sqlite "SELECT encrypt_keyid FROM files WHERE deleted=0"` returns an empty string, not a value
    - `./scripts/smoketests/household/test-actual-service.sh ser8` exits 0
    - `./scripts/smoketests/household/test-actual-endpoint.sh ser8` exits 0
    - `rg -c 'test-actual' scripts/smoketests/household/all.sh` returns 2
  </acceptance_criteria>
  <done>Exactly one unencrypted budget file is proven to exist by direct SQLite query -- not by trusting the human-action report alone -- and both new household smoketest scripts pass and are wired into the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Actual client-side sync protocol | The budget file's encryption choice is made entirely client-side; the server only stores whatever the client uploads |
| account.sqlite | Holds every budget file's encryption metadata -- the single point of truth for this plan's proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Information Disclosure | Household budget-file encryption choice silently reversed | high | mitigate | Task 2 queries `account.sqlite.files.encrypt_keyid` directly rather than trusting the human-action checkpoint's self-report |
| T-11-10 | Repudiation | Duplicate or missing budget file from a mis-clicked wizard | medium | mitigate | Task 2's acceptance criteria require exactly one non-deleted row, failing on both zero and more-than-one |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan |
</threat_model>

<artifacts_this_phase_produces>
**Directories:** none new

**Files:** `scripts/smoketests/household/test-actual-service.sh`, `scripts/smoketests/household/test-actual-endpoint.sh`

**Runtime state newly created (not Nix-managed):** exactly one unencrypted budget file in `account.sqlite`

**Existing symbols extended:** `scripts/smoketests/household/all.sh` gains two `TESTS` entries
</artifacts_this_phase_produces>

<verification>
- `account.sqlite` shows exactly one non-deleted, non-encrypted budget file
- `./scripts/smoketests/household/test-actual-service.sh ser8` and `./scripts/smoketests/household/test-actual-endpoint.sh ser8` both exit 0
</verification>

<success_criteria>
ACT-02 is complete: server password set (11-02), exactly one budget file created with end-to-end encryption explicitly declined, verified by direct SQL query rather than by report.
</success_criteria>

<output>
Create `.planning/phases/11-homebox-actual-budget-and-donetick/11-03-SUMMARY.md` when done.
</output>
