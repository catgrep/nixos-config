---
phase: 11-homebox-actual-budget-and-donetick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/household/default.nix
  - modules/household/homebox.nix
  - hosts/ser8/household/default.nix
  - hosts/ser8/household/homebox.nix
  - hosts/ser8/impermanence.nix
  - modules/gateway/Caddyfile
  - scripts/validation/test-homebox-module.sh
  - Makefile
  - scripts/smoketests/household/all.sh
  - scripts/smoketests/household/test-homebox-service.sh
  - scripts/smoketests/household/test-homebox-endpoint.sh
autonomous: true
requirements: [HBX-01, HBX-02, HBX-03]

user_setup:
  - service: sops (secrets/ser8.yaml)
    why: "Homebox's HBOX_AUTH_API_KEY_PEPPER must exist before the homebox unit can start; the sandboxed executor cannot decrypt secrets/ser8.yaml (confirmed blocked in Phase 9 -- STATE.md 09-01), so a human with the admin_bobby GPG key or the ser8 host age key must add the value."
    env_vars:
      - name: HBOX_AUTH_API_KEY_PEPPER
        source: "Generated by Task 1 via `openssl rand -base64 48`, printed once to the terminal; paste it into `make sops-edit-ser8` under the key `homebox_auth_api_key_pepper`."
    dashboard_config: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both household members (jordan, sawnia) have active Homebox accounts in the same group, verified by GET /api/v1/groups/members returning exactly 2 members from either account's token."
    - "Self-registration without a valid group invite token is rejected once HBX-02's bootstrap is complete: POST /api/v1/users/register with no groupToken returns 403, not 204."
    - "Homebox loads at https://homebox.shad-bangus.ts.net through firebat's Caddy tsnet vhost (curl from firebat returns an accepted status)."
    - "HBOX_OPTIONS_ALLOW_ANALYTICS and HBOX_DEMO are both the string \"false\" in the deployed unit environment."
    - "/var/lib/homebox is a real directory owned homebox:homebox, and /persist/var/lib/homebox exists with matching ownership (HBX-01)."
    - "If the tsnet vhost or DNS is unreachable, the endpoint smoketest fails on a non-accepted status rather than reporting a false pass on empty curl output."
    - statement: "Hostname comparisons in the offline gate and smoketests use exact ASCII string equality against the literal homebox.shad-bangus.ts.net value, not case-insensitive or Unicode-normalized matching."
      verification: backstop
  artifacts:
    - modules/household/homebox.nix
    - hosts/ser8/household/homebox.nix
    - scripts/validation/test-homebox-module.sh
    - scripts/smoketests/household/test-homebox-service.sh
    - scripts/smoketests/household/test-homebox-endpoint.sh
  key_links:
    - "hosts/ser8/household/homebox.nix sops.templates.\"homebox.env\" -> systemd.services.homebox.serviceConfig.EnvironmentFile"
    - "modules/gateway/Caddyfile bind tailscale/homebox -> reverse_proxy 192.168.68.65:7745"
    - "modules/household/default.nix and hosts/ser8/household/default.nix imports -> ./homebox.nix"
  prohibitions:
    - statement: "Must NOT commit, log to a tracked file, or print into any .planning artifact any household member's Homebox password, JWT/API token, or the HBOX_AUTH_API_KEY_PEPPER value itself -- only the generated pepper is shown once in terminal output for the human to paste into sops."
      status: active
      verification: flagged-unverified
    - statement: "Must NOT leave HBOX_DEMO enabled on the deployed instance -- it seeds a publicly-documented demo@example.com account and is a household-facing service, not a scratch instance."
      status: active
      verification: flagged-unverified
    - statement: "SEC-02 (no secret for any household service in the Nix store) governs this plan's sops usage; SEC-02 itself is Deferred (2026-08-20 descope) and is not re-verified here beyond using sops.templates rather than a literal Nix string."
      status: active
      verification: flagged-unverified
---

<objective>
Bring Homebox up on ser8 as the first of the three Phase 11 apps, on the exact `modules/household/` + `hosts/ser8/household/` two-layer pattern Mealie proved in Phase 10: reusable module, host policy, impermanence persistence, sops-backed secret, firebat Caddy tsnet vhost, both household members bootstrapped into one shared group, and self-registration closed afterward.

Purpose: HBX-01/02/03 ask for exactly the Mealie shape, three more times, starting with Homebox since it needs no new packaging (nixpkgs 26.05 ships `pkgs.homebox` at 0.25.0 natively) and its systemd unit is already static-user by design (no DynamicUser override needed, unlike Mealie's PD-01).

Output: two new module files, one new host-policy file, a new sops secret + template, impermanence entries, a Caddy vhost, an offline eval gate, and two new household smoketest scripts -- with both household members having live, group-shared accounts and registration closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/10-household-foundation-and-mealie/10-01-SUMMARY.md
</context>

<flagged_assumptions>
Spec-less edge-probe rows with no clean numeric/boundary edge to check. Surfaced, not dropped.

| Requirement | Probe | Why unresolved |
|---|---|---|
| HBX-01 | unclassified -- review manually | State-directory persistence and ownership has no input-domain edge; covered by the `truths` entry on `/var/lib/homebox` ownership instead. |
| HBX-02 | unclassified -- review manually | "Both members, one shared group" has no boundary condition; covered by the `truths` entry on `GET /api/v1/groups/members` returning exactly 2. |
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Homebox through every layer -- module, secret, persistence, gateway, both hosts activated</name>
  <files>modules/household/default.nix, modules/household/homebox.nix, hosts/ser8/household/default.nix, hosts/ser8/household/homebox.nix, hosts/ser8/impermanence.nix, modules/gateway/Caddyfile, scripts/validation/test-homebox-module.sh, Makefile</files>
  <read_first>
    - modules/household/mealie.nix (the reusable-layer shape to mirror: `lib.mkIf config.services.<svc>.enable` guards, `enable = lib.mkDefault false`, firewall port, no host identity)
    - hosts/ser8/household/mealie.nix (host-policy shape: settings block, `extraOptions`, explicit package pin)
    - hosts/ser8/household/postgresql.nix (the "plain assignment for a one-way pin" convention, for reference on comment style -- not reused here, Homebox uses SQLite)
    - hosts/ser8/impermanence.nix (directories list around `/var/lib/mealie`; tmpfiles rules around lines 108-127, especially the 0750 mealie rule and its comment)
    - modules/gateway/Caddyfile (the `# -- Household services` block at the end containing the `mealie` vhost -- copy its shape exactly, same `log tailscale`/`bind tailscale/<name>`/`reverse_proxy` structure)
    - hosts/ser8/media/frigate.nix or modules/automation/frigate.nix lines 484-495 (the `sops.templates."frigate.env"` + `EnvironmentFile` + `after=["sops-nix.service"]` pattern -- this is the template for injecting HBOX_AUTH_API_KEY_PEPPER)
    - hosts/ser8/configuration.nix lines 196-215 (the `sops.secrets` block shape: owner/group/mode)
    - scripts/validation/test-mealie-module.sh (the `check_eval` helper and assertion shape to mirror for Homebox)
    - Makefile lines 140-147 (the `check` target's validation-script invocations)
  </read_first>
  <action>
Wire Homebox through every layer: module, host policy, secret, persistence, gateway vhost, both hosts activated, reachable end to end. No stub.

Create `modules/household/homebox.nix` with the `{ config, lib, ... }:` signature. Unlike `mealie.nix`, do NOT declare a static user/group here -- nixpkgs' `services.homebox` module already creates a static system user `homebox`/`homebox` (no `DynamicUser`) whenever `services.homebox.user` is left at its default, so there is nothing to override. Set only `services.homebox.enable = lib.mkDefault false;` and open the firewall: `networking.firewall.allowedTCPPorts` gains `7745` under `lib.mkIf config.services.homebox.enable`. Comment why the port is a literal rather than a reference to a typed option: `services.homebox.settings` is a freeform `attrsOf (nullOr str)` with no dedicated `port` option, and the module itself never sets `HBOX_WEB_PORT` at all (only documents 7745 as the *application's* internal default) -- so host policy sets `HBOX_WEB_PORT` explicitly in Task 1 below and this file's firewall rule must match that literal, not read it back from config.

Update `modules/household/default.nix`: add `./homebox.nix` to the imports list, after `./mealie.nix`.

Create `hosts/ser8/household/homebox.nix` with the `{ config, pkgs, ... }:` signature. Set `services.homebox.enable = true;` and `services.homebox.package = pkgs.homebox;` as an explicit (non-`mkDefault`) pin -- nixpkgs 26.05 currently resolves this to 0.25.0, satisfying HBX-01's "pinned 0.25.x"; comment that a future nixpkgs bump moving Homebox to 0.26 would silently drift this pin unless re-verified. Under `services.homebox.settings` set: `HBOX_WEB_PORT = "7745";`, `HBOX_OPTIONS_ALLOW_ANALYTICS = "false";`, `HBOX_OPTIONS_GITHUB_RELEASE_CHECK = "false";`, `HBOX_OPTIONS_HOSTNAME = "homebox.shad-bangus.ts.net";`, `HBOX_DEMO = "false";`. Leave `HBOX_OPTIONS_ALLOW_REGISTRATION` unset in this task (the upstream module's own `mkDefault` leaves it `"true"`, i.e. open -- Task 3 closes it explicitly). Every value must be a literal Nix string; the settings type is `attrsOf (nullOr str)`, so a bare Nix boolean is a hard eval error here (unlike Mealie's `toString`-based module, which fails silently instead).

Declare the secret: `sops.secrets.homebox_auth_api_key_pepper = { owner = "homebox"; group = "homebox"; mode = "0400"; };` and `sops.templates."homebox.env" = { content = "HBOX_AUTH_API_KEY_PEPPER=${config.sops.placeholder.homebox_auth_api_key_pepper}"; owner = "homebox"; group = "homebox"; mode = "0400"; };` following `frigate.env`'s shape. Override `systemd.services.homebox.serviceConfig.EnvironmentFile = config.sops.templates."homebox.env".path;` and add `systemd.services.homebox.after = [ "sops-nix.service" ]; systemd.services.homebox.wants = [ "sops-nix.service" ];`.

Generate the pepper value now with `openssl rand -base64 48` (Homebox's own docs recommend this exact command; the binary refuses to start below 32 bytes). Print it once to the terminal. Do not write it to any file this plan creates or modifies. Instruct the operator (in the SUMMARY, not in a committed file) to run `make sops-edit-ser8` and add it under the key `homebox_auth_api_key_pepper`.

Update `hosts/ser8/household/default.nix`: add `./homebox.nix` to the imports list.

Edit `hosts/ser8/impermanence.nix`: add `"/var/lib/homebox"` to the services block of `environment.persistence."/persist".directories`, and add a tmpfiles rule `d /persist/var/lib/homebox 0750 homebox homebox -` matching the 0750 mode systemd's `StateDirectory = "homebox"` produces by default with an explicit `User`/`Group`.

Edit `modules/gateway/Caddyfile`: append a vhost in the `# -- Household services` block, after the `mealie` entry: `https://homebox.shad-bangus.ts.net`, `bind tailscale/homebox`, `reverse_proxy 192.168.68.65:7745`, same `log tailscale { level DEBUG }` stanza, no `header_up` pair (Homebox needs no WebSocket handling).

Create `scripts/validation/test-homebox-module.sh` on `test-mealie-module.sh`'s exact shape: `check_eval` helper, assertions for `.config.services.homebox.package.version` = `"0.25.0"`, `.config.services.homebox.settings.HBOX_WEB_PORT` = `"7745"`, `.config.services.homebox.settings.HBOX_OPTIONS_ALLOW_ANALYTICS` = `"false"`. Wire it into the Makefile `check` target alongside the existing four validation scripts.

Run `make fmt` and `make fmt-caddy` before committing. Commit as separate logical changes: module scaffold + secret, then persistence, then the Caddy vhost, then the validation script.

Activate: `make switch-ser8` (brings up the homebox unit; it will fail to start until the operator adds the sops secret -- that failure is expected and does not block this task's own verification of the *evaluated* config) and, once the pepper is in place and ser8 re-activated, `make switch-firebat`.
  </action>
  <verify>
    <automated>nix build .#nixosConfigurations.ser8.config.system.build.toplevel --dry-run && nix build .#nixosConfigurations.firebat.config.system.build.toplevel --dry-run && ./scripts/validation/test-homebox-module.sh && caddy adapt --config ./modules/gateway/Caddyfile --adapter caddyfile 2>/dev/null | jq -e '[.apps.http.servers[].listen[]] | index("tailscale/homebox:443")'</automated>
  </verify>
  <acceptance_criteria>
    - `nix eval --json '.#nixosConfigurations.ser8.config.services.homebox.package.version'` outputs `"0.25.0"`
    - `nix eval --json '.#nixosConfigurations.ser8.config.services.homebox.settings.HBOX_OPTIONS_ALLOW_ANALYTICS'` outputs `"false"`
    - `nix eval --json '.#nixosConfigurations.ser8.config.systemd.services.homebox.serviceConfig.EnvironmentFile'` resolves to a `/run/secrets-rendered/homebox.env`-style sops-templates path, not a literal secret value
    - `nix eval --json '.#nixosConfigurations.ser8.config.networking.firewall.allowedTCPPorts' | jq -e 'index(7745)'` exits 0
    - `nix eval --json '.#nixosConfigurations.ser8.config.environment.persistence."/persist".directories' | jq -e 'map(if type=="object" then .directory else . end) | index("/var/lib/homebox")'` exits 0
    - `caddy adapt --config ./modules/gateway/Caddyfile | jq -e '[.apps.http.servers[].listen[]] | index("tailscale/homebox:443")'` exits 0
    - `rg -c 'HBOX_AUTH_API_KEY_PEPPER' modules/household/ hosts/ser8/household/homebox.nix | grep -v ':0'` shows the key name present but `rg -c 'HBOX_AUTH_API_KEY_PEPPER=' hosts/ser8/household/homebox.nix` (a literal assigned value, not a placeholder) returns 0
    - after the operator adds the sops secret and re-activates, `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:7745/` on ser8 returns a 2xx/3xx status
    - `curl -s -o /dev/null -w '%{http_code}' https://homebox.shad-bangus.ts.net` from firebat returns a 2xx/3xx status
  </acceptance_criteria>
  <reversibility rating="reversible">Enabling a new service and adding a gateway vhost are both easily disabled (`services.homebox.enable = false;`, remove the Caddyfile block); no data exists yet to make this one-way.</reversibility>
  <done>Homebox evaluates and builds on ser8 and firebat, the offline gate passes, the Caddy config carries the `tailscale/homebox:443` listener, and -- once the operator has added the sops secret -- the service answers on its loopback port and through the tsnet vhost.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bootstrap both household members into one Homebox group</name>
  <files>none (runtime API calls only; no repository file changes)</files>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-06-SUMMARY.md (the credential-fidelity posture this task must match: generated passwords shown once, never committed, verified via the app's own API rather than trusted by report)
  </read_first>
  <action>
Register the two household members via Homebox's own REST API, matching the flow in Homebox's own upstream CI script (`.github/scripts/upgrade-test/create-test-data.sh`, `register_user`/`login_user` functions): `POST https://homebox.shad-bangus.ts.net/api/v1/users/register` with JSON body `{"email":"jordan@household.internal","name":"Jordan","password":"<generated>"}` for the first member -- this call, with no `groupToken`, both creates the user and a new group with an auto-generated `inviteToken`. Generate the password with `openssl rand -base64 18` per member; never write either password to a file. Capture the response's `.token` is NOT returned by register (register returns 204 with no body per Homebox 0.25's controller) -- instead call `POST /api/v1/users/login` with `{"email":"jordan@household.internal","password":"<same value>"}` to obtain the session token, and separately call `GET /api/v1/groups` (authenticated as jordan) to read the group's `inviteToken`.

Register the second member with the captured invite token: `POST /api/v1/users/register` with `{"email":"sawnia@household.internal","name":"Sawnia","password":"<generated>","groupToken":"<jordan's inviteToken>"}`. Log in as sawnia the same way.

Verify both land in the same group: `GET /api/v1/groups/members` authenticated as jordan's token, and again authenticated as sawnia's token; both responses must list exactly 2 members and the same group `id`.

Display both generated passwords once in this task's terminal output for the operator to store in the household's password manager. Do not place either value in the SUMMARY, in a script, or in any committed file.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{}' https://homebox.shad-bangus.ts.net/api/v1/users/login</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/v1/users/register` for jordan (no groupToken) returns 204
    - `POST /api/v1/users/login` for jordan returns 200 with a non-empty `.token`
    - `GET /api/v1/groups` for jordan returns a non-empty `.inviteToken`
    - `POST /api/v1/users/register` for sawnia (with jordan's groupToken) returns 204
    - `POST /api/v1/users/login` for sawnia returns 200 with a non-empty `.token`
    - `GET /api/v1/groups/members` returns the same 2-member list and identical group `id` regardless of which member's token authenticates the request
    - neither generated password appears in `git diff`, in `.planning/`, or in any file this task wrote
  </acceptance_criteria>
  <done>jordan and sawnia both hold active Homebox sessions in one shared group, proven by both tokens returning the same 2-member `GET /api/v1/groups/members` response.</done>
</task>

<task type="auto">
  <name>Task 3: Close self-registration, extend the offline gate, and add household smoketests</name>
  <files>hosts/ser8/household/homebox.nix, scripts/validation/test-homebox-module.sh, scripts/smoketests/household/all.sh, scripts/smoketests/household/test-homebox-service.sh, scripts/smoketests/household/test-homebox-endpoint.sh</files>
  <read_first>
    - scripts/smoketests/household/test-mealie-service.sh (state-directory shape, persist-dir, and unit-active checks to mirror for Homebox)
    - scripts/smoketests/household/test-mealie-endpoint.sh (base-URL/setting/tsnet-DNS/tsnet-HTTPS checks to mirror)
    - scripts/smoketests/household/all.sh (the `TESTS` array to extend)
    - hosts/ser8/household/homebox.nix (as written in Task 1, the settings block being extended here)
  </read_first>
  <action>
In `hosts/ser8/household/homebox.nix`, add `HBOX_OPTIONS_ALLOW_REGISTRATION = "false";` to the `services.homebox.settings` attrset, overriding the upstream module's `mkDefault "true"`. This closes public self-registration; Homebox's own handler still permits registration when a valid `groupToken` is supplied (an intentional invite-link exception, not a bug), so this does not need to be re-opened for future invites.

Redeploy: `make switch-ser8`.

Extend `scripts/validation/test-homebox-module.sh` with one more `check_eval` assertion: `.config.services.homebox.settings.HBOX_OPTIONS_ALLOW_REGISTRATION` = `"false"`.

Create `scripts/smoketests/household/test-homebox-service.sh` on `test-mealie-service.sh`'s shape: unit-active check (`homebox.service`), port-listening check (7745), no-startup-error journal check, and a state-directory-shape check asserting `/var/lib/homebox` is a real directory owned `homebox:homebox` plus the `/persist/var/lib/homebox` counterpart.

Create `scripts/smoketests/household/test-homebox-endpoint.sh` on `test-mealie-endpoint.sh`'s shape: local-endpoint check on port 7745, a check that `POST /api/v1/users/register` with no `groupToken` now returns 403 (registration closed), a tsnet-DNS check for `homebox.shad-bangus.ts.net`, and a tsnet-HTTPS check.

Add both new scripts to the `TESTS` array in `scripts/smoketests/household/all.sh`, after the two existing Mealie entries.
  </action>
  <verify>
    <automated>./scripts/smoketests/household/test-homebox-service.sh ser8 && ./scripts/smoketests/household/test-homebox-endpoint.sh ser8</automated>
  </verify>
  <acceptance_criteria>
    - `nix eval --json '.#nixosConfigurations.ser8.config.services.homebox.settings.HBOX_OPTIONS_ALLOW_REGISTRATION'` outputs `"false"`
    - `./scripts/validation/test-homebox-module.sh` exits 0 with four `ok:` lines
    - `curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{"email":"stranger@household.internal","name":"Stranger","password":"whatever123"}' http://127.0.0.1:7745/api/v1/users/register` on ser8 returns 403, not 204
    - `./scripts/smoketests/household/test-homebox-service.sh ser8` exits 0
    - `./scripts/smoketests/household/test-homebox-endpoint.sh ser8` exits 0
    - `rg -c 'test-homebox' scripts/smoketests/household/all.sh` returns 2
  </acceptance_criteria>
  <done>Registration is provably closed at both the config layer and the running service, the offline gate asserts it, and both new household smoketest scripts pass and are wired into the household suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet client -> firebat tsnet listener | Untrusted household device traffic terminates TLS at the caddy-tailscale node |
| firebat -> ser8 tcp/7745 | Plaintext HTTP over the LAN, same established pattern as Mealie's tcp/9000 |
| Nix store -> world | Everything in `/nix/store` is world-readable; the pepper must never land there |
| Homebox invite-token exception | `HBOX_OPTIONS_ALLOW_REGISTRATION=false` still permits registration with a valid groupToken -- an intentional design exception, not a residual open-registration bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | HBOX_AUTH_API_KEY_PEPPER in the Nix store | high | mitigate | Delivered via `sops.templates."homebox.env"` + `EnvironmentFile`, never a literal Nix string; Task 1's acceptance criteria grep for a literal assigned value and require it be absent |
| T-11-02 | Spoofing | HBOX_OPTIONS_ALLOW_REGISTRATION | high | mitigate | Closed to the string `"false"` in Task 3, asserted by both the offline eval gate and a live `POST /api/v1/users/register` returning 403 |
| T-11-03 | Elevation of Privilege | HBOX_DEMO seed account | medium | mitigate | Explicitly set to `"false"`; Homebox's own startup guard would otherwise seed a publicly-documented `demo@example.com` account |
| T-11-04 | Denial of Service | Homebox's on-disk journal_mode=WAL SQLite database with no PostgreSQL failover | low | accept | Standalone-by-design (REQUIREMENTS.md Out of Scope: no sync between services); same posture as Donetick and Actual, both SQLite-backed |
| T-11-05 | Elevation of Privilege | ser8 tcp/7745 open on the LAN | medium | transfer | Same established LAN-plaintext pattern as every other household/media service; the negative-access proof is SEC-01, Deferred |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs; `pkgs.homebox` resolves from the pinned `nixpkgs` flake input already in `flake.lock` |
</threat_model>

<artifacts_this_phase_produces>
**Directories:** none new (extends existing `modules/household/`, `hosts/ser8/household/`)

**Files:** `modules/household/homebox.nix`, `hosts/ser8/household/homebox.nix`, `scripts/validation/test-homebox-module.sh`, `scripts/smoketests/household/test-homebox-service.sh`, `scripts/smoketests/household/test-homebox-endpoint.sh`

**NixOS option paths newly set on ser8:** `services.homebox.enable`, `services.homebox.package`, `services.homebox.settings.*`

**systemd units newly present on ser8:** `homebox.service`

**System users and groups newly declared:** user `homebox`, group `homebox` (created by the upstream nixpkgs module, not by this repo)

**Persisted paths newly declared:** `/persist/var/lib/homebox`

**Gateway route newly declared:** the Caddy vhost `https://homebox.shad-bangus.ts.net` bound to the tsnet listener `tailscale/homebox`

**sops secrets newly declared:** `homebox_auth_api_key_pepper` (value added by the operator, not by this plan)

**Existing symbols extended:** `modules/household/default.nix` and `hosts/ser8/household/default.nix` imports gain `./homebox.nix`; `hosts/ser8/impermanence.nix` gains one directory entry and one tmpfiles rule; `modules/gateway/Caddyfile` gains one vhost block; the Makefile `check` target gains one validation-script invocation; `scripts/smoketests/household/all.sh` gains two `TESTS` entries
</artifacts_this_phase_produces>

<verification>
- `make check` exits 0 across all four hosts
- `./scripts/validation/test-homebox-module.sh` exits 0 with four `ok:` lines
- `./scripts/smoketests/household/test-homebox-service.sh ser8` and `./scripts/smoketests/household/test-homebox-endpoint.sh ser8` both exit 0
- `GET /api/v1/groups/members` shows jordan and sawnia sharing one group
- `POST /api/v1/users/register` with no invite token returns 403
</verification>

<success_criteria>
Homebox is live at `https://homebox.shad-bangus.ts.net`, both household members have accounts in the same group, self-registration is closed, analytics and the demo seed are off, and Homebox's state is persisted under the same `modules/household/` + `hosts/ser8/household/` pattern Mealie established.
</success_criteria>

<output>
Create `.planning/phases/11-homebox-actual-budget-and-donetick/11-01-SUMMARY.md` when done.
</output>
