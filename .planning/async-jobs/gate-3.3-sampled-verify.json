{
  "version": "1.0",
  "job_id": "gate-3.3-sampled-verify",
  "plan_id": "13-04",
  "phase": "13",
  "backend": "systemd-run",
  "status": "running",
  "submit_command": "ssh bdhill@192.168.68.65 'sudo systemd-run --unit=gate-33-sampled-verify --description=\"Phase 13 Gate 3.3: sampled + metadata verification /mnt/media vs /mnt/media-staging\" --setenv=PATH=/run/wrappers/bin:/run/current-system/sw/bin -- bash /persist/zfs-migration/sampled-verify.sh /mnt/media /mnt/media-staging /persist/zfs-migration/gate-3.3-manifest.txt'",
  "expected_artifacts": [
    "/persist/zfs-migration/gate-3.3-manifest.txt (ser8) -- PASS/FAIL result with D-07 sampled + metadata diff detail, written on both outcomes",
    ".planning/phases/13-zfs-mirror-migration/evidence/gate-3.3-result.md (repo) -- committed only after a confirmed PASS"
  ],
  "verification_command": "ssh bdhill@192.168.68.65 'systemctl show gate-33-sampled-verify -p ExecMainStatus,Result --value; echo ---; sudo cat /persist/zfs-migration/gate-3.3-manifest.txt'",
  "resume_command": "ssh bdhill@192.168.68.65 'systemctl status gate-33-sampled-verify --no-pager; journalctl -u gate-33-sampled-verify --no-pager | tail -80'",
  "submitted_at": "2026-08-24T18:20:54Z",
  "terminal_details": null,
  "notes": "Task 2 is a checkpoint:decision (gate=blocking) whose <context> states its mutation class is read-only against both /mnt/media and /mnt/media-staging (writes only the manifest file) -- per this phase's operator checkpoint policy ('approval should only be for running commands on ser8 for data migration... not code/doc changes', with read-only/diagnostic ser8 checks explicitly pre-approved), this checkpoint was auto-approved rather than stopped on for live human confirmation. Estimated runtime (~21 min at 0.4% content coverage) exceeds the phase's 10-minute foreground threshold, so per the checkpoint policy this was dispatched as a detached systemd-run transient unit (following the external-job-manifest pattern Plan 13-03 established for media-staging-rsync) and the session returns rather than polling to completion. First dispatch attempt (invocation ID baa18c05-c94b-4be7-8b59-a2f2385f38b4) failed immediately with exit 127 ('mktemp: command not found') -- systemd-run's default minimal unit environment does not include /run/current-system/sw/bin, which is where ser8's coreutils (mktemp/dd/sha256sum/find/stat/etc.) live. Fixed as a Rule 1 blocking bug by re-dispatching with --setenv=PATH=/run/wrappers/bin:/run/current-system/sw/bin, matching root's normal login PATH on ser8. The corrected unit (invocation ID 58a4b786-874f-43ad-bfed-6c2e36d6522c) was confirmed running with accumulating CPU time (10.5s over 20s elapsed) before this manifest was committed. Per this plan's must_haves.prohibitions and STRIDE T-13-11/T-13-12: when this job reaches a terminal state, the MANIFEST FILE CONTENT is the sole source of truth for pass/fail -- never systemctl's ExecMainStatus/Result alone -- and any non-empty manifest output is FAIL with no partial-pass threshold, unconditionally blocking Plan 13-05's disk erase until re-run to a clean PASS."
}
