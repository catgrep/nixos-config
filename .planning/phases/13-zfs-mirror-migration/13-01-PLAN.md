---
phase: 13-zfs-mirror-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/SER8-ZFS-MIRROR-MIGRATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [ZFS-01, ZFS-05]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The migration doc's freeze set, Step 0.4, Stage 5 restore description, and Handoff Status reflect the post-Phase-12 world (no qBittorrent/wgnord/nginx) before any live command runs (D-01)."
    - "Both approved 12 TB disks (wwn-0x5000c500b56ea81a, wwn-0x5000c500b3733a87) pass a short SMART self-test with zero pending/reallocated/offline-uncorrectable sectors, or the phase halts (D-04)."
    - "A source inventory manifest for /mnt/media exists outside /mnt/media and outside any staging dataset, and its byte total reconciles with the live filesystem (Step 0.5)."
    - statement: "Live preflight re-verification of Phase 12's fixes (Step 0.1) surfaces any regression in wgnord/qBittorrent absence, sabnzbd health, or Radarr root-folder state since Phase 12 closed."
      verification: backstop
  artifacts:
    - path: ".planning/SER8-ZFS-MIRROR-MIGRATION.md"
      provides: "Amended migration doc reflecting D-01/D-02/D-03/D-04/D-08/D-16/D-18/D-21 as the phase's single source of truth"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ZFS-01 reworded to short SMART health test gate; ZFS-03 reworded to send/recv + scrub"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 success criterion 1 reworded to short SMART health test gate"
  key_links:
    - from: ".planning/SER8-ZFS-MIRROR-MIGRATION.md#Service-Freeze-Set"
      to: "13-03-PLAN.md quiesce task"
      via: "the freeze set this plan writes is the exact stop list Plan 13-03 executes"
      pattern: "jellyfin\\.service|radarr\\.service|sonarr\\.service"
  prohibitions: []
---

<objective>
Reconcile `.planning/SER8-ZFS-MIRROR-MIGRATION.md` with the post-Phase-12 world and the 23 CONTEXT.md decisions as the phase's first approved step (D-01), then run the doc's Stage 0 preflight: reconcile live state, gate the erase on a short SMART self-test (D-04), and record a source inventory manifest (Step 0.5).

Purpose: Every later plan in this phase reads the migration doc cold in a fresh session (per D-12's stage-per-session cadence). A stale doc — still describing qBittorrent/wgnord/nginx as live, extended SMART scans, and an rsync-based restore — is a live hazard across a multi-week, multi-session migration. This plan makes the doc truthful before any live command touches ser8, and proves the two target disks are healthy before staging begins.
Output: Amended migration doc + reworded ZFS-01/criterion-1 requirement text; a preflight drift report; a short-SMART pass/fail record for both disks; a source inventory manifest.

This plan is the first of the phase's seven doc-stage plans (D-10: one GSD plan per migration-doc stage, plan boundaries = stage boundaries = session boundaries, each SUMMARY.md documenting one Rollback Matrix row). D-14 governs execution mechanics across every plan in this phase: the agent executes commands over SSH only after the applicable gate passes, then immediately verifies that only the approved targets changed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/SER8-ZFS-MIRROR-MIGRATION.md
@.planning/phases/13-zfs-mirror-migration/13-CONTEXT.md
@.planning/phases/12-fleet-repair/12-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Doc reconciliation: amend the migration doc and reword ZFS-01/ZFS-03/criterion 1 (D-01)</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md, .planning/phases/13-zfs-mirror-migration/13-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/12-fleet-repair/12-CONTEXT.md</read_first>
  <decision>Approve rewriting the migration doc (repository-writing, reversible via git) before any live command runs, plus the matching REQUIREMENTS.md/ROADMAP.md wording fixes.</decision>
  <context>
    The doc was written 2026-08-13 and never updated. It still lists `qbittorrent-nox.service`, `wgnord.service`, and `nginx.service` in the Service Freeze Set (lines 311-334); Steps 0.2 (lines 375-391) and 0.3 (392-406) target blockers Phase 12 already fixed; Step 0.4 (407-423) runs a 20+ hour extended SMART scan the operator rejected (D-04); the Goal section (56-74) lists "downloads" among the directories staying in one dataset, which D-21/D-22 now contradicts; Stage 5 (743-828) restores via rsync, which D-08 replaced with `zfs send/recv`; the Repository Changes → Smoketests bullet list (296-310) still requires a cross-directory hardlink test D-22 replaced with an import-write test; Handoff Status (918-938) still marks the qBittorrent/wgnord blocker and Radarr root-folder cleanup unresolved.
    Mutation class: repository-writing only, reversible via `git revert`.
    Rollback: revert the commit; no live system is touched by this step.
  </context>
  <options>
    <option id="approve">
      <name>Approve the doc rewrite as scoped below</name>
      <pros>Every downstream plan and every future fresh session reads a truthful contract; matches D-01's explicit precedent from Phase 12.</pros>
      <cons>A large single-file diff across a 938-line safety-critical document — must be reviewed carefully before commit.</cons>
    </option>
    <option id="abort">
      <name>Abort and revise scope</name>
      <pros>Nothing changes.</pros>
      <cons>Blocks the rest of the phase — every later plan depends on this doc being current.</cons>
    </option>
  </options>
  <action>
    After approval, edit `.planning/SER8-ZFS-MIRROR-MIGRATION.md` with these exact targets (do not touch Approval Contract, Safety Rules, Approved Disk Inventory, or Non-Goals — D-01 requires safety language stay unweakened):
    - Service Freeze Set (lines 311-334): replace the live unit list with exactly `jellyfin.service`, `radarr.service`, `sonarr.service`, `bazarr.service`, `prowlarr.service`, `sabnzbd.service`, `nzbget.service`, `samba-smbd.service`, `samba-wsdd.service`, plus the three orchestration oneshots `media-config.service`, `servarrs-setup.service`, `download-clients-setup.service`, plus household apps `mealie.service`, `homebox.service`, `actual.service`, `donetick.service`, plus `frigate.service`, `home-assistant.service`, `mosquitto.service`. Remove `qbittorrent-nox.service`, `wgnord.service`, `nginx.service` entirely and the paragraph about qBittorrent's `BindsTo=wgnord.service` restart loop. State explicitly that infrastructure (sshd, networking, Tailscale, node/zfs exporters, nix-daemon) stays up, and that quiesce timing moved BEFORE the initial copy per D-03 (no separate "keep media stack running during first pass" step exists anymore — see Stage 2/3 note below).
    - Goal section (56-74): remove "downloads" from the "keeps movies, television, downloads, books, and music as directories in one ZFS dataset so hardlinks can cross those directories" sentence — downloads now live on `rpool/safe/downloads` (D-21). Reword the rationale per D-22: the single `media/data` dataset is for simplicity, not because hardlinks must cross directories (torrenting is retired; imports now copy from the downloads dataset instead of hardlinking).
    - Step 0.2 (375-391) and Step 0.3 (392-406): change status to "Satisfied by Phase 12 — trusted via evidence records per D-05, no live re-verification in this phase" and add a link to `.planning/phases/12-fleet-repair/12-CONTEXT.md` and its `evidence/` directory. Keep the step numbers and original text below the status line for historical record.
    - Step 0.4 (407-423): rewrite from "extended self-test" to: run `smartctl -a` on both approved WWNs, confirm reallocated/pending/offline-uncorrectable sectors are all zero and overall health is PASSED, then run `smartctl -t short` (~2 min/disk) and wait for completion, then re-read `smartctl -a` to confirm the short test result is "completed without error." State explicitly: refuse to proceed to Step 4.3 (erase) if any counter is nonzero or the short test fails. Remove all "extended self-test" and "SMART surface scan" language.
    - Repository Changes → Smoketests (296-310): remove the "hardlink can be created between controlled test paths under downloads and a library directory" bullet; replace with "A controlled import-write test creates a file under the downloads dataset, copies it into a library directory on `media/data`, verifies ownership/permissions, then removes both (D-22)."
    - Stage 5 numbered steps (743-804, Steps 5.1 and 5.2 specifically): rewrite Step 5.1's intended command shape from the `rsync` block to: `zfs snapshot backup/media-staging@verified`, then `zfs send -s backup/media-staging@verified | zfs recv -u media/data`, then `zfs set mountpoint=/mnt/media media/data`, then `zfs mount media/data`. Rewrite Step 5.2 (currently "Perform Full Restore Verification" with an rsync -c command) to "Confirm Restore Integrity and Mount": verify the `zfs recv` exited 0 with no stream errors, verify `zfs get mountpoint,mounted media/data` shows the expected values, verify `mount | grep /mnt/media` shows a zfs filesystem — state explicitly that full content verification is now intrinsic to the block-checksummed send/recv stream (D-08) and that first-scrub-clean (Step 5.5) is the remaining integrity gate, not a second full comparison.
    - Migration State Machine mermaid (335-353): update node "I" from "Restore 7.788 TB from staging" generic rsync language to reference the send/recv mechanism; update node "J" from "Full restore verification" to "Confirm receive integrity and mount".
    - Step 3.3 (619-643): add a note under "Treat any output as a discrepancy" stating the exact gate semantics per D-07/D-09: verification uses deterministic per-file sampling (head + tail + 1 MiB per GiB of file, offsets derived from file size; files under 1 MiB hashed fully) plus a 100%-coverage metadata-only dry run; the gate is "itemized/diff output is empty" — enforced by the verification script, never by rsync's exit status, since rsync exits 0 even when differences are found.
    - Handoff Status (918-938): update "qBittorrent and wgnord blocker" to "resolved — Phase 12 deleted the stack" and "Radarr root-folder cleanup" to "resolved — Phase 12 API cleanup"; both linking `.planning/phases/12-fleet-repair/`. Add a line noting Phase 13 execution is now underway per `.planning/phases/13-zfs-mirror-migration/`.
    - Add a new short paragraph under Desired ZFS Configuration (232-260) noting: downloads relocate to a separate `rpool/safe/downloads` dataset with a 500G quota (D-21), implemented in this phase's final stage — off the storage-freeze critical path.
    Then edit `.planning/REQUIREMENTS.md`: reword ZFS-01's bullet (line 21) from "Extended SMART tests pass" to "Short SMART health test (self-test + zero pending/reallocated/offline-uncorrectable sector counters) passes on both approved 12 TB disks, and the full media tree is staged to `backup/media-staging` with a frozen, checksum-verified final sync reporting zero unexplained differences." Reword ZFS-03 (line 23) to reference `zfs send/recv` intrinsic integrity plus first-scrub-zero-errors, dropping any implied separate rsync-based restore comparison.
    Then edit `.planning/ROADMAP.md` Phase 13 success criterion 1 (around line 102) to read: "Both approved 12 TB disks pass a short SMART health test gate (self-test plus zero pending/reallocated/offline-uncorrectable sectors), and the full media tree is staged to `backup/media-staging` with a frozen, checksum-verified final sync reporting zero unexplained differences."
  </action>
  <verify>
    <automated>grep -c "extended self-test\|qbittorrent-nox.service\|wgnord.service" .planning/SER8-ZFS-MIRROR-MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/SER8-ZFS-MIRROR-MIGRATION.md` contains zero occurrences of `qbittorrent-nox.service`, `wgnord.service`, or `nginx.service` in the Service Freeze Set section.
    - `.planning/SER8-ZFS-MIRROR-MIGRATION.md` Step 0.4 text contains `smartctl -t short` and does not contain "extended self-test".
    - `.planning/REQUIREMENTS.md` ZFS-01 text contains "Short SMART health test".
    - `.planning/ROADMAP.md` Phase 13 criterion 1 text contains "short SMART health test".
    - Approval Contract, Safety Rules, and Approved Disk Inventory sections are byte-identical to before the edit (`git diff` shows no changes in those line ranges).
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize the doc rewrite, or describe changes needed.</resume-signal>
  <done>The migration doc, REQUIREMENTS.md, and ROADMAP.md are committed reflecting D-01 through D-04, D-08, D-16, D-18, D-21, D-22.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 0.1: Reconcile repository and live ser8 state</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-0.1, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Verified-State-at-Handoff</read_first>
  <decision>Approve running the read-only inspection commands against live ser8 to reconcile the doc's snapshot with current reality.</decision>
  <context>
    Mutation class: read-only. Kernel device names swap between boots (the doc's own §Measured facts already recorded sde/sdf swapping since 2026-08-13) — this step re-resolves both approved WWNs to their current kernel devices and confirms nothing else has drifted (git status, live Nix generation, mounts, block devices, pools, datasets, utilization, service states, application roots).
    Do not assume `/dev/sde`/`/dev/sdf` still identify the media disks; resolve via `/dev/disk/by-id/wwn-*` only.
  </context>
  <options>
    <option id="approve"><name>Run the Step 0.1 inspection</name><pros>Establishes ground truth before any write.</pros><cons>None — fully read-only.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks the rest of the phase.</cons></option>
  </options>
  <action>
    After approval, over SSH to ser8: `git status --short` (repo working tree, preserving unrelated changes); `readlink -f /dev/disk/by-id/wwn-0x5000c500b56ea81a` and the second WWN to get current kernel names; `lsblk -o NAME,SIZE,TYPE,FSTYPE,MODEL,SERIAL,MOUNTPOINT`; `zpool status`; `zfs list`; `df -h /mnt/media /mnt/backups`; `mount | grep -E "/mnt/media|/mnt/disk"`; `systemctl is-active jellyfin sonarr radarr bazarr prowlarr sabnzbd nzbget samba-smbd samba-wsdd mealie homebox actual donetick frigate home-assistant mosquitto`; confirm via API (`curl` against the Radarr API using the sops-stored key) that Radarr reports exactly one root folder (`/mnt/media/movies`); confirm `systemctl list-unit-files | grep -E "wgnord|qbittorrent"` returns nothing. Compare every result against the doc's §Verified State at Handoff and §Approved Disk Inventory; record any drift in the plan's SUMMARY.md.
  </action>
  <verify>
    <automated>ssh root@ser8 'zpool status backup | grep -q "state: ONLINE" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Both approved WWNs resolve to a live block device matching the doc's model (`ST12000NM0117-2GY101`) and capacity (`12,000,138,625,024` bytes).
    - `systemctl list-unit-files | grep -E "wgnord|qbittorrent"` returns empty (confirms Phase 12 deletion per D-05).
    - Radarr API reports exactly one movie root folder: `/mnt/media/movies`.
    - `git status --short` on ser8 shows no unexpected local drift from the deployed generation.
    - No unexplained mount or pool exists beyond `rpool`, `backup`, and the current MergerFS `/mnt/media`.
  </acceptance_criteria>
  <resume-signal>Type "approved" to run the Step 0.1 inspection commands.</resume-signal>
  <done>Live ser8 state matches the doc's expectations (or drift is explicitly documented) before Stage 1 begins.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 0.4: Short SMART health test gate on both approved media disks (D-04)</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-0.4 (as amended by Task 1 of this plan), .planning/SER8-ZFS-MIRROR-MIGRATION.md#Safety-Rules</read_first>
  <decision>Approve running `smartctl -a` (counters) and `smartctl -t short` (self-test) against both approved WWNs.</decision>
  <context>
    Mutation class: live device diagnostic (non-destructive to data; a self-test briefly uses drive controller cycles). This is the D-04 replacement for the doc's original 20+ hour extended scan: counters read plus a ~2 min/disk short self-test. The Safety Rules refuse-if-failing clause is unchanged — nonzero pending/reallocated/offline-uncorrectable sectors, or a failed short test, blocks the erase step (Step 4.3, Plan 13-05) entirely.
  </context>
  <options>
    <option id="approve"><name>Run the SMART gate</name><pros>~2 min/disk, near-zero cost, confirms drive health before committing days to staging.</pros><cons>None.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks staging — the erase step later requires this gate to have passed.</cons></option>
  </options>
  <action>
    After approval, over SSH: `smartctl -a /dev/disk/by-id/wwn-0x5000c500b56ea81a` and the same for `wwn-0x5000c500b3733a87`; confirm `SMART overall-health self-assessment test result: PASSED` and that Reallocated_Sector_Ct, Current_Pending_Sector, and Offline_Uncorrectable raw values are all `0` on both disks. Then `smartctl -t short` on each disk (sequentially, not concurrently, per the doc's thermal-safety note), wait ~2 minutes, then `smartctl -a` again to confirm `Self-test execution status: ... completed without error`. If ANY counter is nonzero or either self-test fails: STOP, do not proceed to Plan 13-02, and surface the failure to the operator as a phase-blocking finding — never rationalize a nonzero counter as acceptable.
  </action>
  <verify>
    <automated>ssh root@ser8 'smartctl -H /dev/disk/by-id/wwn-0x5000c500b56ea81a | grep -q PASSED && smartctl -H /dev/disk/by-id/wwn-0x5000c500b3733a87 | grep -q PASSED && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Both disks report `SMART overall-health self-assessment test result: PASSED`.
    - Both disks report `0` for reallocated, pending, and offline-uncorrectable sector counts.
    - Both disks' short self-test completed without error.
    - The result (pass/fail per disk, raw counter values) is recorded in the plan's SUMMARY.md as the ZFS-01 evidence record.
  </acceptance_criteria>
  <resume-signal>Type "approved" to run the SMART gate on both disks.</resume-signal>
  <done>Both approved disks pass the short SMART health test gate; the phase may proceed to Plan 13-02, or halts here with a documented failure.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 0.5: Create the source inventory manifest</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-0.5</read_first>
  <decision>Approve writing a manifest of `/mnt/media`'s current state (path, type, size, ownership, permissions, mtime, inode metadata, xattrs) to a location outside `/mnt/media` and outside the future staging dataset.</decision>
  <context>
    Mutation class: read-only except for writing the task-owned manifest file. This manifest is the baseline against which Plan 13-04's gate 3.3 sampled verification, and Plan 13-06's restore confirmation, both compare. It must survive the multi-day outage — store it on ser8 under `/persist/zfs-migration/` (impermanence-persisted, survives reboots) and copy a summary into `.planning/phases/13-zfs-mirror-migration/evidence/` for the repo-side audit trail.
  </context>
  <options>
    <option id="approve"><name>Write the manifest</name><pros>Establishes the frozen baseline every later verification step compares against.</pros><cons>None — read-only against media, writes only to the new manifest location.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks Plan 13-04's verification gate, which needs this baseline.</cons></option>
  </options>
  <action>
    After approval, over SSH: `mkdir -p /persist/zfs-migration`; run `find /mnt/media -xdev -printf '%y\t%s\t%U\t%G\t%m\t%T@\t%i\t%p\n'` (type, size, uid, gid, mode, mtime, inode, path) redirected to `/persist/zfs-migration/media-inventory-preflight.tsv`; record total regular-file count (`find /mnt/media -xdev -type f | wc -l`) and total apparent bytes (`du -sb --apparent-size /mnt/media`); do not store secrets or API keys. Copy the count/byte-total summary (not the full per-file manifest) into `.planning/phases/13-zfs-mirror-migration/evidence/step-0.5-source-inventory-summary.md` in the repo, and commit it.
  </action>
  <verify>
    <automated>ssh root@ser8 'test -s /persist/zfs-migration/media-inventory-preflight.tsv && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `/persist/zfs-migration/media-inventory-preflight.tsv` exists on ser8, is non-empty, and its total byte column reconciles with `du -sb --apparent-size /mnt/media` within rounding.
    - The manifest location is outside `/mnt/media` and outside any staging path.
    - `.planning/phases/13-zfs-mirror-migration/evidence/step-0.5-source-inventory-summary.md` exists in the repo with file count and byte total.
  </acceptance_criteria>
  <resume-signal>Type "approved" to write the source inventory manifest.</resume-signal>
  <done>A frozen-baseline manifest of /mnt/media exists on ser8 and survives the outage; its summary is committed to the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| SSH session (sandboxed executor) -> ser8 root shell | Every command in this plan crosses this boundary; Phase 12 proved passwordless sudo works here |
| Migration doc contract -> future fresh sessions | The doc is the sole source of truth a stale/fresh session reads cold with no chat history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | Step 0.4 SMART gate rationalized past a failing result | high | mitigate | Task 3 action explicitly forbids proceeding on any nonzero counter or failed self-test; result recorded in SUMMARY.md as an evidence record, not a judgment call |
| T-13-02 | Repudiation | Doc amendment silently weakens Approval Contract / Safety Rules language | high | mitigate | Task 1 acceptance criteria requires `git diff` to show zero changes in those exact sections |
| T-13-03 | Information Disclosure | Source inventory manifest captures secrets/credentials from file paths | low | accept | `/mnt/media` contains only media library files; no credential files live under this mount; explicit "do not store secrets" instruction in Task 4 |
| T-13-04 | Denial of Service | SMART self-test run concurrently on both disks stresses shared enclosure/controller | medium | mitigate | Task 3 action runs self-tests sequentially per the doc's existing thermal-safety note |
</threat_model>

<verification>
Run `make check` after committing the doc/requirements/roadmap edits. Confirm `git diff` on the migration doc shows zero changes to Approval Contract, Safety Rules, and Approved Disk Inventory sections. Confirm SMART gate result and source-inventory summary are both recorded in this plan's SUMMARY.md before Plan 13-02 begins.
</verification>

<success_criteria>
The migration doc, ZFS-01/ZFS-03, and ROADMAP criterion 1 all describe the post-Phase-12, D-04/D-08/D-21-amended world. Both approved disks are confirmed healthy via the short SMART gate. A frozen source-inventory baseline exists on ser8 and in the repo's evidence trail.
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback)

ZFS-01 was classified `unclassified` by the automated edge-case probe (no SPEC.md exists for this phase; edge classification does not map cleanly onto an already-fully-specified infrastructure runbook requirement). This is not a gap: ZFS-01's behavior is fully pinned by CONTEXT.md D-04 (short SMART test scope) and D-07/D-09 (sampled verification gate semantics), both implemented as explicit acceptance criteria in Tasks 3 and (for the staging/verification half) Plans 13-03/13-04. Reviewed and accepted as covered by the locked decisions rather than by an automated edge-classification.
</flagged_assumptions>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification as pre-existing):

- `.planning/phases/13-zfs-mirror-migration/evidence/step-0.5-source-inventory-summary.md` (new file, repo)
- `/persist/zfs-migration/` (new directory, ser8)
- `/persist/zfs-migration/media-inventory-preflight.tsv` (new file, ser8)

<output>
Create `.planning/phases/13-zfs-mirror-migration/13-01-SUMMARY.md` when done, documenting this plan's rollback-matrix row: "Before Step 3.1 (now folded into Plan 13-03's freeze) — Original ext4 plus MergerFS — Continue normal operation" (per the doc's Rollback Matrix, unchanged at this stage).
</output>
