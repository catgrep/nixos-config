---
phase: 11-homebox-actual-budget-and-donetick
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - modules/household/default.nix
  - modules/household/actual.nix
  - hosts/ser8/household/default.nix
  - hosts/ser8/household/actual.nix
  - hosts/ser8/impermanence.nix
  - modules/gateway/Caddyfile
  - scripts/validation/test-actual-module.sh
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "services.actual resolves the 26.05 native module (user/group are nullOr-typed options, confirmed by the already-existing scripts/validation/test-actual-module.sh from Phase 9) with user=\"actual\" group=\"actual\" set explicitly, not DynamicUser."
    - "Actual loads at https://actual.shad-bangus.ts.net through firebat's Caddy tsnet vhost (curl from firebat returns an accepted status)."
    - "GET /account/needs-bootstrap returns bootstrapped:false before Task 2 and bootstrapped:true after it, and a second /account/bootstrap call returns already-bootstrapped (genuinely one-way)."
    - "/var/lib/actual is a real directory owned actual:actual mode 0700, and /persist/var/lib/actual exists with matching ownership (ACT-01)."
    - "If the tsnet vhost or DNS is unreachable, the endpoint check fails on a non-accepted status rather than reporting a false pass on empty curl output."
    - statement: "Hostname comparisons in the offline gate use exact ASCII string equality against the literal actual.shad-bangus.ts.net value, not case-insensitive or Unicode-normalized matching."
      verification: backstop
  artifacts:
    - modules/household/actual.nix
    - hosts/ser8/household/actual.nix
  key_links:
    - "modules/household/actual.nix users.users.actual -> services.actual.user (static system user, mirrors Mealie's PD-01)"
    - "modules/gateway/Caddyfile bind tailscale/actual -> reverse_proxy 192.168.68.65:3000"
  prohibitions:
    - statement: "Must NOT commit, log to a tracked file, or print into any .planning artifact the Actual server password -- it is generated by Task 2, shown once in terminal output, and stored only in the household's own password manager."
      status: active
      verification: flagged-unverified
    - statement: "SEC-02 (no secret for any household service in the Nix store) is Deferred and not re-verified here; this plan adds no sops secret (the server password lives only in account.sqlite's password hash, set at runtime via the API, never declared in Nix)."
      status: active
      verification: flagged-unverified
---

<objective>
Bring Actual Budget up on ser8, second of the three Phase 11 apps, on the Phase 10 household pattern, and set its server password. Actual needs no new packaging or sops secret -- the 26.05 native module is already proven to resolve correctly by Phase 9's `scripts/validation/test-actual-module.sh` -- so this plan's real work is the static-user override (repeating Mealie's PD-01 pattern, since `services.actual` defaults to `DynamicUser = true` exactly like Mealie's did), the gateway vhost, and the server-password bootstrap. The budget-file creation itself (a browser-only, one-time household decision) is a separate plan (11-03) so a `checkpoint:human-action` task never shares a plan with autonomous implementation tasks.

Purpose: ACT-01/03 and the first half of ACT-02.

Output: two new module files, impermanence entries, a Caddy vhost, three new offline-gate assertions, and a live, verified server password.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/11-homebox-actual-budget-and-donetick/11-01-SUMMARY.md
</context>

<flagged_assumptions>
| Requirement | Probe | Why unresolved |
|---|---|---|
| ACT-01 | unclassified -- review manually | Data-dir persistence has no input-domain edge; covered by the `truths` entry on `/var/lib/actual` ownership. |
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Actual through every layer -- static user, persistence, gateway, both hosts activated</name>
  <files>modules/household/default.nix, modules/household/actual.nix, hosts/ser8/household/default.nix, hosts/ser8/household/actual.nix, hosts/ser8/impermanence.nix, modules/gateway/Caddyfile, scripts/validation/test-actual-module.sh</files>
  <read_first>
    - modules/household/mealie.nix (PD-01's static-user override shape: `users.groups`, `users.users` with `isSystemUser`, `DynamicUser = lib.mkForce false`, explicit `Group`)
    - hosts/ser8/household/mealie.nix (host-policy shape, for structural reference)
    - scripts/validation/test-actual-module.sh (ALREADY EXISTS from Phase 9 -- extend it, do not create a new file; read its full current content and header comment before editing)
    - hosts/ser8/impermanence.nix (directories list and tmpfiles rules around the mealie/postgresql entries -- Actual's `StateDirectoryMode` is `0700`, not Mealie's `0750`, so match that mode exactly)
    - modules/gateway/Caddyfile (the `# -- Household services` block, now containing `mealie` and `homebox`)
  </read_first>
  <action>
Wire Actual through every layer. If any `services.actual` option name is unclear, resolve its declaring file via `nix eval --json '.#nixosConfigurations.ser8.options.services.actual.enable.declarations'` and read it -- do not guess option paths.

Create `modules/household/actual.nix` with the `{ config, lib, ... }:` signature, mirroring `mealie.nix`'s PD-01 shape exactly: `services.actual` has NO auto-create-user branch (unlike Homebox), so declare `users.groups.actual = lib.mkIf config.services.actual.enable { };` and `users.users.actual = lib.mkIf config.services.actual.enable { isSystemUser = true; group = "actual"; home = "/var/lib/actual"; description = "Actual Budget"; };`. Set `services.actual.enable = lib.mkDefault false;` and `services.actual.openFirewall = lib.mkDefault false;` (host policy turns it on). Every stanza guarded by `lib.mkIf config.services.actual.enable`.

Update `modules/household/default.nix`: add `./actual.nix` after `./homebox.nix`.

Create `hosts/ser8/household/actual.nix` with the `{ ... }:` signature: `services.actual.enable = true;`, `services.actual.user = "actual";`, `services.actual.group = "actual";`, `services.actual.openFirewall = true;`. Leave `services.actual.settings.hostname`, `.port` (3000), `.dataDir` (`/var/lib/actual`) at their module defaults -- no conflicts exist on ser8's port map. Do not set anything under `settings` that isn't a documented option; the freeform type accepts arbitrary JSON but Actual's server only reads what its own config schema defines.

Update `hosts/ser8/household/default.nix`: add `./actual.nix`.

Edit `hosts/ser8/impermanence.nix`: add `"/var/lib/actual"` to the persistence directories list, and add a tmpfiles rule `d /persist/var/lib/actual 0700 actual actual -` -- 0700, matching `StateDirectoryMode = "0700"` the actual.nix module sets unconditionally (do not copy Mealie's 0750; Actual's module hardcodes a stricter mode and a 0750 rule would fight it on every start).

Edit `modules/gateway/Caddyfile`: append a vhost after the `homebox` entry: `https://actual.shad-bangus.ts.net`, `bind tailscale/actual`, `reverse_proxy 192.168.68.65:3000`, same `log tailscale { level DEBUG }` stanza, no `header_up` pair.

Extend the EXISTING `scripts/validation/test-actual-module.sh` (do not create a new file) with three more `check_eval` assertions, following its existing `check_eval label expr expected` helper: `.config.services.actual.user` = `"actual"`, `.config.services.actual.group` = `"actual"`, `.config.services.actual.openFirewall` = `true`. Update its header comment to note these new ACT-01-scoped assertions alongside the existing FOUND/Phase-9 module-resolution ones.

Run `make fmt` and `make fmt-caddy`. Commit as separate logical changes: module + host policy, then persistence, then the Caddy vhost, then the extended validation script.

Activate: `make switch-ser8`, then `make switch-firebat`.
  </action>
  <verify>
    <automated>nix build .#nixosConfigurations.ser8.config.system.build.toplevel --dry-run && nix build .#nixosConfigurations.firebat.config.system.build.toplevel --dry-run && ./scripts/validation/test-actual-module.sh && caddy adapt --config ./modules/gateway/Caddyfile --adapter caddyfile 2>/dev/null | jq -e '[.apps.http.servers[].listen[]] | index("tailscale/actual:443")'</automated>
  </verify>
  <acceptance_criteria>
    - `nix eval --json '.#nixosConfigurations.ser8.config.services.actual.user'` outputs `"actual"`
    - `nix eval --json '.#nixosConfigurations.ser8.config.services.actual.group'` outputs `"actual"`
    - `nix eval --json '.#nixosConfigurations.ser8.config.systemd.services.actual.serviceConfig.DynamicUser'` outputs `false`
    - `nix eval --json '.#nixosConfigurations.ser8.config.networking.firewall.allowedTCPPorts' | jq -e 'index(3000)'` exits 0
    - `nix eval --json '.#nixosConfigurations.ser8.config.environment.persistence."/persist".directories' | jq -e 'map(if type=="object" then .directory else . end) | index("/var/lib/actual")'` exits 0
    - `caddy adapt --config ./modules/gateway/Caddyfile | jq -e '[.apps.http.servers[].listen[]] | index("tailscale/actual:443")'` exits 0
    - `./scripts/validation/test-actual-module.sh` exits 0 with six `ok:` lines (three original plus three new)
    - `curl -s http://127.0.0.1:3000/account/needs-bootstrap` on ser8 returns `{"status":"ok","data":{"bootstrapped":false,...}}`
    - `curl -s https://actual.shad-bangus.ts.net/account/needs-bootstrap` from firebat returns the same `bootstrapped:false` shape
  </acceptance_criteria>
  <reversibility rating="reversible">Enabling a new service and adding a gateway vhost are both easily disabled; no budget data exists yet to make this one-way.</reversibility>
  <done>Actual evaluates and builds on ser8 and firebat with a static `actual:actual` user (no DynamicUser), the extended offline gate passes, the Caddy config carries the `tailscale/actual:443` listener, and `/account/needs-bootstrap` answers `bootstrapped:false` both on loopback and through the tsnet vhost.</done>
</task>

<task type="auto">
  <name>Task 2: Set the Actual server password</name>
  <files>none (runtime API call only; no repository file changes)</files>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-06-SUMMARY.md (credential-fidelity posture: generated once, never committed, verified via the app's own API)
  </read_first>
  <action>
Generate a strong password with `openssl rand -base64 24`. Bootstrap the server: `POST https://actual.shad-bangus.ts.net/account/bootstrap` with JSON body `{"password":"<generated>"}`. Actual's `isValidPassword` accepts any non-empty string -- there is no length/complexity floor to satisfy, but generate a strong value anyway since this password is shared by the whole household and gates access to financial data.

Verify the bootstrap took effect: `GET /account/needs-bootstrap` must now return `bootstrapped:true`. Verify the password actually works: `POST /account/login` with the correct password returns 200 with a non-empty `.data.token`; the same call with an incorrect password returns a non-200/error response.

Display the generated password once in this task's terminal output for the operator to store in the household's password manager. Do not place it in the SUMMARY, in a script, or in any committed file.
  </action>
  <verify>
    <automated>curl -s https://actual.shad-bangus.ts.net/account/needs-bootstrap | jq -e '.data.bootstrapped == true'</automated>
  </verify>
  <acceptance_criteria>
    - `POST /account/bootstrap` with a password returns `{"status":"ok",...}`, not an `error` status
    - `GET /account/needs-bootstrap` before this task returns `bootstrapped:false`; after it, `bootstrapped:true`
    - `POST /account/login` with the correct password returns 200 with a non-empty token
    - `POST /account/login` with an incorrect password does not return 200
    - `POST /account/bootstrap` called a second time returns `{"status":"error","reason":"already-bootstrapped"}` (proves this is a genuinely one-way action, not silently repeatable)
    - the generated password appears in no file this task wrote, in no commit, and in no `.planning/` artifact
  </acceptance_criteria>
  <reversibility rating="reversible">The `/account/bootstrap` endpoint itself only accepts one call, but the decision it implements -- what the password is -- carries no migration or broken-contract cost: changing it afterward is an ordinary `/account/login` plus a settings-UI password change, the same cost as rotating any other application password.</reversibility>
  <done>The server password is live and verified working via both a successful and a failing login attempt; the shipped never-bootstrapped state is provably gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet client -> firebat tsnet listener | Untrusted household device traffic terminates TLS at the caddy-tailscale node |
| firebat -> ser8 tcp/3000 | Plaintext HTTP over the LAN, same established pattern as Mealie/Homebox |
| account.sqlite | Holds the hashed server password -- the single point of truth for the login-works/login-fails proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Spoofing | Actual server password | high | mitigate | Generated via `openssl rand -base64 24`, set via `/account/bootstrap`, verified by both a successful and a failing login attempt; never committed |
| T-11-08 | Repudiation | `/account/bootstrap` callable more than once | medium | mitigate | Verified as a genuinely one-way action (`already-bootstrapped` on retry) in Task 2's acceptance criteria |
| T-11-09 | Elevation of Privilege | ser8 tcp/3000 open on the LAN | medium | transfer | Same established LAN-plaintext pattern as every other household/media service; SEC-01's negative-access proof is Deferred |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs; `services.actual.package` resolves from the pinned `nixpkgs` flake input |
</threat_model>

<artifacts_this_phase_produces>
**Directories:** none new

**Files:** `modules/household/actual.nix`, `hosts/ser8/household/actual.nix`

**NixOS option paths newly set on ser8:** `services.actual.enable`, `services.actual.user`, `services.actual.group`, `services.actual.openFirewall`

**systemd units newly present on ser8:** `actual.service`

**System users and groups newly declared:** user `actual`, group `actual`

**Persisted paths newly declared:** `/persist/var/lib/actual`

**Gateway route newly declared:** the Caddy vhost `https://actual.shad-bangus.ts.net` bound to the tsnet listener `tailscale/actual`

**Runtime state newly created (not Nix-managed):** the Actual server password hash in `account.sqlite`

**Existing symbols extended:** `modules/household/default.nix` and `hosts/ser8/household/default.nix` imports gain `./actual.nix`; `hosts/ser8/impermanence.nix` gains one directory entry and one tmpfiles rule; `modules/gateway/Caddyfile` gains one vhost block; `scripts/validation/test-actual-module.sh` (existing file from Phase 9) gains three assertions
</artifacts_this_phase_produces>

<verification>
- `make check` exits 0 across all four hosts
- `./scripts/validation/test-actual-module.sh` exits 0 with six `ok:` lines
- Actual answers at both the loopback port and the tsnet vhost with `bootstrapped:true`
- login with the correct password succeeds; login with an incorrect password fails
</verification>

<success_criteria>
Actual Budget is live at `https://actual.shad-bangus.ts.net` with a static system user, persisted state, and a verified server password. The budget-file creation itself is 11-03.
</success_criteria>

<output>
Create `.planning/phases/11-homebox-actual-budget-and-donetick/11-02-SUMMARY.md` when done.
</output>
