---
phase: 12-fleet-repair
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-03"]
files_modified:
  - .planning/PROJECT.md
autonomous: false
requirements: [FLEET-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Archive-before-delete ordering: the pre-deletion archive of /var/lib/qbittorrent and /var/lib/wgnord is created and verified non-empty BEFORE any live-state deletion; an interrupted teardown leaves either intact live state or verified archived state, never neither."
  artifacts:
    - .planning/PROJECT.md
  key_links:
    - "plan 12-03's orchestration.nix/permissions.nix/configuration.nix edits -> make switch-ser8 in this plan's Task 1 -> live qbittorrent-nox.service and wgnord.service actually stop existing on ser8"
  prohibitions:
    - statement: "The archive-then-delete step must not run a recursive delete against any path other than the exact two archived directories (/var/lib/qbittorrent, /var/lib/wgnord) — never a wildcard, never /mnt/media, /persist, or any pool mountpoint."
      status: unverified
      flagged: true
    - statement: "Must not discard or skip archiving live qBittorrent/wgnord state before deletion — the archive must be created and verified non-empty before any deletion command runs."
      status: unverified
      flagged: true
---

<objective>
Deploy plan 12-03's code removal to ser8, archive the live qBittorrent/wgnord state before deleting
it (D-03), remove the now-orphaned NordVPN/qBittorrent SOPS secrets, and record the FLEET-01
retirement decision in PROJECT.md Key Decisions (D-06).

Purpose: FLEET-01 requires the live state to actually be gone from ser8, not just the code —
D-03's archive-then-delete mechanism is the locked decision; this plan implements it with a human
checkpoint verifying the archive before the irreversible deletion.
Output: qbittorrent-nox.service and wgnord.service gone from the running ser8 system; a verified
archive of their prior state on the backup pool; the deprecated secrets removed; a new PROJECT.md
Key Decisions row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-fleet-repair/12-CONTEXT.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy the FLEET-01 removal to ser8</name>
  <precondition>plan 12-03's `make dry-activate-ser8` and `make check` both passed (Task 1/Task 3 of that plan).</precondition>
  <files></files>
  <read_first>.planning/phases/12-fleet-repair/12-CONTEXT.md</read_first>
  <action>
    Run `make test-ser8` (temporary activation per the safety ladder). Confirm the qBittorrent/NordVPN units are gone from the running system: `ssh bdhill@192.168.68.65 'systemctl status qbittorrent-nox.service'` and `ssh bdhill@192.168.68.65 'systemctl status wgnord.service'` both report "could not be found" (unit no longer exists), not "inactive" or "failed" (which would mean the unit still exists but isn't running).
    Confirm no restart-loop residue: `ssh bdhill@192.168.68.65 'journalctl --since "-5 minutes" | grep -ci wgnord'` returns `0`.
    Run `./scripts/smoketests/media/all.sh ser8` directly (not via `make smoketests-ser8`, to avoid pulling in unrelated household/frigate/home-assistant suites) and confirm it passes now that plan 12-03's edit removed the qBittorrent entries — this is the first live proof that the edited smoketest script itself runs clean against the deployed change.
    Run `make switch-ser8` to persist the removal as the boot default.
  </action>
  <verify>
    <automated>ssh bdhill@192.168.68.65 'systemctl status qbittorrent-nox.service' 2>&1 | grep -qi "could not be found"</automated>
  </verify>
  <done>ser8 is switched onto the generation with qBittorrent/NordVPN fully removed; both units report as nonexistent; no wgnord restart-loop activity in recent logs; the edited media smoketest passes.</done>
  <acceptance_criteria>
    - `ssh bdhill@192.168.68.65 'systemctl status qbittorrent-nox.service'` reports the unit could not be found.
    - `ssh bdhill@192.168.68.65 'systemctl status wgnord.service'` reports the unit could not be found.
    - `ssh bdhill@192.168.68.65 'journalctl --since "-5 minutes" | grep -ci wgnord'` returns `0`.
    - `./scripts/smoketests/media/all.sh ser8` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Archive live qBittorrent/wgnord state (D-03, pre-deletion)</name>
  <precondition>Task 1's switch-ser8 completed and both units are confirmed gone.</precondition>
  <files></files>
  <read_first>.planning/phases/12-fleet-repair/12-CONTEXT.md</read_first>
  <action>
    On ser8, create an archive directory on the durable backup pool (not /mnt/media, not /persist): `ssh bdhill@192.168.68.65 'sudo mkdir -p /mnt/backups/phase12-fleet-repair-archive'`.
    Archive each directory to its own tarball with today's date: `ssh bdhill@192.168.68.65 'sudo tar czf /mnt/backups/phase12-fleet-repair-archive/qbittorrent-$(date +%Y%m%d).tar.gz -C /var/lib qbittorrent'` and `ssh bdhill@192.168.68.65 'sudo tar czf /mnt/backups/phase12-fleet-repair-archive/wgnord-$(date +%Y%m%d).tar.gz -C /var/lib wgnord'`.
    Verify both archives are non-empty and list expected top-level content: `ssh bdhill@192.168.68.65 'tar tzf /mnt/backups/phase12-fleet-repair-archive/qbittorrent-*.tar.gz | wc -l'` and the same for wgnord — both must report a nonzero file count.
  </action>
  <verify>
    <automated>ssh bdhill@192.168.68.65 'test -s /mnt/backups/phase12-fleet-repair-archive/qbittorrent-*.tar.gz && test -s /mnt/backups/phase12-fleet-repair-archive/wgnord-*.tar.gz'</automated>
  </verify>
  <done>Both /var/lib/qbittorrent and /var/lib/wgnord are archived to /mnt/backups/phase12-fleet-repair-archive/ on ser8's durable backup pool, and both archives are confirmed non-empty.</done>
  <acceptance_criteria>
    - `ssh bdhill@192.168.68.65 'tar tzf /mnt/backups/phase12-fleet-repair-archive/qbittorrent-*.tar.gz | wc -l'` reports a number greater than 0.
    - `ssh bdhill@192.168.68.65 'tar tzf /mnt/backups/phase12-fleet-repair-archive/wgnord-*.tar.gz | wc -l'` reports a number greater than 0.
    - The live directories `/var/lib/qbittorrent` and `/var/lib/wgnord` still exist on ser8 at this point (deletion has not yet occurred).
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: confirm the archive before irreversible deletion (D-03)</name>
  <decision>Proceed with deleting the live /var/lib/qbittorrent and /var/lib/wgnord directories on ser8, now that both are archived to /mnt/backups/phase12-fleet-repair-archive/.</decision>
  <context>
    D-03 already locked the archive-then-delete mechanism (this checkpoint does not re-ask whether to retire qBittorrent/wgnord or whether to use archive-then-delete). What is being confirmed here is narrower and genuinely undecided until now: whether THIS specific archive, taken in Task 2, is complete and trustworthy enough that the next task's deletion is safe — qBittorrent session/history state does not regenerate once discarded (reversibility: one-way), and the archive is the only recovery path after this point.
    Run `ssh bdhill@192.168.68.65 'ls -lh /mnt/backups/phase12-fleet-repair-archive/'` to see both archive file sizes, and `ssh bdhill@192.168.68.65 'tar tzf /mnt/backups/phase12-fleet-repair-archive/qbittorrent-*.tar.gz | head -20'` (and the equivalent for wgnord) to spot-check the contents look like real state (config files, BT_backup, session data for qbittorrent; wgnord.conf/template.conf for wgnord).
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with deletion</name>
      <pros>Archive verified non-empty and contains recognizable state; live directories can be safely removed.</pros>
      <cons>Irreversible once run — this is the last checkpoint before Task 3's `rm -rf`.</cons>
    </option>
    <option id="abort">
      <name>Do not proceed — investigate the archive</name>
      <pros>Avoids deleting live state on top of an incomplete or suspect archive.</pros>
      <cons>Delays FLEET-01 closeout until the archive is redone or the discrepancy is understood.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed (Task 3 runs the deletion), or abort (describe the issue found with the archive).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete archived live state, remove secrets, and record the PROJECT.md decision (D-03/D-06)</name>
  <reversibility rating="one-way">D-03: qBittorrent session/history state does not regenerate once discarded; the archive taken in Task 2 is the only recovery path after this task runs.</reversibility>
  <precondition>The archive checkpoint above was approved.</precondition>
  <files>.planning/PROJECT.md</files>
  <read_first>.planning/PROJECT.md, .planning/phases/12-fleet-repair/12-CONTEXT.md</read_first>
  <action>
    On ser8, delete exactly the two archived directories and nothing else: `ssh bdhill@192.168.68.65 'sudo rm -rf /var/lib/qbittorrent /var/lib/wgnord'` — these two exact absolute paths only, no wildcard, no parent-directory target.
    Remove the now-orphaned SOPS secrets from secrets/ser8.yaml via the Makefile target (never edit the encrypted file directly, never print decrypted values): `make sops-edit-ser8`, and within the interactive editor remove the `nordvpn_access_token`, `qbittorrent_admin_password`, and `qbittorrent_admin_password_hash` keys.
    Add a new row to .planning/PROJECT.md's Key Decisions table (after the FLEET-03 row added by plan 12-01, so both FLEET-01 and FLEET-03 decisions from this phase sit adjacent per D-06) recording: torrents retired, download path is usenet-only (SABnzbd/NZBGet), the NordVPN + qBittorrent stack removed entirely from code and state via archive-then-delete, citing D-01/D-02/D-03/D-06.
  </action>
  <verify>
    <automated>ssh bdhill@192.168.68.65 'test -d /var/lib/qbittorrent || test -d /var/lib/wgnord' && echo STILL_PRESENT || echo REMOVED</automated>
  </verify>
  <done>/var/lib/qbittorrent and /var/lib/wgnord no longer exist on ser8; the three orphaned SOPS secrets are removed from secrets/ser8.yaml; PROJECT.md records the FLEET-01 retirement decision adjacent to the FLEET-03 row.</done>
  <acceptance_criteria>
    - `ssh bdhill@192.168.68.65 'test -d /var/lib/qbittorrent'` fails (directory gone).
    - `ssh bdhill@192.168.68.65 'test -d /var/lib/wgnord'` fails (directory gone).
    - `sops -d secrets/ser8.yaml | grep -c 'nordvpn_access_token\|qbittorrent_admin_password'` returns `0` (run locally against the decrypted content only to verify absence of the keys — never print the full decrypted output).
    - `.planning/PROJECT.md` contains a new Key Decisions row referencing FLEET-01 and citing D-01/D-03.
    - The archive at `/mnt/backups/phase12-fleet-repair-archive/` on ser8 is untouched and still present after this task.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> ser8 (SSH, root-equivalent via sudo) | Deployment and destructive deletion commands execute with root privileges on a live production host. |
| Human checkpoint -> irreversible deletion | The archive-verification checkpoint is the last point at which a bad archive can be caught before data loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-10 | Tampering / Data Loss | `rm -rf` against the wrong path (typo, glob) | critical | mitigate | Hardcoded exact absolute paths only (`/var/lib/qbittorrent`, `/var/lib/wgnord`), never a wildcard; human checkpoint reviews the archive before this task runs. |
| T-12-11 | Repudiation / Data Loss | Deleting live state before the archive is verified | high | mitigate | Archive-then-verify-then-checkpoint-then-delete ordering enforced by task sequence and `<precondition>` on Task 3. |
| T-12-12 | Information Disclosure | Decrypted SOPS secret values printed during removal | medium | mitigate | Secrets removed only via the interactive `make sops-edit-ser8` editor; verification greps for key absence, never dumps full decrypted content. |
</threat_model>

<verification>
- Task 1: both units report "could not be found" on ser8 after switch.
- Task 2: both archives non-empty before any deletion.
- Checkpoint: human confirms archive contents before Task 3 runs.
- Task 3: both live directories confirmed gone; secrets removed; PROJECT.md updated.
</verification>

<success_criteria>
The NordVPN + qBittorrent stack is gone from ser8's live running state (not just the repo), the prior
state is safely archived first, and the retirement decision is recorded in PROJECT.md Key Decisions —
completing ROADMAP Phase 12 success criterion 1 together with plan 12-03.
</success_criteria>

## Artifacts this phase produces (this plan)

- Live-only (ser8, no repo artifact): `/mnt/backups/phase12-fleet-repair-archive/qbittorrent-*.tar.gz`, `/mnt/backups/phase12-fleet-repair-archive/wgnord-*.tar.gz`.
- Deleted (live, ser8): `/var/lib/qbittorrent`, `/var/lib/wgnord`.
- Deleted (secrets, `secrets/ser8.yaml`): `nordvpn_access_token`, `qbittorrent_admin_password`, `qbittorrent_admin_password_hash`.
- New: one row in `.planning/PROJECT.md`'s Key Decisions table (FLEET-01 / D-01/D-02/D-03/D-06), adjacent to plan 12-01's FLEET-03 row.

<output>
Create `.planning/phases/12-fleet-repair/12-04-SUMMARY.md` when done
</output>
