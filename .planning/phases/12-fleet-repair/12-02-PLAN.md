---
phase: 12-fleet-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/media/sabnzbd.nix
  - .planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md
autonomous: true
requirements: [FLEET-02]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "sabnzbd.service reports active (running) on ser8 after the repair, and the firebat Caddy https_sabnzbd route (https://sabnzbd.shad-bangus.ts.net) returns a healthy HTTP response."
    - "The repair mechanism is a one-time scoped chown of /var/lib/sabnzbd to the declared identity, paired with a static uid/gid pin in modules/media/sabnzbd.nix — no standing tmpfiles re-chown rule is added (D-12)."
  artifacts:
    - modules/media/sabnzbd.nix
    - .planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md
  key_links:
    - "modules/media/sabnzbd.nix users.users.sabnzbd (static uid/gid pin) -> hosts/ser8/impermanence.nix tmpfiles rule 'd /persist/var/lib/sabnzbd 0755 sabnzbd media -' -> live /var/lib/sabnzbd ownership -> sabnzbd.service ExecStart -> firebat Caddyfile https_sabnzbd vhost"
  prohibitions:
    - statement: "Must not apply a standing/recurring re-chown mechanism (e.g. a tmpfiles rule) for /var/lib/sabnzbd — the fix is a one-time scoped chown paired with a static uid/gid pin (D-12), not an ongoing reconciliation rule."
      status: unverified
      flagged: true
    - statement: "Must not delete, truncate, or recreate SABnzbd's existing queue/history/admin/config data while repairing ownership — the fix re-owns the existing files in place only."
      status: unverified
      flagged: true

flagged_assumptions:
  - "Unresolved probe edge (FLEET-02, unclassified): the diagnosis in Task 1 may surface a root cause other than simple uid/gid drift (e.g. a config corruption, a permission issue unrelated to numeric ownership, or a different failure mode entirely). D-11 explicitly requires the repair to be gated on the diagnosis findings, not assumed in advance — if Task 1 surfaces something other than the known 38:194 ownership drift, Task 2's action must be adapted to address the actual finding rather than proceeding with the chown+pin plan as written. Never silently substitute the presumed fix for an undiagnosed one."
---

<objective>
Diagnose exactly how /var/lib/sabnzbd/admin came to be owned 38:194 (D-11, full diagnosis first, no
timeboxed shortcut), then repair it with a one-time scoped chown paired with a static uid/gid pin in
Nix so the drift cannot recur (D-12), and verify sabnzbd.service is active with the firebat
https_sabnzbd route healthy (D-13, verified manually/directly this once — the known-blind smoketest
itself is not fixed in this phase).

Purpose: FLEET-02 requires sabnzbd.service active and the gateway https_sabnzbd route healthy again,
via a durable Nix fix rather than a manual clear.
Output: A diagnosis evidence file; modules/media/sabnzbd.nix with a static uid/gid pin; sabnzbd.service
active on ser8.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-fleet-repair/12-CONTEXT.md
@modules/media/sabnzbd.nix
@modules/media/radarr.nix
@hosts/ser8/impermanence.nix
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Diagnose the sabnzbd 38:194 ownership drift (D-11, gates Task 2)</name>
  <files>.planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md</files>
  <read_first>modules/media/sabnzbd.nix, hosts/ser8/media/sabnzbd.nix, hosts/ser8/impermanence.nix</read_first>
  <action>
    Over SSH to ser8 (bdhill@192.168.68.65), gather the full picture before touching anything:
    `ls -lan /var/lib/sabnzbd/admin` (confirm the 38:194 ownership and which files/dirs are affected — all of /var/lib/sabnzbd or just admin/),
    `getent passwd sabnzbd; getent group sabnzbd` (sabnzbd's current live uid/gid — modules/media/sabnzbd.nix declares no explicit uid today, so NixOS auto-allocates one from the system range on each fresh account creation),
    `getent passwd 38; getent group 194` (what uid 38 / gid 194 map to on ser8 today, if anything — these numbers do not match any currently-declared media-stack identity, so identify whether they are stale from a prior nixpkgs channel's system-uid allocation, a leftover from an older module version, or something else),
    `systemctl status sabnzbd.service` and `journalctl -u sabnzbd -n 100 --no-pager` (the actual failure mode — permission denied on admin dir, or something else),
    and cross-reference against modules/media/sabnzbd.nix's current `lib.mkForce "media"` group override (the group is forced but the uid was never pinned, so a NixOS channel bump's shift in the auto-allocated system-uid range is the leading hypothesis — confirm or refute it against the generation history if `nix-env --list-generations` or `/nix/var/nix/profiles/system-*-link` evidence is available).
    Write the full findings — root cause, evidence commands run, and their output — to .planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md. State explicitly whether the root cause matches the "uid auto-allocation drifted across a channel bump" hypothesis or something else; Task 2's repair must be adapted to whatever this task actually finds (per this plan's flagged_assumptions — do not skip this task or assume the answer).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md && grep -qi "root cause" .planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md</automated>
  </verify>
  <done>sabnzbd-diagnosis.md documents the confirmed root cause of the 38:194 ownership, backed by command output, with an explicit statement of whether it matches the uid-drift hypothesis or something else.</done>
  <acceptance_criteria>
    - .planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md exists and contains the raw output of `ls -lan /var/lib/sabnzbd/admin`, `getent passwd sabnzbd`, and `journalctl -u sabnzbd` (or the relevant excerpt).
    - The file states a specific root cause conclusion, not just raw command output with no analysis.
    - No file under modules/media/ was modified in this task.
  </acceptance_criteria>
</task>

<task type="tracer" tdd="false">
  <name>Task 2: Repair — scoped chown, static uid/gid pin, deploy, and verify (D-12/D-13)</name>
  <precondition>Task 1's diagnosis (.planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md) is complete and states a root cause.</precondition>
  <files>modules/media/sabnzbd.nix</files>
  <read_first>modules/media/sabnzbd.nix, modules/media/radarr.nix, .planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md</read_first>
  <action>
    Using Task 1's findings, determine the correct target uid for sabnzbd (the uid currently reflected in `getent passwd sabnzbd` on ser8, i.e. the live-correct identity, not 38) and gid 992 (the media group's target gid per D-07/plan 12-01 — cite that value here rather than re-deriving it).
    Add explicit `uid = &lt;confirmed-value&gt;;` and `gid = 992;` fields to the `users.users.sabnzbd` block in modules/media/sabnzbd.nix (follow the same block shape already used by `lib.mkIf cfg.enable { group = lib.mkForce cfg.group; }` — add the two new fields alongside the existing `group` field, do not restructure the block). Before adding, run the same collision pre-flight as plan 12-01: `ssh bdhill@192.168.68.65 'getent passwd &lt;confirmed-uid&gt;'` must show only sabnzbd (or be unclaimed).
    Do NOT add a systemd.tmpfiles rule that re-chowns /var/lib/sabnzbd on every activation — hosts/ser8/impermanence.nix's existing `"d /persist/var/lib/sabnzbd 0755 sabnzbd media -"` line already references the service by name, not numeric id, and needs no edit (per this plan's prohibitions, no standing re-chown mechanism).
    Run `make dry-activate-ser8`, then `make test-ser8`. On ser8, run the one-time scoped chown against the existing data in place (do not delete/recreate): `ssh bdhill@192.168.68.65 'sudo chown -R &lt;confirmed-uid&gt;:992 /var/lib/sabnzbd'` (this single explicit path is the intentional exception to the "no blind chown" rule elsewhere in this phase — it is scoped to exactly this one service's own state directory, not the shared media tree).
    Run `make switch-ser8` to persist. Verify: `ssh bdhill@192.168.68.65 'systemctl is-active sabnzbd'` returns `active`, and `curl -sS -o /dev/null -w '%{http_code}' https://sabnzbd.shad-bangus.ts.net` returns 200 (or the SABnzbd login page's actual success code — confirm it is not a 5xx/502).
  </action>
  <verify>
    <automated>ssh bdhill@192.168.68.65 'systemctl is-active sabnzbd' | grep -qx active</automated>
  </verify>
  <done>modules/media/sabnzbd.nix pins a static uid/gid for sabnzbd, /var/lib/sabnzbd is chowned to the correct identity, sabnzbd.service is active on ser8, and the firebat https_sabnzbd route responds healthy.</done>
  <acceptance_criteria>
    - modules/media/sabnzbd.nix's `users.users.sabnzbd` block contains explicit `uid = ...;` and `gid = 992;` fields.
    - hosts/ser8/impermanence.nix has NO new chown/re-chown tmpfiles rule added for sabnzbd (`git diff hosts/ser8/impermanence.nix` shows no changes from this plan).
    - `ssh bdhill@192.168.68.65 'systemctl is-active sabnzbd'` returns `active`.
    - `curl -sS -o /dev/null -w '%{http_code}\n' https://sabnzbd.shad-bangus.ts.net` returns `200`.
    - `ssh bdhill@192.168.68.65 'stat -c "%u:%g" /var/lib/sabnzbd/admin'` matches the confirmed uid and `992`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> ser8 (SSH, root-equivalent via sudo) | Diagnosis and chown commands execute with root privileges on a live production host over SSH. |
| Static uid/gid pin -> fleet-wide user allocation | A hardcoded uid could collide with another system account allocated dynamically by another module. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | Diagnosis skipped, wrong fix applied | medium | mitigate | D-11 gate enforced via `<precondition>` on Task 2; diagnosis must state a root cause before repair proceeds. |
| T-12-05 | Denial of Service | Static uid/gid pin collides with another declared system account | high | mitigate | Pre-flight `getent passwd &lt;uid&gt;` collision check before pinning (Task 2). |
| T-12-06 | Repudiation | No record of why 38:194 occurred or what was changed | low | mitigate | Full diagnosis and repair evidence recorded in sabnzbd-diagnosis.md. |
</threat_model>

<verification>
- sabnzbd-diagnosis.md documents a confirmed root cause before any repair action.
- `systemctl is-active sabnzbd` on ser8 returns `active`.
- `https://sabnzbd.shad-bangus.ts.net` returns HTTP 200.
- modules/media/sabnzbd.nix pins a static uid/gid; no standing re-chown tmpfiles rule was added.
</verification>

<success_criteria>
sabnzbd.service is active and the firebat gateway's https_sabnzbd route is healthy, fixed durably in
Nix via a diagnosed root cause and a static identity pin rather than a manual one-off clear —
satisfying ROADMAP Phase 12 success criterion 2.
</success_criteria>

## Artifacts this phase produces (this plan)

- Modified: `modules/media/sabnzbd.nix` (adds explicit `uid`/`gid = 992` fields to `users.users.sabnzbd`).
- New: `.planning/phases/12-fleet-repair/evidence/sabnzbd-diagnosis.md`.
- Live-only: one-time chown of `/var/lib/sabnzbd` on ser8 (no repo artifact).

<output>
Create `.planning/phases/12-fleet-repair/12-02-SUMMARY.md` when done
</output>
