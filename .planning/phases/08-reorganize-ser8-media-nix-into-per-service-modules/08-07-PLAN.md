---
phase: 08-reorganize-ser8-media-nix-into-per-service-modules
plan: 07
type: execute
wave: 7
depends_on:
  - 08-06
files_modified:
  - modules/media/sonarr.nix
  - modules/media/radarr.nix
  - modules/media/prowlarr.nix
  - modules/media/qbittorrent.nix
  - modules/media/sabnzbd.nix
  - modules/media/nzbget.nix
  - modules/media/jellyfin.nix
  - modules/media/jellyfin-exporter.nix
autonomous: true
requirements:
  - N/A
must_haves:
  truths:
    - "Every remaining reusable media module has an explicit audit result."
    - "The final structure and evaluated behavior match the phase contract with only approved deltas."
    - "make check and make build-ser8 pass with zero repository-owned warnings and no live activation."
  artifacts:
    - path: modules/media/sonarr.nix
      provides: "Audited generic Sonarr provider"
    - path: modules/media/radarr.nix
      provides: "Audited generic Radarr provider"
    - path: modules/media/prowlarr.nix
      provides: "Audited generic Prowlarr provider"
    - path: modules/media/qbittorrent.nix
      provides: "Audited generic qBittorrent provider"
    - path: modules/media/sabnzbd.nix
      provides: "Audited generic SABnzbd provider"
    - path: modules/media/nzbget.nix
      provides: "Audited generic NZBGet provider"
    - path: modules/media/jellyfin.nix
      provides: "Audited generic Jellyfin provider"
    - path: modules/media/jellyfin-exporter.nix
      provides: "Audited generic Jellyfin exporter provider"
  key_links:
    - from: scripts/validation/check-ser8-media-parity.sh
      to: nixosConfigurations.ser8
      via: "final structural and normalized behavior gates"
      pattern: "check"
---

<objective>
Complete the remaining reusable-module audit and run the final Phase 8 acceptance gates.

Purpose: Close D-22 coverage and prove D-15 through D-18 across the finished tree.
Output: Fully audited reusable media modules and final automated evidence.
</objective>

<execution_context>
@/Users/bobby/.codex/gsd-core/workflows/execute-plan.md
@/Users/bobby/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_environment>
Enter the repository development shell with `make dev` before task actions and keep all automated verification inside that shell.
</execution_environment>

<context>
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-06-SUMMARY.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-VALIDATION.md
@scripts/validation/check-ser8-media-parity.sh
</context>

## Artifacts this phase produces

- Audited `modules/media/sabnzbd.nix` and `modules/media/nzbget.nix` generic providers.
- Audited `modules/media/jellyfin.nix` and `modules/media/jellyfin-exporter.nix` generic providers.
- Audited `modules/media/sonarr.nix`, `modules/media/radarr.nix`, `modules/media/prowlarr.nix`, and `modules/media/qbittorrent.nix` generic providers.
- Final validation evidence recorded in `08-07-SUMMARY.md` for parity, `make check`, and `make build-ser8`.

<tasks>

<task type="auto">
  <name>Task 1: Audit Arr and qBittorrent reusable implementations</name>
  <files>modules/media/sonarr.nix, modules/media/radarr.nix, modules/media/prowlarr.nix, modules/media/qbittorrent.nix</files>
  <read_first>
    - modules/media/sonarr.nix
    - modules/media/radarr.nix
    - modules/media/prowlarr.nix
    - modules/media/qbittorrent.nix
    - hosts/ser8/media/sonarr.nix
    - hosts/ser8/media/radarr.nix
    - hosts/ser8/media/prowlarr.nix
    - hosts/ser8/media/qbittorrent.nix
    - scripts/validation/ser8-media-projection.nix
  </read_first>
  <action>
Audit each named reusable module individually under D-22. Remove only unused module arguments, commented-out restrictions, obsolete implementation fragments, or other code proven unreachable by the active import graph. Preserve active options, users/groups, packages, overlays, systemd hardening, network namespace behavior, firewall rules, ports, and service definitions. Do not introduce shared abstractions or move host settings back into reusable modules. Use the corresponding host module and evaluated projection to prove every retained boundary.
  </action>
  <verify>
    <automated>nixfmt --check modules/media/sonarr.nix modules/media/radarr.nix modules/media/prowlarr.nix modules/media/qbittorrent.nix &amp;&amp; statix check modules/media &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - Each file has an explicit audit result, including no-change when every line is active.
    - No commented-out implementation or unused module argument remains.
    - Active accounts, package selections, unit hardening, VPN namespace, and firewall behavior equal baseline.
  </acceptance_criteria>
  <done>The Arr and qBittorrent generic providers contain only active, behavior-equivalent code.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the remaining download and Jellyfin providers</name>
  <files>modules/media/sabnzbd.nix, modules/media/nzbget.nix, modules/media/jellyfin.nix, modules/media/jellyfin-exporter.nix</files>
  <read_first>
    - modules/media/sabnzbd.nix
    - modules/media/nzbget.nix
    - modules/media/jellyfin.nix
    - modules/media/jellyfin-exporter.nix
    - hosts/ser8/media/sabnzbd.nix
    - hosts/ser8/media/nzbget.nix
    - hosts/ser8/media/jellyfin.nix
    - scripts/validation/ser8-media-projection.nix
  </read_first>
  <action>
Audit SABnzbd, NZBGet, Jellyfin, and Jellyfin exporter under D-22. Remove only unused arguments, commented-out implementation, or code proven unreachable by the active import graph. Preserve every active package override, account, service option, environment value, hardening control, network/firewall rule, port, wrapper, and the typed exporter option boundary from Plan 04. Keep shared credential policy in the host SABnzbd module, do not rename or split credential names, and confirm no household identity or host-secret dependency returns to reusable Jellyfin code.
  </action>
  <verify>
    <automated>nixfmt --check modules/media/sabnzbd.nix modules/media/nzbget.nix modules/media/jellyfin.nix modules/media/jellyfin-exporter.nix &amp;&amp; statix check modules/media &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - All four providers have explicit audit outcomes and no genuinely dead/commented implementation.
    - Active accounts, packages, hardening, network, firewall, ports, and exporter options equal baseline.
    - Shared secret names remain unchanged and reusable Jellyfin code has no household identity or host-secret dependency.
  </acceptance_criteria>
  <done>The remaining download and Jellyfin providers are clean, generic, and behavior-equivalent.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final no-activation phase gate</name>
  <files>modules/media/sabnzbd.nix, modules/media/nzbget.nix, modules/media/jellyfin.nix, modules/media/jellyfin-exporter.nix</files>
  <read_first>
    - modules/media/sabnzbd.nix
    - modules/media/nzbget.nix
    - modules/media/jellyfin.nix
    - modules/media/jellyfin-exporter.nix
    - scripts/validation/check-ser8-media-parity.sh
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-VALIDATION.md
    - hosts/ser8/media/default.nix
    - modules/media/default.nix
  </read_first>
  <action>
Run the structural and normalized parity gates, then `make check` and `make build-ser8` per D-17. Treat every warning as a failure. Compare warning output to the Wave 1 inventory, fail on new warnings, and keep the pre-existing Home Manager release mismatch as a hard prerequisite that must be resolved outside this phase before success; do not suppress it or change dependency pins. Evaluate the final `config.system.build.toplevel`, compare it with `08-ser8-store-path-before.txt`, and record both paths plus the expected difference caused by D-14 in the plan summary; semantic parity, not equality, is the acceptance proof. Record all command results. Do not run `make test-ser8`, switch, apply, reboot, or any other live activation per D-18.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/*.nix modules/media/*.nix hosts/ser8/configuration.nix hosts/ser8/impermanence.nix flake.nix &amp;&amp; statix check hosts/ser8/media modules/media &amp;&amp; shellcheck hosts/ser8/media/*.sh scripts/validation/check-ser8-media-parity.sh &amp;&amp; shfmt -d hosts/ser8/media/*.sh scripts/validation/check-ser8-media-parity.sh &amp;&amp; scripts/validation/check-ser8-media-parity.sh structure &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json &amp;&amp; scripts/validation/check-ser8-media-parity.sh run-clean make check &amp;&amp; scripts/validation/check-ser8-media-parity.sh run-clean make build-ser8</automated>
  </verify>
  <acceptance_criteria>
    - Every current `modules/media/*.nix` file is covered by Plan 06 or Plan 07.
    - Structural and normalized behavior checks pass with only explicitly allowed deltas.
    - The before and after ser8 store paths are both recorded and their expected D-14 difference is explained.
    - `make check` and `make build-ser8` pass with zero repository-owned warnings.
    - No live ser8 activation command is run.
  </acceptance_criteria>
  <done>The complete Phase 8 automated acceptance suite passes without changing the running host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reusable providers to host policy | Final cleanup must retain generic security and runtime contracts. |
| Evaluated build to running ser8 | Build validation is allowed; activation is outside authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-18 | Elevation of Privilege | reusable service hardening | high | mitigate | Project and compare users, groups, hardening, namespace, firewall, and credential boundaries. |
| T-08-19 | Denial of Service | remaining provider audit | high | mitigate | Compare packages, users, services, hardening, network, and firewall after each audit. |
| T-08-20 | Information Disclosure | final parity artifacts | high | mitigate | Re-run fixture safety and never evaluate decrypted secret values. |
| T-08-21 | Tampering | live system boundary | medium | mitigate | Limit verification to evaluation/build commands and explicitly prohibit activation targets. |
| T-08-SC | Tampering | package installs | low | accept | No package installation occurs. |

## ASVS Level 1 Control Mapping

| ASVS family | Phase control | Verification evidence |
|-------------|---------------|-----------------------|
| V2 Authentication | Preserve hashed-password references, API-key runtime paths, and existing authentication settings. | Non-secret evaluated projection and exact-delta comparison. |
| V4 Access Control | Preserve users, groups, file modes, service hardening, firewall, and namespace boundaries. | Projection assertions plus focused formatter, linter, and build gates. |
| V5 Validation | Use typed NixOS options, structured JSON comparison, strict shell mode, and fail-fast command handling. | Parity runner, Nix evaluation, ShellCheck, statix, and shfmt. |
| V6 Stored Cryptography | Keep sops-nix placeholders and runtime secret paths; never serialize or decrypt secret values. | Fixture-safety checks and unchanged encrypted SOPS sources. |

**ASVS L1 execution gate:** Any critical or high-severity threat with disposition `mitigate` blocks task and plan completion until its cited automated verification passes. No unresolved high-severity threat may advance to the next wave.
</threat_model>

<verification>
Run focused provider gates, full structural/parity checks, `make check`, and `make build-ser8` with zero warnings and no activation.
</verification>

<success_criteria>
- All reusable media providers are audited.
- Final evaluated behavior matches the locked contract and approved deltas.
- Required repository and ser8 build gates pass without warnings or live changes.
</success_criteria>

<output>
Create `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-07-SUMMARY.md` when done.
</output>
