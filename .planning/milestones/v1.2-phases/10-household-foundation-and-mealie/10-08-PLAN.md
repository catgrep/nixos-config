---
phase: 10-household-foundation-and-mealie
plan: 08
type: execute
wave: 5
depends_on: [10-02]
files_modified:
  - .planning/research/google-tasks-takeout.md
autonomous: false
requirements: [IMP-01]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The delivered Google Takeout archive has been opened and its real JSON structure recorded: exact top-level key names, exact nesting, and the full field list on a single task, quoted from the actual file (IMP-01)."
    - "The recurrence question is answered as a literal observation against a task known to repeat, replacing RESEARCH.md assumption A2 with a fact that determines whether Phase 13's hand-authoring work is necessary (IMP-01)."
    - "The archive's SHA-256 and its storage location outside the repository are recorded, so Phase 13's importer is designed against the same bytes that were inspected (IMP-01)."
    - "No import code is written in this phase, and no task title, note body, or list name from the household's real data appears anywhere in the repository (IMP-01)."
    - "All eight inspection points in .planning/research/google-tasks-takeout.md are checked off with a recorded answer, and the status line no longer reads that the archive is undelivered (IMP-01)."
    - statement: "The number of task lists in the archive matches the count recorded at request time in plan 10-02, or the discrepancy is recorded as a partial-export finding."
      verification: backstop
  artifacts:
    - .planning/research/google-tasks-takeout.md
  key_links:
    - ".planning/research/google-tasks-takeout.md -> Phase 13 IMP-02 (one-off todo import with dry-run and idempotency guard)"
    - ".planning/research/google-tasks-takeout.md -> Phase 13 IMP-03 (recurring chores hand-authored from a reviewed recurrence map)"
  prohibitions:
    - statement: "Must NOT commit the Google Takeout archive, or any household member's personal task titles, notes, or contents, into the repository or into .planning/ artifacts."
      status: active
      verification: flagged-unverified
---

<objective>
Open the archive that plan 10-02 requested and replace RESEARCH.md's hypothesis about the Google Takeout Tasks envelope with observed structure.

Purpose: IMP-01 exists because the Takeout envelope is undocumented and every community description of it is second-hand. Phase 13 has to build a one-off-todo importer with a dry-run mode and an idempotency guard (IMP-02) and decide whether recurring chores must be hand-authored (IMP-03). Both decisions are currently resting on assumptions A1 and A2. This plan converts them into facts or falsifies them, and writes no import code either way.

Output: `.planning/research/google-tasks-takeout.md` with all eight inspection points answered, quoted from the real file, and carrying structure and counts only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-household-foundation-and-mealie/10-CONTEXT.md
@.planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md
@.planning/phases/10-household-foundation-and-mealie/10-02-SUMMARY.md
@CLAUDE.md
</context>

<flagged_assumptions>
| Requirement | Probe | Why unresolved at plan time |
|---|---|---|
| IMP-01 | unclassified — review manually | The requirement is fact-finding, so its edges become answerable only against the delivered archive. Three structural edges are nonetheless written into the checklist as things to look for rather than left implicit: an empty task list, a list with a single task, and a `parent` reference that appears before the parent task in document order. Each is recorded as an observation, not asserted in advance. |

RESEARCH.md assumptions this plan exists to resolve: A1, the nested envelope of an array of lists each holding a nested array of tasks with Google Tasks API style task fields; and A2, that Takeout exports no usable recurrence data. A2 is the higher-value one — if recurrence data is present, Phase 13's hand-authoring plan is unnecessary work, and if it is genuinely absent that absence must be recorded as an observation rather than inherited from community tooling.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Retrieve the delivered archive</name>
  <precondition>Google has delivered the Takeout export requested in plan 10-02 and the download link in the notification email has not expired.</precondition>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-02-SUMMARY.md (the request date and the count of task lists visible at request time, which the exported count is compared against)
    - .planning/research/google-tasks-takeout.md (the status block this retrieval updates, and the eight inspection points Task 2 fills in)
    - CLAUDE.md (Secrets and Safety: never commit personal data; the trash command rather than rm for local cleanup)
  </read_first>
  <action>
Halt and hand control to the human: this is a blocking checkpoint. The full directive is in <instructions> and the operator steps are in <how-to-verify>. Extract the archive outside this repository's working tree.
  </action>
  <verify>
    <human-check>The human reports the archive retrieved, its absolute path outside the working tree, its SHA-256, and the exported list count. See <verification> for why no automated check exists.</human-check>
  </verify>
  <instructions>
Halt and hand control to the human with the steps in the how-to-verify block. Retrieval needs an authenticated browser session and a bearer download link in a personal inbox, neither of which is reachable from this session.

Record into the plan working notes: the absolute path to the extracted Tasks JSON file, the archive's SHA-256, and the exported task-list count. Confirm before proceeding that the extraction path is outside this repository's working tree, because a stray `git add -A` against an extracted archive commits household personal data permanently.
  </instructions>
  <verification>
    <human-check>The human reports the archive downloaded, extracted outside the working tree, and its absolute path plus SHA-256 recorded. There is no automated check: the download link is bearer-authenticated to a personal inbox and no CLI or API path reaches another account's Takeout output. This is one of the two manual-only verifications 10-VALIDATION.md records for this phase.</human-check>
  </verification>
  <acceptance_criteria>
    - the human reports the export delivered and the link live rather than expired; a re-request and its delay are recorded if the link had lapsed
    - the absolute path to the extracted Tasks JSON file is recorded and is outside this repository's working tree
    - the archive's SHA-256 is recorded so Phase 13 works against the same bytes
    - the exported task-list count is recorded
    - `git status --porcelain` inside the repository shows no archive, no `.zip`, and no raw Tasks JSON
    - no file content was captured into a transcript, a commit, or a planning document by this task
  </acceptance_criteria>
  <what-built>Plan 10-02 requested the export at the start of the phase. Retrieving it requires an authenticated browser session against the household's own Google account and a download link sent to that account's inbox — there is no CLI, no API, and no service-account path that reaches another person's Tasks data.</what-built>
  <how-to-verify>
    1. Open the Takeout notification email in the household account's inbox and confirm the export is ready. Google's links expire, so if it has lapsed, re-request from `takeout.google.com` and note the delay in the SUMMARY rather than working from a stale partial archive.
    2. Download the archive and extract it to a directory **outside this repository** — a scratch or downloads location, never anywhere under the working tree. The extraction path must not be inside the repository even temporarily, because a stray `git add -A` would commit household personal data permanently.
    3. Record the absolute path to the extracted directory and to the Tasks JSON file inside it.
    4. Record the archive's SHA-256 so Phase 13's importer can be designed and tested against the same bytes that were inspected here.
    5. Record how many task lists Google exported, and compare against the count of lists visible in Google Tasks at request time, recorded in plan 10-02. A mismatch means a partial export and is a finding, not a rounding error.
    6. Do not open the file contents in any tool whose output would be captured into a transcript, a commit, or a planning document. The next task reads structure only.
  </how-to-verify>
  <resume-signal>Type "delivered" plus the absolute path to the Tasks JSON file, its SHA-256, and the exported list count, or describe what blocked retrieval</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Inspect the archive and record its real structure</name>
  <precondition>The archive has been extracted to a path outside the repository, and that path plus the file's SHA-256 were recorded in Task 1.</precondition>
  <files>.planning/research/google-tasks-takeout.md</files>
  <read_first>
    - .planning/research/google-tasks-takeout.md (created in plan 10-02: the status block, the hypothesis section to be corrected or confirmed, and the eight unchecked inspection points to fill in)
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (the "Google Takeout Tasks Archive (IMP-01)" section: the hypothesised envelope, the expected task fields, the recurrence claim, and the inspection checklist with its rationale for each point)
    - .planning/phases/10-household-foundation-and-mealie/10-02-SUMMARY.md (the request date and the list count recorded at request time)
    - .planning/REQUIREMENTS.md (IMP-02 and IMP-03, so each finding is recorded with the Phase 13 decision it feeds)
    - CLAUDE.md (Secrets and Safety: never commit personal or credential material)
  </read_first>
  <action>
Read the archive for shape and answer all eight points. Record structure, field names, and counts. Record no content.

Work through the checklist in `.planning/research/google-tasks-takeout.md` and replace each unchecked item with its answer:

Quote the exact top-level key names and the exact nesting from the real file, using an empty or redacted value for anything that would carry a task title or note. If the envelope differs from the hypothesised array-of-lists-each-holding-an-array-of-tasks, say so plainly and record what it actually is. A confirmed hypothesis and a falsified one are equally valuable outcomes; an unstated one is not.

Quote the full field list present on a single task, verbatim as key names. Note which of the hypothesised Google Tasks API fields are absent and which unhypothesised fields are present.

Answer the recurrence question against a task the household knows repeats. Do not answer it by searching for a field name you expect; pick a specific recurring chore, locate its entries, and record literally whether any recurrence key appears anywhere on them, and if so what it is called and what shape its value takes. This single answer decides whether Phase 13's IMP-03 hand-authoring work is necessary or redundant, so record the method as well as the result.

Count entries by status and record how far back completion timestamps go. Long-lived lists carry years of completed noise, and IMP-02's importer needs to know the volume it will be filtering.

Record whether deleted or hidden entries are present and how many.

Check whether `parent` and `position` ordering ever places a child before its parent in document order. A single-pass importer breaks on that, and it is cheaper to know now than to debug in Phase 13.

Record the timezone handling of the due field: confirm or refute that it is date-only anchored at UTC midnight. If it is, note explicitly that rendering it in America/Los_Angeles shifts every due date back by one day, because that is a silent off-by-one that would land in every imported chore.

Record the archive's SHA-256 and its storage location from Task 1.

Also record the three structural edges named in the flagged assumptions, as observations: whether any exported list is empty, whether any list holds exactly one task, and the parent-ordering answer above. These are the inputs a Phase 13 importer will be handed whether or not anyone planned for them.

Update the status line so it no longer says the archive is undelivered, and replace the "Hypothesis to falsify" section header with a resolution that states, item by item, which parts of the hypothesis held and which did not. Do not delete the hypothesis: keeping it beside the finding is what shows a future reader that the guess was tested rather than quietly replaced.

Fill in the Phase 13 handoff section, naming for each finding whether it feeds IMP-02, IMP-03, or both.

Write no import code, no parser, no schema file, and no fixture. IMP-01 is inspection only and IMP-02 and IMP-03 are Phase 13's work; a helper script written now would be written against one archive with no consumer and would rot before it is used.

Every sentence on its own physical line. No em dashes.
  </action>
  <verify>
    <automated>grep -c '^- \[x\]' .planning/research/google-tasks-takeout.md; grep -vc 'Status: requested' .planning/research/google-tasks-takeout.md; git status --porcelain | grep -c 'Takeout\|\.zip$\|Tasks\.json'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\]' .planning/research/google-tasks-takeout.md` returns at least 8 (every inspection point answered)
    - `grep -c '^- \[ \]' .planning/research/google-tasks-takeout.md` returns 0 (no point left unanswered)
    - `grep -c 'Status: requested' .planning/research/google-tasks-takeout.md` returns 0 (the status line was updated)
    - the file contains a section stating item by item which parts of the hypothesis held and which did not, with the original hypothesis still present alongside it
    - the recurrence answer is recorded as a literal observation naming the specific recurring chore that was examined and the method used, not as a field-name search result
    - the archive's SHA-256 and its storage location outside the repository are both recorded
    - the exported list count is recorded and compared against the count from plan 10-02, with any mismatch recorded as a partial-export finding
    - the Phase 13 handoff section names IMP-02, IMP-03, or both for every finding
    - `git status --porcelain` shows no archive file, no `.zip`, and no raw Tasks JSON staged or untracked inside the repository
    - `rg -c 'Takeout.*\.zip|Tasks\.json' --glob '!.planning/research/google-tasks-takeout.md' .` finds no archive committed anywhere in the working tree
    - no file larger than 64 KiB is added by this task
    - no task title, note body, or list name from the household's real data appears in the file
    - no import script, parser, schema, or fixture file is created anywhere in the repository by this plan
    - no em dash character appears in the file
  </acceptance_criteria>
  <done>All eight inspection points carry recorded answers quoted from the real archive, the recurrence question is settled as an observation with its method recorded, the hypothesis sits beside its resolution, Phase 13's two import requirements each have their inputs named, and nothing personal and no import code entered the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google account inbox -> download link -> local disk | Household personal data leaves Google's custody; the link is bearer-authenticated and expires |
| extracted archive -> repository working tree | The point at which personal data could become version-controlled and permanent |
| observed structure -> planning artifact | The filter that must let field names and counts through while stopping content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Information Disclosure | Google Takeout archive contents | high | mitigate | The archive is extracted outside the working tree so a stray `git add -A` cannot reach it; the artifact records field names, structure, and counts only; acceptance criteria grep the working tree for a committed archive and cap the size of any file this task adds |
| T-10-25 | Information Disclosure | task titles or note bodies leaking through quoted examples | high | mitigate | Every structural quote uses an empty or redacted value where a title or note would sit, and an explicit acceptance criterion forbids real content in the file. Quoting a "harmless looking" example task is the most likely way this fails |
| T-10-12 | Tampering | a partial or wrong-account export accepted as complete | medium | mitigate | The exported list count is compared against the count recorded at request time in plan 10-02, and a mismatch is recorded as a finding rather than absorbed. The archive's SHA-256 pins the exact bytes Phase 13 works against |
| T-10-26 | Spoofing | an expired or re-shared Takeout download link | low | mitigate | Task 1 requires confirming the link is live and re-requesting rather than working from a stale partial archive, with the delay recorded |
| T-10-27 | Repudiation | Phase 13 designed against an assumption rather than the observation | high | mitigate | The recurrence answer must name the specific recurring chore examined and the method used, and the original hypothesis stays in the file beside its resolution so a future reader can see the guess was tested rather than quietly replaced |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan, and no import code, parser, or fixture is written (RESEARCH.md Package Legitimacy Audit: not applicable to this phase, zero `[SLOP]`, zero `[SUS]`) |
</threat_model>

<artifacts_this_phase_produces>
**Files modified:** `.planning/research/google-tasks-takeout.md` — created in plan 10-02 as an empty form, completed here with observed structure.

**External state consumed, not created:** the delivered Google Takeout archive, stored at a recorded path outside this repository with a recorded SHA-256. It is deliberately not an artifact of the repository and should be deleted from local disk once Phase 13's importer has been built and tested against it.

**Deliberately not produced:** no import script, no parser, no schema file, no fixture, no test data. IMP-01 is inspection only; IMP-02 and IMP-03 are Phase 13's work.
</artifacts_this_phase_produces>

<verification>
- All eight inspection points are answered with content quoted from the real archive
- The recurrence question carries a literal observation, the chore examined, and the method used
- The hypothesis remains in the file beside an item-by-item resolution
- The archive's SHA-256 and external storage location are recorded
- The exported list count is compared against the request-time count
- Every finding names the Phase 13 requirement it feeds
- No archive, raw JSON, task title, note body, or list name entered the repository
- No import code, parser, schema, or fixture was written
</verification>

<success_criteria>
Phase 13 inherits observed structure instead of community guesswork, and the one question that determines half its scope — whether Takeout carries usable recurrence data — is answered from the household's own archive with the method recorded. IMP-01 is complete with no import code written, exactly as the requirement specifies.
</success_criteria>

<output>
Create `.planning/phases/10-household-foundation-and-mealie/10-08-SUMMARY.md` when done.
</output>
