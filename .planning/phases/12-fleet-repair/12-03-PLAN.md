---
phase: 12-fleet-repair
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/nordvpn/default.nix
  - modules/nordvpn/service.nix
  - modules/nordvpn/template.conf
  - modules/media/qbittorrent.nix
  - hosts/ser8/media/qbittorrent.nix
  - modules/media/default.nix
  - hosts/ser8/media/default.nix
  - hosts/ser8/configuration.nix
  - hosts/ser8/media/orchestration.nix
  - hosts/ser8/media/orchestration-helpers.sh
  - hosts/ser8/media/permissions.nix
  - hosts/ser8/impermanence.nix
  - scripts/smoketests/nordvpn/all.sh
  - scripts/smoketests/nordvpn/disruptive.sh
  - scripts/smoketests/nordvpn/test-anonymity.sh
  - scripts/smoketests/nordvpn/test-forwarding.sh
  - scripts/smoketests/nordvpn/test-netns.sh
  - scripts/smoketests/nordvpn/test-qbittorrent-confinement.sh
  - scripts/smoketests/nordvpn/test-qbittorrent.sh
  - scripts/smoketests/nordvpn/test-veth-interfaces.sh
  - scripts/smoketests/ser8/all.sh
  - scripts/smoketests/media/all.sh
  - Makefile
  - scripts/sops/gen-hash-qbittorrent.py
  - modules/servers/monitoring.nix
  - modules/gateway/prometheus.nix
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FLEET-01]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "make dry-activate-ser8 exits 0 after all NordVPN/qBittorrent code and wiring is removed — no dangling reference to the deleted modules.nordvpn option namespace, config.services.qbittorrent-nox, or config.sops.secrets.qbittorrent_admin_password remains anywhere in the evaluated configuration."
    - "make check (which builds all four hosts, including firebat) exits 0 after the monitoring.nix and prometheus.nix edits."
  artifacts:
    - modules/media/default.nix
    - hosts/ser8/media/default.nix
    - hosts/ser8/configuration.nix
    - hosts/ser8/media/orchestration.nix
    - hosts/ser8/media/orchestration-helpers.sh
    - hosts/ser8/media/permissions.nix
  key_links:
    - "hosts/ser8/media/orchestration.nix download-clients-setup.script -> config.sops.secrets.\"qbittorrent_admin_password\".path (breaks if qbittorrent.nix's secret declaration is deleted without this being fixed in the same task)"
    - "hosts/ser8/media/permissions.nix mediaAccounts -> config.users.users.${account}.group assertion (breaks if \"qbittorrent\" stays in the list after the qbittorrent user declaration is deleted)"
  prohibitions:
    - statement: "Must not leave the qBittorrent nginx reverse-proxy exposure or its systemd unit partially removed such that a dead or unauthenticated endpoint remains reachable — the module deletion and its host wiring must land together in one atomic set of edits, not staggered across separate uncoordinated commits."
      status: unverified
      flagged: true
    - statement: "Must not delete or alter any file or directory under /mnt/media/downloads (per D-04) while removing the qBittorrent-specific impermanence.nix tmpfiles rules — only the declarative provisioning rule lines are removed, never the on-disk directories or their contents."
      status: unverified
      flagged: true
---

<objective>
Remove the NordVPN + qBittorrent stack entirely from the repository per D-01/D-02: delete
modules/nordvpn/, the qBittorrent service modules, and the nginx exposure; fix every downstream
consumer that would otherwise break `make dry-activate-ser8` (host wiring, orchestration scripts,
permissions assertions); retire the associated smoketests, Makefile target, and orphaned script;
clean the cross-host monitoring/blackbox probes that reference the deleted service; and rewrite the
stale torrent wording across REQUIREMENTS.md and ROADMAP.md (Phases 12-15) per D-05.

Purpose: FLEET-01 requires the retired stack to be gone from code with no dead code or `enable =
false` shims (D-02), and every file that references the deleted modules must be fixed in the same
pass or the fleet's Nix evaluation breaks.
Output: modules/nordvpn/, modules/media/qbittorrent.nix, hosts/ser8/media/qbittorrent.nix, and
scripts/smoketests/nordvpn/ deleted; every downstream consumer fixed; REQUIREMENTS.md and
ROADMAP.md rescoped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-fleet-repair/12-CONTEXT.md
@.planning/phases/12-fleet-repair/12-PATTERNS.md
@modules/media/default.nix
@hosts/ser8/media/default.nix
@hosts/ser8/media/orchestration.nix
@hosts/ser8/media/permissions.nix
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Delete NordVPN/qBittorrent modules and fix every eval-critical consumer</name>
  <reversibility rating="costly">D-01: torrents are retired in favor of a usenet-only download path; resurrecting this later means restoring the module from git history, re-provisioning a NordVPN token, and re-adding client entries to three arr apps. Git history is the archive (D-02) — this task's own deletions are cheap to revert via git, but the underlying product decision to retire torrents is the costly part.</reversibility>
  <files>modules/nordvpn/default.nix, modules/nordvpn/service.nix, modules/nordvpn/template.conf, modules/media/qbittorrent.nix, hosts/ser8/media/qbittorrent.nix, modules/media/default.nix, hosts/ser8/media/default.nix, hosts/ser8/configuration.nix, hosts/ser8/media/orchestration.nix, hosts/ser8/media/orchestration-helpers.sh, hosts/ser8/media/permissions.nix</files>
  <read_first>modules/media/default.nix, hosts/ser8/media/default.nix, hosts/ser8/configuration.nix, hosts/ser8/media/orchestration.nix, hosts/ser8/media/orchestration-helpers.sh, hosts/ser8/media/permissions.nix, modules/nordvpn/default.nix, modules/media/qbittorrent.nix, hosts/ser8/media/qbittorrent.nix</read_first>
  <action>
    Delete the entire modules/nordvpn/ directory (default.nix, service.nix, template.conf), modules/media/qbittorrent.nix, and hosts/ser8/media/qbittorrent.nix.
    In modules/media/default.nix, remove the `./qbittorrent.nix` line from the `imports` list.
    In hosts/ser8/media/default.nix, remove the `./qbittorrent.nix` line from the `imports` list.
    In hosts/ser8/configuration.nix, remove the `"nordvpn_access_token"` entry (and its preceding comment) from `sops.secrets`, and remove the entire `nordvpn = { enable = true; accessTokenFile = ...; dnsServers = ...; localNetworkAccess = ...; }` block (and its preceding comment) — this is the block that turns on `modules.nordvpn`'s option namespace, which no longer exists once modules/nordvpn/ is deleted.
    In hosts/ser8/media/orchestration.nix: in the `media-config` service's script, remove "qBittorrent, " from the echo text listing configured services. In the `download-clients-setup` service, remove `"qbittorrent-nox.service"` from the `after` list; remove the commented-out `# wait_for_api "qBittorrent" "http://localhost:8080/api/v2/app/version" 30` line; remove the `# Configure qBittorrent for arr services` comment and the two `setup_qbittorrent_client "Sonarr" ...` / `setup_qbittorrent_client "Radarr" ...` calls that follow it; change the service's `description` from "Configure download clients (qBittorrent, NZBGet, SABnzbd) for all Servarr services" to "Configure download clients (NZBGet, SABnzbd) for all Servarr services". Also update the module's header-comment references to qBittorrent (the "Service Hierarchy" block near the top) to drop it from the listed services for accuracy — this is comment-only, not required for eval.
    In hosts/ser8/media/orchestration-helpers.sh, delete the `setup_qbittorrent_client()` function in its entirety (the `# Function to configure qBittorrent...` comment plus the function body, ending at its closing `}`) — it has no remaining callers after the orchestration.nix edit above.
    In hosts/ser8/media/permissions.nix, remove `"qbittorrent"` from the `mediaAccounts` list and `"qbittorrent-nox.service"` from the `mediaServices` list — leaving `"qbittorrent"` in `mediaAccounts` would make the `assertions` block's `config.users.users.qbittorrent.group` reference fail evaluation once the qbittorrent user declaration is deleted.
    Run `make dry-activate-ser8` and confirm it exits 0 — this is the real end-to-end proof that every eval-critical reference to the deleted modules has been fixed together.
  </action>
  <verify>
    <automated>make dry-activate-ser8; echo "exit:$?"</automated>
  </verify>
  <done>modules/nordvpn/, modules/media/qbittorrent.nix, and hosts/ser8/media/qbittorrent.nix are deleted; every consumer that referenced them (import lists, configuration.nix, orchestration.nix/helpers, permissions.nix) is fixed; `make dry-activate-ser8` exits 0.</done>
  <acceptance_criteria>
    - `test -d modules/nordvpn` fails (directory does not exist).
    - `test -f modules/media/qbittorrent.nix` fails and `test -f hosts/ser8/media/qbittorrent.nix` fails.
    - `grep -rc 'qbittorrent' modules/media/default.nix hosts/ser8/media/default.nix` both return `0`.
    - `grep -c 'nordvpn' hosts/ser8/configuration.nix` returns `0`.
    - `grep -c 'qbittorrent' hosts/ser8/media/permissions.nix` returns `0`.
    - `grep -c 'setup_qbittorrent_client' hosts/ser8/media/orchestration.nix hosts/ser8/media/orchestration-helpers.sh` both return `0`.
    - `make dry-activate-ser8` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Retire persistence rules, NordVPN smoketests, and orphaned Makefile/script residue</name>
  <files>hosts/ser8/impermanence.nix, scripts/smoketests/nordvpn/all.sh, scripts/smoketests/nordvpn/disruptive.sh, scripts/smoketests/nordvpn/test-anonymity.sh, scripts/smoketests/nordvpn/test-forwarding.sh, scripts/smoketests/nordvpn/test-netns.sh, scripts/smoketests/nordvpn/test-qbittorrent-confinement.sh, scripts/smoketests/nordvpn/test-qbittorrent.sh, scripts/smoketests/nordvpn/test-veth-interfaces.sh, scripts/smoketests/ser8/all.sh, scripts/smoketests/media/all.sh, Makefile, scripts/sops/gen-hash-qbittorrent.py</files>
  <read_first>hosts/ser8/impermanence.nix, scripts/smoketests/ser8/all.sh, scripts/smoketests/media/all.sh, Makefile</read_first>
  <action>
    In hosts/ser8/impermanence.nix: remove `"/var/lib/qbittorrent"` from the `environment.persistence."/persist".directories` list. In `systemd.tmpfiles.rules`, remove the "# qBittorrent download directories in media filesystem" comment and its three following lines (the duplicate `/mnt/media/downloads` entry plus the `/mnt/media/downloads/complete` and `/mnt/media/downloads/incomplete` entries — these are qBittorrent-exclusive paths, distinct from SABnzbd's `/mnt/media/downloads/usenet/...` paths declared separately); remove the "# qBittorrent config directories" comment and its three following `/var/lib/qbittorrent...` lines; remove the `"d /persist/var/lib/qbittorrent 0755 qbittorrent media -"` line. Per D-04, do NOT delete any actual files or directories under /mnt/media/downloads — this only removes the Nix rule that declaratively provisions those paths; whatever exists on disk today is left untouched and migrates as-is in Phase 13. Leave the general (non-qBittorrent-labeled) `/mnt/media/downloads`, `/mnt/media/downloads/tv`, `/mnt/media/downloads/movies` rules and the SABnzbd usenet rules completely unchanged.
    Delete the entire scripts/smoketests/nordvpn/ directory (all.sh, disruptive.sh, test-anonymity.sh, test-forwarding.sh, test-netns.sh, test-qbittorrent-confinement.sh, test-qbittorrent.sh, test-veth-interfaces.sh).
    In scripts/smoketests/ser8/all.sh, remove the `./scripts/smoketests/nordvpn/all.sh` line from the `TESTS` array, and update the file's header comment describing the fan-out (which currently explains why the disruptive NordVPN suite is excluded) to remove the now-stale reference to `scripts/smoketests/nordvpn/`.
    In scripts/smoketests/media/all.sh, remove the `"qBittorrent:torrent.vofi:8080:qbittorrent"` entry from the `MEDIA_SERVICES` array and remove `qbittorrent` from the `MEDIA_ACCOUNTS` array. Do NOT modify any other service's entry in either array, and do NOT touch the SABnzbd entries or the completed-download/media-library permission checks later in the file (per D-13, the SABnzbd smoketest fix is explicitly out of scope for this phase).
    In Makefile, delete the `$(call help_option,"sops-gen-hash-qbittorrent",...)` help line and the `sops-gen-hash-qbittorrent:` target plus its `@./scripts/sops/gen-hash-qbittorrent.py` recipe line.
    Delete scripts/sops/gen-hash-qbittorrent.py (now orphaned — its only caller was the Makefile target just removed).
    Run `shellcheck` and `shfmt -d` against every modified/remaining shell script in scripts/smoketests/ser8/ and scripts/smoketests/media/, and confirm both are clean.
  </action>
  <verify>
    <automated>test -d scripts/smoketests/nordvpn && echo FAIL || echo OK; grep -c 'qbittorrent' scripts/smoketests/ser8/all.sh scripts/smoketests/media/all.sh Makefile; test -f scripts/sops/gen-hash-qbittorrent.py && echo FAIL || echo OK; shellcheck scripts/smoketests/ser8/all.sh scripts/smoketests/media/all.sh && shfmt -d scripts/smoketests/ser8/all.sh scripts/smoketests/media/all.sh</automated>
  </verify>
  <done>hosts/ser8/impermanence.nix has no qBittorrent persistence/tmpfiles entries; scripts/smoketests/nordvpn/ is deleted; scripts/smoketests/ser8/all.sh and scripts/smoketests/media/all.sh no longer reference qBittorrent or the nordvpn suite; the Makefile target and orphaned gen-hash script are gone; shellcheck/shfmt are clean.</done>
  <acceptance_criteria>
    - `test -d scripts/smoketests/nordvpn` fails (directory does not exist).
    - `grep -c 'qbittorrent' hosts/ser8/impermanence.nix` returns `0`.
    - `grep -c '/mnt/media/downloads' hosts/ser8/impermanence.nix` still returns at least `3` (the general downloads/tv/movies rules remain untouched).
    - `grep -c 'nordvpn' scripts/smoketests/ser8/all.sh` returns `0`.
    - `grep -c 'qbittorrent' scripts/smoketests/media/all.sh` returns `0`, and `grep -c 'sabnzbd' scripts/smoketests/media/all.sh` is unchanged from before this task.
    - `grep -c 'sops-gen-hash-qbittorrent' Makefile` returns `0`.
    - `test -f scripts/sops/gen-hash-qbittorrent.py` fails (file does not exist).
    - `shellcheck scripts/smoketests/ser8/all.sh scripts/smoketests/media/all.sh` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Clean cross-host monitoring/blackbox probes and rescope planning docs (D-05)</name>
  <files>modules/servers/monitoring.nix, modules/gateway/prometheus.nix, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>modules/servers/monitoring.nix, modules/gateway/prometheus.nix, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
    In modules/servers/monitoring.nix (imported fleet-wide via flake.nix's baseModules — affects all four hosts' eval): remove `"qbittorrent-nox.service"` from the `monitoredUnits` list, and remove the process-exporter entry `{ name = "qbittorrent"; comm = [ "qbittorrent-nox" ]; }` from the processes list.
    In modules/gateway/prometheus.nix (firebat only): remove the line `"http://192.168.68.65:8080" # qBittorrent via nginx (ser8)` from the `blackbox-http` job's `targets` list — this probe would otherwise fail permanently (connection refused) once the qBittorrent nginx exposure is deleted, producing a false, un-actionable alert.
    In .planning/REQUIREMENTS.md, per D-05: replace the **FLEET-01** line's text with "Torrents are retired; the download path is usenet-only (SABnzbd/NZBGet). The NordVPN + qBittorrent stack is removed entirely from code and state with no restart loop recurrence" (keep the `- [ ] **FLEET-01**:` prefix). In **BKP-07**'s text, remove "qBittorrent, " from the service list. In **NIX-03**'s text, change "download clients (qBittorrent, NZBGet, SABnzbd)" to "download clients (NZBGet, SABnzbd)". In **NIX-05**'s text, change "imports succeed from all three download clients" to "imports succeed from both download clients", and change "a representative torrent import creates a verified hardlink" to "a representative usenet import creates a verified hardlink".
    In .planning/ROADMAP.md, per D-05: in Phase 12's Success Criteria, replace criterion 1's text ("qBittorrent stays active with a working internet path through the NordVPN namespace, with no recurring wgnord restart loop") with "The NordVPN + qBittorrent stack is removed from code and state; the download path is usenet-only (SABnzbd/NZBGet) with no drift or restart-loop recurrence" (keep criteria 2-4 unchanged — those are owned by plans 12-02, 12-01, and 12-05). In Phase 13's criterion 4, remove "qBittorrent, " from the "(Jellyfin, Sonarr, Radarr, Bazarr, qBittorrent, SABnzbd, NZBGet, Samba)" list. In Phase 14's criterion 2, remove "qBittorrent, " from the "(Sonarr, Radarr, Prowlarr, Jellyfin, Bazarr, qBittorrent, SABnzbd, NZBGet)" list. In Phase 15's criterion 3, change "download clients (qBittorrent, NZBGet, SABnzbd)" to "download clients (NZBGet, SABnzbd)". In Phase 15's criterion 4, change "imports succeed from all three download clients" to "imports succeed from both download clients", and change "a representative torrent import creates a verified hardlink" to "a representative usenet import creates a verified hardlink".
    Use `Edit` (scoped replacement) for every REQUIREMENTS.md/ROADMAP.md change — never rewrite either file wholesale.
    Run `make check` (builds all four hosts including firebat, plus flake check and statix) and confirm it exits 0.
  </action>
  <verify>
    <automated>make check; echo "exit:$?"</automated>
  </verify>
  <done>modules/servers/monitoring.nix and modules/gateway/prometheus.nix no longer reference qBittorrent/qbittorrent-nox; REQUIREMENTS.md and ROADMAP.md read as usenet-only across FLEET-01, BKP-07, NIX-03, NIX-05, and Phase 12-15's success criteria; `make check` exits 0.</done>
  <acceptance_criteria>
    - `grep -c 'qbittorrent' modules/servers/monitoring.nix modules/gateway/prometheus.nix` both return `0`.
    - `grep -c 'qbittorrent\|qBittorrent' .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c 'qbittorrent\|qBittorrent' .planning/ROADMAP.md` returns `0` (Phase 12-15 sections only reference NordVPN retirement in past/removed tense, not as an active service).
    - `.planning/REQUIREMENTS.md` still contains the literal string `**FLEET-01**` and now contains "usenet-only".
    - `make check` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deleted Nix module -> remaining consumers | Any file still referencing `modules.nordvpn.*`, `config.services.qbittorrent-nox.*`, or the deleted SOPS secret paths breaks fleet-wide evaluation, not just ser8. |
| firebat blackbox probe -> deleted ser8 endpoint | A stale probe target for a deleted service produces a permanently-failing, un-actionable alert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Tampering | Partial deletion leaving a dangling nginx proxy or systemd reference | high | mitigate | All eval-critical deletions and their consumers land in one task (Task 1), verified end-to-end by `make dry-activate-ser8`. |
| T-12-08 | Denial of Service | firebat blackbox probe left pointed at a now-deleted qBittorrent endpoint | medium | mitigate | Probe target removed from modules/gateway/prometheus.nix in Task 3, verified by `make check`. |
| T-12-09 | Repudiation | Planning docs (REQUIREMENTS.md/ROADMAP.md) still describe a retired subsystem as active | low | mitigate | D-05 rescope applied across FLEET-01, BKP-07, NIX-03, NIX-05, and Phase 12-15 success criteria in Task 3. |
</threat_model>

<verification>
- `make dry-activate-ser8` exits 0 after Task 1.
- `make check` exits 0 after Task 3 (all four hosts evaluate cleanly).
- No remaining `grep` hit for `qbittorrent`/`nordvpn`/`wgnord` in any file this plan claims to have cleaned, except intentionally-preserved historical references (e.g. git history, archived phase docs).
- shellcheck/shfmt clean on every modified shell script.
</verification>

<success_criteria>
The NordVPN + qBittorrent stack is fully removed from code, wiring, monitoring, and planning docs,
with the fleet's Nix evaluation green end-to-end — satisfying the code-side half of ROADMAP Phase 12
success criterion 1 (the live-state half is plan 12-04).
</success_criteria>

## Artifacts this phase produces (this plan)

- Deleted: `modules/nordvpn/` (default.nix, service.nix, template.conf), `modules/media/qbittorrent.nix`, `hosts/ser8/media/qbittorrent.nix`, `scripts/smoketests/nordvpn/` (8 files), `scripts/sops/gen-hash-qbittorrent.py`.
- Deleted symbol: `setup_qbittorrent_client()` function in `hosts/ser8/media/orchestration-helpers.sh`.
- Deleted symbol: `sops-gen-hash-qbittorrent` Makefile target and its help text.
- Modified (import/wiring fixes): `modules/media/default.nix`, `hosts/ser8/media/default.nix`, `hosts/ser8/configuration.nix`, `hosts/ser8/media/orchestration.nix`, `hosts/ser8/media/permissions.nix`, `hosts/ser8/impermanence.nix`.
- Modified (monitoring): `modules/servers/monitoring.nix`, `modules/gateway/prometheus.nix`.
- Modified (docs, D-05): `.planning/REQUIREMENTS.md` (FLEET-01, BKP-07, NIX-03, NIX-05), `.planning/ROADMAP.md` (Phase 12 criterion 1, Phase 13 criterion 4, Phase 14 criterion 2, Phase 15 criteria 3-4).

<output>
Create `.planning/phases/12-fleet-repair/12-03-SUMMARY.md` when done
</output>
