---
phase: 10-household-foundation-and-mealie
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-03]
files_modified:
  - scripts/smoketests/household/test-mealie-service.sh
autonomous: false
requirements: [FOUND-03, FOUND-04, MEAL-01, MEAL-04]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Before PostgreSQL ever starts on ser8, /persist/var/lib/postgresql has been inspected and confirmed to hold no pre-existing major-version subdirectory, so FOUND-04's 'before any service data exists' premise is proven rather than assumed."
    - "ser8 runs mealie.service and postgresql.service, with SHOW server_version reporting a 17 series version and the data directory at /var/lib/postgresql/17 owned postgres:postgres at mode 750 (FOUND-04, D-08)."
    - "/var/lib/mealie on ser8 is a real directory owned mealie:mealie, not a symbolic link into the transient private state tree, proving the static-user override took effect (FOUND-03, PD-01)."
    - "The mealie unit reports active and its journal for the current boot carries no error-priority entries, proving the Alembic migration that runs as ExecStartPre completed (MEAL-01, D-06)."
    - "A loopback request to port 9000 on ser8 returns a serving status, proving the application answers independently of the gateway path (MEAL-01)."
    - "The Foods and Units table names asserted by scripts/smoketests/household/test-mealie-service.sh have been confirmed against the live Mealie 3.22.0 schema, replacing RESEARCH.md assumption A3 with an observed fact (MEAL-04)."
    - "The household smoketest area exits 0 on its own, judged separately from make smoketests-ser8, which exits 1 for the pre-existing NordVPN failure this phase does not own."
    - statement: "Activating ser8 introduces no regression in the media, Frigate, or Home Assistant areas relative to the pre-activation transcript."
      verification: backstop
  artifacts: []
  key_links:
    - "make test-ser8 (temporary activation) -> household smoketest area -> make switch-ser8 (boot default)"
    - "services.mealie.database.createLocally -> postgresql.service -> /var/lib/postgresql/17 -> /persist/var/lib/postgresql/17"
    - "systemd StateDirectory=mealie -> /var/lib/mealie -> impermanence bind mount -> /persist/var/lib/mealie"
  prohibitions:
    - statement: "Must NOT destroy or overwrite pre-existing PostgreSQL data under /persist/var/lib/postgresql without explicit human confirmation."
      status: active
      verification: flagged-unverified
    - statement: "Must NOT write any household member's Mealie password, or the changed admin password, into the repository, .planning/ artifacts, commit messages, or command output."
      status: active
      verification: flagged-unverified
---

<objective>
Take the configuration proven at evaluation time in plan 10-01 and make it real on ser8, crossing the two one-way thresholds this phase contains: PostgreSQL initialises its on-disk format, and Mealie runs its Alembic migrations.

Purpose: D-06 and D-08 are both rated one-way, and no backups exist until Phase 11. The irreversible moment is not writing the Nix files; it is this activation. That is why the pre-flight inspection and the decision checkpoint sit here rather than in plan 10-01, and why the smoketest suite from plan 10-03 had to exist first.

Output: ser8 running Mealie on pinned PostgreSQL 17 as its boot default, with the household smoketest area green and one research assumption replaced by an observed schema fact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-household-foundation-and-mealie/10-CONTEXT.md
@.planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md
@.planning/phases/10-household-foundation-and-mealie/10-01-SUMMARY.md
@.planning/phases/10-household-foundation-and-mealie/10-03-SUMMARY.md
@CLAUDE.md
</context>

<flagged_assumptions>
| Requirement | Probe | Why unresolved |
|---|---|---|
| FOUND-04 | unclassified — review manually | The one genuine edge is a pre-existing PostgreSQL major already on disk, which is an environment precondition rather than an input-domain edge. Task 1 turns it into a blocking check rather than leaving it as an assumption. |
| FOUND-03 | unclassified — review manually | Structural conformance is asserted by plan 10-01's grep criteria and by this plan's runtime proof that the static-user override took effect. No boundary condition applies. |
| MEAL-01 | unclassified — review manually | Service liveness is binary. |
| MEAL-04 | adjacency, empty, ordering | Owned by plans 10-03 (authored as truths and backstops) and 10-06 (observed during acceptance). This plan only resolves the table-name assumption A3. |

Inherited environment hazards from Phase 9 that this plan must work around and does not own: ser8's NordVPN tunnel is down and `make smoketests-ser8` exits 1 pre-existing; `nordvpn/test-forwarding.sh` still queries the retired pi4 resolver; `make rollback-HOST` prints TODO and is not a recovery path. RESEARCH.md Assumption A7 (both hosts reachable at their `deploy.yaml` addresses) is proven or falsified by Task 1's precondition check.
</flagged_assumptions>

<tasks>

<!-- planner-discipline-allow: MEALIE_ALLOW_UNSEEDED -->

<task type="auto">
  <name>Task 1: Pre-flight — prove the "before any data exists" premise before anything starts</name>
  <precondition>ser8 answers at the `targetHost` address recorded in `deploy.yaml` and accepts an ssh session as its `targetUser`; if it does not, halt rather than proceeding to activation.</precondition>
  <files></files>
  <read_first>
    - deploy.yaml (the ser8 entry: targetHost, targetUser, buildOnTarget, and the smoketests path)
    - Makefile (the status, test-HOST, smoketests-HOST, and switch-HOST targets, and the confirmation prompt behaviour)
    - hosts/ser8/impermanence.nix (the persisted directory entries and the tmpfiles rules added in plan 10-01)
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (Open Question 5 on whether the persisted PostgreSQL directory is genuinely empty; Runtime State Inventory; Pitfall 3 on why a wrong major becomes unrecoverable once data exists)
    - .planning/STATE.md (the Phase 9 outstanding items and the recorded environment hazards)
  </read_first>
  <action>
Establish, with evidence rather than inference, that ser8 is reachable and that PostgreSQL has never held data on this host. FOUND-04's whole premise is "before any service data exists", and RESEARCH.md Open Question 5 flags that nobody has actually looked.

Run `make status` and confirm ser8 responds at the address `deploy.yaml` records. If it does not, stop here: nothing downstream in this plan is meaningful against an unreachable host.

On ser8, list the persisted PostgreSQL directory and its parent, recording the exact output. Three outcomes and three responses:

If the directory does not exist, or exists and is empty, the premise holds. Record the evidence and proceed.

If it contains a subdirectory named for any major version, the premise is false and the plan changes shape. Do not delete it, do not move it, do not initialise over it. Stop, record what was found including sizes and modification times, and surface it to the user as a blocking finding. A directory named for a major other than 17 means either the pin did not land or someone created data by hand, and the recovery path is a deliberate migration, not a fresh start.

If it contains anything else, treat that as the same blocking finding and record it verbatim.

Also record, before activation, the current generation number on ser8 and the output of the bootloader entry listing. `make rollback-HOST` prints a TODO and is not a recovery path; the real recovery for a bad activation is selecting the previous generation in the systemd-boot menu, and that only works if the generation number was written down first.

Capture a pre-activation transcript of the ser8 smoketest suite so the post-activation comparison is per-area rather than a single pass or fail. `make smoketests-ser8` exits 1 today because of the pre-existing NordVPN tunnel failure, so the top-level exit status is not usable as a gate; the per-area transcript is. This is the pattern Phase 9's plan 05 established. Store the transcript under the phase directory.

Confirm the mealie unit and the postgresql unit do not already exist on the host, so a later "it is active" observation cannot be reading a leftover.
  </action>
  <verify>
    <automated>make status; ssh "$(yq -r '.hosts.ser8.targetUser' deploy.yaml)@$(yq -r '.hosts.ser8.targetHost' deploy.yaml)" 'ls -la /persist/var/lib/postgresql 2>&1; echo "---"; ls -1 /persist/var/lib/postgresql 2>/dev/null | grep -cE "^[0-9]+$"'</automated>
  </verify>
  <acceptance_criteria>
    - `make status` reports ser8 reachable
    - an ssh session to ser8 as its `deploy.yaml` user succeeds
    - the count of numeric major-version subdirectories under `/persist/var/lib/postgresql` on ser8 is 0, and that output is recorded verbatim in the plan SUMMARY
    - `systemctl list-unit-files 'mealie.service' 'postgresql.service'` on ser8 lists neither unit before activation, and that output is recorded
    - the current ser8 generation number and the bootloader entry listing are recorded in the plan SUMMARY as the named recovery path
    - a pre-activation ser8 smoketest transcript exists on disk under the phase directory and its per-area results are recorded
    - if any numeric subdirectory or unexpected content was found, the plan halts with a blocking finding and no activation is attempted
  </acceptance_criteria>
  <done>ser8 is reachable, PostgreSQL has provably never held data on this host, the pre-activation smoketest baseline and the generation-number recovery path are both recorded, and nothing has been changed on the host.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: authorise the one-way activation of PostgreSQL 17 and Mealie 3.22.0</name>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-CONTEXT.md (D-06 and D-08, both rated one-way, and D-07's rule gating any future version move)
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (Pitfall 3 on the unrecoverable postgres major; Pitfall 6 on the runtime-only breakage risk of a future unstable bump)
    - the Task 1 record of the pre-activation generation number and the empty persisted PostgreSQL directory
  </read_first>
  <action>
Halt and present the decision below. Do not run any activation command until the human selects an option. If the selection is anything other than `proceed`, stop the plan and record the selection and its reasoning in the SUMMARY rather than proceeding with a modified approach.
  </action>
  <verify>
    <human-check>The human selects one of the three options and the selection is recorded verbatim in the plan SUMMARY along with the Task 1 evidence it was made against.</human-check>
  </verify>
  <acceptance_criteria>
    - the human has selected exactly one of `proceed`, `defer-until-backups`, or `revisit-pins`
    - the selection is recorded verbatim in the plan SUMMARY
    - if the selection is not `proceed`, no activation command was run and the plan halted
  </acceptance_criteria>
  <decision>Cross the two one-way thresholds on ser8: initialise PostgreSQL 17 on disk and run Mealie 3.22.0's Alembic migrations for the first time.</decision>
  <context>
Everything up to this point is reversible with `git revert`. This activation is not.

**PostgreSQL 17 (D-08, FOUND-04).** The first start initialises the on-disk data format at `/var/lib/postgresql/17`, persisted through impermanence. Changing majors after data exists requires a manual `pg_upgrade` against an impermanence-managed directory. Task 1 has confirmed no other major is present. The locked 26.05 revision carries 17.7 — note that CONTEXT.md's D-08 parenthetical says 17.11, which RESEARCH.md corrects; the pin itself is unaffected.

**Mealie 3.22.0 (D-06, D-07).** Mealie runs Alembic migrations as an `ExecStartPre` on every start, and there is no downgrade path. No backups exist until Phase 11. D-06's reasoning is precisely this: start on the version we intend to keep rather than standing up an older one "to test" and then being forced through a one-way migration. D-07 then gates any future move: `unstable.mealie` shifts only on a deliberate, staged flake update commit, and no such bump lands until Phase 11 backups exist and a fresh dump has been taken.

**What is NOT irreversible here.** The static-user choice (PD-01) is reversible today at the cost of a manual move and chown, and free right now because no state exists. The Caddy vhost is plain configuration. The module layout is plain configuration.

**Recovery if the activation goes badly.** Select the previous generation in the systemd-boot menu — the generation number was recorded in Task 1. `make rollback-HOST` prints a TODO and is not a recovery path. Note that recovery restores the system closure; it does not un-initialise a PostgreSQL data directory or un-run an Alembic migration.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with activation as planned</name>
      <pros>Phase 10 delivers its goal; Phase 11 gets a real Mealie to back up and restore; the version pins are the ones the milestone was researched against; no throwaway migration is incurred later</pros>
      <cons>PostgreSQL 17 and Mealie 3.22.0 become facts on disk with no backup safety net until Phase 11</cons>
    </option>
    <option id="defer-until-backups">
      <name>Defer activation until Phase 11's backup engine exists</name>
      <pros>No irreversible data-format change without a restore path</pros>
      <cons>Inverts the roadmap: Phase 11's stated goal is a Mealie restore drill, which requires a Mealie with data to restore. Deferring makes Phase 11 unexecutable and blocks Phases 12-14 behind it</cons>
    </option>
    <option id="revisit-pins">
      <name>Revisit the version pins before activating</name>
      <pros>A last chance to change either major or either package version at zero cost</pros>
      <cons>Both pins were researched from the locked store paths this milestone; changing either invalidates plan 10-01's evaluation gate and RESEARCH.md's verified version table</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, defer-until-backups, or revisit-pins</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Activate ser8, verify both state tiers, and confirm the live schema</name>
  <precondition>The decision checkpoint above resolved to `proceed`, and Task 1 recorded zero pre-existing PostgreSQL major-version directories on ser8.</precondition>
  <files>scripts/smoketests/household/test-mealie-service.sh</files>
  <read_first>
    - Makefile (the test-HOST, smoketests-HOST, and switch-HOST targets and their confirmation behaviour)
    - scripts/smoketests/household/test-mealie-service.sh (written in plan 10-03: the table names asserted under PD-04 and the MEALIE_ALLOW_UNSEEDED escape hatch this task is the sole legitimate user of)
    - scripts/smoketests/household/all.sh (the area entry point this task runs directly)
    - hosts/ser8/household/mealie.nix and hosts/ser8/household/postgresql.nix (the values being activated)
    - hosts/ser8/impermanence.nix (the tmpfiles rules whose on-disk effect is being verified)
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (Pitfall 4 on the 0750 ownership target; Pitfall 7 on the two state stores; Assumption A3 on the unconfirmed table names; the note that make smoketests-ser8 exits 1 pre-existing and must not be a phase gate)
    - CLAUDE.md (Host and Deployment Commands: make test-HOST activates temporarily and is safer than switching; NO_CONFIRM is only for intentional non-interactive operations)
  </read_first>
  <action>
Walk the repository's rollout ladder rather than jumping to a switch, then prove both of Mealie's state tiers and replace the last standing schema assumption with an observed fact.

Run `make test-ser8` first. This activates the configuration temporarily without making it the boot default, which is the safe order for a change that starts a database for the first time. Watch the activation output for warnings: the `services.postgresql` module is newly enabled on this host and anything it emits about stateVersion, data directory, or package selection is a finding to resolve, not a baseline to record.

With the temporary activation live, verify the database tier directly before trusting any script: the postgresql unit is active; `SHOW server_version` reports a 17 series version; `stat` on the PostgreSQL data directory reports owner postgres, group postgres, mode 750. The mode target is 750 and not 700 because the upstream module sets its state directory mode to 0750 for every major at or above 11 — a 700 expectation fights systemd on every start. If a manual chown feels necessary at any point, stop: that is the tell that something declarative is wrong, and fixing it by hand hides the defect.

Verify the application tier: the mealie unit is active; its journal for the current boot carries no error-priority entries, which is what proves the Alembic `ExecStartPre` migration completed rather than merely that the process is up; a loopback request to port 9000 returns a serving status.

Verify the persistence tier: `/var/lib/mealie` is a real directory and not a symbolic link, owned mealie:mealie, and the persist-side path exists with the same ownership. A symlink here means the static-user override did not take effect and state is landing in the transient private tree, which would silently break Phase 11's backup job.

Now resolve RESEARCH.md Assumption A3. List the Mealie database's tables against the live 3.22.0 schema and read the actual names of the Foods table and the Units table. If they match the names plan 10-03 wrote, record the confirmation and change nothing. If they do not, correct `scripts/smoketests/household/test-mealie-service.sh` to the observed names in this task, and record both the guess and the fact. Do not guess a second time and do not leave the assertion pointing at a table that does not exist.

Run the household smoketest area directly, not through `make smoketests-ser8`. The top-level ser8 suite exits 1 today because of the pre-existing NordVPN tunnel failure this phase does not own, so it is unusable as a gate; the household area's own exit status is the gate. This is the single legitimate run of `MEALIE_ALLOW_UNSEEDED=1` in the entire phase: Mealie has not been bootstrapped yet, so there are no Foods, no Units, no recipes, and no images. Pass it on the command line only. Do not add it to any committed file — plan 10-03's acceptance criteria grep for exactly that, and a committed escape hatch turns a real check into an always-passing one permanently.

Re-run the full ser8 suite once and diff its per-area results against the Task 1 pre-activation transcript. The pass condition is per-area equality plus a green household area, not a zero top-level exit status.

Only once all of the above holds, run `make switch-ser8` to make the configuration the boot default. Record the new generation number alongside the previous one from Task 1.
  </action>
  <verify>
    <automated>MEALIE_ALLOW_UNSEEDED=1 ./scripts/smoketests/household/all.sh ser8</automated>
  </verify>
  <acceptance_criteria>
    - `make test-ser8` completes and its output contains no warning emitted by the postgresql or mealie modules, or every such warning is named and resolved
    - on ser8, `systemctl is-active postgresql` outputs `active` and `systemctl is-active mealie` outputs `active`
    - on ser8, `sudo -u postgres psql -tAc 'SHOW server_version'` outputs a version beginning `17.`
    - on ser8, `stat -c '%U %G %a' /var/lib/postgresql/17` outputs `postgres postgres 750`
    - on ser8, `test -L /var/lib/mealie` exits non-zero (it is a real directory, not a symlink)
    - on ser8, `stat -c '%U %G' /var/lib/mealie` outputs `mealie mealie`
    - on ser8, `stat -c '%U %G' /persist/var/lib/mealie` outputs `mealie mealie`
    - on ser8, `journalctl -b -u mealie --priority=err --no-pager -q -o cat` produces no output
    - on ser8, `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:9000/` returns a status in the set 200, 301, 302, 303, 401, 403
    - the live table names for Foods and Units are recorded verbatim in the plan SUMMARY, and `scripts/smoketests/household/test-mealie-service.sh` asserts those exact names
    - `MEALIE_ALLOW_UNSEEDED=1 ./scripts/smoketests/household/all.sh ser8` exits 0
    - `grep -c 'MEALIE_ALLOW_UNSEEDED' deploy.yaml scripts/smoketests/ser8/all.sh scripts/smoketests/household/all.sh` returns 0 across all three
    - the post-activation ser8 suite transcript matches the Task 1 pre-activation transcript per area, with the household area newly present and green
    - `make switch-ser8` completes and the new boot-default generation number is recorded alongside the previous one
    - no manual `chown` or `chmod` was run against any PostgreSQL or Mealie path
  </acceptance_criteria>
  <reversibility rating="one-way">This activation initialises PostgreSQL 17's on-disk data format and runs Mealie 3.22.0's Alembic migrations, neither of which has a downgrade path, and no backups exist until Phase 11. Selecting the previous generation in systemd-boot restores the system closure but does not un-initialise the data directory or un-run the migration.</reversibility>
  <done>ser8 runs Mealie on pinned PostgreSQL 17 as its boot default; both state tiers are verified on disk with correct ownership; the migration completed cleanly; the Foods and Units table names are observed fact rather than assumption; and the household smoketest area exits 0 on its own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> ser8 activation | A configuration closure built on the target is made the running and then the boot-default system |
| system.stateVersion -> PostgreSQL on-disk format | An implicit input that silently selects a one-way data format, crossed for real at this activation |
| impermanence bind mount -> service state directories | Where ownership set by systemd must agree with ownership set by tmpfiles, or the service refuses to start |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Denial of Service | PostgreSQL major initialised on disk | high | mitigate | Task 1 proves no other major exists before anything starts; Task 2 asserts `SHOW server_version` reports 17 at runtime; the blocking decision checkpoint makes the one-way step an explicit human choice rather than a side effect of a deploy command |
| T-10-15 | Denial of Service | a failed Alembic migration leaving a half-migrated schema | high | mitigate | The journal-error assertion is what proves the `ExecStartPre` migration completed; "the unit is active" alone would pass on a later restart after a failed first migration. The previous generation number is recorded before activation so the closure is recoverable |
| T-10-06 | Denial of Service | Mealie state landing in the transient private tree | medium | mitigate | Task 2 asserts the data directory is not a symbolic link and is owned by the static user on both sides of the bind mount, catching a failed `DynamicUser` override before Phase 11 depends on it |
| T-10-16 | Tampering | destructive "cleanup" of a pre-existing persisted data directory | high | mitigate | Task 1 forbids delete, move, and initialise-over on any unexpected content, requires the finding be recorded verbatim, and halts the plan for human decision. Recorded as an active prohibition in `must_haves` |
| T-10-14 | Repudiation | a committed escape hatch turning the household gate into an always-passing check | high | mitigate | `MEALIE_ALLOW_UNSEEDED=1` is passed on the command line for exactly one pre-bootstrap run; acceptance criteria grep `deploy.yaml` and both suite entry points to prove it is nowhere in version control |
| T-10-09 | Elevation of Privilege | ser8 tcp/9000 reachable on the LAN after activation | medium | transfer | The LAN-plaintext hop is the established pattern for all eleven existing services. The negative-access proof is Phase 14 SEC-01; this plan does not claim it and must not be read as achieving it |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No public-registry install occurs; the closure is built entirely from the two revision-pinned nixpkgs flake inputs (RESEARCH.md Package Legitimacy Audit: zero `[SLOP]`, zero `[SUS]`) |
</threat_model>

<artifacts_this_phase_produces>
**Runtime state created on ser8 by this plan (not repository state):** the PostgreSQL cluster at `/var/lib/postgresql/17` and its persisted counterpart `/persist/var/lib/postgresql/17`; the `mealie` database and the `mealie` role inside that cluster; the Mealie data directory `/var/lib/mealie` and its persisted counterpart `/persist/var/lib/mealie`; the Mealie schema as created by Alembic at first start; the Mealie token-signing secret inside the data directory.

**systemd units newly running on ser8:** `postgresql.service`, `mealie.service`

**System users and groups newly materialised on ser8:** `mealie` user and group; `postgres` user and group

**Files modified:** `scripts/smoketests/household/test-mealie-service.sh` (only if the live schema contradicts the table names plan 10-03 asserted)

**Recorded artifacts under the phase directory:** the pre-activation ser8 smoketest transcript, the pre-activation and post-activation generation numbers, and the verbatim listing of the persisted PostgreSQL directory
</artifacts_this_phase_produces>

<verification>
- No pre-existing PostgreSQL major-version directory was found, and the evidence is recorded verbatim
- The pre-activation generation number and bootloader entry listing are recorded as the named recovery path
- Both units are active; the mealie journal for the current boot is free of error-priority entries
- `SHOW server_version` reports a 17 series version and the data directory is owned postgres:postgres at mode 750
- `/var/lib/mealie` is a real directory owned mealie:mealie on both sides of the bind mount
- The household smoketest area exits 0 on its own, judged separately from the top-level ser8 suite
- The post-activation ser8 transcript matches the pre-activation one per area, with the household area newly present
- The Foods and Units table names are observed fact recorded in the SUMMARY
- `MEALIE_ALLOW_UNSEEDED` appears in no committed file
- No manual chown or chmod was performed
</verification>

<success_criteria>
Mealie is live on ser8 on the exact versions this milestone was researched against, both state tiers verified on disk, the migration provably complete, and the boot default switched — with the one irreversible step taken as a recorded human decision rather than a side effect of running a deploy command.
</success_criteria>

<output>
Create `.planning/phases/10-household-foundation-and-mealie/10-04-SUMMARY.md` when done.
</output>
