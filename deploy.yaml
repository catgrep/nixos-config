# SPDX-License-Identifier: GPL-3.0-or-later

# deploy.yaml - Host deployment metadata
# All the static IPs were reserved using TP Link Deco's DHCP server

# Tailscale configuration for remote access
tailscale:
  domain: "shad-bangus.ts.net"

hosts:
  ser8:
    targetHost: "192.168.68.65"
    targetUser: "bdhill"
    buildOnTarget: true
    tags: ["media", "x86_64"]
    smoketests: "./scripts/smoketests/ser8/all.sh"

  firebat:
    targetHost: "192.168.68.63"
    targetUser: "bdhill"
    buildOnTarget: true
    tags: ["gateway", "x86_64"]
    smoketests: "./scripts/smoketests/gateway/all.sh"

  pi4:
    targetHost: "192.168.68.56"
    targetUser: "bdhill"
    buildOnTarget: true
    tags: ["arm", "raspberrypi"]
    smoketests: "test"

  pi5:
    targetHost: "192.168.0.110"
    targetUser: "nixos"
    buildOnTarget: true
    tags: ["arm", "raspberrypi"]
    smoketests: "test"
