---
phase: 10-household-foundation-and-mealie
plan: 06
type: execute
wave: 4
depends_on: [10-05]
files_modified: []
autonomous: false
requirements: [MEAL-02, MEAL-04]
user_setup:
  - service: mealie
    why: "D-10 makes one-time setup manual UI work — no bootstrap scripting through the Mealie API. Account creation, the credential change, and the reference-data seed all require an authenticated browser session"
    dashboard_config:
      - task: "Change the shipped administrator email and password at first login"
        location: "https://mealie.shad-bangus.ts.net, admin user menu, then the profile and settings pages"
      - task: "Create the second household member's account in the SAME household as the first, then seed Foods and Units exactly once"
        location: "https://mealie.shad-bangus.ts.net, admin area for users and households; Manage Data for the reference-data seed"

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The shipped default administrator email and password no longer authenticate: a token request using them returns a status other than 200 (MEAL-02)."
    - "Both household members have accounts, and both accounts are in the same Mealie household, not merely the same group (MEAL-02, MEAL-04)."
    - "Registration is closed and was closed by configuration rather than by a UI toggle: the deployed unit environment carries the non-empty string form of the signup setting (MEAL-02, D-09)."
    - "Foods and Units are seeded exactly once, and their row counts in the mealie database are greater than zero (MEAL-04)."
    - "Both members see the same single shopping list, and an item added by one member is visible to the other (MEAL-04)."
    - "A freshly bootstrapped Mealie has exactly one shopping list visible to both members and renders without error while that list is empty (MEAL-04, empty-input edge)."
    - "The household smoketest area exits 0 with no escape-hatch variable set, closing the seeded-data subtests that plan 10-05 recorded as expected failures."
    - statement: "When the same food is added twice to the shared shopping list, the resulting row count and quantity are recorded during this plan's acceptance rather than assumed."
      verification: backstop
    - statement: "When two shopping-list items share a label or sort key, no phase acceptance check asserts their relative display order."
      verification: backstop
  artifacts: []
  key_links:
    - "Mealie group scope -> Foods, Units, Labels (seed once per group)"
    - "Mealie household scope -> shopping lists and meal plans (both members must land in the SAME household or MEAL-04 fails silently)"
    - "services.mealie.settings.ALLOW_SIGNUP -> the deployed unit environment -> the registration control asserted by test-mealie-endpoint.sh"
  prohibitions:
    - statement: "Must NOT write any household member's Mealie password, or the changed admin password, into the repository, .planning/ artifacts, commit messages, or command output."
      status: active
      verification: flagged-unverified
---

<objective>
Bootstrap Mealie into the state MEAL-02 and MEAL-04 describe: two accounts in one household, the shipped default credentials dead, registration closed, reference data seeded, and one shared shopping list both members can edit.

Purpose: this is the half of the phase that no amount of correct Nix produces. RESEARCH.md Pitfall 8 is the reason it gets its own plan — Mealie moved shopping lists to household scope in v2.0.0, and two accounts placed in different households produce a silently wrong result where every page still returns HTTP 200 and every check passes. MEAL-04 is an assertion about where the second account lands, not a formality.

Output: a Mealie instance in daily-usable household condition, with the household smoketest area passing unaided for the first time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-household-foundation-and-mealie/10-CONTEXT.md
@.planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md
@.planning/phases/10-household-foundation-and-mealie/10-05-SUMMARY.md
@CLAUDE.md
</context>

<flagged_assumptions>
| Requirement | Probe | Why unresolved |
|---|---|---|
| MEAL-02 | unclassified — review manually | The genuine risk is RESEARCH.md Open Question 4: whether closing signup declaratively from first boot also blocks admin-created accounts. It is not an input-domain edge; it is unverified application behaviour, and Task 1 checks it first rather than discovering it after the credential change. |
| MEAL-04 | adjacency — do equal things merge, collide, or separate? | Whether adding the same food twice merges into one row with an incremented quantity or appends a second row is upstream behaviour. Authored as a backstop truth and observed during Task 2's acceptance rather than asserted in advance. |
| MEAL-04 | ordering — is order specified and stable for equal elements? | Display order for items sharing a label is not specified by any requirement and this phase deliberately does not pin it. Authored as a backstop truth stating that no acceptance check asserts it. |
| MEAL-04 | empty — what is the result for empty input? | Resolved: a freshly bootstrapped instance has exactly one shopping list, visible to both members, rendering without error while empty. Authored as a plain acceptance truth. |

RESEARCH.md Assumption A4 (that seeding is still a Manage Data button with a language picker in 3.22.0's interface) is carried into Task 1's instructions. If the interface differs, the operator will find the right control anyway; record what the real path was so Phase 12's Homebox bootstrap does not inherit stale directions. Assumption A8 (the second account is created by the administrator rather than by invite link, because invites need working SMTP which is out of scope) is the path Task 1 takes.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bootstrap accounts, credentials, and reference data in the Mealie interface</name>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-CONTEXT.md (D-05 that the second member is already on the tailnet, D-09 that registration is closed declaratively, D-10 that one-time setup is manual UI work with no API scripting)
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (the "Mealie Application Model" table showing Foods and Units are group-scoped while shopping lists are household-scoped; the bootstrap defaults table; the seeding procedure and the unique constraint a second seed violates; Open Question 4 on whether closing signup blocks admin-created accounts)
    - .planning/phases/10-household-foundation-and-mealie/10-05-SUMMARY.md (confirmation that the endpoint is reachable with a trusted certificate before any login is attempted)
    - CLAUDE.md (Secrets and Safety: never commit plaintext credentials or expose secret values in logs, test output, documentation, or diffs)
  </read_first>
  <action>
Halt and hand control to the human: this is a blocking checkpoint. The full directive is in <instructions> and the ordered operator steps are in <how-to-verify>. Per D-10 no bootstrap step is scripted through the Mealie API.
  </action>
  <verify>
    <human-check>The human reports both accounts in one household, changed administrator credentials, and a single reference-data seed. Task 3 corroborates all three mechanically. See <verification>.</human-check>
  </verify>
  <instructions>
Halt and hand control to the human with the ordered steps in the how-to-verify block. The order is load-bearing: step 2 checks Open Question 4 before the credential change, so a blocked administrator-creation path is discovered while the fallback is still cheap.

Record into the plan working notes: the answer to step 2, the real interface path used for the reference-data seed, and confirmation that both accounts landed in one household. Record no password, no session token, and no email address that would identify a household member beyond what the repository already contains.
  </instructions>
  <verification>
    <human-check>The human confirms both accounts exist in one household, the administrator credentials were changed away from the shipped defaults, and Foods and Units were seeded exactly once. Task 3 then verifies all three mechanically against the database and the deployed unit environment, so this checkpoint's report is corroborated rather than trusted.</human-check>
  </verification>
  <acceptance_criteria>
    - the human reports both household member accounts created, and reports which household each landed in
    - the answer to Open Question 4 is recorded: whether an administrator could create an account with signup closed
    - the administrator email and password are both reported as changed away from the shipped defaults
    - Foods and Units are reported as seeded exactly once, and the real interface path is recorded
    - self-service signup is reported as unavailable when logged out
    - no password value appears in the resume signal, the working notes, the SUMMARY, or any committed file
  </acceptance_criteria>
  <what-built>Everything mechanical is already done. Mealie 3.22.0 is running on ser8 against pinned PostgreSQL 17, reachable at its tsnet endpoint with a trusted certificate, registration closed by configuration, and the state directory persisted. What remains cannot be automated: D-10 makes one-time setup manual UI work with no bootstrap scripting through the Mealie API, and every step below requires an authenticated browser session holding credentials this repository must never contain.</what-built>
  <how-to-verify>
    Do these steps in this order. The order matters for step 2 specifically.

    1. Open `https://mealie.shad-bangus.ts.net` and log in with the shipped default administrator account: email `changeme@example.com`, password `MyPassword`. These are public upstream defaults, not secrets. If they do not work, stop and report it — it would mean the database was initialised by something other than this deployment.

    2. **Check this before changing anything.** Registration was closed by configuration from the very first boot (D-09), which is *before* the second account exists. RESEARCH.md Open Question 4 flags that it is unverified whether that also blocks an administrator from creating accounts. Go to the admin area and try to create the second household member's account now, while you can still fall back cheaply. If administrator creation works, continue to step 3. If it is blocked, stop and report it: the fallback is a temporary configuration change to reopen signup, create the accounts, and close it again — which must be a deliberate two-deploy sequence recorded in Key Decisions, not an undocumented UI toggle. Phase 12's Homebox has the identical shape and this answer carries forward.

    3. Confirm the second account is in the **same household** as the administrator, not merely the same group. Mealie v2.0.0 made Groups contain Households and moved shopping lists and meal plans to household scope. The admin interface lets you pick a household when creating a user, and picking the wrong one produces a silently wrong result: every page still returns HTTP 200, nothing errors, and the two members simply never see each other's list. Open the household's member list and confirm both accounts appear under one household. The default group is named `Home` and the default household `Family` unless you renamed them.

    4. Change the administrator email and password away from the shipped defaults. Use the household's own password manager. Do not type either value into a terminal, a commit message, a planning document, or a chat message — the repository must contain no credential material at all, which is the posture Phase 14's SEC-02 later automates a check for.

    5. Set the second member's password, or have them set it themselves. Same rule: it is recorded nowhere in this project.

    6. Seed the reference data **exactly once**. From the user menu go to Manage Data, then Foods, then Seed, and pick the language. Wait for it to finish, then switch to Units and seed those the same way. If a separate seed action exists for Labels in this version, seed those too — Labels are what make shopping lists auto-categorise. Do not seed twice: re-seeding is known to fail on a unique constraint over the food name and group, and the failure leaves partial data behind. Note that this is group-scoped work, so seeding once covers both members.

    7. Record what the real interface path was for step 6 if it differed from the description above, so Phase 12's bootstrap instructions are written against reality.

    8. Confirm registration is closed by trying the signup route while logged out. It should not offer self-service account creation. This is a confirmation of the configured posture, not a place to toggle anything — D-09 puts that control in configuration deliberately so it survives a redeploy.
  </how-to-verify>
  <resume-signal>Type "bootstrapped" plus the answer to step 2 (whether administrator-created accounts worked with signup closed) and the real seeding path from step 7, or describe what blocked you</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm both members share one shopping list</name>
  <read_first>
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (Pitfall 8 and the Mealie Application Model: shopping lists are household-scoped, so two accounts in different households produce a silently wrong result with every page still returning HTTP 200)
    - the Task 1 record of which household each account landed in
  </read_first>
  <action>
Halt and hand control to the human with the steps in the how-to-verify block. Two authenticated browser sessions are required, so this cannot be scripted without the repository holding a household credential.

Record into the plan working notes: the duplicate-item behaviour observed in step 7, the ordering observation from step 8, and confirmation that both accounts appear under one household in the admin view. Both observations are backstop truths — the phase requires them observed and recorded, not any particular answer.
  </action>
  <verify>
    <human-check>An item added by household member A is visible and editable by household member B in a separate session. Task 3 corroborates the same fact independently by asserting a household count of 1 and a shopping list count of 1 in the database, so a display that disagrees with the data is caught.</human-check>
  </verify>
  <acceptance_criteria>
    - exactly one shopping list is visible to member A, and it rendered without error while empty
    - member B sees the same single list carrying member A's item
    - an edit made by member B is visible in member A's session after a refresh
    - the duplicate-item behaviour from step 7 is recorded literally (merged row with increased quantity, or a second row)
    - the ordering observation from step 8 is recorded without being treated as a requirement
    - both accounts appear under one household in the admin view
    - no password value appears in the resume signal, the working notes, the SUMMARY, or any committed file
  </acceptance_criteria>
  <what-built>Both accounts now exist and, per Task 1 step 3, should be in one household. Mealie scopes shopping lists to the household rather than the group, so this is the single check that distinguishes "two accounts exist" from "two people share a list". No automated check can stand in for it: proving shared editing needs two authenticated sessions and credentials this repository must not hold.</what-built>
  <how-to-verify>
    1. Log in as household member A at `https://mealie.shad-bangus.ts.net`. Open Shopping Lists.
    2. Confirm exactly one shopping list is present, and that it renders without error while it is still empty. An empty list that errors, or a second list that appeared from somewhere, are both findings.
    3. Add one item to that list as member A.
    4. In a separate browser profile or a private window, log in as household member B. Open Shopping Lists.
    5. Confirm member B sees the same single list, with member A's item on it. If member B sees a different list or an empty one, the two accounts are in different households: go back to the admin area and move the second account, then repeat from step 1. Do not accept "both can log in" as satisfying this criterion.
    6. Edit the item as member B — change its quantity or check it off — and confirm the change is visible back in member A's session after a refresh.
    7. Add a food to the list that is already on it, and record what happens: does it merge into the existing row with an increased quantity, or appear as a second row? Record the literal observed behaviour. This is a backstop item — the phase does not require a particular answer, it requires the answer to be observed rather than assumed.
    8. Note whether items sharing a label appear in a consistent order. Record what you see, but do not treat any particular ordering as required — no acceptance check asserts it.
    9. Confirm both members' accounts appear under one household in the admin view, as a second independent check on step 5's result.
  </how-to-verify>
  <resume-signal>Type "approved" plus the duplicate-item behaviour observed in step 7, or describe which household each account landed in and what you saw</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close the household gate — full suite green with no escape hatch</name>
  <precondition>Tasks 1 and 2 completed: both accounts exist in one household, Foods and Units are seeded, and the administrator credentials have been changed away from the shipped defaults.</precondition>
  <files></files>
  <read_first>
    - scripts/smoketests/household/all.sh, scripts/smoketests/household/test-mealie-service.sh, scripts/smoketests/household/test-mealie-endpoint.sh (the suite written in plan 10-03, including the seeded-data and recipe assertions and the escape hatch that must not be used from here on)
    - .planning/phases/10-household-foundation-and-mealie/10-05-SUMMARY.md (the verbatim list of subtests that were expected to fail before bootstrap — this task closes exactly those and no others)
    - .planning/phases/10-household-foundation-and-mealie/10-04-SUMMARY.md (the confirmed Foods and Units table names the seeded-data assertion queries)
    - .planning/phases/10-household-foundation-and-mealie/10-RESEARCH.md (the note that make smoketests-ser8 exits 1 pre-existing for the NordVPN failure, so the household area's own exit status is the gate)
  </read_first>
  <action>
Turn the bootstrap into evidence, and prove the gate that plan 10-03 built now discriminates in the direction it was designed to.

Run the household smoketest area against ser8 with no escape-hatch variable set anywhere — not on the command line, not exported into the shell, not in a file. Plan 10-04 used it once for the single pre-bootstrap run and plan 10-05 recorded exactly which subtests failed without it. Those subtests are the seeded-data and recipe-persistence ones, and they should now pass because Task 1 seeded Foods and Units.

The recipe-persistence subtest may still fail if no recipe with an image exists yet. If it does, create one recipe with an uploaded image now, through the interface, as household member A. That recipe is not throwaway test data: plan 10-07 uses this exact recipe and image as the subject of the two-reboot persistence proof, so it needs to be a real recipe the household would keep, with a real uploaded image rather than a scraped URL. Record its title and the image filename so plan 10-07 can look for the same bytes.

Independently of the suite, confirm the two security-relevant outcomes directly rather than trusting a script you also wrote. Post a token request using the shipped default administrator email and password against the loopback port on ser8 and confirm the response status is not 200. Read the deployed unit environment and confirm the signup variable holds the non-empty string form rather than an empty value — an empty value is the exact footprint of a Nix boolean surviving the module's stringification, and it would mean the registration control silently does not exist.

Query the database directly for the counts that MEAL-04 turns on: the Foods row count and the Units row count, using the table names confirmed in plan 10-04, and the count of households and of shopping lists. Record all four numbers in the SUMMARY. One household containing both members and one shopping list is the shape MEAL-04 describes; two households, or two lists, means Task 2's verification passed against a display that is not what the data says.

Re-run the full ser8 suite once and compare per-area against the plan 10-04 baseline. The top-level exit status remains 1 because of the pre-existing NordVPN tunnel failure this phase does not own, so the gate is per-area equality plus a green household area — not a zero top-level status.
  </action>
  <verify>
    <automated>./scripts/smoketests/household/all.sh ser8</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/smoketests/household/all.sh ser8` exits 0 with no escape-hatch variable set in the environment or on the command line
    - `env | grep -c MEALIE_ALLOW_UNSEEDED` returns 0 in the shell that ran the suite
    - every subtest that plan 10-05 recorded as an expected pre-bootstrap failure now passes, and no subtest that was passing has started failing
    - on ser8, a token request to the authentication endpoint using the shipped default administrator email and password returns a status other than 200
    - on ser8, `systemctl show mealie -p Environment` contains the signup variable set to the non-empty string form and not to an empty value
    - the Foods row count and the Units row count in the mealie database are each greater than zero, and both numbers are recorded in the SUMMARY
    - the household count in the mealie database is 1 and both member accounts belong to it
    - the shopping list count in the mealie database is 1
    - one recipe with an uploaded image exists, and its title and image filename are recorded in the SUMMARY for plan 10-07 to reuse
    - the post-bootstrap ser8 suite transcript matches the plan 10-04 baseline per area, with the household area green
    - no password appears in the plan SUMMARY, in any command recorded in it, or in any committed file
  </acceptance_criteria>
  <done>The household gate passes unaided, the default administrator credentials are provably dead, the registration control provably exists in the deployed environment, and the database says one household with two members and one shopping list — matching what Task 2 saw in the interface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| household member -> Mealie authentication | The application's own account boundary, established for the first time by this plan |
| Mealie group -> Mealie household | The authorization boundary that decides which members see which shopping lists and meal plans |
| operator browser session -> repository and planning artifacts | Where credential material could accidentally cross from a password manager into version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Spoofing | Mealie shipped default administrator account | high | mitigate | Task 1 changes both email and password at first login, and Task 3 proves it independently by posting a token request with the shipped defaults and requiring a status other than 200 rather than trusting that the change was made |
| T-10-01 | Spoofing | open registration on a freshly deployed instance | high | mitigate | Registration is closed by configuration from first boot (D-09), and Task 3 reads the deployed unit environment to confirm the value is the non-empty string form rather than the empty string a Nix boolean would produce |
| T-10-07 | Information Disclosure | cross-household exposure inside one Mealie group | medium | mitigate | Both members are deliberately placed in the same household so recipes and lists are shared by design; Task 2 verifies it in the interface and Task 3 verifies it independently in the database. Starting on 3.22.0 puts the instance past the v2.5.0 household privilege-escalation fixes |
| T-10-04 | Information Disclosure | credential material reaching the repository or planning artifacts | high | mitigate | Both credential steps route through the household password manager; no value is typed into a terminal, a commit message, or a planning document. Recorded as an active prohibition in `must_haves`, with an explicit acceptance criterion that no password appears in the SUMMARY or any committed file |
| T-10-20 | Repudiation | accepting "both members can log in" as proof of a shared list | high | mitigate | Task 2 requires an item added by one member to be visible and editable by the other, and Task 3 independently asserts a household count of 1 and a shopping list count of 1 in the database. RESEARCH.md Pitfall 8 is explicit that the wrong placement produces no error anywhere |
| T-10-21 | Tampering | duplicate or partial reference data from a second seed | medium | mitigate | Task 1 states the seed is a once-only action and names the unique constraint a re-seed violates, and Task 3 records the resulting row counts so a partial seed is visible as a number rather than inferred from the interface |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan (RESEARCH.md Package Legitimacy Audit: not applicable to this phase, zero `[SLOP]`, zero `[SUS]`) |
</threat_model>

<artifacts_this_phase_produces>
**Application state created inside Mealie (not repository state, and not committed anywhere):** the administrator account with changed credentials; the second household member's account; one household containing both members; the seeded Foods, Units, and (if a separate action exists) Labels reference data; one shopping list; one recipe with an uploaded image that plan 10-07 reuses as the subject of its persistence proof.

**Files:** none. This plan changes no repository file.

**Recorded artifacts under the phase directory:** the answer to RESEARCH.md Open Question 4 (whether administrator-created accounts work with signup closed), the real interface path for the reference-data seed, the observed duplicate-item behaviour on the shopping list, the four database counts, and the title and image filename of the recipe plan 10-07 will look for.

No credential value of any kind is an artifact of this plan.
</artifacts_this_phase_produces>

<verification>
- The shipped default administrator credentials return a status other than 200 against the authentication endpoint
- The deployed unit environment carries the signup variable in its non-empty string form
- The database reports one household containing both member accounts and exactly one shopping list
- Foods and Units row counts are both greater than zero
- An item added by one member is visible and editable by the other, verified in two browser sessions
- The empty shopping list renders without error before any item is added
- The household smoketest area exits 0 with no escape-hatch variable anywhere in the environment
- Exactly the subtests plan 10-05 recorded as expected failures have turned green, with no new failures
- No password appears in the SUMMARY or in any committed file
</verification>

<success_criteria>
Mealie is in daily household condition: two people, one household, one shared shopping list, seeded reference data, dead default credentials, and a registration control that lives in configuration rather than in a toggle someone has to remember. The suite that plan 10-03 wrote before any of it existed now passes on its own merits.
</success_criteria>

<output>
Create `.planning/phases/10-household-foundation-and-mealie/10-06-SUMMARY.md` when done.
</output>
