---
phase: 11-homebox-actual-budget-and-donetick
plan: 06
type: execute
wave: 6
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05"]
files_modified: []
autonomous: true
requirements: [HBX-01, ACT-01, DTK-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Homebox's group membership (jordan+sawnia, same group id) is identical before and after a real ser8 reboot."
    - "Actual's account.sqlite files table still shows exactly one non-deleted, unencrypted budget file after a real ser8 reboot."
    - "Donetick's circle membership (jordan+sawnia, same circle id, both active) is identical before and after a real ser8 reboot."
    - "All three services' /persist/var/lib/<svc> directories and their bind-mounted /var/lib/<svc> counterparts exist with correct ownership immediately after the reboot, before any manual intervention."
    - "make reboot-test-ser8 exits 0, meaning the full ser8 smoketest suite -- including the six new household service/endpoint scripts from plans 11-01, 11-03, and 11-05 -- passes against the post-reboot state, not just a hand-picked subset."
  artifacts: []
  key_links:
    - "environment.persistence.\"/persist\".directories entries for homebox/actual/donetick -> the bind mounts that must survive stage-1 boot"
  prohibitions:
    - statement: "Must NOT treat a partial or timed-out post-reboot check as a pass -- if any of the three apps' post-reboot snapshot cannot be captured (service down, SSH timeout), the plan reports that app's persistence as UNVERIFIED, not as passing by omission."
      status: active
      verification: flagged-unverified
---

<objective>
Prove -- not assume -- that all three Phase 11 apps survive a real ser8 reboot, closing ROADMAP.md's phase-level Success Criterion 3 for real rather than accepting "configuration evidence" the way Phase 10 waived its own two-reboot drill (10-07, dropped 2026-08-20). One reboot covers all three services; this plan does not repeat plan 10-07's per-service ceremony.

Purpose: HBX-01, ACT-01, and DTK-02 each promise persisted state; this is the plan that actually tests the promise against a live reboot instead of just the impermanence directory list.

Output: one dated evidence file under `baseline/` recording the pre/post snapshot comparison for all three apps, and a green `make reboot-test-ser8`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/11-homebox-actual-budget-and-donetick/11-01-SUMMARY.md
@.planning/phases/11-homebox-actual-budget-and-donetick/11-03-SUMMARY.md
@.planning/phases/11-homebox-actual-budget-and-donetick/11-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot, reboot ser8 once, verify all three apps' state survived</name>
  <files>none (produces one evidence file under .planning/phases/11-homebox-actual-budget-and-donetick/baseline/, no repository code changes)</files>
  <read_first>
    - .planning/phases/09-channel-bump-to-nixos-26-05/baseline (Phase 9's precedent for a pre/post-reboot evidence record and the impermanence-marker technique, if a baseline directory exists there -- otherwise skip)
    - hosts/ser8/impermanence.nix (the full list of persisted directories this task is proving, including the three added across plans 11-01/11-02/11-04)
    - scripts/smoketests/household/all.sh (the full TESTS array this reboot must leave green)
  </read_first>
  <action>
Before rebooting, capture a snapshot on ser8 for each app, saved to `.planning/phases/11-homebox-actual-budget-and-donetick/baseline/reboot-pre-<date>.md`:
- Homebox: `GET /api/v1/groups/members` (any valid token from plan 11-01) -- record the member count and group id.
- Actual: `sudo sqlite3 /var/lib/actual/server-files/account.sqlite "SELECT count(*), group_concat(id) FROM files WHERE deleted=0"` -- record the count and file id.
- Donetick: `GET /api/v1/circles/members` (any valid token from plan 11-05) -- record the active-member count and circle id.
- For all three: `stat -c '%U %G %a' /var/lib/<svc> /persist/var/lib/<svc>` -- record ownership/mode on both sides of the bind mount.

This reboot affects a live household server for the duration of the reboot cycle (expect roughly one to two minutes of downtime for every service on ser8, not just the three new ones). Proceed -- this is the explicit purpose of this plan, not an incidental side effect.

Reboot and re-run the full ser8 smoketest suite in one step: `make reboot-test-ser8`. This both reboots the host and runs `scripts/smoketests/ser8/all.sh`, which fans out into `scripts/smoketests/household/all.sh` and therefore exercises all six new household service/endpoint scripts from plans 11-01, 11-03, and 11-05 (plus the pre-existing Mealie ones) against the post-reboot state.

After the reboot completes, re-capture the same four data points for each app and compare against the pre-reboot snapshot. Write both snapshots and the diff into `.planning/phases/11-homebox-actual-budget-and-donetick/baseline/reboot-pre-<date>.md` and `reboot-post-<date>.md` (or one combined file, following the Phase 10 `baseline/` naming convention). If any app's post-reboot snapshot cannot be captured (service down, SSH timeout, unexpected schema), record that app's persistence as UNVERIFIED explicitly -- do not infer a pass from partial data.
  </action>
  <verify>
    <automated>make reboot-test-ser8</automated>
  </verify>
  <acceptance_criteria>
    - `make reboot-test-ser8` exits 0
    - Homebox's `GET /api/v1/groups/members` count and group id are identical pre- and post-reboot
    - Actual's `files` table count, id, and (still-empty) `encrypt_keyid` are identical pre- and post-reboot
    - Donetick's `GET /api/v1/circles/members` count and circle id are identical pre- and post-reboot
    - all three services' `/var/lib/<svc>` and `/persist/var/lib/<svc>` ownership/mode checks match their plan's original acceptance criteria (homebox:homebox, actual:actual mode 0700, donetick:donetick) immediately after the reboot
    - a dated evidence file exists under `.planning/phases/11-homebox-actual-budget-and-donetick/baseline/` recording both snapshots and the comparison
  </acceptance_criteria>
  <reversibility rating="reversible">A reboot is a routine, fully reversible operation on hosts already rebooted repeatedly in Phase 9; the persistence pattern under test is impermanence's entire purpose.</reversibility>
  <done>All three apps' household-scoped state (group/circle membership, budget-file existence) and their persisted directories are proven identical before and after a real ser8 reboot, and the full ser8 smoketest suite -- including all six new household scripts -- passes against the post-reboot state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stage-1 boot -> impermanence rollback | The window where `/var/lib` is erased and bind-mounted back from `/persist`; a persistence-entry mistake in any of plans 11-01/11-02/11-04 is invisible until this exact moment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-17 | Denial of Service | A missed persistence entry silently loses one app's state on reboot | high | mitigate | This entire plan exists to catch exactly this before the household discovers it themselves; pre/post snapshots are compared value-by-value, not just directory-existence checks |
| T-11-18 | Repudiation | A partial post-reboot failure reported as a pass | medium | mitigate | Task 1's action and acceptance criteria explicitly forbid inferring a pass from a service that could not be reached after reboot |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan |
</threat_model>

<artifacts_this_phase_produces>
**Directories:** none new (writes into the existing `.planning/phases/11-homebox-actual-budget-and-donetick/baseline/`)

**Files:** one or two dated evidence files under `baseline/` (`reboot-pre-<date>.md` / `reboot-post-<date>.md` or a combined file)

**Existing symbols extended:** none -- this plan changes no repository code
</artifacts_this_phase_produces>

<verification>
- `make reboot-test-ser8` exits 0
- pre/post snapshots for all three apps match exactly
- a dated evidence record exists under `baseline/`
</verification>

<success_criteria>
ROADMAP.md's Phase 11 Success Criterion 3 -- "survives a ser8 reboot" -- is proven by a real reboot and a value-level snapshot comparison, not accepted on configuration evidence alone.
</success_criteria>

<output>
Create `.planning/phases/11-homebox-actual-budget-and-donetick/11-06-SUMMARY.md` when done.
</output>
