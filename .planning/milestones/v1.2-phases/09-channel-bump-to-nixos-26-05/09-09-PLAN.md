---
phase: 09-channel-bump-to-nixos-26-05
plan: 09
type: execute
wave: 2
depends_on: ["08"]
gap_closure: true
files_modified:
  - .planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements: [FOUND-01, FOUND-02]

user_setup:
  - service: local-nix-daemon
    why: "Revoking the third-party binary cache trust rewrites /etc/nix/nix.custom.conf and restarts the Determinate nix-daemon, both of which need root on the developer machine (threat T-09-06)"
    dashboard_config:
      - task: "Run `sudo make update-nix-conf` from the repository root, then confirm `grep -c cachix /etc/nix/nix.custom.conf` outputs 0"
        location: "Developer machine, local terminal"
  - service: ser8
    why: "The impermanence-rollback and ZFS userland/kmod proofs require a live reboot of the production host; no command available to an agent session can produce them"
    dashboard_config:
      - task: "Reboot ser8 and collect the marker, zfs version, and scrub evidence"
        location: "ser8 console or ssh session"

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The third-party nixos-raspberrypi cachix substituter, trusted substituter, and trusted public key are absent from the installed daemon configuration this machine actually uses, not merely from the repository copy (VERIFICATION gap 2, threat T-09-06)"
    - "ROADMAP Phase 9 SC1's dry-activate clause has an explicit, recorded human accept-or-reject decision for pi4 and pi5 — the evaluation-level evidence bar is either formally accepted as an override or the gap stays open with a named condition for closing it (VERIFICATION gap 1)"
    - "The outcome of that decision is written into 09-VERIFICATION.md and STATE.md, so a later reader can tell an accepted override from an unnoticed omission"
    - "ser8's first boot on 26.05 has either been performed with the impermanence and ZFS proofs recorded, or is explicitly deferred with both outstanding proofs still visible in STATE.md — it is never silently assumed to have happened"
    - "If ser8 is rebooted, /IMPERMANENCE-MARKER-09-05 is absent afterward, proving the stage-1 systemd rollback migrated in 09-01 actually fires"
    - "If ser8 is rebooted, `zfs version` reports matching userland and kmod versions and a scrub completes without the module-support error"
    - "A reboot that goes badly has a stated recovery path that does not depend on `make rollback-HOST`, which prints TODO and is not a recovery mechanism (edge: failure, FOUND-01)"
  prohibitions:
    - statement: "MUST NOT run `make update-nix-conf`, `make reboot-ser8`, or any privileged host operation autonomously. Every task in this plan is gated on a human because the action needs root on the developer machine or interrupts a live household service."
      category: safety
    - statement: "MUST NOT record the SC1 override as accepted without an explicit human answer. The verifier named this a judgment call the plan's own authors assigned to the user; an agent-supplied acceptance would launder a gap into a decision."
      category: transparency
    - statement: "MUST NOT mark the ser8 reboot proofs satisfied on the basis of a successful `switch`. The switch already happened; the booted system is still the 25.11 kernel, and only a real reboot exercises the stage-1 initrd path these proofs are about."
      category: transparency
  artifacts:
    - path: ".planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md"
      provides: "Updated gap dispositions: the cachix gap closed on evidence, the SC1 gap either overridden with rationale or left explicitly open"
      contains: "overrides_applied"
    - path: ".planning/STATE.md"
      provides: "Accumulated-context entries recording the cachix revocation, the SC1 decision, and the reboot-proof status"
      contains: "T-09-06"
  key_links:
    - from: "etc/nix/nix.custom.conf (repository copy, already clean)"
      to: "/etc/nix/nix.custom.conf (installed daemon configuration)"
      via: "the `update-nix-conf` Makefile target copies the repository file over the installed one and restarts the nix-daemon"
      pattern: "update-nix-conf"
---

<objective>
Close the two phase-09 verification gaps that no agent can close, and settle the two reboot proofs the phase left outstanding — each through an explicit human gate rather than an assumption.

Purpose: 09-VERIFICATION.md records three gaps. Plan 09-08 closes the one made of code. The other two are human-only by nature: revoking the third-party binary cache trust rewrites `/etc/nix` and restarts the nix-daemon on the developer machine, and deciding whether evaluation-level evidence satisfies ROADMAP SC1's "every host dry-activates" wording is a judgment the verifier explicitly assigned to the user. Two further items — the impermanence-rollback proof and the ZFS userland/kmod skew — need a live reboot of ser8 and are routed here so they stay visible rather than being quietly treated as settled by the switch that already happened.

Output: an installed daemon config with no third-party cache trust, a recorded accept-or-reject on the SC1 evidence bar, and either the ser8 reboot evidence or an explicit deferral that keeps both proofs outstanding in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-channel-bump-to-nixos-26-05/09-CONTEXT.md
@.planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md
@CLAUDE.md
</context>

## Executor notes

Every task here blocks on a human. The executor's job is to present the exact commands, collect the human's output verbatim, and record the result — not to attempt the privileged action, and not to supply the decision. If a checkpoint is declined or deferred, record the deferral and its consequence in the SUMMARY and STATE.md and move to the next task; a declined checkpoint is a valid outcome, an undocumented one is not.

`workflow.auto_advance` does not apply to any task in this plan.

## Carried forward, deliberately not in scope

- pi5's `deploy.yaml` entry (`192.168.0.110`, `targetUser: nixos`) is stale (09-02 blocker). Correcting it needs the board's real address, which only the user has. It stays a recorded blocker; plan 09-08's pi5 entry point names it as one of the conditions for closing that host's coverage gap.
- The Frigate go2rtc live-stream 403 is pre-existing and its remedy trades away a CSRF protection — a separate decision against `modules/automation/frigate.nix`, not this phase's.
- ser8's media UID/GID drift (group 992→1100, user 1002→1100) predates the bump by a year and needs its own plan with a deliberate re-chown.

## Artifacts this phase produces

Created or modified by this plan (not code):
- `.planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md` — gap dispositions updated; an `overrides` entry added if the SC1 checkpoint is accepted
- `.planning/STATE.md` — accumulated-context entries for the cachix revocation, the SC1 decision, and the ser8 reboot-proof status

This plan creates no source files, no scripts, and no new symbols.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Revoke the third-party binary cache trust on the installed nix daemon config</name>
  <files>.planning/STATE.md, .planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md (gap 2: the repository copy is clean, the installed copy is not; `grep -c cachix /etc/nix/nix.custom.conf` returned 3 at verification time)
    - Makefile lines 129-137 (the `update-nix-conf` recipe: it backs up `/etc/nix/machines` and `/etc/nix/nix.custom.conf` to `.old`, copies the repository copies over them, then restarts the daemon with `launchctl kickstart -k system/systems.determinate.nix-daemon`)
    - etc/nix/nix.custom.conf (the repository copy that will be installed — already free of cachix references)
  </read_first>
  <what-built>
    09-02 removed the `nixos-raspberrypi` fork input along with its cachix substituter, trusted substituter, and trusted public key from `flake.nix`'s `nixConfig` block and from the repository's `etc/nix/nix.custom.conf`. Both repository files are confirmed clean. Nothing installed the cleaned file onto the machine, so the daemon that actually resolves substituters still trusts `nixos-raspberrypi.cachix.org`. Threat T-09-06 stays open until the installed file matches the repository one.
  </what-built>
  <action>
    Present this checkpoint to the user and wait. Do not attempt `sudo make update-nix-conf` — the sandboxed session has no root, and an ad hoc edit of `/etc/nix/nix.custom.conf` would recreate the repository-versus-machine divergence that produced this gap. When the user pastes their output, record it verbatim in the SUMMARY, then update 09-VERIFICATION.md's gap 2 entry and replace the STATE.md blocker entries (recorded under 09-02 and 09-07) that state the third-party trust is still live.
  </action>
  <instructions>
    From the repository root on the developer machine:

    1. Record the starting state: `grep -c cachix /etc/nix/nix.custom.conf` (expected 3).
    2. Run `sudo make update-nix-conf`. It copies `./etc/nix/machines` and `./etc/nix/nix.custom.conf` over the installed files (keeping `.old` backups of both) and restarts the Determinate nix-daemon.
    3. Confirm the installed file is clean: `grep -c cachix /etc/nix/nix.custom.conf` must output 0.
    4. Confirm the running daemon agrees, not just the file on disk: `nix config show substituters` and `nix config show trusted-public-keys` must contain no `nixos-raspberrypi.cachix.org` entry and no key attributed to it.
    5. Confirm the daemon is healthy after the restart: `nix store ping` succeeds.

    Paste the output of steps 1, 3, 4, and 5. If step 2 fails, paste the error rather than editing `/etc/nix` by hand — an ad hoc edit leaves the repository and the machine out of sync again, which is how this gap arose.
  </instructions>
  <verification>
    The gap is closed only when all four of these hold together: `grep -c cachix /etc/nix/nix.custom.conf` outputs 0, `nix config show substituters` names no cachix host, `nix config show trusted-public-keys` carries no key attributed to `nixos-raspberrypi.cachix.org`, and `nix store ping` succeeds after the daemon restart. The file alone is not sufficient evidence — the daemon must be re-read, which is why the two `nix config show` calls are part of the proof rather than a formality.
  </verification>
  <acceptance_criteria>
    - `grep -c cachix /etc/nix/nix.custom.conf` outputs `0`.
    - `nix config show substituters` lists no `nixos-raspberrypi.cachix.org` entry.
    - `nix config show trusted-public-keys` lists no `nixos-raspberrypi.cachix.org` key.
    - `nix store ping` succeeds after the daemon restart.
    - 09-VERIFICATION.md's gap 2 entry is updated from `failed` to closed, citing the command output the user pasted as the evidence.
    - STATE.md records that threat T-09-06 is closed, replacing the 09-02 and 09-07 blocker entries that say the trust is still live.
  </acceptance_criteria>
  <verify>
    <automated>grep -c cachix /etc/nix/nix.custom.conf | grep -qx 0 && nix config show substituters | grep -c 'nixos-raspberrypi' | grep -qx 0 && nix store ping</automated>
  </verify>
  <reversibility rating="reversible">The recipe keeps `/etc/nix/nix.custom.conf.old`; restoring it and restarting the daemon returns the prior configuration.</reversibility>
  <resume-signal>Paste the outputs of the grep, the two `nix config show` commands, and `nix store ping`, or describe what went wrong.</resume-signal>
  <done>The installed daemon configuration and the running daemon both carry no third-party cache trust, and 09-VERIFICATION.md plus STATE.md say so with the evidence attached.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide whether evaluation-level evidence satisfies SC1 for pi4 and pi5</name>
  <files>.planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md (gap 1 and the third Human Verification item — the verifier calls this "a strong override candidate" but declines to decide it)
    - .planning/phases/09-channel-bump-to-nixos-26-05/09-02-SUMMARY.md (the executor's own disclosure: "The criterion is not met literally by either Pi, and that gap is named here rather than redefined away")
    - .planning/phases/09-channel-bump-to-nixos-26-05/09-CONTEXT.md (D-06 pi4 disconnected and pending retirement, D-07 pi5 build-level only, D-13 the FOUND-02 evidence bar is evaluation/dry-activate level)
    - .planning/ROADMAP.md lines 60-66 (Phase 9 Success Criteria — SC1's literal wording)
  </read_first>
  <decision>
    ROADMAP Phase 9 SC1 says every host (ser8, firebat, pi4, pi5) dry-activates without evaluation errors. ser8 and firebat have real `make dry-activate-<host>` transcripts. pi4 and pi5 have local `nix build --dry-run ... toplevel` evaluation plus `test-pi-bootloader.sh` only, because pi4 is physically disconnected and pi5's deploy.yaml address does not answer. Is the evaluation-level bar accepted as sufficient for this milestone, or does the gap stay open until the hosts are reachable?
  </decision>
  <context>
    This matters beyond bookkeeping. Evaluation is a materially weaker guarantee than a remote activation preview: 09-01 hit a caddy vendor-hash mismatch that `nix build --dry-run` could not see and only a real build surfaced. That is precisely the failure class pi4 and pi5 have no coverage for. Against that, D-13 already defined FOUND-02's evidence bar as evaluation level for exactly these hosts, D-06 records pi4 as disconnected and pending retirement or repurpose, and D-07 scopes pi5 to build only. Neither Pi runs a service this milestone depends on, and pi4's own smoketest gate is being made loudly red by plan 09-08.
  </context>
  <options>
    <option id="accept-override">
      <name>Accept the evaluation-level bar; record a formal override</name>
      <pros>Matches D-13, which set this bar before the work started; the disclosure trail is thorough and was never redefined away; the phase closes on the strength of what was actually proven rather than staying open on hosts nobody can reach</pros>
      <cons>SC1's literal wording is marked satisfied for two hosts that never received the stronger evidence class; a build-time-only failure on either Pi would still be undiscovered</cons>
    </option>
    <option id="leave-open">
      <name>Leave the gap open until pi4 is reconnected and pi5's address is corrected</name>
      <pros>Keeps SC1 honest at face value; the missing evidence stays visible in the roadmap rather than in an override footnote</pros>
      <cons>Blocks phase 09 closure on a physically disconnected board and a stale address, neither of which the milestone needs; the same gap will be re-litigated at every later audit</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The user's answer is recorded verbatim in the SUMMARY, attributed as a user decision rather than an executor conclusion.
    - If `accept-override`: 09-VERIFICATION.md gains an `overrides` entry naming the gap, the decision date, the rationale in the user's terms, and the residual risk (no build-time-only coverage for either Pi); `overrides_applied` is incremented from 0 to 1; the gap 1 entry's status reflects the override rather than being deleted.
    - If `leave-open`: 09-VERIFICATION.md's gap 1 stays `partial` and gains an explicit closing condition (a real `make dry-activate-pi4` / `make dry-activate-pi5` transcript), and `overrides_applied` stays 0.
    - Either way STATE.md records the decision under Accumulated Context so a later phase inherits it, and the record names which of the two options was chosen.
    - No ROADMAP success criterion is reworded. An override is recorded next to the criterion, never in place of it.
  </acceptance_criteria>
  <verify>
    <human-check>Read the recorded override or the recorded closing condition in 09-VERIFICATION.md and confirm it says what you decided, in the terms you decided it.</human-check>
  </verify>
  <reversibility rating="reversible">A recorded disposition in planning documents; revisable by a later audit with no system impact.</reversibility>
  <resume-signal>Select: accept-override or leave-open, with one sentence of rationale to record.</resume-signal>
  <done>09-VERIFICATION.md and STATE.md both carry the user's explicit disposition of SC1's pi4/pi5 clause, and neither presents an agent's judgment as the user's.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reboot ser8 and collect the impermanence and ZFS proofs, or defer them explicitly</name>
  <files>.planning/STATE.md, .planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-channel-bump-to-nixos-26-05/09-VERIFICATION.md (`behavior_unverified_items` and the first two Human Verification entries — both proofs, their expected outcomes, and why a session cannot produce them)
    - .planning/STATE.md (the 09-05 blocker entry: generation 268 is the boot default, the marker is planted and still present, the ZFS skew is userland 2.4.3-1 against kmod 2.3.7-1, and generation 267 in the systemd-boot menu is the recovery path)
    - scripts/smoketests/ser8/test-zfs-health.sh (the committed assertions on pool health, the `rpool/local/root@blank` rollback snapshot, and the deliberate no-upgrade check — the suite that should be re-run after the reboot)
  </read_first>
  <what-built>
    09-01 migrated ser8's erase-your-darlings rollback from `boot.initrd.postDeviceCommands` to a stage-1 systemd oneshot, because 26.05 asserts on the old form under its new systemd-initrd default. 09-05 switched ser8 to 26.05 (generation 268 is the boot default) and planted `/IMPERMANENCE-MARKER-09-05` on `rpool/local/root` as a tripwire. ser8 has not been rebooted: `/run/booted-system` is still the 25.11 kernel, so the new initrd path has never executed, and the ZFS userland shipped by 26.05 (2.4.3-1) is running against the 25.11 kernel module (2.3.7-1), which makes `zfs scrub` refuse to start. Both conditions clear — or fail visibly — on the first reboot, and nothing short of a reboot tests them.
  </what-built>
  <how-to-verify>
    Reboot is optional at this checkpoint. Deferring is a valid answer; assuming is not.

    Before rebooting, confirm the recovery path exists: `ssh bdhill@192.168.68.65 'bootctl list | head -40'` must still show the generation-267 (25.11) entry. If a boot on 26.05 fails, select that entry in the systemd-boot menu at the console. `make rollback-ser8` prints TODO and is not a recovery mechanism.

    Then reboot (`make reboot-ser8`, or from the host) and collect, in this order:

    1. Booted onto the new generation: `ssh bdhill@192.168.68.65 'nixos-version; readlink -f /run/booted-system'` — the booted system must now be the 26.05 generation, not just the profile.
    2. Impermanence proof: `ssh bdhill@192.168.68.65 'test -e /IMPERMANENCE-MARKER-09-05 && echo PRESENT || echo ABSENT'` — must print ABSENT. PRESENT means the stage-1 rollback did not fire and impermanence is silently broken on the new initrd; stop and report it rather than continuing, because every later phase's persistence assumptions rest on it.
    3. ZFS skew cleared: `ssh bdhill@192.168.68.65 'zfs version'` — the userland and kmod lines must report the same version.
    4. Scrub completes: `ssh bdhill@192.168.68.65 'sudo systemctl start zfs-scrub.service'`, then `systemctl status zfs-scrub.service` and `zpool status rpool backup` — the unit must complete without the loaded-module-does-not-support-an-option error, and both pools must stay healthy.
    5. Suite still green after a real boot: `make smoketests-ser8` — media, zfs, vaapi, frigate, and home-assistant areas all pass, with only the pre-existing NordVPN `test-forwarding.sh` red.

    If you choose not to reboot now, say so. The two proofs then stay open in STATE.md with the marker still planted, and the scrub timer's next fire (2026-08-24) becomes the natural deadline.
  </how-to-verify>
  <acceptance_criteria>
    - Either: all five outputs above are pasted, `/IMPERMANENCE-MARKER-09-05` is ABSENT, `zfs version` shows matching userland and kmod, `zfs-scrub.service` completes, both pools are healthy, and `make smoketests-ser8` matches its documented result — in which case 09-VERIFICATION.md's two `behavior_unverified_items` are marked verified with the transcripts cited and the corresponding STATE.md blocker entries are closed.
    - Or: the reboot is explicitly deferred, and both proofs remain listed as outstanding in STATE.md with the marker's continued presence noted, the ZFS skew unchanged, and the generation-267 recovery path restated.
    - The SUMMARY never claims the reboot happened on the basis of the switch. The switch is already recorded; only `/run/booted-system` resolving to the 26.05 generation proves a boot.
    - If the marker survives the reboot, that outcome is recorded as a failure of the 09-01 rollback migration and raised as a blocker, not smoothed over.
  </acceptance_criteria>
  <verify>
    <human-check>Confirm from the pasted output that the booted system is the 26.05 generation, the marker is absent, `zfs version` reports matching userland and kmod, and the scrub completed — or confirm that the reboot was deferred and both proofs are still listed as open.</human-check>
  </verify>
  <reversibility rating="costly">A reboot cannot be undone, and a failed 26.05 boot interrupts household media, Home Assistant, and Frigate until recovery. The recovery path is real and confirmed present (select generation 267 at the systemd-boot menu, which requires console access to ser8), which is why this is rated costly rather than one-way.</reversibility>
  <resume-signal>Paste the five outputs, or type "deferred" to keep both proofs open.</resume-signal>
  <done>ser8's first 26.05 boot is either proven — marker gone, ZFS skew cleared, scrub complete, suite green — or explicitly deferred with both proofs still visible in STATE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party binary cache → local nix daemon | A trusted substituter and public key let an external party's store paths be accepted without local rebuild |
| planning record → future phases | An override recorded here becomes the evidence bar a later audit inherits |
| ser8 boot path → household services | The stage-1 initrd rollback and the bootloader entry are the only things standing between a reboot and an unavailable media, Home Assistant, and Frigate stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06 | Tampering | `/etc/nix/nix.custom.conf` on the developer machine — `nixos-raspberrypi.cachix.org` still trusted as substituter and public key | high | mitigate | Task 1: install the already-clean repository copy with `sudo make update-nix-conf`, then confirm both the file and the running daemon (`nix config show substituters`, `trusted-public-keys`) carry no reference to it |
| T-09-09-01 | Repudiation | 09-VERIFICATION.md gap dispositions | medium | mitigate | Task 2 requires the user's own words for the SC1 disposition and forbids an agent-supplied acceptance; the override is recorded beside the criterion with its residual risk, never in place of it |
| T-09-09-02 | Denial of service | ser8 first boot on 26.05 (stage-1 systemd initrd, updated systemd-boot on the ESP) | medium | mitigate | Task 3 requires confirming the generation-267 (25.11) boot entry is still listed before rebooting, states the console-menu recovery path, and records explicitly that `make rollback-ser8` is a placeholder rather than a recovery mechanism |
| T-09-09-03 | Information disclosure | Command output pasted into the checkpoint (nix config, bootctl, zpool status) | low | accept | None of these commands emit secret material; `nix config show` prints public keys and substituter URLs only |
| T-09-09-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages from any language package manager; it runs one Makefile target that copies repository-tracked configuration and, optionally, reboots a host |
</threat_model>

<verification>
1. `grep -c cachix /etc/nix/nix.custom.conf` outputs 0 and `nix config show substituters` names no cachix host (Task 1).
2. 09-VERIFICATION.md's gap 2 entry reflects closure with the pasted evidence cited.
3. 09-VERIFICATION.md carries an explicit disposition for gap 1: an `overrides` entry with rationale and residual risk, or an unchanged `partial` status with a stated closing condition (Task 2).
4. STATE.md's Accumulated Context reflects all three outcomes, and the stale blocker entries claiming the cachix trust is still live are replaced rather than left alongside the new ones.
5. If ser8 was rebooted, the marker-absent, matching-`zfs version`, completed-scrub, and green-suite transcripts are quoted in the SUMMARY. If it was not, both proofs remain listed as open in STATE.md.
</verification>

<success_criteria>
- VERIFICATION gap 2 is closed on pasted evidence from the user's own machine, not inferred from the clean repository copy.
- VERIFICATION gap 1 has an explicit human accept-or-reject on the record, with the phase's residual risk stated either way.
- The two reboot-dependent proofs are either satisfied with transcripts or explicitly still open — never quietly assumed.
- No privileged or service-interrupting action in this plan was performed without the human gate that owns it.
</success_criteria>

<output>
Create `.planning/phases/09-channel-bump-to-nixos-26-05/09-09-SUMMARY.md` when done.
</output>
