---
phase: 13-zfs-mirror-migration
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified: []
autonomous: false
requirements: [ZFS-02, ZFS-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 7
  confidence: low

must_haves:
  truths:
    - "Both approved WWNs are re-resolved and re-confirmed (model, serial, size, filesystem, mounts) immediately before the erase, independent of any earlier confirmation (Step 4.1's own approval does not carry into Step 4.3, D-11 Additional Disk Approval Gate)."
    - "Approval for one numbered step never authorizes the next, even when steps are directly adjacent -- every step in this plan (4.1, 4.2, 4.3, 4.4, 4.5-test, 4.5-switch) requires its own separate approval before it runs (Approval Contract; D-11)."
    - "No destructive command targets any device in the Explicitly Protected Devices list (system NVMe, four backup-pool WWNs)."
    - "Media units and orchestration oneshots are masked before make test-ser8/switch-ser8 run, so activation cannot start services against the empty mirror (D-16)."
    - "zpool status media shows exactly one mirror-0 vdev with both approved WWNs online, and nothing else, before make test-ser8 runs."
  artifacts:
    - path: "zpool media (live ZFS pool on ser8)"
      provides: "The two-disk mirror vdev, empty, ready for restore"
    - path: "media/data (live ZFS dataset on ser8)"
      provides: "Mounted at /mnt/media once the matching Nix generation activates"
  key_links:
    - from: "hosts/ser8/disko-config.nix (from Plan 13-02)"
      to: "live zpool media on ser8"
      via: "make switch-ser8 activates the exact declaration Plan 13-02 wrote and this plan validated with make build-ser8"
      pattern: "zpool create.*media|mirror"
  prohibitions:
    - statement: "Never infer approval for a destructive step from general conversational affirmation ('looks good', 'continue', 'go ahead') -- steps 4.2 through 4.4 require the exact typed echo-back (step id or WWN suffix) specified in the Approval Contract and D-11; every other step requires the structured gate response, never an inferred green light."
      status: unverified
      flagged: true
    - statement: "Never delete or destroy staging or media data via a recursive filesystem command through a mountpoint (rm -rf, cp with delete, etc.) -- dataset creation and destruction go through zpool create / zfs create / zfs destroy only, matching the Safety Rules' explicit ban on recursive deletion against /mnt/media, /mnt/media-staging, /mnt/backups, or any pool mountpoint."
      status: unverified
      flagged: true
---

<objective>
Execute the doc's Stage 4: reconfirm the two approved disk identities, unmount MergerFS and both ext4 members, erase and partition only the approved disks, create the empty `media` mirror, then activate the matching Nix generation with media services masked (D-16).

Purpose: This is the migration's point of no return -- once the ext4 filesystems are erased, `backup/media-staging` (verified clean by Plan 13-04's gate 3.3) becomes the sole remaining copy of the media tree until Plan 13-06's restore completes. Every step here gets its own separate approval per the Approval Contract; disk-mutating steps (4.2-4.4) require a typed echo-back, not a structured-gate click, per D-11.
Output: A live two-disk `media` mirror (empty), ser8 activated on the `zfs-media-mirror` branch with media services masked, the branch merged to `main`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-zfs-mirror-migration/13-CONTEXT.md
@.planning/phases/13-zfs-mirror-migration/13-04-SUMMARY.md
@.planning/SER8-ZFS-MIRROR-MIGRATION.md
@hosts/ser8/media/orchestration.nix
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 4.1: Reconfirm destructive targets</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-4.1, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Approved-Disk-Inventory, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Explicitly-Protected-Devices</read_first>
  <precondition>Plan 13-04's gate 3.3 verification task passed with zero unexplained differences.</precondition>
  <decision>Approve running the read-only identity resolution commands and reviewing the exact upcoming destructive commands (not yet run).</decision>
  <context>
    Mutation class: read-only identity gate. Kernel device names swap between boots (already observed once: sde/sdf swapped since the doc's 2026-08-13 baseline) -- only `/dev/disk/by-id/wwn-*` paths are trustworthy.
    The approved identity tuple that must match exactly: `wwn-0x5000c500b56ea81a / ST12000NM0117-2GY101 / ZJV4C2NZ / 12,000,138,625,024 bytes` and `wwn-0x5000c500b3733a87 / ST12000NM0117-2GY101 / ZJV2V0K9 / 12,000,138,625,024 bytes`. Protected devices that must NEVER appear in a destructive command: the system NVMe and all four `backup-disk*` WWNs.
  </context>
  <options>
    <option id="approve"><name>Run the identity reconfirmation</name><pros>Establishes the exact targets for every subsequent destructive step.</pros><cons>None -- read-only.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks the rest of this plan.</cons></option>
  </options>
  <action>
    After approval, over SSH: `lsblk -o NAME,SIZE,TYPE,FSTYPE,MODEL,SERIAL,MOUNTPOINT`; `readlink -f /dev/disk/by-id/wwn-0x5000c500b56ea81a` and the second WWN; `udevadm info --query=all --name=<resolved-kernel-device>` for both; `smartctl -a` for both (re-confirm zero pending/reallocated/offline-uncorrectable sectors, same rule as the D-04 gate); `findmnt` for `/mnt/media`, `/mnt/disk1`, `/mnt/disk2`; `blkid` for both disks' partition table/filesystem signatures. Display the complete resolved tuple for both disks plus the full protected-device list in the same message. Display (but do not run) the exact commands planned for Steps 4.2-4.4.
  </action>
  <verify>
    <automated>ssh root@ser8 'readlink -f /dev/disk/by-id/wwn-0x5000c500b56ea81a && readlink -f /dev/disk/by-id/wwn-0x5000c500b3733a87'</automated>
  </verify>
  <acceptance_criteria>
    - Both resolved tuples exactly match the approved inventory (model, serial, capacity).
    - Neither resolved kernel device matches the system NVMe or any backup-disk WWN's resolved kernel device.
    - Both current ext4 mount sources (`/mnt/disk1`, `/mnt/disk2`) are accounted for and match the approved WWNs.
    - Both disks report zero pending/reallocated/offline-uncorrectable sectors.
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm the displayed targets, or describe a mismatch.</resume-signal>
  <done>Both approved WWNs are re-confirmed live, matching the doc's inventory exactly; no protected device is implicated.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 4.2: Unmount MergerFS and both ext4 members</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-4.2</read_first>
  <decision>Approve unmounting `/mnt/media`, then `/mnt/disk1`, then `/mnt/disk2` -- typed echo-back required (D-11 Additional Disk Approval Gate).</decision>
  <context>
    Mutation class: live mount change (this step mutates neither disk's data, only unmounts them -- but it IS one of the disk-mutation-adjacent steps D-11 requires typed echo-back for). Confirm the media stack is still stopped before unmounting.
    No erasure happens in this step.
  </context>
  <options>
    <option id="approve"><name>Unmount</name><pros>Prerequisite for the erase step; fully reversible by remounting.</pros><cons>None if the stack is confirmed stopped.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks Step 4.3.</cons></option>
  </options>
  <action>
    After approval and RECEIVING THE TYPED ECHO-BACK (operator types the step id "4.2" or the exact WWN suffixes `a81a`/`3a87` back, per D-11 -- a general "looks good" or "continue" does NOT satisfy this gate), over SSH: re-confirm all 18 freeze-set units are still stopped; `umount /mnt/media`; `umount /mnt/disk1`; `umount /mnt/disk2`; verify `mount | grep -E "/mnt/media|/mnt/disk1|/mnt/disk2"` returns empty.
  </action>
  <verify>
    <automated>ssh root@ser8 'mount | grep -cE "/mnt/media|/mnt/disk1|/mnt/disk2"'</automated>
  </verify>
  <acceptance_criteria>
    - The operator's typed response contained the step id "4.2" or a WWN suffix, not a generic affirmation.
    - `mount | grep -E "/mnt/media|/mnt/disk1|/mnt/disk2"` returns zero lines after the unmount.
    - No data on either disk was erased in this step.
  </acceptance_criteria>
  <resume-signal>Type "4.2" (or the WWN suffixes a81a/3a87) to confirm this exact unmount.</resume-signal>
  <done>MergerFS and both ext4 members are unmounted; nothing has been erased yet.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 4.3 gate: approve the irreversible disk erase (one-way door)</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-4.3, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Rollback-Matrix</read_first>
  <decision>This is the migration's point of no return: erasing both approved 12 TB disks permanently destroys the original ext4 filesystems. After this step, `backup/media-staging` (verified clean by Plan 13-04's gate 3.3) is the ONLY remaining copy of the media tree until Plan 13-06's restore completes.</decision>
  <context>
    Undo cost: none for the ext4 data itself -- it cannot be recovered after this step. The only recovery path afterward is the already-verified staging copy (temporarily mountable at /mnt/media, or the source for recreating the mirror). This is exactly the Rollback Matrix's "After ext4 erase" row.
    Typed echo-back required (D-11): the operator must type both WWN suffixes, not click an option.
  </context>
  <options>
    <option id="approve-erase">
      <name>Approve the erase -- proceed past the point of no return</name>
      <pros>Gate 3.3 already proved zero unexplained differences; staging is the frozen, verified copy this decision relies on.</pros>
      <cons>The original ext4 filesystems are gone permanently the moment this runs; there is no path back to the pre-migration state.</cons>
    </option>
    <option id="abort">
      <name>Abort -- do not erase</name>
      <pros>Preserves the original ext4 disks exactly as they are.</pros>
      <cons>Blocks the rest of the migration; the app stack remains frozen with services down until resolved.</cons>
    </option>
  </options>
  <action>
    Present the complete resolved WWN paths, expected GPT + single ZFS-partition layout, and the full protected-device list again (even though Step 4.1 already confirmed them -- D-11: approval does not carry across steps). Do not run any command in this task -- execution is the next task, gated on receiving the typed echo-back here.
  </action>
  <verify>
    <manual>Operator types both WWN suffixes (a81a, 3a87) in response to this gate -- a generic "approved" without the suffixes does not satisfy it.</manual>
  </verify>
  <acceptance_criteria>
    - The operator's response contains both WWN suffixes `a81a` and `3a87`, typed explicitly.
    - No command has run yet -- this task only gates the next one.
  </acceptance_criteria>
  <resume-signal>Type both WWN suffixes (a81a and 3a87) to authorize the erase.</resume-signal>
  <done>The erase is explicitly authorized via typed echo-back; execution proceeds to the next task.</done>
</task>

<task type="auto">
  <name>Step 4.3 execution: erase and partition only the two approved media disks</name>
  <reversibility rating="one-way">Destroys the original ext4 filesystems on both approved disks permanently; the only recovery afterward is the already gate-3.3-verified backup/media-staging copy. There is no undo for the erased ext4 data itself.</reversibility>
  <precondition>The prior checkpoint:decision task received the typed WWN-suffix echo-back (a81a, 3a87) authorizing this exact erase.</precondition>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-4.3</read_first>
  <files></files>
  <action>
    Over SSH, using only the persistent WWN paths (never `/dev/sde`/`/dev/sdf`): create a fresh GPT partition table on each of `/dev/disk/by-id/wwn-0x5000c500b56ea81a` and `/dev/disk/by-id/wwn-0x5000c500b3733a87`, then a single ZFS-type partition spanning 100% of each disk (matching the `hosts/ser8/disko-config.nix` declaration from Plan 13-02 -- use `sgdisk` or the equivalent tool the disko config would produce, e.g. `sgdisk --zap-all <device>` then `sgdisk -n1:0:0 -t1:BF01 <device>`). Do not run any whole-host Disko destroy/format/mount command -- only these two devices. After execution, verify: `lsblk` shows only these two devices changed; `blkid` on the system NVMe and all four backup-disk WWNs shows their original filesystem signatures untouched; each of the two media disks now has exactly one GPT partition with a ZFS type signature.
  </action>
  <verify>
    <automated>ssh root@ser8 'blkid /dev/disk/by-id/wwn-0x5000c500ea5da96a | grep -q zfs_member && echo "protected-intact"'</automated>
  </verify>
  <acceptance_criteria>
    - Only the two approved WWNs show a new GPT + single ZFS partition.
    - `blkid` on the system NVMe and all four backup-disk WWNs is unchanged from before this step.
    - No whole-host disko command ran.
  </acceptance_criteria>
  <done>Both approved disks have a fresh GPT with one ZFS partition each; every protected device is verified unchanged.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 4.4: Create and verify the empty media mirror</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-4.4, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Desired-ZFS-Configuration</read_first>
  <decision>Approve creating `zpool media` as a mirror using only the two approved WWN partitions, then `media/data`.</decision>
  <context>
    Mutation class: live ZFS pool creation. Typed echo-back required (D-11 -- this is a disk-mutation step, 4.2-4.4). Must produce exactly one `mirror-0` vdev with exactly two members, both online, both resolving to the approved WWNs -- never a stripe.
  </context>
  <options>
    <option id="approve"><name>Create the mirror</name><pros>The last purely-additive step before Nix activation; nothing existing is destroyed here.</pros><cons>None.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks activation and restore.</cons></option>
  </options>
  <action>
    After receiving the typed echo-back (step id "4.4" or WWN suffixes), over SSH: `zpool create -o ashift=12 -o autotrim=on -O acltype=posixacl -O compression=lz4 -O recordsize=1M -O atime=off -O xattr=sa -O normalization=formD -O dedup=off -O mountpoint=none -O canmount=off media mirror /dev/disk/by-id/wwn-0x5000c500b56ea81a-part1 /dev/disk/by-id/wwn-0x5000c500b3733a87-part1` (partition suffix per the actual partition created in the prior task); then `zfs create -o mountpoint=/mnt/media -o com.sun:auto-snapshot=false media/data`. Do not continue until `zpool status media` shows exactly: pool `media`, state `ONLINE`, one `mirror-0` vdev, exactly two members, both `ONLINE`, both resolving to the approved WWNs. Confirm `/mnt/media` is NOT yet mounted from `media/data` (dataset created but Nix has not activated the mount config yet -- that is Step 4.5).
  </action>
  <verify>
    <automated>ssh root@ser8 'zpool status media | grep -c "wwn-0x5000c500b56ea81a\|wwn-0x5000c500b3733a87"'</automated>
  </verify>
  <acceptance_criteria>
    - `zpool status media` shows exactly one `mirror-0` vdev with exactly two members, both `ONLINE`.
    - Both members resolve to the two approved WWNs (grep count above returns 2).
    - `zfs list media/data` shows the dataset exists with `mountpoint` set to `/mnt/media`.
    - No other vdev topology (stripe, single-disk) exists in the pool.
  </acceptance_criteria>
  <resume-signal>Type "4.4" (or the WWN suffixes) to confirm this exact mirror creation.</resume-signal>
  <done>The empty media mirror exists with exactly the two approved WWNs online in one mirror-0 vdev; media/data is created.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 4.5a: Mask media units and validate the matching Nix generation (make test-ser8)</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-4.5, hosts/ser8/media/orchestration.nix</read_first>
  <decision>Approve masking all 18 D-02 freeze-set units, then running `make test-ser8` from the `zfs-media-mirror` branch (temporary activation, not persistent).</decision>
  <context>
    Mutation class: live NixOS activation (temporary). D-16: every unit in the freeze set is `wantedBy = multi-user.target`, so an unmasked activation would start Radarr/Sonarr against the now-empty `/mnt/media` and mangle their library records before the restore even begins. Masking must happen before `make test-ser8`, not after.
  </context>
  <options>
    <option id="approve"><name>Mask units and run make test-ser8</name><pros>Fails fast on mount/import config errors against the real (empty) pool; masking prevents any library-scan hazard.</pros><cons>None if masking happens first.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks the persistent switch.</cons></option>
  </options>
  <action>
    After approval, over SSH: `systemctl mask jellyfin.service radarr.service sonarr.service bazarr.service prowlarr.service sabnzbd.service nzbget.service samba-smbd.service samba-wsdd.service media-config.service servarrs-setup.service download-clients-setup.service mealie.service homebox.service actual.service donetick.service frigate.service home-assistant.service mosquitto.service`; verify every unit shows `masked` via `systemctl list-unit-files | grep -E "jellyfin|radarr|sonarr|bazarr|prowlarr|sabnzbd|nzbget|samba|media-config|servarrs|download-clients|mealie|homebox|actual|donetick|frigate|home-assistant|mosquitto"`. Then from the local checkout on the `zfs-media-mirror` branch, run `make test-ser8`. Verify the evaluated and live mount configuration agree (`/mnt/media` sourced from `media/data`, ZFS not fuse.mergerfs), MergerFS is not active, and the empty mirror remains online.
  </action>
  <verify>
    <automated>ssh root@ser8 'systemctl is-active jellyfin.service 2>&1 | grep -q "masked\|inactive"'</automated>
  </verify>
  <acceptance_criteria>
    - All 18 freeze-set units show `masked` in `systemctl list-unit-files`.
    - `make test-ser8` exits 0.
    - `mount | grep /mnt/media` shows a ZFS mount from `media/data`, not `fuse.mergerfs`.
    - `zpool status media` remains ONLINE after activation.
  </acceptance_criteria>
  <resume-signal>Type "approved" to mask units and run make test-ser8.</resume-signal>
  <done>The matching Nix generation is temporarily active with media services masked; the mount config is verified correct against the empty mirror.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 4.5b: Persist the activation (make switch-ser8) and merge to main</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-4.5</read_first>
  <decision>Approve `make switch-ser8` as its own separately-scoped step beyond the prior `make test-ser8` approval, then merging `zfs-media-mirror` to `main` (D-15: merge immediately after the switch succeeds).</decision>
  <context>
    Mutation class: live NixOS activation (persistent, survives reboot). The doc explicitly requires this be a separate approval from Step 4.5a's `make test-ser8` -- "verify the temporary activation before presenting make switch-ser8 as its own separately approved step." Do not pass `NO_CONFIRM=true` unless the operator explicitly requests non-interactive activation.
    D-16 rationale for masking-before-restore also covers the reboot path: if ser8 reboots unexpectedly during Plan 13-06's ~10.7h restore, the boot default must not reference the erased ext4 disks -- it must boot into the masked, mirror-aware generation.
  </context>
  <options>
    <option id="approve"><name>Switch and merge</name><pros>ser8's boot default now matches the live activated state; the branch's mid-migration divergence from main ends now that main can truthfully describe a live media pool.</pros><cons>None -- the mirror is empty but the pool physically exists, so main is truthful.</cons></option>
    <option id="abort"><name>Abort -- keep on test activation only</name><pros>Preserves the ability to revert the boot default.</pros><cons>A reboot would fall back to the previous generation, which still references the erased ext4 disks -- risk grows the longer this is deferred.</cons></option>
  </options>
  <action>
    After approval: `make switch-ser8` on the `zfs-media-mirror` branch (interactive confirmation, not `NO_CONFIRM=true`, unless explicitly requested). Verify the new generation is the boot default (`nixos-rebuild list-generations` or equivalent). Then open a PR from `zfs-media-mirror` to `main` (`gh pr create`), and merge it (per repository convention: never push directly to main) once CI/`make check` is clean. Verify `main` now contains the media pool declaration.
  </action>
  <verify>
    <automated>ssh root@ser8 'zpool status media | grep -q "state: ONLINE" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `make switch-ser8` exits 0 and the new generation is the boot default.
    - `zpool status media` remains ONLINE, masked units remain masked, no service started.
    - A PR from `zfs-media-mirror` to `main` is created and merged.
    - `git log main` contains the media pool declaration commits.
  </acceptance_criteria>
  <resume-signal>Type "approved" to run make switch-ser8 and merge the branch.</resume-signal>
  <done>ser8's persistent boot default declares and mounts the media mirror; the zfs-media-mirror branch is merged to main; media services remain masked pending Plan 13-06's restore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Typed echo-back gate -> disk erase / mirror creation | The only barrier between a structural approval click and an irreversible command; D-11's strongest control in the whole phase |
| Empty mirror -> masked services | Radarr/Sonarr library scans against an empty pool would mangle records permanently; masking is the sole barrier during the activation window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-14 | Tampering | Wrong disk erased due to a stale kernel-name reference | critical | mitigate | Every command in Steps 4.2-4.4 uses `/dev/disk/by-id/wwn-*` exclusively; Step 4.1 and the Step 4.3 gate both re-resolve and re-display the WWNs immediately before mutation |
| T-13-15 | Elevation of Privilege | Executor infers approval from conversational affirmation instead of the required typed echo-back | critical | mitigate | Prohibition P2 (must_haves.prohibitions); Tasks for Steps 4.2, 4.3-gate, and 4.4 explicitly require the typed step id or WWN suffix, documented in `<resume-signal>` |
| T-13-16 | Tampering | A protected device (system NVMe, backup-disk WWN) is accidentally included in a destructive command | critical | mitigate | Step 4.1 displays the full protected list alongside the targets; Step 4.3 execution verifies protected-device signatures are unchanged afterward |
| T-13-17 | Denial of Service | make test-ser8/switch-ser8 activates before masking, starting services against the empty pool | high | mitigate | Task 6's action masks all 18 units BEFORE running make test-ser8; verified via systemctl list-unit-files before proceeding |
| T-13-18 | Repudiation | Feature branch merged to main before the switch actually succeeded, or never merged at all (stranded branch) | medium | mitigate | Task 7 gates the merge on `make switch-ser8` exiting 0 and the new generation being the boot default, verified before opening the PR |
</threat_model>

<verification>
`zpool status media` shows one ONLINE mirror-0 vdev with exactly the two approved WWNs. All 18 freeze-set units show masked. `make switch-ser8` succeeded and main now declares the media pool.
</verification>

<success_criteria>
The two approved ext4 disks are erased and reformatted as an online two-disk ZFS mirror; ser8's persistent boot default activates it with all media services masked; the feature branch is merged to main.
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback)

ZFS-02 was classified `unclassified` by the automated edge-case probe; it is fully pinned by CONTEXT.md's Desired ZFS Configuration table and this plan's Step 4.4 acceptance criteria. ZFS-05's "adjacency" edge probe ("When two things are exactly equal or just touch, do they merge, collide, or separate?") resolves explicitly here: approval gates never merge across adjacent steps -- see `must_haves.truths` above -- even the five back-to-back gates in this single plan (4.1 through 4.5b) each require their own separate response.
</flagged_assumptions>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification as pre-existing):

- `media` (new live ZFS pool, ser8 -- two-disk mirror)
- `media/data` (new live ZFS dataset, ser8 -- mounted at /mnt/media after Step 4.5)
- A merged PR bringing the `zfs-media-mirror` branch into `main`

<output>
Create `.planning/phases/13-zfs-mirror-migration/13-05-SUMMARY.md` when done, documenting this plan's rollback-matrix row: "After ext4 erase -- backup/media-staging -- Temporarily mount staging at /mnt/media or retry mirror creation" (the original ext4 filesystems are gone; staging is now the sole copy).
</output>
