---
phase: 08-reorganize-ser8-media-nix-into-per-service-modules
plan: 06
type: execute
wave: 6
depends_on:
  - 08-05
files_modified:
  - modules/media/default.nix
  - modules/media/exportarr.nix
  - modules/media/alldebrid-proxy.nix
  - modules/media/transmission.nix
  - hosts/ser8/impermanence.nix
  - flake.nix
autonomous: true
requirements:
  - N/A
must_haves:
  truths:
    - "Dead Exportarr grouping, AllDebrid, and Transmission implementations are absent."
    - "AllDebrid persistence state and stale flake comments are removed as explicitly approved."
  artifacts:
    - path: modules/media/default.nix
      provides: "Import-only list of active reusable media implementations"
    - path: hosts/ser8/impermanence.nix
      provides: "Persistence inventory without obsolete AllDebrid state"
  key_links:
    - from: modules/media/default.nix
      to: hosts/ser8/media/default.nix
      via: "generic implementations consumed by host policy modules"
      pattern: "jellyfin-exporter|sonarr|radarr|prowlarr|qbittorrent|sabnzbd|nzbget"
---

<objective>
Remove the approved dead modules and adjacent AllDebrid remnants.

Purpose: Implement the deletion portion of D-21 and D-22, including the user-approved Transmission and adjacent AllDebrid cleanup, without mixing it with active-provider audits.
Output: Removed obsolete modules, persistence state, and comments.
</objective>

<execution_context>
@/Users/bobby/.codex/gsd-core/workflows/execute-plan.md
@/Users/bobby/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_environment>
Enter the repository development shell with `make dev` before task actions and keep all automated verification inside that shell.
</execution_environment>

<context>
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-05-SUMMARY.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-RESEARCH.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-PATTERNS.md
@modules/media/default.nix
@hosts/ser8/impermanence.nix
@flake.nix
</context>

## Artifacts this phase produces

- Final `modules/media/default.nix` importing active generic providers only.
- Removal of `modules/media/exportarr.nix`, `modules/media/alldebrid-proxy.nix`, and `modules/media/transmission.nix`.
- Updated `hosts/ser8/impermanence.nix` and `flake.nix` without obsolete AllDebrid state/comments.

<tasks>

<task type="auto">
  <name>Task 1: Remove drained and dead reusable modules</name>
  <files>modules/media/default.nix, modules/media/exportarr.nix, modules/media/alldebrid-proxy.nix, modules/media/transmission.nix</files>
  <read_first>
    - modules/media/default.nix
    - modules/media/exportarr.nix
    - modules/media/alldebrid-proxy.nix
    - modules/media/transmission.nix
    - TODO.md
    - hosts/ser8/media/default.nix
    - hosts/ser8/media/sonarr.nix
    - hosts/ser8/media/radarr.nix
    - hosts/ser8/media/prowlarr.nix
  </read_first>
  <action>
Delete `modules/media/exportarr.nix` only after confirming all three instances are owned by the Arr host modules. Delete `modules/media/alldebrid-proxy.nix` and remove its commented import per D-21. Delete the unimported `modules/media/transmission.nix` under D-22, as explicitly authorized by the user after verifying qBittorrent is the active replacement and no import or caller remains. Remove the Exportarr import from `modules/media/default.nix`, leaving an import-only list of active generic modules. Do not edit encrypted SOPS YAML, rename shared credentials, or remove active qBittorrent/Arr behavior.
  </action>
  <verify>
    <automated>nixfmt --check modules/media/default.nix &amp;&amp; ! rg -n 'alldebrid|transmission|exportarr\.nix' modules/media &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - The three deleted module files are absent and have no imports or source references.
    - Encrypted SOPS files and all active credential names are untouched.
    - Normalized evaluated media behavior differs only by approved removals/addition/path changes.
  </acceptance_criteria>
  <done>All explicitly approved dead reusable modules are removed without active-service drift.</done>
</task>

<task type="auto">
  <name>Task 2: Remove adjacent AllDebrid persistence and stale comments</name>
  <files>hosts/ser8/impermanence.nix, flake.nix</files>
  <read_first>
    - hosts/ser8/impermanence.nix
    - flake.nix
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-RESEARCH.md
  </read_first>
  <action>
Remove the active obsolete AllDebrid tmpfiles/persistence entry from `hosts/ser8/impermanence.nix` and stale AllDebrid comments from `flake.nix`, exactly as explicitly approved by the user. Do not alter other persistence paths, flake inputs, encrypted SOPS data, or service definitions. Confirm the parity allowlist treats the removed persistence entry as an approved cleanup delta and rejects every unrelated change.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/impermanence.nix flake.nix &amp;&amp; ! rg -n 'alldebrid' hosts/ser8/impermanence.nix flake.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json &amp;&amp; scripts/validation/check-ser8-media-parity.sh run-clean make build-ser8</automated>
  </verify>
  <acceptance_criteria>
    - No AllDebrid persistence or stale flake comment remains.
    - No unrelated persistence path or flake input changes.
    - Encrypted SOPS files remain untouched.
  </acceptance_criteria>
  <done>Adjacent AllDebrid state and comments are removed within the approved boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reusable provider modules to active host policy | Cleanup can remove behavior consumed by ser8. |
| Repository evaluation to live host | Build output is validated but explicitly not activated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-16 | Denial of Service | dead-code audit | high | mitigate | Prove import/caller absence before deletion and compare active evaluated behavior after each group. |
| T-08-17 | Tampering | adjacent AllDebrid cleanup | medium | mitigate | Limit changes to user-approved persistence/comments and retain encrypted SOPS data. |
| T-08-SC | Tampering | package installs | low | accept | No package installation occurs. |

## ASVS Level 1 Control Mapping

| ASVS family | Phase control | Verification evidence |
|-------------|---------------|-----------------------|
| V2 Authentication | Preserve hashed-password references, API-key runtime paths, and existing authentication settings. | Non-secret evaluated projection and exact-delta comparison. |
| V4 Access Control | Preserve users, groups, file modes, service hardening, firewall, and namespace boundaries. | Projection assertions plus focused formatter, linter, and build gates. |
| V5 Validation | Use typed NixOS options, structured JSON comparison, strict shell mode, and fail-fast command handling. | Parity runner, Nix evaluation, ShellCheck, statix, and shfmt. |
| V6 Stored Cryptography | Keep sops-nix placeholders and runtime secret paths; never serialize or decrypt secret values. | Fixture-safety checks and unchanged encrypted SOPS sources. |

**ASVS L1 execution gate:** Any critical or high-severity threat with disposition `mitigate` blocks task and plan completion until its cited automated verification passes. No unresolved high-severity threat may advance to the next wave.
</threat_model>

<verification>
Run formatting, statix, and parity after each cleanup group; final repository gates run in Plan 07.
</verification>

<success_criteria>
- Drained Exportarr, dead AllDebrid, and dead Transmission modules are removed.
- Approved adjacent AllDebrid state/comments are removed.
</success_criteria>

<output>
Create `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-06-SUMMARY.md` when done.
</output>
