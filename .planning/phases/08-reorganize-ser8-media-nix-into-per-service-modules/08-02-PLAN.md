---
phase: 08-reorganize-ser8-media-nix-into-per-service-modules
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - hosts/ser8/media/default.nix
  - hosts/ser8/media.nix
  - hosts/ser8/configuration.nix
  - hosts/ser8/media/sonarr.nix
  - hosts/ser8/media/radarr.nix
  - hosts/ser8/media/prowlarr.nix
  - modules/media/exportarr.nix
autonomous: true
requirements:
  - N/A
must_haves:
  truths:
    - "Each Arr service is discoverable as one complete ser8 host-policy slice."
    - "Each Arr service owns and enables its matching Exportarr instance."
    - "Arr secrets, templates, deployment order, and runtime behavior remain unchanged."
  artifacts:
    - path: hosts/ser8/media/sonarr.nix
      provides: "Sonarr enablement, secrets, template, exporter, and ordered deployment contribution"
    - path: hosts/ser8/media/radarr.nix
      provides: "Radarr enablement, secrets, template, exporter, and ordered deployment contribution"
    - path: hosts/ser8/media/prowlarr.nix
      provides: "Prowlarr enablement, secrets, template, exporter, and ordered deployment contribution"
  key_links:
    - from: hosts/ser8/media/sonarr.nix
      to: systemd.services.media-config
      via: "lib.mkOrder 200 script and before contribution"
      pattern: "mkOrder 200"
    - from: hosts/ser8/media/prowlarr.nix
      to: config.sops.secrets.prowlarr_api_key.path
      via: "Exportarr apiKeyFile"
      pattern: "exportarr-prowlarr"
---

<objective>
Extract Sonarr, Radarr, and Prowlarr into complete host service modules.

Purpose: Fulfill D-01, D-03, D-07, D-09, D-10, and D-11 for the Arr services without altering their evaluated configuration.
Output: Three service-owned modules and a drained grouped Exportarr source ready for later deletion.
</objective>

<execution_context>
@/Users/bobby/.codex/gsd-core/workflows/execute-plan.md
@/Users/bobby/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_environment>
Enter the repository development shell with `make dev` before task actions and keep all automated verification inside that shell.
</execution_environment>

<context>
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-01-SUMMARY.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-PATTERNS.md
@hosts/ser8/media.nix
@hosts/ser8/configuration.nix
@modules/media/exportarr.nix
</context>

## Artifacts this phase produces

- `hosts/ser8/media/sonarr.nix` defining `services.sonarr`, `sops.secrets.sonarr_*`, `sops.templates."sonarr-config.xml"`, `services.prometheus.exporters.exportarr-sonarr`, and its `media-config` contribution.
- `hosts/ser8/media/radarr.nix` defining the equivalent Radarr-owned contracts at deployment order 300.
- `hosts/ser8/media/prowlarr.nix` defining the equivalent Prowlarr-owned contracts at deployment order 400.

<tasks>

<task type="auto">
  <name>Task 1: Extract the complete Sonarr host slice</name>
  <files>hosts/ser8/media/sonarr.nix, hosts/ser8/media/default.nix, hosts/ser8/media.nix, hosts/ser8/configuration.nix, modules/media/exportarr.nix</files>
  <read_first>
    - hosts/ser8/media/sonarr.nix
    - hosts/ser8/media.nix
    - hosts/ser8/configuration.nix
    - modules/media/exportarr.nix
    - modules/media/sonarr.nix
    - hosts/ser8/media/default.nix
    - scripts/validation/ser8-media-projection.nix
  </read_first>
  <action>
Create the Sonarr host module with its existing enablement and host settings, both Sonarr SOPS declarations, the full XML template, and the Sonarr Exportarr instance per D-01, D-03, and D-07. Move rather than copy each definition by deleting its former block from `hosts/ser8/media.nix`, `hosts/ser8/configuration.nix`, and `modules/media/exportarr.nix`. Contribute `before = lib.mkOrder 200 [ "sonarr.service" ]` and the exact existing `configure_arr sonarr` deployment line as the order-200 script fragment per D-09 through D-11. Add the module import to the directory entry point. Preserve endpoint, port 9707, firewall setting, secret names, owners, modes, placeholder names, template content, and file destination exactly.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/sonarr.nix hosts/ser8/media/default.nix hosts/ser8/media.nix hosts/ser8/configuration.nix modules/media/exportarr.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - Sonarr host policy exists once, in `hosts/ser8/media/sonarr.nix`.
    - Evaluated Sonarr service, template, secrets, exporter, and deployment fragment match the baseline.
    - The ordered script still deploys Sonarr before Radarr.
  </acceptance_criteria>
  <done>Sonarr is a complete service-owned slice with no duplicate former definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the complete Radarr host slice</name>
  <files>hosts/ser8/media/radarr.nix, hosts/ser8/media/default.nix, hosts/ser8/media.nix, hosts/ser8/configuration.nix, modules/media/exportarr.nix</files>
  <read_first>
    - hosts/ser8/media/radarr.nix
    - hosts/ser8/media/default.nix
    - hosts/ser8/media.nix
    - hosts/ser8/configuration.nix
    - modules/media/exportarr.nix
    - modules/media/radarr.nix
    - hosts/ser8/media/sonarr.nix
    - hosts/ser8/media/default.nix
  </read_first>
  <action>
Apply the Sonarr extraction pattern to Radarr per D-01, D-03, and D-07. Move Radarr enablement, both secrets, full XML template, and Exportarr settings into `hosts/ser8/media/radarr.nix`; remove their old owners and import the new file. Preserve port 9708, URL, firewall behavior, authentication fields, ownership, and modes. Contribute `radarr.service` and the exact `configure_arr radarr` command at order 300 per D-09 through D-11, with no reliance on import order.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/radarr.nix hosts/ser8/media/default.nix hosts/ser8/media.nix hosts/ser8/configuration.nix modules/media/exportarr.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - Radarr host policy exists once, in its owning module.
    - Evaluated Radarr behavior and Exportarr configuration match the baseline.
    - The generated deployment script orders Sonarr at 200 and Radarr at 300.
  </acceptance_criteria>
  <done>Radarr is a complete service-owned slice with deterministic deployment order.</done>
</task>

<task type="auto">
  <name>Task 3: Extract the complete Prowlarr host slice</name>
  <files>hosts/ser8/media/prowlarr.nix, hosts/ser8/media/default.nix, hosts/ser8/media.nix, hosts/ser8/configuration.nix, modules/media/exportarr.nix</files>
  <read_first>
    - hosts/ser8/media/prowlarr.nix
    - hosts/ser8/media/default.nix
    - hosts/ser8/media.nix
    - hosts/ser8/configuration.nix
    - modules/media/exportarr.nix
    - modules/media/prowlarr.nix
    - hosts/ser8/media/sonarr.nix
    - hosts/ser8/media/radarr.nix
  </read_first>
  <action>
Move Prowlarr enablement, secrets, complete XML template, and Exportarr instance into its host module per D-01, D-03, and D-07. Preserve port 9709, URL, firewall setting, secret metadata, and template content. Remove the former definitions and import the module. Contribute `prowlarr.service` plus `configure_arr prowlarr` at order 400 per D-09 through D-11. Leave `modules/media/exportarr.nix` as a valid empty module after its final instance moves; Plan 06 deletes the drained file and import.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/prowlarr.nix hosts/ser8/media/default.nix hosts/ser8/media.nix hosts/ser8/configuration.nix modules/media/exportarr.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json &amp;&amp; scripts/validation/check-ser8-media-parity.sh run-clean make build-ser8</automated>
  </verify>
  <acceptance_criteria>
    - All three Arr host modules own their complete instance policy and exporter configuration.
    - `modules/media/exportarr.nix` contains no host exporter instance.
    - The deployment sequence remains Sonarr 200, Radarr 300, Prowlarr 400.
    - The normalized projection and ser8 build pass.
  </acceptance_criteria>
  <done>All Arr services are self-contained host modules and grouped Exportarr policy is drained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host modules to runtime-rendered service config | SOPS placeholders cross into Arr XML templates. |
| Arr services to Prometheus exporters | Exporters consume API key file paths and service endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Information Disclosure | Arr templates and exporters | high | mitigate | Preserve placeholders and runtime key paths; compare only non-secret metadata and template placeholders. |
| T-08-05 | Tampering | ordered deployment fragments | high | mitigate | Use explicit unique mkOrder values and compare the evaluated generated script after every extraction. |
| T-08-06 | Denial of Service | media-config.before | medium | mitigate | Preserve each exact service dependency separately from the script ordering. |
| T-08-SC | Tampering | package installs | low | accept | No dependency installation occurs. |

## ASVS Level 1 Control Mapping

| ASVS family | Phase control | Verification evidence |
|-------------|---------------|-----------------------|
| V2 Authentication | Preserve hashed-password references, API-key runtime paths, and existing authentication settings. | Non-secret evaluated projection and exact-delta comparison. |
| V4 Access Control | Preserve users, groups, file modes, service hardening, firewall, and namespace boundaries. | Projection assertions plus focused formatter, linter, and build gates. |
| V5 Validation | Use typed NixOS options, structured JSON comparison, strict shell mode, and fail-fast command handling. | Parity runner, Nix evaluation, ShellCheck, statix, and shfmt. |
| V6 Stored Cryptography | Keep sops-nix placeholders and runtime secret paths; never serialize or decrypt secret values. | Fixture-safety checks and unchanged encrypted SOPS sources. |

**ASVS L1 execution gate:** Any critical or high-severity threat with disposition `mitigate` blocks task and plan completion until its cited automated verification passes. No unresolved high-severity threat may advance to the next wave.
</threat_model>

<verification>
Run the projection comparison after each service and `make build-ser8` after all three extractions.
</verification>

<success_criteria>
- Sonarr, Radarr, and Prowlarr each own the complete host-specific slice required by D-01 and D-03.
- No Arr secret, template, exporter, or deployment behavior changes.
- The grouped Exportarr module contains no host policy.
</success_criteria>

<output>
Create `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-02-SUMMARY.md` when done.
</output>
