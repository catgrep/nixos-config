---
phase: 08-reorganize-ser8-media-nix-into-per-service-modules
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - hosts/ser8/media/default.nix
  - hosts/ser8/media.nix
  - hosts/ser8/media/jellyfin.nix
  - modules/media/jellyfin.nix
  - modules/media/jellyfin-exporter.nix
autonomous: true
requirements:
  - N/A
must_haves:
  truths:
    - "Jellyfin household identities live only in ser8 host policy."
    - "ser8 owns both Jellyfin service enablement and declarative Jellyfin enablement."
    - "Sawnia has the same non-admin permissions and preferences as Jordan with her own password reference."
    - "The reusable Jellyfin modules have no ser8 enablement, secret, or household-user policy."
  artifacts:
    - path: hosts/ser8/media/jellyfin.nix
      provides: "Both Jellyfin enablement decisions, household users, four secrets, API-key wiring, and exporter instance policy"
    - path: modules/media/jellyfin-exporter.nix
      provides: "Generic services.jellyfin-exporter enable and apiKeyFile option boundary"
    - path: modules/media/jellyfin.nix
      provides: "Generic account, networking, and firewall implementation without enablement policy"
  key_links:
    - from: hosts/ser8/media/jellyfin.nix
      to: modules/media/jellyfin-exporter.nix
      via: "services.jellyfin-exporter.apiKeyFile"
      pattern: "apiKeyFile"
    - from: services.declarative-jellyfin.users.sawnia
      to: config.sops.secrets.jellyfin_sawnia_password.path
      via: "hashedPasswordFile"
      pattern: "jellyfin_sawnia_password"
    - from: hosts/ser8/media/jellyfin.nix
      to: modules/media/jellyfin.nix
      via: "host-owned services.jellyfin.enable and services.declarative-jellyfin.enable activate generic implementation"
      pattern: "services\\.(jellyfin|declarative-jellyfin).*enable"
---

<objective>
Move Jellyfin household policy to ser8 and make reusable Jellyfin modules genuinely generic.

Purpose: Implement D-01, D-02, D-04, and D-05 while preserving existing enablement and admin/Jordan behavior and applying the user-approved Jordan-equivalent policy for Sawnia.
Output: A complete host Jellyfin slice and a minimal generic exporter configuration interface.
</objective>

<execution_context>
@/Users/bobby/.codex/gsd-core/workflows/execute-plan.md
@/Users/bobby/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_environment>
Enter the repository development shell with `make dev` before task actions and keep all automated verification inside that shell.
</execution_environment>

<context>
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-03-SUMMARY.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-RESEARCH.md
@hosts/ser8/media.nix
@modules/media/jellyfin.nix
@modules/media/jellyfin-exporter.nix
</context>

## Artifacts this phase produces

- `hosts/ser8/media/jellyfin.nix` defining `services.jellyfin.enable`, `services.declarative-jellyfin.enable`, `services.declarative-jellyfin.users.{admin,jordan,sawnia}`, Jellyfin SOPS declarations, `apikeys.jellyfinarr`, and exporter host wiring.
- `services.jellyfin-exporter.enable` and `services.jellyfin-exporter.apiKeyFile` options in `modules/media/jellyfin-exporter.nix`.
- A host-independent Jellyfin reusable module containing generic account, networking, and firewall behavior without enablement or household identity policy.

<tasks>

<task type="auto">
  <name>Task 1: Move Jellyfin enablement, identities, secrets, and API-key policy to ser8</name>
  <files>hosts/ser8/media/jellyfin.nix, hosts/ser8/media/default.nix, hosts/ser8/media.nix, modules/media/jellyfin.nix</files>
  <read_first>
    - hosts/ser8/media/jellyfin.nix
    - modules/media/jellyfin.nix
    - hosts/ser8/media.nix
    - hosts/ser8/media/default.nix
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-RESEARCH.md
  </read_first>
  <action>
Create the host Jellyfin module and import it. Move `services.jellyfin.enable = true` and `services.declarative-jellyfin.enable = lib.mkDefault true` from `modules/media/jellyfin.nix` into the host module per D-01, preserving their evaluated true values while changing policy ownership. Move all existing Jellyfin secret declarations, `apikeys.jellyfinarr`, and the complete `admin` and `jordan` records into it per D-04 and D-07; remove their old definitions. Add `sawnia` with Jordan's exact non-administrator permissions and preferences, changing only the identity key and `hashedPasswordFile` to `config.sops.secrets.jellyfin_sawnia_password.path`, as explicitly decided by the user. Keep all permission and preference fields complete rather than relying on defaults. Leave only the generic `jellyfin` account, networking defaults, and firewall behavior in `modules/media/jellyfin.nix` per D-05; the reusable module must contain neither Jellyfin enablement assignment. Extend the parity assertions to compare both evaluated enablement values as true and to treat only the complete Sawnia user record as an approved functional addition.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/jellyfin.nix hosts/ser8/media/default.nix hosts/ser8/media.nix modules/media/jellyfin.nix &amp;&amp; rg -q 'services\.jellyfin\.enable = true' hosts/ser8/media/jellyfin.nix &amp;&amp; rg -q 'services\.declarative-jellyfin\.enable = lib\.mkDefault true' hosts/ser8/media/jellyfin.nix &amp;&amp; ! awk '/^  services\.jellyfin = \{/,/^  \};/' modules/media/jellyfin.nix | rg -q '^    enable = true;$' &amp;&amp; ! awk '/^  services\.declarative-jellyfin = \{/,/^  \};/' modules/media/jellyfin.nix | rg -q '^    enable = lib\.mkDefault true;$' &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - Admin and Jordan evaluate identically to baseline.
    - Sawnia has field-for-field Jordan-equivalent non-admin permissions/preferences and her own password path.
    - `hosts/ser8/media/jellyfin.nix` owns both enablement assignments and both evaluate to true.
    - The bounded `services.jellyfin` and `services.declarative-jellyfin` attrsets in `modules/media/jellyfin.nix` contain neither prior nested enablement assignment; the reusable module also contains no `admin`, `jordan`, `sawnia`, or SOPS reference.
    - Generic Jellyfin account, network, and firewall values remain equal to baseline.
  </acceptance_criteria>
  <done>Jellyfin enablement, household identity, and credential policy is owned by ser8, with reusable generic behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Make the Jellyfin exporter host-agnostic</name>
  <files>hosts/ser8/media/jellyfin.nix, modules/media/jellyfin-exporter.nix</files>
  <read_first>
    - modules/media/jellyfin-exporter.nix
    - modules/media/qbittorrent.nix
    - modules/dns/adguard-exporter.nix
    - hosts/ser8/media/jellyfin.nix
    - scripts/validation/ser8-media-projection.nix
  </read_first>
  <action>
Add `options.services.jellyfin-exporter.enable` with `lib.mkEnableOption` and required path option `apiKeyFile`; bind `cfg = config.services.jellyfin-exporter`, guard the complete exporter implementation with `lib.mkIf cfg.enable`, and use `cfg.apiKeyFile` in `LoadCredential` per D-02 and D-05. In the host Jellyfin module, enable the exporter and set `apiKeyFile = config.sops.secrets.jellyfin_api_key.path`. Preserve package, wrapper, environment, credential name, service hardening, port 9711, and firewall behavior. Do not retain SOPS, ser8, or household identity references in the reusable exporter.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/jellyfin.nix modules/media/jellyfin-exporter.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json &amp;&amp; scripts/validation/check-ser8-media-parity.sh run-clean make build-ser8</automated>
  </verify>
  <acceptance_criteria>
    - The exporter exposes exactly the generic enable and API-key-file host inputs it needs.
    - The reusable module contains no SOPS or household-specific reference.
    - Evaluated `LoadCredential`, port, firewall, environment, and service hardening match baseline.
    - Normalized parity and `make build-ser8` pass.
  </acceptance_criteria>
  <done>Jellyfin host policy is self-contained and both reusable Jellyfin modules are host-independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Household policy to Jellyfin authorization | User records determine administrative and content access. |
| Host SOPS policy to generic exporter | A runtime key path crosses the reusable module option boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Elevation of Privilege | Sawnia user record | high | mitigate | Copy Jordan's complete non-admin contract field-for-field and compare evaluated permissions. |
| T-08-11 | Information Disclosure | exporter LoadCredential | high | mitigate | Pass only the runtime SOPS path through a typed option and preserve systemd credentials. |
| T-08-12 | Tampering | Jellyfin enablement and existing admin/Jordan records | medium | mitigate | Projection compares both enablement values and the complete evaluated user records, while source gates enforce host ownership. |
| T-08-SC | Tampering | package installs | low | accept | No package installation occurs. |

## ASVS Level 1 Control Mapping

| ASVS family | Phase control | Verification evidence |
|-------------|---------------|-----------------------|
| V2 Authentication | Preserve hashed-password references, API-key runtime paths, and existing authentication settings. | Non-secret evaluated projection and exact-delta comparison. |
| V4 Access Control | Preserve users, groups, file modes, service hardening, firewall, and namespace boundaries. | Projection assertions plus focused formatter, linter, and build gates. |
| V5 Validation | Use typed NixOS options, structured JSON comparison, strict shell mode, and fail-fast command handling. | Parity runner, Nix evaluation, ShellCheck, statix, and shfmt. |
| V6 Stored Cryptography | Keep sops-nix placeholders and runtime secret paths; never serialize or decrypt secret values. | Fixture-safety checks and unchanged encrypted SOPS sources. |

**ASVS L1 execution gate:** Any critical or high-severity threat with disposition `mitigate` blocks task and plan completion until its cited automated verification passes. No unresolved high-severity threat may advance to the next wave.
</threat_model>

<verification>
Compare both Jellyfin enablement values, complete users, reusable-module ownership, and exporter service definitions, then run `make build-ser8`.
</verification>

<success_criteria>
- All three household users are complete host-owned records.
- Both Jellyfin enablement decisions are host-owned and remain evaluated true.
- Sawnia matches the explicitly approved Jordan policy.
- Reusable Jellyfin code contains only generic implementation and typed host inputs.
</success_criteria>

<output>
Create `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-04-SUMMARY.md` when done.
</output>
