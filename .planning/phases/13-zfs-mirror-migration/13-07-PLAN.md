---
phase: 13-zfs-mirror-migration
plan: 07
type: execute
wave: 7
depends_on: ["13-06"]
files_modified:
  - CLAUDE.md
  - hosts/ser8/README.md
  - hosts/ser8/disko-config.nix
  - hosts/ser8/impermanence.nix
  - hosts/ser8/media/sabnzbd.nix
  - hosts/ser8/media/nzbget.nix
autonomous: false
requirements: [ZFS-03, ZFS-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 8
  confidence: low

must_haves:
  truths:
    - "The first scrub of the media pool completes with zero data errors and repairs 0B (or a fully explained condition), with both mirror members remaining online (ZFS-03)."
    - "backup/media-staging is destroyed only after the first clean scrub AND Step 5.4's application tests have passed -- same day, no multi-day observation window (D-18)."
    - "The staging destroy remains its own individually-approved step with the scrub result and application validation results presented in the approval, using the exact typed echo-back gate (D-11)."
    - "MergerFS is fully absent from CLAUDE.md, hosts/ser8/README.md, and code comments after this plan (D-20)."
    - "rpool/safe/downloads exists with a 500G quota, and SABnzbd/NZBGet write completed downloads there instead of under /mnt/media/downloads (D-21)."
  artifacts:
    - path: "rpool/safe/downloads (live ZFS dataset on ser8)"
      provides: "NVMe download staging with a 500G hard quota, off the media mirror's write path (D-21)"
    - path: ".planning/phases/13-zfs-mirror-migration/evidence/scrub-and-staging-destroy.md"
      provides: "Durable record of the scrub result and the staging-destroy approval transcript"
  key_links:
    - from: "rpool/safe/downloads"
      to: "hosts/ser8/media/sabnzbd.nix, hosts/ser8/media/nzbget.nix"
      via: "download_dir/complete_dir (sabnzbd) and InterDir/DestDir/CategoryN.DestDir (nzbget) point at the new dataset's mountpoint"
      pattern: "download_dir|InterDir|DestDir"
  prohibitions:
    - statement: "Never infer approval for the staging-destroy step from general conversational affirmation -- it requires the exact typed echo-back (dataset name or step id) specified in the Approval Contract and D-11, and the destroy command must go through zfs destroy only, never a recursive filesystem delete through /mnt/media-staging."
      status: unverified
      flagged: true
---

<objective>
Run the first media pool scrub, observe the cutover, destroy `backup/media-staging` once the scrub and Step 5.4 application tests both clean (D-18), sweep MergerFS references out of the docs (D-20), and relocate downloads to a dedicated NVMe dataset with a 500G quota (D-21/D-22).

Purpose: This is the phase's final plan. The scrub is the last independent integrity check on the new mirror; staging is freed the same day it's proven safe (D-18); the doc sweep keeps every future session from reading a stale storage description (D-20); and moving downloads off the mirror removes the write-churn/random-I/O burden that torrent-era hardlink design left behind, now that torrenting is retired (D-21).
Output: A scrubbed, clean media mirror; staging destroyed; CLAUDE.md/README.md/code comments truthful; downloads staging on `rpool/safe/downloads` with SABnzbd/NZBGet pointed at it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-zfs-mirror-migration/13-CONTEXT.md
@.planning/phases/13-zfs-mirror-migration/13-06-SUMMARY.md
@.planning/SER8-ZFS-MIRROR-MIGRATION.md
@.planning/phases/13-zfs-mirror-migration/13-PATTERNS.md
@CLAUDE.md
@hosts/ser8/README.md
@hosts/ser8/disko-config.nix
@hosts/ser8/impermanence.nix
@hosts/ser8/media/sabnzbd.nix
@hosts/ser8/media/nzbget.nix
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 5.5: Launch the first media pool scrub</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-5.5</read_first>
  <precondition>Plan 13-06's Step 5.4 task confirmed the full smoketest suite and manual application checks passed.</precondition>
  <decision>Approve starting `zpool scrub media` and monitoring it to completion.</decision>
  <context>
    Mutation class: live ZFS maintenance. This is the last independent verification of the restored mirror's integrity, complementing the intrinsic block-checksum guarantee of the send/recv restore (D-08). Do not remove staging while the scrub is running. Estimated ~11.7h at measured throughput with services up.
  </context>
  <options>
    <option id="approve"><name>Launch the scrub</name><pros>Reads every block on both mirror members, the strongest available end-to-end integrity check.</pros><cons>Multi-hour operation with services live -- some I/O contention possible, acceptable per the doc.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks ZFS-03 and the staging-destroy step, which both require a clean scrub.</cons></option>
  </options>
  <action>
    After approval, over SSH: launch detached per D-13: `systemd-run --unit=media-scrub --description="Phase 13: first scrub of media pool" -- zpool scrub media`. Confirm the unit is running and `zpool status media` shows `scan: scrub in progress`. End the session here per D-12.
  </action>
  <verify>
    <automated>ssh root@ser8 'zpool status media | grep -q "scrub in progress" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `zpool status media` shows a scrub in progress immediately after launch.
    - `backup/media-staging` still exists and is not touched by this task.
  </acceptance_criteria>
  <resume-signal>Type "approved" to launch the first media pool scrub.</resume-signal>
  <done>The scrub is running detached on ser8; the session ends here, to be verified in the next task/session.</done>
</task>

<task type="auto">
  <name>Verify the scrub completed with zero data errors (ZFS-03)</name>
  <precondition>The media-scrub systemd-run unit has finished (check `systemctl status media-scrub` first -- do not proceed until it reports an exited/inactive state).</precondition>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-5.5</read_first>
  <files></files>
  <action>
    Over SSH: `zpool status media` (confirm `scan: scrub repaired 0B in ... with 0 errors on ...`, or a fully-explained nonzero-repair condition if one occurred — never silently accept an unexplained repair); confirm `errors: No known data errors`; confirm both mirror members remain `ONLINE`. Write the full scrub result to `.planning/phases/13-zfs-mirror-migration/evidence/scrub-and-staging-destroy.md` in the repo and commit it — this becomes the evidence the next task presents in its staging-destroy approval.
  </action>
  <verify>
    <automated>ssh root@ser8 'zpool status media | grep -q "0 errors" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `zpool status media` shows the scrub completed with `0 errors` and `repaired 0B` (or a documented, fully-explained exception).
    - Both mirror members remain `ONLINE`.
    - `.planning/phases/13-zfs-mirror-migration/evidence/scrub-and-staging-destroy.md` is committed with the scrub result.
  </acceptance_criteria>
  <done>The first scrub proves the restored mirror end-to-end with zero data errors, satisfying ZFS-03.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 6.1: Observe the cutover</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-6.1</read_first>
  <decision>Approve a same-day observation pass (D-18: no multi-day window) confirming no unexplained service or storage errors before staging is destroyed.</decision>
  <context>
    Mutation class: read-only monitoring. D-13/D-18: the trigger for staging destroy is "first clean scrub AND Step 5.4 app tests passed" — both already true by this point. This task is the operator's final sign-off that the cutover is accepted, not a new multi-day waiting period.
  </context>
  <options>
    <option id="approve"><name>Run the observation pass</name><pros>Final human sign-off before the one-way staging destroy.</pros><cons>None -- read-only.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Blocks the staging-destroy step.</cons></option>
  </options>
  <action>
    After approval: review service logs for the last several hours (`journalctl -u jellyfin -u radarr -u sonarr -u sabnzbd -u nzbget --since "-6 hours" | grep -i error` and similar for the rest of the freeze set), `zpool status media` and `zpool status backup` (both clean), `df -h /mnt/media /mnt/backups` (capacity sane), `mount | grep /mnt/media` (still ZFS, still stable). Present the scrub result from the prior task and the Step 5.4 results from Plan 13-06 together as the cutover-acceptance summary. Confirm the operator accepts the cutover.
  </action>
  <verify>
    <automated>ssh root@ser8 'zpool status media backup | grep -c "state: ONLINE"'</automated>
  </verify>
  <acceptance_criteria>
    - No unexplained service or storage errors found in the observation pass.
    - Both `media` and `backup` pools report `ONLINE` with no errors.
    - The operator explicitly accepts the cutover in this task's response.
  </acceptance_criteria>
  <resume-signal>Type "approved" or "accepted" to confirm the cutover is accepted.</resume-signal>
  <done>The operator has explicitly accepted the cutover based on a clean scrub, clean app tests, and a same-day observation pass.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Step 6.2 gate: approve destroying backup/media-staging (one-way door, D-18)</name>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-6.2, .planning/SER8-ZFS-MIRROR-MIGRATION.md#Rollback-Matrix</read_first>
  <decision>D-18 explicitly rates this one-way: after this destroy, the `media` mirror is the sole copy of the media library. Recovery from any later fault is limited to whatever ZFS mirror redundancy provides -- there is no second copy anymore.</decision>
  <context>
    Present: the exact dataset name (`backup/media-staging`), its used bytes (`zfs get -H -o value used backup/media-staging`), the exact destroy command, the final mirror status from Task 2, and the Step 5.4 application validation results from Plan 13-06. Typed echo-back required (D-11): the operator types the dataset name or "6.2", not a generic affirmation.
  </context>
  <options>
    <option id="approve-destroy">
      <name>Approve the destroy -- staging is no longer needed</name>
      <pros>Frees ~5.7 TB on the backup pool immediately; the scrub already proved the mirror end-to-end; app tests already proved the stack works against it.</pros>
      <cons>After this, the media mirror is the sole copy -- media content is explicitly NOT part of Phase 14's application-state backups (BKP-07 scope), by design (D-18).</cons>
    </option>
    <option id="abort">
      <name>Abort -- keep staging longer</name>
      <pros>Preserves a second copy a while longer.</pros>
      <cons>Holds ~5.7 TB captive on the backup pool for no additional integrity benefit once the scrub and app tests have both passed (D-18's explicit rationale for the earliest-safe-point trigger).</cons>
    </option>
  </options>
  <action>
    Present the destroy contract exactly as described above. Do not run any command in this task -- execution is the next task, gated on receiving the typed echo-back here.
  </action>
  <verify>
    <manual>Operator types "backup/media-staging" or "6.2" in response to this gate -- a generic "approved" alone does not satisfy it.</manual>
  </verify>
  <acceptance_criteria>
    - The operator's response contains the dataset name `backup/media-staging` or the step id `6.2`, typed explicitly.
    - The scrub result and Step 5.4 results are both presented in this gate before the response is accepted.
  </acceptance_criteria>
  <resume-signal>Type "backup/media-staging" (or "6.2") to authorize the destroy.</resume-signal>
  <done>The staging destroy is explicitly authorized via typed echo-back; execution proceeds to the next task.</done>
</task>

<task type="auto">
  <name>Step 6.2 execution: destroy backup/media-staging</name>
  <reversibility rating="one-way">After this, the media mirror is the sole copy of the media library; media content is explicitly out of scope for Phase 14's application-state backups by design (D-18).</reversibility>
  <precondition>The prior checkpoint:decision task received the typed echo-back ("backup/media-staging" or "6.2") authorizing this exact destroy.</precondition>
  <read_first>.planning/SER8-ZFS-MIRROR-MIGRATION.md#Step-6.2</read_first>
  <files></files>
  <action>
    Over SSH: `zfs destroy backup/media-staging` (dataset destroy only -- never a recursive filesystem delete through `/mnt/media-staging`). Verify: `zfs list | grep -c media-staging` returns 0; `mount | grep -c media-staging` returns 0; `zfs list -o avail backup` shows capacity returned; `zfs list backup/cameras backup/cameras/recordings backup/cameras/clips` shows the camera datasets unaffected; `zpool status media` still shows the mirror as the live, healthy media source.
  </action>
  <verify>
    <automated>ssh root@ser8 'zfs list -H -o name | grep -c media-staging'</automated>
  </verify>
  <acceptance_criteria>
    - `backup/media-staging` no longer exists in `zfs list`.
    - `/mnt/media-staging` is not mounted.
    - Camera datasets (`backup/cameras*`) are unchanged.
    - `media` mirror remains the live, healthy media source.
  </acceptance_criteria>
  <done>backup/media-staging is destroyed; capacity is returned to the backup pool; the media mirror is confirmed as the sole live source.</done>
</task>

<task type="auto">
  <name>MergerFS documentation sweep (D-20)</name>
  <files>CLAUDE.md, hosts/ser8/README.md</files>
  <read_first>CLAUDE.md, hosts/ser8/README.md</read_first>
  <action>
    Edit `CLAUDE.md`'s ser8 section (currently: "The host uses ZFS for the system and backup pool, MergerFS for `/mnt/media`, Samba for file sharing, and AMD hardware acceleration for media workloads."): rewrite to describe ZFS as the storage layer for all persistent storage -- rpool (system, impermanence rollback), backup (RAID-Z2, archives and camera recordings), media (two-disk mirror, unified library mount), and rpool/safe/downloads (NVMe staging, 500G quota, added by this plan's next task). State that Samba exposes `/mnt/media` for shared access and that downloads stage on the NVMe before import into the mirror. Do not use GSD terminology (plan/phase references) per the operator's standing instruction (D-23) -- describe the design in plain terms.
    Edit `hosts/ser8/README.md`: remove any MergerFS-specific instructions (e.g., references to `/mnt/disk1`/`/mnt/disk2` or fuse.mergerfs behavior) and add a short "Storage Architecture" section describing the four ZFS pools/datasets (rpool, backup, media, rpool/safe/downloads) in plain language -- what each holds and why media stays a single dataset (simplicity, not a hardlink requirement, per D-22).
    Search the repo for remaining MergerFS/`/mnt/disk1`/`/mnt/disk2` references in code comments (`grep -rn "mergerfs\|MergerFS\|/mnt/disk1\|/mnt/disk2" hosts/ser8/ modules/ CLAUDE.md hosts/ser8/README.md`) and update or remove any that still describe the retired topology as current.
  </action>
  <verify>
    <automated>grep -ric "mergerfs" CLAUDE.md hosts/ser8/README.md 2>/dev/null | awk -F: '{s+=$2} END{print s+0}'</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md`'s ser8 section contains zero occurrences of "MergerFS".
    - `hosts/ser8/README.md` contains zero occurrences of "MergerFS", `/mnt/disk1`, or `/mnt/disk2` describing current state.
    - No code comment in `hosts/ser8/` or `modules/` still describes MergerFS as the current media storage mechanism.
    - No GSD terminology (plan numbers, phase references) appears in either updated file.
  </acceptance_criteria>
  <done>CLAUDE.md and hosts/ser8/README.md accurately describe the ZFS-only storage architecture in plain language; no stale MergerFS references remain.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Declare and activate rpool/safe/downloads (D-21)</name>
  <files>hosts/ser8/disko-config.nix, hosts/ser8/impermanence.nix</files>
  <read_first>hosts/ser8/disko-config.nix, hosts/ser8/impermanence.nix, .planning/phases/13-zfs-mirror-migration/13-PATTERNS.md</read_first>
  <reversibility rating="reversible">Moving downloads back onto the mirror later is a config change plus a data move -- reversible per D-21's own rating.</reversibility>
  <decision>Approve declaring `rpool/safe/downloads` (500G quota, NVMe) and activating it via `make switch-ser8`, before migrating the SABnzbd/NZBGet configs in the next task.</decision>
  <context>
    Mutation class: repository-writing plus live activation. Measured basis: `rpool` had 853 GB free with the NVMe at 6% wear (D-21). The quota IS the hard-cap download governance the operator asked for; failed imports can never again bloat the media pool.
  </context>
  <options>
    <option id="approve"><name>Declare and activate</name><pros>Off the storage-freeze critical path — the media mirror is already live and stable; this is purely additive.</pros><cons>None.</cons></option>
    <option id="abort"><name>Abort — defer to a later milestone</name><pros>No new dataset yet.</pros><cons>Leaves downloads writing to the media mirror indefinitely, which D-21 explicitly identified as unwanted write churn.</cons></option>
  </options>
  <action>
    After approval: in `hosts/ser8/disko-config.nix`, add `"safe/downloads"` to the existing `zpool.rpool.datasets` block (sibling to `"safe/home"` and `"safe/persist"`), with `type = "zfs_fs"` and `options = { mountpoint = "/mnt/downloads"; quota = "500G"; compression = "lz4"; recordsize = "128K"; atime = "off"; dedup = "off"; }`. Add a D-23-style plain-language comment: NVMe staging for download+unpack churn, 500G quota as the hard cap, imports copy to the mirror instead of hardlinking because nothing seeds anymore. In `hosts/ser8/impermanence.nix`, add tmpfiles rules for the new mount: `d /mnt/downloads 0775 media media -`, `d /mnt/downloads/incomplete 0775 media media -`, `d /mnt/downloads/complete 0775 media media -`, `d /mnt/downloads/complete/tv 2775 media media -`, `d /mnt/downloads/complete/movies 2775 media media -`, `d /mnt/downloads/complete/prowlarr 2775 media media -`, `d /mnt/downloads/complete/default 2775 media media -` (mirroring the existing `/mnt/media/downloads/usenet/...` category structure so the next task's path swap is mechanical). Run `make fmt`, `statix check`, `make build-ser8`, then `make test-ser8`, then `make switch-ser8`.
  </action>
  <verify>
    <automated>ssh root@ser8 'zfs get -H -o value quota rpool/safe/downloads'</automated>
  </verify>
  <acceptance_criteria>
    - `zfs get quota rpool/safe/downloads` reports `500G`.
    - `mount | grep /mnt/downloads` shows a ZFS mount.
    - `make switch-ser8` exits 0.
    - The new dataset has zero impact on `media` or `backup` pool state.
  </acceptance_criteria>
  <resume-signal>Type "approved" to declare and activate rpool/safe/downloads.</resume-signal>
  <done>rpool/safe/downloads exists live, quota-enforced at 500G, mounted at /mnt/downloads, activated via a normal switch (not part of the storage-freeze critical path).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Migrate SABnzbd/NZBGet download paths to rpool/safe/downloads (D-21/D-22)</name>
  <files>hosts/ser8/media/sabnzbd.nix, hosts/ser8/media/nzbget.nix</files>
  <read_first>hosts/ser8/media/sabnzbd.nix, hosts/ser8/media/nzbget.nix</read_first>
  <decision>Approve changing SABnzbd's `download_dir`/`complete_dir` and NZBGet's `InterDir`/`DestDir`/`CategoryN.DestDir` settings from `/mnt/media/downloads/usenet/...` to `/mnt/downloads/...`, then activating and re-running the import-write test against the new path.</decision>
  <context>
    Mutation class: live application configuration plus repository change. Since Radarr/Sonarr discover completed-download file locations via the download client's own reported path (not a hardcoded config), no Radarr/Sonarr configuration change is expected — verify this assumption explicitly in this task rather than skip it.
  </context>
  <options>
    <option id="approve"><name>Migrate the paths</name><pros>Completes D-21: downloads fully off the media mirror's write path.</pros><cons>Requires a service restart for SABnzbd/NZBGet; brief reconfiguration window.</cons></option>
    <option id="abort"><name>Abort</name><pros>N/A</pros><cons>Leaves D-21 incomplete — downloads keep writing to the mirror.</cons></option>
  </options>
  <action>
    After approval: in `hosts/ser8/media/sabnzbd.nix`, change `download_dir = /mnt/media/downloads/usenet/incomplete` to `download_dir = /mnt/downloads/incomplete`, `complete_dir = /mnt/media/downloads/usenet/complete/default` to `complete_dir = /mnt/downloads/complete/default`, and each category's `dir = /mnt/media/downloads/usenet/complete/{tv,movies,prowlarr,default}` to `dir = /mnt/downloads/complete/{tv,movies,prowlarr,default}`. In `hosts/ser8/media/nzbget.nix`, change `InterDir=/mnt/media/downloads/usenet/incomplete` to `InterDir=/mnt/downloads/incomplete`, `DestDir=/mnt/media/downloads/usenet/complete/default` to `DestDir=/mnt/downloads/complete/default`, and each `CategoryN.DestDir` similarly. Run `make fmt`, `make build-ser8`, `make test-ser8`, `make switch-ser8` (this restarts `sabnzbd.service`/`nzbget.service` via the `media-config.service` oneshot's config redeploy). Confirm both services are active after restart. Check Radarr and Sonarr's "Remote Path Mappings" settings via their APIs — confirm neither has an existing mapping referencing the old `/mnt/media/downloads/usenet/...` path (same-host bare-metal deployment should need none); if one exists, remove or update it. Re-run `scripts/smoketests/media/test-zfs-media.sh ser8`'s import-write test to confirm imports still land on `media/data` correctly from the new downloads location.
  </action>
  <verify>
    <automated>ssh root@ser8 'systemctl is-active sabnzbd.service nzbget.service'</automated>
  </verify>
  <acceptance_criteria>
    - `hosts/ser8/media/sabnzbd.nix` and `hosts/ser8/media/nzbget.nix` contain zero occurrences of `/mnt/media/downloads`.
    - Both `sabnzbd.service` and `nzbget.service` are active after the switch.
    - No Radarr/Sonarr remote path mapping references the old path (confirmed via API, not assumed).
    - `scripts/smoketests/media/test-zfs-media.sh`'s import-write test passes against the new downloads location.
  </acceptance_criteria>
  <resume-signal>Type "approved" to migrate SABnzbd/NZBGet download paths.</resume-signal>
  <done>SABnzbd and NZBGet write completed downloads to rpool/safe/downloads exclusively; the media mirror receives writes only from confirmed imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Staging destroy approval -> permanent single-copy state | The last one-way door in the phase; after this, ZFS mirror redundancy is the sole protection for the media library |
| Downloads dataset quota -> media pool write path | The 500G quota is the governance mechanism preventing a repeat of the pre-cleanup 1.98 TB failed-import bloat incident |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-22 | Elevation of Privilege | Staging destroy approval inferred from conversational affirmation instead of the typed dataset-name echo-back | critical | mitigate | Prohibition (must_haves.prohibitions above); Task 4's `<resume-signal>` requires the exact dataset name or step id |
| T-13-23 | Tampering | `zfs destroy` executed via a recursive filesystem delete through the mountpoint instead of the ZFS command | critical | mitigate | Task 5's action explicitly uses `zfs destroy backup/media-staging` only, matching the Safety Rules' explicit ban on recursive deletion through mountpoints |
| T-13-24 | Denial of Service | 500G downloads quota exhausted by a burst of large simultaneous imports, blocking new downloads | low | accept | Operator-requested hard cap (D-21); failed/stuck imports blocking further downloads is the intended fail-fast behavior, not a defect |
| T-13-25 | Tampering | SABnzbd/NZBGet path migration silently breaks in-flight downloads mid-transition | medium | mitigate | Task 7 restarts both services via the existing config-redeploy oneshot and verifies active status before declaring done; migration happens post-cutover with no time pressure |
</threat_model>

<verification>
`zpool status media` shows a clean scrub with zero errors. `backup/media-staging` no longer exists. `grep -ri mergerfs CLAUDE.md hosts/ser8/README.md` returns nothing. `rpool/safe/downloads` exists with a 500G quota and SABnzbd/NZBGet write there exclusively.
</verification>

<success_criteria>
The media mirror is scrubbed clean, the cutover is observed and accepted, staging is destroyed only after both gates pass, documentation is truthful, and downloads are fully relocated off the media mirror's write path.
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback)

ZFS-04 was classified `unclassified` by the automated edge-case probe for the "MergerFS gone from active configuration" clause specifically; it is fully pinned by D-20's explicit doc-sweep scope and this plan's Task 6 acceptance criteria (zero MergerFS references remaining in docs/comments, on top of Plan 13-02's repository-level removal). Reviewed and accepted as covered by the locked decision rather than an automated edge classification.
</flagged_assumptions>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification as pre-existing):

- `media-scrub` (new systemd-run transient unit name)
- `.planning/phases/13-zfs-mirror-migration/evidence/scrub-and-staging-destroy.md` (new file, repo)
- `rpool/safe/downloads` (new live ZFS dataset, ser8 -- 500G quota, NVMe)
- `/mnt/downloads` and its `incomplete`/`complete/{tv,movies,prowlarr,default}` subdirectories (new mountpoint + directory tree, ser8)

<output>
Create `.planning/phases/13-zfs-mirror-migration/13-07-SUMMARY.md` when done, documenting this plan's rollback-matrix row: "After staging deletion -- New mirror only -- Restore from whatever external backups exist" (explicitly none for media content, by design per D-18), and the phase's overall completion against ROADMAP Phase 13's five success criteria.
</output>
