---
phase: 08-reorganize-ser8-media-nix-into-per-service-modules
plan: 05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - hosts/ser8/media/default.nix
  - hosts/ser8/media.nix
  - hosts/ser8/systemd_helpers.sh
  - hosts/ser8/media/sops.nix
  - hosts/ser8/media/orchestration.nix
  - hosts/ser8/media/deployment-helpers.sh
  - hosts/ser8/media/orchestration-helpers.sh
autonomous: true
requirements:
  - N/A
must_haves:
  truths:
    - "Shared SOPS defaults are isolated while every individual secret remains service-owned."
    - "Cross-service setup stays behind the same systemd unit and target interfaces."
    - "Deployment and orchestration helper responsibilities are split without changing commands, error handling, or secret sanitization."
    - "The old aggregate media module and helper file are gone, and the directory entry point is import-only."
  artifacts:
    - path: hosts/ser8/media/sops.nix
      provides: "Shared SOPS file, format, and age-key defaults"
    - path: hosts/ser8/media/orchestration.nix
      provides: "media-config envelope, cross-service setup units, and media-setup target"
    - path: hosts/ser8/media/deployment-helpers.sh
      provides: "configure_arr deployment function"
    - path: hosts/ser8/media/orchestration-helpers.sh
      provides: "Sanitized API readiness and idempotent registration functions"
  key_links:
    - from: hosts/ser8/media/orchestration.nix
      to: hosts/ser8/media/deployment-helpers.sh
      via: "order-100 media-config prologue sources helper"
      pattern: "deployment-helpers.sh"
    - from: hosts/ser8/media/orchestration.nix
      to: hosts/ser8/media/orchestration-helpers.sh
      via: "servarrs and download-client setup scripts source helper"
      pattern: "orchestration-helpers.sh"
---

<objective>
Extract shared SOPS and orchestration responsibilities, split the helpers, and remove the legacy aggregate.

Purpose: Complete D-06 and D-08 through D-20 while preserving stable unit names, dependency semantics, generated commands, and sanitized failure behavior.
Output: Final import-only host media directory with focused shared support modules and no legacy aggregate files.
</objective>

<execution_context>
@/Users/bobby/.codex/gsd-core/workflows/execute-plan.md
@/Users/bobby/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_environment>
Enter the repository development shell with `make dev` before task actions and keep all automated verification inside that shell.
</execution_environment>

<context>
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-04-SUMMARY.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-PATTERNS.md
@hosts/ser8/media.nix
@hosts/ser8/systemd_helpers.sh
@hosts/ser8/media/default.nix
</context>

## Artifacts this phase produces

- `hosts/ser8/media/sops.nix` defining shared `sops.defaultSopsFile`, `defaultSopsFormat`, and `age.sshKeyPaths` only.
- `hosts/ser8/media/deployment-helpers.sh` exporting `configure_arr` with all active service branches.
- `hosts/ser8/media/orchestration-helpers.sh` exporting `sanitize_api_key`, `curl_safe`, readiness, idempotent upsert, Arr registration, and download-client registration functions.
- `hosts/ser8/media/orchestration.nix` defining `media-config`, `servarrs-setup`, `download-clients-setup`, and `media-setup.target`.
- Final `hosts/ser8/media/default.nix` importing only SOPS, seven services, and orchestration.

<tasks>

<task type="auto">
  <name>Task 1: Split deployment and orchestration helper functions</name>
  <files>hosts/ser8/media/deployment-helpers.sh, hosts/ser8/media/orchestration-helpers.sh, hosts/ser8/systemd_helpers.sh</files>
  <read_first>
    - hosts/ser8/media/deployment-helpers.sh
    - hosts/ser8/media/orchestration-helpers.sh
    - hosts/ser8/systemd_helpers.sh
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-RESEARCH.md
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-PATTERNS.md
    - scripts/validation/check-ser8-media-parity.sh
  </read_first>
  <action>
Split `hosts/ser8/systemd_helpers.sh` per D-13. Put the complete `configure_arr` function and every active service branch in `deployment-helpers.sh`; put `sanitize_api_key`, `curl_safe`, readiness checks, idempotent upserts, Arr application registration, and download-client registration in `orchestration-helpers.sh`. Preserve function bodies, external commands, quoting, payloads, return codes, retries, failure context, and sanitization of command text, URLs, stderr, and response bodies. Add the SPDX header and `set -euo pipefail` to both files, format them, and add only narrow explained SC2016 ignores immediately around single-quoted jq programs whose dollar variables belong to jq. Keep the old helper file until Task 3 rewires every source call, then delete it there.
  </action>
  <verify>
    <automated>shellcheck hosts/ser8/media/deployment-helpers.sh hosts/ser8/media/orchestration-helpers.sh &amp;&amp; shfmt -d hosts/ser8/media/deployment-helpers.sh hosts/ser8/media/orchestration-helpers.sh &amp;&amp; rg -q '^set -euo pipefail$' hosts/ser8/media/deployment-helpers.sh &amp;&amp; rg -q '^set -euo pipefail$' hosts/ser8/media/orchestration-helpers.sh &amp;&amp; test "$(rg -c '^[a-zA-Z_][a-zA-Z0-9_]*\(\) \{' hosts/ser8/media/deployment-helpers.sh)" -eq 1 &amp;&amp; rg -q '^configure_arr\(\) \{' hosts/ser8/media/deployment-helpers.sh &amp;&amp; test "$(rg -c '^[a-zA-Z_][a-zA-Z0-9_]*\(\) \{' hosts/ser8/media/orchestration-helpers.sh)" -eq 13 &amp;&amp; for fn in sanitize_api_key curl_safe download_client_id upsert_arr_download_client wait_for_api wait_for_api_basic_auth setup_qbittorrent_client setup_sabnzbd_client setup_nzbget_client add_arr_application upsert_prowlarr_download_client add_sabnzbd_to_prowlarr add_nzbget_to_prowlarr; do rg -q "^${fn}\\(\\) \\{" hosts/ser8/media/orchestration-helpers.sh || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Every original active function exists exactly once in the appropriate split helper.
    - Both files use strict mode and produce no ShellCheck or shfmt output.
    - Sanitization still covers command text, URLs, stderr, and returned error bodies.
    - No credential literal is introduced.
  </acceptance_criteria>
  <done>The helper responsibilities are cleanly split and statically equivalent.</done>
</task>

<task type="auto">
  <name>Task 2: Extract shared SOPS defaults and cross-service orchestration</name>
  <files>hosts/ser8/media/sops.nix, hosts/ser8/media/orchestration.nix, hosts/ser8/media.nix, hosts/ser8/media/default.nix</files>
  <read_first>
    - hosts/ser8/media/sops.nix
    - hosts/ser8/media/orchestration.nix
    - hosts/ser8/media.nix
    - hosts/ser8/media/default.nix
    - hosts/ser8/media/deployment-helpers.sh
    - hosts/ser8/media/orchestration-helpers.sh
    - scripts/validation/ser8-media-projection.nix
  </read_first>
  <action>
Move only `defaultSopsFile`, `defaultSopsFormat`, and `age.sshKeyPaths` into `sops.nix` per D-06, updating the deeper relative path to `../../../secrets/ser8.yaml`; keep every individual declaration in its owning service per D-07. Create `orchestration.nix` with the unchanged `media-config` envelope, order-100 prologue that sources `deployment-helpers.sh`, order-800 completion log, `servarrs-setup`, `download-clients-setup`, and `media-setup.target` per D-09 and D-12. Preserve `before`, `after`, `requires`, `wants`, `wantedBy`, serviceConfig, API payloads, readiness checks, and unit names as distinct contracts. Source `orchestration-helpers.sh` from both setup scripts. Remove the moved blocks from the aggregate and add both imports to the directory entry point.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/sops.nix hosts/ser8/media/orchestration.nix hosts/ser8/media.nix hosts/ser8/media/default.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - Shared SOPS defaults evaluate identically from the deeper module path.
    - No individual secret or template declaration exists in `sops.nix`.
    - Cross-service units and target retain every dependency and activation field.
    - Generated script order is prologue 100, service fragments 200 through 700, completion 800.
  </acceptance_criteria>
  <done>Shared support and cross-service wiring have focused owners with evaluated parity.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the import-only directory and remove legacy aggregates</name>
  <files>hosts/ser8/media/default.nix, hosts/ser8/media.nix, hosts/ser8/systemd_helpers.sh, hosts/ser8/media/orchestration.nix</files>
  <read_first>
    - hosts/ser8/media/default.nix
    - hosts/ser8/media.nix
    - hosts/ser8/systemd_helpers.sh
    - hosts/ser8/media/orchestration.nix
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
  </read_first>
  <action>
Remove the aggregate import from `default.nix` and leave only explicit imports for `sops.nix`, all seven active service modules, and `orchestration.nix` per D-08. Delete `hosts/ser8/media.nix` and `hosts/ser8/systemd_helpers.sh` after confirming no remaining source expression references them. The aggregate deletion removes the unused AllDebrid secret declarations and commented service block per D-19 and D-20; do not edit encrypted `secrets/ser8.yaml` or rename any credential. Confirm the approved helper split is the only generated-script structural difference per D-14 and D-16. Stage all new and deleted files before evaluation.
  </action>
  <verify>
    <automated>scripts/validation/check-ser8-media-parity.sh structure &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json &amp;&amp; shellcheck hosts/ser8/media/*.sh &amp;&amp; shfmt -d hosts/ser8/media/*.sh &amp;&amp; nixfmt --check hosts/ser8/media/*.nix hosts/ser8/configuration.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh run-clean make build-ser8</automated>
  </verify>
  <acceptance_criteria>
    - `hosts/ser8/media.nix` and `hosts/ser8/systemd_helpers.sh` do not exist.
    - `default.nix` is import-only and lists exactly shared SOPS, seven active services, and orchestration.
    - Only approved Sawnia, AllDebrid, and helper-path deltas differ from baseline.
    - Linters, formatter, structural checks, parity comparison, and ser8 build pass without new warnings.
  </acceptance_criteria>
  <done>The legacy aggregates are removed and the final focused host module tree is active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API helper logs to journal | Authenticated URLs and response errors can expose API keys. |
| systemd dependency graph to service startup | Ordering and activation semantics control availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Information Disclosure | orchestration helpers | high | mitigate | Preserve sanitize_api_key coverage and verify no raw authenticated error path remains. |
| T-08-14 | Denial of Service | setup units and media target | high | mitigate | Compare before/after/requires/wants/wantedBy independently and retain stable unit names. |
| T-08-15 | Tampering | distributed media-config script | medium | mitigate | Assign explicit order 100 through 800 and compare normalized generated scripts. |
| T-08-SC | Tampering | package installs | low | accept | No package installation occurs. |

## ASVS Level 1 Control Mapping

| ASVS family | Phase control | Verification evidence |
|-------------|---------------|-----------------------|
| V2 Authentication | Preserve hashed-password references, API-key runtime paths, and existing authentication settings. | Non-secret evaluated projection and exact-delta comparison. |
| V4 Access Control | Preserve users, groups, file modes, service hardening, firewall, and namespace boundaries. | Projection assertions plus focused formatter, linter, and build gates. |
| V5 Validation | Use typed NixOS options, structured JSON comparison, strict shell mode, and fail-fast command handling. | Parity runner, Nix evaluation, ShellCheck, statix, and shfmt. |
| V6 Stored Cryptography | Keep sops-nix placeholders and runtime secret paths; never serialize or decrypt secret values. | Fixture-safety checks and unchanged encrypted SOPS sources. |

**ASVS L1 execution gate:** Any critical or high-severity threat with disposition `mitigate` blocks task and plan completion until its cited automated verification passes. No unresolved high-severity threat may advance to the next wave.
</threat_model>

<verification>
Run helper linters, Nix formatting, structural assertions, full projection comparison, and `make build-ser8` after aggregate removal.
</verification>

<success_criteria>
- Shared and cross-service behavior have truthful owners.
- Helper safety, unit dependencies, and deployment order are preserved.
- The old aggregate files are absent and `default.nix` is import-only.
</success_criteria>

<output>
Create `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-05-SUMMARY.md` when done.
</output>
