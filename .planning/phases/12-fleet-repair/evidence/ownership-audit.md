# Ownership Audit — Phase 12 Fleet Repair (D-09)

**Gathered:** 2026-08-24, via SSH to `bdhill@192.168.68.65` (ser8, live production).
**Scope:** Targeted, non-recursive-blind ownership audit against the media tree and
media-service state directories, per D-09.
**Status:** Complete for the small, individually-reviewed mismatches. One large finding is
recorded and explicitly deferred (see "Deferred finding" below) rather than acted on, per the
plan's own prohibition against a mass re-chown.

## Premise correction

The plan's original audit target was "paths not owned by 1002:992." That target was invalidated
during Task 1's pre-flight check: live ser8's media user/group is already 1100/1100, matching the
repo's declaration exactly (see `evidence/identity-audit.md`). The audit below instead targets
the mismatches actually found on ser8: individual files under `/mnt/media` whose owning uid does
not correspond to any current service or the shared `media` account.

## Method

```
ssh bdhill@192.168.68.65 'find /mnt/media -uid <candidate-uid> -type f -printf "%u:%g\t%s\t%p\n"'
```

run separately per candidate uid (not a single blind full-tree scan), for each numerically
distinct owner observed during Task 1's spot-check sampling that did not resolve to a known
live account.

## Findings and disposition

### uid 1002 — resolved (chowned)

14 files found on first pass; a live re-check immediately before remediation found 12 substantive
files plus a growing set of macOS `.DS_Store` files (see note below) — this server's `/mnt/media`
Samba share is actively mounted by a macOS client during this audit, and every `.DS_Store` write
lands with the client's local uid (1002), which is not a NixOS account and not evidence of any
Nix/service identity drift.

**9 of the 12 substantive files already had `group=media`** (correct group, uid stale/orphaned —
likely leftover from a direct SMB write or an old copy tool that preserved a foreign client uid).
**3 of the 12 had `group=mealie`** (gid 992) — a genuine group mismatch on media library content;
`mealie` has no business owning a movie/book file.

All 12 substantive files were individually reviewed and chowned to `1100:1100` (media:media):

| Path | Before | After | Disposition |
|------|--------|-------|-------------|
| `/mnt/media/downloads/complete/the.real.housewives.of.beverly.hills.s01e03.1080p.webrip.x264-skyfire.mkv` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/downloads/alldebrid/The.Real.Housewives.Of.Beverly.Hills.S05E01.../The.Real.Housewives.Of.Beverly.Hills.S05E01....mkv` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/downloads/usenet/complete/tv/_UNPACK_.../Screens/screen0001.png` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/downloads/usenet/complete/tv/_UNPACK_.../Screens/screen0002.png` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/downloads/usenet/complete/tv/_UNPACK_.../Screens/screen0003.png` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/downloads/usenet/complete/tv/_UNPACK_.../Screens/screen0004.png` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/downloads/usenet/complete/tv/_UNPACK_.../the.real.housewives.of.beverly.hills.s12e21.1080p.web.h264-spamneggs.mkv` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/books/safe_by_design.pdf` | 1002:**mealie** | 1100:1100 | chowned (real group mismatch corrected) |
| `/mnt/media/movies/Bugonia (2025)/Bugonia.2025.2160p.Hybrid.UHD.BluRay.REMUX.DV.HDR10+.MULTi[Ben The Men].mkv` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/tv/The Real Housewives of Beverly Hills/Season 12/The.Real.Housewives.of.Beverly.Hills.S12E20.1080p.WEB.H264-SPAMnEGGS.mkv` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/tv/.../the.real.housewives.of.beverly.hills.s12e09.1080p.web.h264-spamneggs.mkv` | 1002:media | 1100:1100 | chowned |
| `/mnt/media/tv/.../The.Real.Housewives.of.Beverly.Hills.S12E03.1080p.WEB.h264-KOGi.mkv` | 1002:media | 1100:1100 | chowned |

**Excluded, not chowned:** `/mnt/media/.DS_Store`, `/mnt/media/downloads/.DS_Store`,
`/mnt/media/downloads/usenet/.DS_Store`, `/mnt/media/downloads/usenet/complete/.DS_Store`,
`/mnt/media/movies/.DS_Store` — transient Finder housekeeping files continuously regenerated by
the live macOS SMB client during this session (confirmed by the file count shifting between two
read-only passes taken minutes apart). Fixing ownership on a file that respawns on the next
Finder directory listing provides no lasting value and was excluded deliberately, not missed.

Post-fix verification: `find /mnt/media -uid 1002 -type f` now returns only the 5 excluded
`.DS_Store` paths.

### uid 755 — resolved (chowned)

6 files found, all already `group=media`, all under one show's season directory. All individually
reviewed and chowned to `1100:1100`:

| Path | Before | After |
|------|--------|-------|
| `The Real Housewives of Beverly Hills/Season 12/The.Real.Housewives.of.Beverly.Hills.S12E20.1080p.WEB.H264-SPAMnEGGS.mkv` | 755:media | 1100:1100 |
| `.../the.real.housewives.of.beverly.hills.s12e09.1080p.web.h264-spamneggs.mkv` | 755:media | 1100:1100 |
| `.../The.Real.Housewives.of.Beverly.Hills.S12E03.1080p.WEB.h264-KOGi.mkv` | 755:media | 1100:1100 |
| `.../The.Real.Housewives.of.Beverly.Hills.S12E02.1080p.WEB.h264-KOGi.mkv` | 755:media | 1100:1100 |
| `.../the.real.housewives.of.beverly.hills.s12e08.1080p.web.h264-spamneggs.mkv` | 755:media | 1100:1100 |
| `.../The.Real.Housewives.of.Beverly.Hills.S12E19.We.Need.to.Talk.About.Kathy.1080p.AMZN.WEB-DL.DDP2.0.H.264-NTb.mkv` | 755:media | 1100:1100 |

Post-fix verification: `find /mnt/media -uid 755 -type f` returns zero results.

### uid 38 — deferred, NOT chowned (scope exceeds this plan's authorization)

Initial spot-sampling (maxdepth-limited) under-counted this finding as "dozens of files." A full,
read-only census (no chown performed) found:

- **1,443 files**, **~3.27 TiB**, spanning roughly 120 distinct movies and TV show/season
  directories across a large fraction of the library (`/mnt/media/movies` and `/mnt/media/tv`),
  plus files under `/mnt/media/downloads/usenet/complete` (e.g. an entire `Children.of.Men.2006`
  Blu-ray BDMV/CERTIFICATE backup structure). Group is `media` (1100) on all of them — no group
  mismatch, only the uid is non-standard.
- `getent passwd 38` on ser8 returns no match — uid 38 is not a currently-declared account.
- **Correlated with FLEET-02:** `/var/lib/sabnzbd` (the SABnzbd state directory already tracked
  as a separate uid-drift issue) has 1,258 files owned by uid 38 and 1,262 by gid 194 — the same
  `38:194` pattern documented in STATE.md's deferred items for the SABnzbd repair (plan 12-02,
  D-11/D-12). This strongly suggests both drifts share a common historical origin (very likely a
  prior SABnzbd/download-pipeline identity, possibly from a pre-NixOS or containerized
  deployment, before the current auto-allocated system accounts existed).

**Disposition: left untouched, documented only.** Chowning 1,443 files across ~3.27 TiB of the
active library is not a "small, individually reviewed" fix — it is functionally the mass
re-chown this plan's frontmatter explicitly prohibits (`Must not perform a blind recursive
re-chown of the media tree ... or any full-tree census`). This finding is recorded here as a
successful audit outcome (a correctly-scoped audit that surfaced a large item and correctly
declined to act on it, per this plan's must_haves.truths on zero-mismatch/no-action being a valid
outcome) and flagged for a follow-up decision: either (a) plan 12-02's SABnzbd diagnosis (D-11)
resolves the shared root cause and the media-tree files get fixed as a natural side effect or
deliberate follow-up, or (b) a dedicated future plan handles the /mnt/media portion once its
origin is understood, informed by whatever plan 12-02 discovers about the historical uid 38/gid
194 identity.

## Zero-mismatch scoped audit note

Per this plan's must_haves.truths (D-09): a zero-mismatch result is itself a valid, successful
outcome. This audit did not find zero mismatches overall, but the *uid 1002 / gid 992 collision
audit as originally specified* effectively resolves to zero mismatches once the premise
correction is applied — because 1002/992 was never live ser8's real media identity, there is
nothing to reconcile on that axis. The mismatches actually found and remediated (uid 1002, uid
755 stray file ownership) were discovered independently during Task 1's pre-flight sampling, not
because 1002 was a target identity — they happened to number the same as the abandoned target
uid, which is coincidental.

## Verification re-run

```
$ ssh bdhill@192.168.68.65 'find /mnt/media -uid 1002 -type f'
(only the 5 excluded .DS_Store paths, as expected)

$ ssh bdhill@192.168.68.65 'find /mnt/media -uid 755 -type f'
(no output)
```

## Follow-up: uid-38 remediation executed post-phase (2026-08-23)

After phase 12 completed and verification passed, the deferred uid-38 finding above was resolved
as a standalone, user-authorized follow-up.
The precondition named in the deferral — plan 12-02 confirming the historical origin (SABnzbd's
vacated auto-allocated identity `38:194`, root cause fixed by the static `uid = 985` pin) — was met,
so the fix could no longer recur and became a bounded one-time operation.

Re-census before the fix found 1,597 paths owned by uid 38 (the original 1,443 files plus 154
directories the files-only census did not count).
The full path list was preserved on ser8 at
`/mnt/backups/phase12-fleet-repair-archive/uid38-ownership-before-20260823.txt`.

```
$ ssh bdhill@192.168.68.65 'sudo find /mnt/media -uid 38 -exec chown media:media {} +'
$ ssh bdhill@192.168.68.65 'sudo find /mnt/media -uid 38 | wc -l'
0
```

The ownership-predicate form (`find -uid 38 -exec chown`) touches exactly the drifted paths —
this is the targeted mechanism this audit's prohibition distinguishes from a blind recursive
re-chown of the tree.

The same sweep surfaced 11 further stray paths the original files-only, sampled audit missed:
directories and `.DS_Store` files owned uid 1002, the stale
`_UNPACK_The.Real.Housewives...` directory, and two paths still carrying mealie's gid 992
(`/mnt/media/lost+found`, `/mnt/media/.DS_Store`).
All were chowned to `media:media` by the same predicate mechanism.

Final verification: `find /mnt/media \( -uid 38 -o -uid 1002 -o -uid 755 \)` and
`find /mnt/media \( -gid 992 -o -gid 194 \)` both return zero paths, and the full media smoketest
suite (`./scripts/smoketests/media/all.sh ser8`) passes.
The media tree enters the Phase 13 storage freeze with uniform `media:media` (1100:1100) ownership
on every previously drifted path.
