---
phase: 09-channel-bump-to-nixos-26-05
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-06"]
files_modified:
  - scripts/smoketests/ser8/all.sh
  - scripts/smoketests/ser8/test-zfs-health.sh
  - scripts/smoketests/ser8/test-vaapi.sh
  - scripts/smoketests/ser8/test-frigate.sh
  - scripts/smoketests/ser8/test-home-assistant.sh
  - deploy.yaml
autonomous: true
requirements: [FOUND-01]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`make smoketests-ser8` reaches the media, NordVPN, ZFS, hardware-acceleration, Frigate, and Home Assistant checks through a single deploy.yaml entry, and its exit status reflects every one of them"
    - "Every new smoketest exits non-zero when its subject is unhealthy — no check always exits 0, and each has been observed red under an injected fault"
    - "The new smoketests were run against ser8 on its pre-bump generation and their results recorded, so a post-activation failure is attributable to the channel bump rather than to a test written after the fact"
    - "ZFS pool health and the impermanence rollback snapshot on ser8 are covered by an automated check"
    - "AMD hardware acceleration is proven by a real VAAPI encode executed as the jellyfin user and as the frigate user against the render node, not by device presence or unit state alone"
    - "Frigate's MQTT connection is proven by receiving a Frigate-owned topic from the broker within a bounded timeout, not inferred from configuration or unit state"
    - "Frigate and Home Assistant each have an automated reachability and service-state check, closing the gap that made Success Criterion 4 unsatisfiable"
    - "A smoketest run with the `.vofi` skip guard active and one with it inactive both terminate with an explicit pass or fail per service — a skipped DNS lookup is reported as skipped, never silently counted as success without a message (edge: empty, FOUND-01)"
    - statement: "When a service's DNS name resolves and its Host-header fallback would also succeed, the two paths do not both run and double-count the service in the pass tally — each service contributes exactly one result to the summary (edge: adjacency, FOUND-01)"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT write a smoketest that always exits 0. A check that reports a diagnostic dump without asserting is indistinguishable from a passing gate, and this phase's entire regression argument rests on these scripts being able to fail."
      category: safety
    - statement: "MUST NOT let a missing tool turn a hardware-acceleration or MQTT assertion into a pass. If the evidence cannot be gathered, the check has not been satisfied; a skip-as-pass on the exact subsystem the kernel bump endangers is the failure mode this plan exists to remove."
      category: safety
    - statement: "MUST NOT prove a check can fail by stopping a live household service. Fault injection belongs in a PATH shim over `ssh`; taking Jellyfin, Frigate, or Home Assistant down to watch a test go red trades the reliability this phase is defending for evidence obtainable another way."
      category: safety
    - statement: "MUST NOT hard-code a host IP address in a smoketest. deploy.yaml is the source of truth for addresses; a literal address silently tests the wrong machine once an address changes, and the one existing violation in the tree is being retired rather than copied."
      category: transparency
  artifacts:
    - path: "scripts/smoketests/ser8/all.sh"
      provides: "Single ser8 entry point that fans out to the media, nordvpn, and new host-level suites through the shared run_suite helper"
      contains: "run_suite"
    - path: "scripts/smoketests/ser8/test-zfs-health.sh"
      provides: "ZFS pool health and impermanence rollback snapshot assertions"
      contains: "zpool status"
    - path: "scripts/smoketests/ser8/test-vaapi.sh"
      provides: "Real VAAPI encode executed as the jellyfin and frigate users against the render node"
      contains: "renderD128"
    - path: "scripts/smoketests/ser8/test-frigate.sh"
      provides: "Frigate service-state, HTTP reachability, and live MQTT-publication assertions"
    - path: "scripts/smoketests/ser8/test-home-assistant.sh"
      provides: "Home Assistant service-state, HTTP reachability, and startup-error assertions"
  key_links:
    - from: "deploy.yaml"
      to: "scripts/smoketests/ser8/all.sh"
      via: "the ser8 smoketests entry, which make smoketests-ser8 reads"
      pattern: "scripts/smoketests/ser8/all\\.sh"
    - from: "scripts/smoketests/ser8/all.sh"
      to: "scripts/smoketests/lib/fanout.sh"
      via: "the entry point sources the shared helper plan 06 created and calls run_suite"
      pattern: "run_suite"
---

<objective>
Build the automated regression gate that the channel bump is judged against, on top of the suite infrastructure plan 06 repaired.

Purpose: Success Criterion 4 says ser8's media, Frigate, and Home Assistant smoketests must still pass after activation — but no Frigate or Home Assistant smoketest exists, and ser8's single deploy.yaml entry reaches only the media suite, so the NordVPN checks are unreachable through `make smoketests-ser8`. Meanwhile the kernel bump touches exactly three things nothing currently asserts: ZFS, the VPN network namespace, and AMD hardware acceleration (D-14). Without this plan, "smoketests still pass" would be a claim about a suite that cannot detect the failures this phase can cause.

Running this before activation matters as much as its content: ser8 is still on its pre-bump generation until plan 05 switches it, so these tests get a real green baseline on 25.11. A failure after activation is then unambiguous.

Output: a `scripts/smoketests/ser8/` area with a fan-out entry point and four new host-level checks, each proven able to go red under injected fault and green against the live pre-bump host.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-channel-bump-to-nixos-26-05/09-CONTEXT.md
@.planning/phases/09-channel-bump-to-nixos-26-05/09-RESEARCH.md
@.planning/phases/09-channel-bump-to-nixos-26-05/09-PATTERNS.md
@.planning/phases/09-channel-bump-to-nixos-26-05/09-REVIEWS.md
@.planning/phases/09-channel-bump-to-nixos-26-05/09-06-SUMMARY.md
@CLAUDE.md
</context>

## Executor note: context budget

This plan creates five scripts and edits one YAML entry. `scripts/smoketests/gateway/test-tailscale.sh` is the structural analog for all five and needs one careful read, after which each script is that skeleton with different assertions. PATTERNS.md already carries every block of that skeleton verbatim — the header, the test harness, the individual test-function shape, and the summary block — so use those excerpts rather than re-reading the analog per script. `09-06-SUMMARY.md` carries the `run_suite` calling contract; read it before writing the fan-out rather than inferring the contract from the helper's source.

New files in this plan are written tab-indented so they are clean under `shfmt`'s default style from the start.

## Review feedback incorporated (09-REVIEWS.md, Codex sol run)

- **HIGH — the VAAPI proposal did not test a transcode.** It checked device presence, an optional `vainfo`, and unit state, and passed when `vainfo` was unavailable, even though Frigate configures VAAPI ffmpeg acceleration (`modules/automation/frigate.nix:78-81`). Task 2 now runs a real VAAPI encode under the jellyfin and frigate users against the render node, and a missing tool fails the check instead of skipping it.
- **MEDIUM — "Frigate connected to MQTT" had no defined observable.** The source only establishes intended broker settings (`modules/automation/frigate.nix:65`). Task 3 now requires receiving a Frigate-owned topic from the broker within a bounded timeout, plus the retained availability message, and resolves the MQTT client from the running unit's own store path so no host package change is needed.
- **MEDIUM — deliberately failing each check had no safe mechanism.** Every red observation in this plan is produced by a temporary `PATH` shim over `ssh`, never by stopping a live service.
- **HIGH (addressed in plan 06) — the fan-out would have run the disruptive kill-switch test and would have swallowed intermediate failures.** This plan now depends on 09-06, uses its `run_suite` helper, and fans out to the routine NordVPN suite it produced. That dependency is why this plan moved from wave 2 to wave 3.
- **Deferred here, owned by plan 06:** the `.vofi` skip guard, the gateway route cleanup, and the pi4 deploy.yaml and DNS-suite retirement all moved into 09-06 so this plan can stay within a reviewable size. They are not dropped.

## Artifacts this phase produces

Created by this plan (new symbols — not drift):
- `scripts/smoketests/ser8/all.sh`, `scripts/smoketests/ser8/test-zfs-health.sh`, `scripts/smoketests/ser8/test-vaapi.sh`, `scripts/smoketests/ser8/test-frigate.sh`, `scripts/smoketests/ser8/test-home-assistant.sh`

This plan removes nothing. The full phase-wide artifact list is in `09-01-PLAN.md`.

<tasks>

<task type="auto">
  <name>Task 1: Create the ser8 fan-out entry point and the ZFS health check</name>
  <precondition>ser8 is reachable over SSH at the address deploy.yaml records for it, as the user deploy.yaml records, AND `ssh <ser8> nixos-version` still reports a pre-26.05 version — this plan's whole value is that its baseline predates the bump, so if ser8 has already been activated out of band the capture is not a pre-bump baseline and the executor must halt and say so rather than record it as one</precondition>
  <read_first>
    - `.planning/phases/09-channel-bump-to-nixos-26-05/09-06-SUMMARY.md` (the `run_suite` calling contract — the caller defines a `TESTS` array and forwards its arguments; this is the interface, not a suggestion)
    - `scripts/smoketests/lib/fanout.sh` (the helper itself, to confirm the contract matches the summary)
    - `scripts/smoketests/nordvpn/all.sh` (the converted entry point to copy for shape)
    - `scripts/smoketests/gateway/test-tailscale.sh` (the structural analog for the test script — copy its header, argument handling, `run_test` harness, individual test-function shape, section grouping, and summary block; this is the only test in the tree with a real pass/fail harness and a non-zero exit)
    - `scripts/lib/yq.sh` (the `get_ip` / `get_user` helpers — deploy.yaml is the source of truth for addresses)
    - `scripts/lib/logging.sh` (the `info` / `warn` / `pass` / `fail` / `title` / `fmt_bold` helpers; never echo a result directly)
    - `hosts/ser8/configuration.nix` (lines 85-95 — the initrd rollback command naming the impermanence blank snapshot)
    - `hosts/ser8/disko-config.nix` (lines 175-235 — the pool and dataset names: `rpool` with `local/root`, `local/nix`, `safe/home`, `safe/persist`, and the separate `backup` pool)
    - `.planning/phases/09-channel-bump-to-nixos-26-05/09-PATTERNS.md` (the new-smoketests section carries every skeleton block verbatim plus the per-test assertion table and the indentation rule)
  </read_first>
  <files>scripts/smoketests/ser8/all.sh, scripts/smoketests/ser8/test-zfs-health.sh, deploy.yaml</files>
  <action>
Create `scripts/smoketests/ser8/all.sh` using the `run_suite` helper plan 06 added, in the same shape the converted `scripts/smoketests/nordvpn/all.sh` now uses: source the shared library, define a `TESTS` array, call `run_suite` forwarding the host argument. Do not write a bare loop — the whole point of depending on plan 06 is that this entry point's exit status must reflect every suite it runs, and a bare loop reports only the last one.

Its entries are, in order, the existing `scripts/smoketests/media/all.sh`, the routine `scripts/smoketests/nordvpn/all.sh` (which plan 06 stripped of the disruptive kill-switch test), and the four new ser8 checks — the one in this task and the three in Tasks 2 and 3. List all four now even though two are created later in this plan; the suite is verified at the end of the plan, not after each task. Leave `scripts/smoketests/media/all.sh` itself untouched — it is a monolithic script and restructuring it is not this phase's work.

Point deploy.yaml's ser8 `smoketests` entry at this new fan-out. That single change is what makes the NordVPN suite reachable through `make smoketests-ser8` for the first time.

Create `scripts/smoketests/ser8/test-zfs-health.sh` following the tailscale test's skeleton exactly: shebang, SPDX header, `set -euo pipefail` before the `. ./scripts/lib/all.sh` source line, `title "$0"`, the argument check, `get_ip` / `get_user`, the `run_test` harness with `tests_run` / `tests_passed` counters, one function per assertion, section grouping, and the summary block that exits non-zero unless every test passed.

It asserts over SSH that `zpool status -x` reports both `rpool` and `backup` healthy, that the `rpool/local/root@blank` snapshot the impermanence rollback depends on still exists, and that `zpool status` reports no pending feature-flag upgrade prompt. The snapshot assertion is the load-bearing one: if that snapshot ever disappears, ser8 stops rolling back its root on boot and the impermanence guarantee silently ends, with no other symptom until someone notices the root filesystem accumulating state.

Resolve every address through `get_ip`; do not write a literal address into the script. Where a remote command is built from an array rather than a single-quoted literal, use the `printf -v ... '%q '` escaping pattern the tailscale test uses, with its accompanying shellcheck directive.

Prove the check can fail before declaring it done, by injected fault rather than by touching the pool: create a temporary directory, put an executable `ssh` in it that prints an unhealthy pool status and exits 0, prepend it to `PATH`, and confirm the script exits non-zero and names the failing assertion. Then remove the shim and run the script against ser8 on its current generation, recording the output as part of the pre-bump baseline.
  </action>
  <behavior>
    - `make smoketests-ser8` invokes the fan-out and reaches the media, NordVPN, and new ser8 checks.
    - The fan-out exits non-zero when any suite beneath it fails, including a non-final one.
    - The ZFS check exits 0 when both pools are healthy and the blank snapshot exists, and non-zero when either condition breaks.
  </behavior>
  <verify>
    <automated>shellcheck -x scripts/smoketests/ser8/all.sh scripts/smoketests/ser8/test-zfs-health.sh && shfmt -d scripts/smoketests/ser8/all.sh scripts/smoketests/ser8/test-zfs-health.sh && grep -q 'run_suite' scripts/smoketests/ser8/all.sh && yq -e '.hosts.ser8.smoketests == "./scripts/smoketests/ser8/all.sh"' deploy.yaml && ./scripts/smoketests/ser8/test-zfs-health.sh ser8</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoketests/ser8/all.sh` and `scripts/smoketests/ser8/test-zfs-health.sh` exist and are executable, each with `SPDX-License-Identifier: GPL-3.0-or-later` on line 2
    - `scripts/smoketests/ser8/all.sh` contains `run_suite` and contains no `for test in` loop
    - `scripts/smoketests/ser8/all.sh` lists `scripts/smoketests/media/all.sh`, `scripts/smoketests/nordvpn/all.sh`, and all four ser8 test scripts
    - `grep -c 'disruptive.sh' scripts/smoketests/ser8/all.sh` outputs `0` (the kill-switch suite stays off the routine path)
    - `yq eval '.hosts.ser8.smoketests' deploy.yaml` outputs `./scripts/smoketests/ser8/all.sh`
    - `grep -c 'set -euo pipefail' scripts/smoketests/ser8/test-zfs-health.sh` outputs `1`
    - `scripts/smoketests/ser8/test-zfs-health.sh` contains `zpool status`, `rpool`, `backup`, and `root@blank`
    - It contains `tests_passed` and an `exit 1` in its summary block
    - `grep -Ec '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' scripts/smoketests/ser8/test-zfs-health.sh` outputs `0`
    - Under a `PATH` shim whose `ssh` reports an unhealthy pool, the script exits non-zero and names the failing assertion; the SUMMARY records the shim and the observed output
    - `./scripts/smoketests/ser8/test-zfs-health.sh ser8` exits 0 against the pre-bump generation and that output is recorded
    - `shellcheck -x` exits 0 and `shfmt -d` produces no diff for both files
  </acceptance_criteria>
  <done>ser8 has one smoketest entry point that reaches every area with an honest exit status, and the storage layer the impermanence guarantee rests on is covered by a check that has been seen both green and red.</done>
</task>

<task type="auto">
  <name>Task 2: Prove AMD hardware acceleration with a real VAAPI encode under the service users</name>
  <precondition>ser8 is reachable over SSH and the deploy.yaml user can run `sudo -n -u <service-user>` on it, the same non-interactive pattern `scripts/smoketests/media/all.sh` already uses for the bazarr checks</precondition>
  <read_first>
    - `scripts/smoketests/gateway/test-tailscale.sh` (structural analog again)
    - `scripts/smoketests/media/all.sh` (the tail of the file — the existing `sudo -n -u bazarr` remote invocations establish that non-interactive user-switching is an available and already-used pattern in this suite)
    - `modules/automation/frigate.nix` (lines 78-85 — Frigate's ffmpeg is `ffmpeg-headless` with the VAAPI hwaccel preset, which is exactly the path this check must exercise; and the note that inference runs on CPU while VAAPI decode is retained)
    - `hosts/ser8/configuration.nix` (lines 241-266 — `ffmpeg` is in the host's system packages, so the encode can run without adding anything to the host)
    - `.planning/phases/09-channel-bump-to-nixos-26-05/09-RESEARCH.md` (the kernel-bump risk section — the AMD render node and its group permissions are one of the three subsystems D-14 names)
    - `.planning/phases/09-channel-bump-to-nixos-26-05/09-PATTERNS.md` (the new-smoketests section)
  </read_first>
  <files>scripts/smoketests/ser8/test-vaapi.sh</files>
  <action>
Create `scripts/smoketests/ser8/test-vaapi.sh` in the tailscale test's skeleton. The review's finding is that device presence plus unit state plus an optional `vainfo` proves nothing about whether transcoding works, and passing when `vainfo` is absent actively weakens the gate. So this check runs a real hardware encode.

It asserts four things. First, that `/dev/dri/renderD128` exists on the host. Second, that a VAAPI encode succeeds when run as the `jellyfin` user. Third, that the same encode succeeds when run as the `frigate` user. Fourth, that the jellyfin and frigate units are active.

Build the encode from a synthetic source so no media file or camera stream is needed: use ffmpeg's built-in test-pattern input, initialise a VAAPI device explicitly against `/dev/dri/renderD128`, upload frames to the hardware surface, encode a few dozen frames with the VAAPI H.264 encoder, and discard the output. Run it non-interactively as each service user with `sudo -n -u`, matching the pattern the media suite already uses. Keep it to well under a second of video so the check stays fast. Quiet ffmpeg's banner and set its log level to errors so a pass produces no noise and a failure produces the actual driver message, which is the diagnostic that matters when a kernel bump breaks the render node.

Running it as each service user rather than as the deploy user is the entire point: the failure mode a kernel bump produces is a permission or driver-ABI change that leaves the device present and readable by root while the unprivileged service user can no longer open it. A check that runs as the deploy user would pass straight through that.

If ffmpeg is missing, or if the device is missing, or if `sudo -n` is refused, the check fails. Do not add a skip-with-pass branch for any of them. This is the subsystem the kernel bump most directly endangers, and a pass on absent evidence here would be worse than having no check. `vainfo` may be reported as supplementary diagnostic output if it happens to be present, but it must not be an assertion and its absence must not affect the result.

Resolve every address through `get_ip`; do not write a literal address into the script.

Prove it can fail by injected fault: shim `ssh` so the encode invocation exits non-zero, run the script, and confirm it exits non-zero naming which user's encode failed. Then remove the shim and run it against ser8's current generation, recording the output as part of the pre-bump baseline. Record in the SUMMARY which VAAPI encoder and driver the successful run reported, because that string is the thing to compare against after activation.
  </action>
  <behavior>
    - The check fails when the jellyfin or frigate user cannot complete a hardware encode, even though the device file still exists.
    - The check fails rather than skipping when ffmpeg, the device, or non-interactive user-switching is unavailable.
    - A successful run leaves no output file and no unit state change on the host.
  </behavior>
  <verify>
    <automated>shellcheck -x scripts/smoketests/ser8/test-vaapi.sh && shfmt -d scripts/smoketests/ser8/test-vaapi.sh && grep -q 'renderD128' scripts/smoketests/ser8/test-vaapi.sh && grep -q 'h264_vaapi' scripts/smoketests/ser8/test-vaapi.sh && ./scripts/smoketests/ser8/test-vaapi.sh ser8</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoketests/ser8/test-vaapi.sh` exists, is executable, and carries the SPDX line on line 2
    - It contains `renderD128`, `h264_vaapi`, `sudo -n -u jellyfin`, and `sudo -n -u frigate`
    - `grep -c 'vainfo' scripts/smoketests/ser8/test-vaapi.sh` may be non-zero only in a diagnostic context; the script contains no branch in which a missing tool or device calls `pass` or increments the passed counter
    - It contains `tests_passed` and an `exit 1` in its summary block
    - `grep -Ec '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' scripts/smoketests/ser8/test-vaapi.sh` outputs `0`
    - Under a `PATH` shim whose `ssh` makes the encode exit non-zero, the script exits non-zero and its output names which service user's encode failed; the SUMMARY records the shim and the output
    - `./scripts/smoketests/ser8/test-vaapi.sh ser8` exits 0 against the pre-bump generation, and the SUMMARY records the encoder and driver strings the run reported
    - `shellcheck -x` exits 0 and `shfmt -d` produces no diff
  </acceptance_criteria>
  <done>Hardware acceleration is proven by a real encode executed under the two service users that depend on it, and the check has been seen both green against the live host and red under injected fault.</done>
</task>

<task type="auto">
  <name>Task 3: Cover Frigate and Home Assistant, with MQTT proven by a live publication</name>
  <precondition>ser8 is reachable over SSH at the address deploy.yaml records for it, and the mosquitto unit is running on it</precondition>
  <read_first>
    - `scripts/smoketests/gateway/test-tailscale.sh` (structural analog again — this task adds two more scripts in the same shape)
    - `modules/gateway/Caddyfile` (lines 45-62 — the Frigate and Home Assistant upstreams record the real ports: Frigate is served by nginx on port 80 and Home Assistant on 8123)
    - `modules/automation/frigate.nix` (lines 60-90 for the MQTT block and the ffmpeg block, and lines 505-530 for the unit name, its `mosquitto.service` ordering, and the firewall port comment)
    - `modules/automation/home-assistant.nix` (lines 420-470 — the mosquitto listener bound to 127.0.0.1:1883 with anonymous access, and the home-assistant unit's ordering after mosquitto)
    - `hosts/ser8/configuration.nix` (line 216 — mosquitto is enabled as a service; note that the NixOS module does not put the mosquitto client tools into the host's system packages, which is why the client must be resolved from the running unit)
    - `.planning/phases/09-channel-bump-to-nixos-26-05/09-RESEARCH.md` (Pitfall 7 explains why the mosquitto auth-plugin change makes the Frigate-to-Home-Assistant MQTT path the coupling worth asserting; Pitfall 4 explains the six-release Home Assistant jump)
  </read_first>
  <files>scripts/smoketests/ser8/test-frigate.sh, scripts/smoketests/ser8/test-home-assistant.sh</files>
  <action>
Create `scripts/smoketests/ser8/test-frigate.sh` in the tailscale test's skeleton. It asserts three things: that the Frigate systemd unit is active, that Frigate's web interface answers over HTTP on port 80 on ser8, and — the one the review required a real observable for — that Frigate is publishing to the MQTT broker right now.

Prove the MQTT coupling by subscribing, not by reading configuration. Two assertions together give both liveness and freshness: subscribe to Frigate's retained availability topic with a short timeout and require the payload to report the service online, then subscribe to Frigate's periodic statistics topic and require one message to arrive within a bounded wait comfortably longer than Frigate's publication interval. The retained message proves Frigate connected to this broker at some point; the periodic message proves the connection is live now. Unit state and an HTTP response prove neither, which is exactly the gap the review named.

The mosquitto client is not in ser8's system packages, and this check has to run against the pre-bump generation, so do not add it to the host configuration. Resolve the client from the running broker instead: read the mosquitto unit's executable path from systemd and take the subscriber binary from the same directory, since the nixpkgs mosquitto package ships the broker and the client tools together. If that resolution fails, or the binary is not there, the check fails — it does not skip. Subscribe against the loopback address on the broker's port, because the listener is bound to localhost only.

Create `scripts/smoketests/ser8/test-home-assistant.sh` in the same shape. It asserts that the Home Assistant systemd unit is active, that it answers over HTTP on port 8123 on ser8, and that its journal shows no fatal startup error in the current boot. Home Assistant jumps roughly six months of releases across this channel bump and writes its own storage migration on first start with no supported downgrade, so a passing unit state alone is not enough evidence — a Home Assistant that started, failed to load a component, and stayed up is exactly the outcome the bump can produce.

Both scripts are already listed in the fan-out array created in Task 1; confirm the entries resolve.

Resolve every address through `get_ip`. Do not hard-code an address in either script.

Prove both can fail by injected fault: shim `ssh` so the unit-state query reports inactive for one run and so the MQTT subscription times out for another, and confirm each produces a non-zero exit naming the failing assertion. Then remove the shim, run both against ser8's current generation, and finally run the whole `make smoketests-ser8` fan-out end to end. Record the complete output as the pre-bump baseline plan 05 compares against.
  </action>
  <behavior>
    - The Frigate check fails when Frigate is not publishing to the broker, even while its unit is active and its web interface answers.
    - The Frigate check fails rather than skipping when the MQTT client cannot be resolved from the running unit.
    - The Home Assistant check fails when a fatal startup error appears in the current boot, even while the unit reports active.
    - `make smoketests-ser8` exercises Frigate and Home Assistant, so Success Criterion 4 has automated coverage for every service it names.
  </behavior>
  <verify>
    <automated>shellcheck -x scripts/smoketests/ser8/test-frigate.sh scripts/smoketests/ser8/test-home-assistant.sh && shfmt -d scripts/smoketests/ser8/test-frigate.sh scripts/smoketests/ser8/test-home-assistant.sh && ./scripts/smoketests/ser8/test-frigate.sh ser8 && ./scripts/smoketests/ser8/test-home-assistant.sh ser8 && make smoketests-ser8</automated>
  </verify>
  <acceptance_criteria>
    - Both scripts exist, are executable, and carry the SPDX line on line 2
    - `scripts/smoketests/ser8/test-frigate.sh` contains `frigate`, an HTTP check against port `80`, `mosquitto_sub`, `frigate/available`, and a bounded-wait subscription to Frigate's statistics topic
    - `scripts/smoketests/ser8/test-frigate.sh` resolves the MQTT client from the running unit rather than assuming a `PATH` binary, and contains no branch where a missing client calls `pass`
    - `scripts/smoketests/ser8/test-home-assistant.sh` contains `8123`, `home-assistant`, and a journal query scoped to the current boot
    - Each script contains `tests_passed` and an `exit 1` in its summary block
    - `grep -Ec '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' ` outputs `0` for each of the two new scripts
    - Under a `PATH` shim that reports the unit inactive, each script exits non-zero naming the failing assertion; under a shim that makes the MQTT subscription time out, the Frigate script exits non-zero naming the MQTT assertion. Both observations are recorded in the SUMMARY
    - Each script exits 0 against ser8's pre-bump generation
    - `make smoketests-ser8` exits 0 against ser8's pre-bump generation, and its complete output is recorded in the SUMMARY as the baseline
    - The SUMMARY records the `ssh <ser8> nixos-version` output captured at the moment of the baseline run, and it is a pre-26.05 version — this is what makes the recorded output a pre-bump baseline rather than a claim that it is one
    - The recorded baseline output contains a result line for every one of the six areas: media, NordVPN, ZFS, hardware acceleration, Frigate, and Home Assistant
    - `shellcheck -x` exits 0 and `shfmt -d` produces no diff for both files
  </acceptance_criteria>
  <done>Frigate and Home Assistant each have an automated check that can fail, Frigate's MQTT coupling is proven by a live publication rather than by configuration, and the complete pre-bump `make smoketests-ser8` baseline is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → ser8 over SSH | Every smoketest runs remote commands as the deploy.yaml user |
| unprivileged service user → AMD render node | The kernel bump can change device permissions or driver ABI while leaving the device file present |
| Frigate → mosquitto → Home Assistant | The detection pipeline crosses two services and a broker whose generated auth configuration changes in this release |
| smoketest output → deployment decision | A test that cannot fail turns the activation gate into theatre |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Repudiation | A smoketest that always exits 0 certifying an activation that was never verified | high | mitigate | Recorded as a plan prohibition; every new script carries the pass/fail harness and summary block from the tailscale analog, acceptance criteria require an `exit 1` path in each, and each must be observed red under an injected fault |
| T-09-27 | Denial of Service | Hardware transcoding silently breaking for the service users after the kernel bump while the device file remains present | high | mitigate | Task 2 runs a real VAAPI encode as the jellyfin user and as the frigate user; a missing tool or device fails the check rather than skipping it |
| T-09-28 | Denial of Service | Frigate detections silently ceasing to reach Home Assistant after the mosquitto auth-configuration change | medium | mitigate | Task 3 requires a live Frigate-owned publication within a bounded timeout plus the retained availability message, rather than inferring the coupling from unit state |
| T-09-25 | Denial of Service | Proving a check can fail by stopping a live household service | medium | mitigate | Recorded as a plan prohibition; every red observation here is produced by a temporary `PATH` shim over `ssh` |
| T-09-11 | Spoofing | A hard-coded address silently testing the wrong machine | medium | mitigate | Recorded as a plan prohibition; every address resolves through `get_ip` against deploy.yaml, asserted by criteria that grep each new script for literal dotted-quad addresses |
| T-09-12 | Information Disclosure | Remote command construction injecting unescaped arguments over SSH | low | mitigate | Remote commands built from arrays use the `printf -v ... '%q '` escaping pattern the tailscale analog established |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No npm, PyPI, or crates packages are introduced. These are shell scripts using tools already present on the target host or resolved from a running unit's own store path. |
</threat_model>

<verification>
1. `make smoketests-ser8` exits 0 against ser8's pre-bump generation and its output shows the media, NordVPN, ZFS, hardware-acceleration, Frigate, and Home Assistant sections
2. `shellcheck -x` exits 0 and `shfmt -d` produces no diff for every file this plan creates
3. Each new script has been observed exiting non-zero under a `PATH` shim that injects the relevant fault, with the shim and the observed output recorded — a check that has never been seen red is not a gate, and no household service was stopped to obtain the red
4. The VAAPI encode succeeded as both the jellyfin user and the frigate user, and the encoder and driver strings are recorded for post-activation comparison
5. The Frigate MQTT assertion received a live statistics message within its bounded wait, and that message's arrival is recorded
6. The full pre-bump smoketest output is captured into the SUMMARY as the baseline plan 05 compares against
</verification>

<success_criteria>
- `make smoketests-ser8` reaches every ser8 area through one deploy.yaml entry with an exit status that reflects all of them
- Frigate and Home Assistant have automated checks, closing the gap that made Success Criterion 4 unsatisfiable
- ZFS health, hardware acceleration, and VPN confinement are covered before the kernel changes under them
- Hardware acceleration is proven by a real encode under the service users, and MQTT by a live publication
- Every new check has been observed failing under injected fault, without stopping a live service
</success_criteria>

<output>
Create `.planning/phases/09-channel-bump-to-nixos-26-05/09-03-SUMMARY.md` when done. Record the complete pre-bump `make smoketests-ser8` output as the baseline, the observed red result and injection method for each new check, the VAAPI encoder and driver strings, and the Frigate MQTT topic and payload received.
</output>
