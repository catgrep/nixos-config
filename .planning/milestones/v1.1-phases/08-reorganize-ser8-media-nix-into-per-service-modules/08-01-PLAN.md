---
phase: 08-reorganize-ser8-media-nix-into-per-service-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/ser8-media-projection.nix
  - scripts/validation/check-ser8-media-parity.sh
  - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json
  - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt
  - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-store-path-before.txt
  - hosts/ser8/media/default.nix
  - hosts/ser8/configuration.nix
  - hosts/ser8/media.nix
autonomous: false
requirements:
  - N/A
must_haves:
  truths:
    - "The pre-refactor ser8 media behavior is captured without plaintext secret values."
    - "Repository-owned evaluation warnings block the refactor instead of being hidden or changed out of scope."
    - "ser8 can enter the media directory through a behavior-neutral extraction seam."
  artifacts:
    - path: scripts/validation/ser8-media-projection.nix
      provides: "Serializable non-secret projection of the ser8 media behavior contract"
    - path: scripts/validation/check-ser8-media-parity.sh
      provides: "Capture, compare, and structural-check CLI for Phase 8"
    - path: .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json
      provides: "Durable before-refactor projection fixture"
    - path: .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-store-path-before.txt
      provides: "Pre-refactor ser8 system store-path reference for final comparison"
    - path: hosts/ser8/media/default.nix
      provides: "Evaluable directory entry point used throughout atomic extraction"
  key_links:
    - from: scripts/validation/check-ser8-media-parity.sh
      to: scripts/validation/ser8-media-projection.nix
      via: "nix eval of nixosConfigurations.ser8.config"
      pattern: "ser8-media-projection.nix"
    - from: hosts/ser8/configuration.nix
      to: hosts/ser8/media/default.nix
      via: "imports entry"
      pattern: "\\./media"
    - from: hosts/ser8/media.nix
      to: hosts/ser8/systemd_helpers.sh
      via: "lib.mkMerge script prologue at mkOrder 100 before all configure_arr fragments"
      pattern: "mkOrder 100"
---

<objective>
Establish the non-secret behavior baseline and an atomic directory-import seam before any service moves.

Purpose: Make every later extraction mechanically comparable per D-15 and D-16, while failing closed on the known Home Manager release warning instead of changing dependencies outside this behavior-preserving phase.
Output: Projection tooling, durable baseline artifacts, and an evaluable `hosts/ser8/media/` entry point.
</objective>

<execution_context>
@/Users/bobby/.codex/gsd-core/workflows/execute-plan.md
@/Users/bobby/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_environment>
Enter the repository development shell with `make dev` before task actions and keep all automated verification inside that shell.
</execution_environment>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-CONTEXT.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-RESEARCH.md
@.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-VALIDATION.md
@hosts/ser8/media.nix
@hosts/ser8/configuration.nix
@modules/media/jellyfin.nix
@modules/media/exportarr.nix
</context>

## Artifacts this phase produces

- `scripts/validation/ser8-media-projection.nix` with a projection function from evaluated ser8 config to JSON-safe service, SOPS metadata, template, identity, exporter, nginx, unit, generated-script, account, firewall, and target contracts.
- `scripts/validation/check-ser8-media-parity.sh` with `capture BASELINE`, `check BASELINE`, and `structure` commands.
- `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json` containing the non-secret pre-refactor contract.
- `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt` containing classified baseline warning text without credentials.
- `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-store-path-before.txt` containing the evaluated pre-refactor ser8 store path.
- `hosts/ser8/media/default.nix` as the directory module entry point.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Clear the external Home Manager release mismatch prerequisite</name>
  <files>.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt</files>
  <read_first>
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt
    - flake.nix
    - flake.lock
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-VALIDATION.md
  </read_first>
  <action>
Before creating or changing any Phase 8 implementation artifact, evaluate the current ser8 configuration inside the repository development shell and capture stderr in `08-warning-baseline.txt`. If the Home Manager 25.05 versus nixpkgs 25.11 mismatch appears, stop and ask the user to resolve that dependency mismatch outside Phase 8. Do not suppress the warning, disable Home Manager release checks, edit dependency pins, or begin the refactor. Resume only after the user confirms the external prerequisite is resolved and the same evaluation emits no repository-owned warning. Environment-only unsupported local-setting notices may be recorded but must be explicitly classified by exact line.
  </action>
  <verify>
    <automated>nix develop --command bash -lc 'set -euo pipefail; warning_file=.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt; nix eval .#nixosConfigurations.ser8.config.system.build.toplevel 2&gt;"$warning_file" &gt;/dev/null; ! rg -n "Home Manager.*25\\.05.*25\\.11|Home Manager version.*Nixpkgs" "$warning_file"'</automated>
    <human-check>Confirm the mismatch was resolved outside Phase 8 and approve continuation after the clean automated rerun.</human-check>
  </verify>
  <acceptance_criteria>
    - The preflight runs before any Phase 8 implementation edit.
    - The Home Manager release mismatch is absent from captured stderr.
    - No dependency pin or warning-suppression setting is changed by this phase.
    - The user explicitly approves continuation after resolving the external prerequisite.
  </acceptance_criteria>
  <done>The external zero-warning prerequisite is resolved and Phase 8 may begin.</done>
</task>

<task type="auto">
  <name>Task 2: Build the non-secret parity projection and capture the baseline</name>
  <files>scripts/validation/ser8-media-projection.nix, scripts/validation/check-ser8-media-parity.sh, .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json, .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt, .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-store-path-before.txt</files>
  <read_first>
    - scripts/validation/ser8-media-projection.nix
    - scripts/validation/check-ser8-media-parity.sh
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-warning-baseline.txt
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-store-path-before.txt
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-RESEARCH.md
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-VALIDATION.md
    - hosts/ser8/media.nix
    - hosts/ser8/configuration.nix
    - hosts/ser8/systemd_helpers.sh
    - modules/media/jellyfin.nix
    - modules/media/jellyfin-exporter.nix
    - modules/media/exportarr.nix
    - scripts/smoketests/nordvpn/all.sh
  </read_first>
  <action>
Create the projection and fail-fast runner before modifying evaluated media configuration, per D-15 and D-16. Project service enablement and settings for Jellyfin, Sonarr, Radarr, Prowlarr, qBittorrent, NZBGet, and SABnzbd; all media SOPS declaration names with owner/group/mode/path metadata; all rendered-template content and ownership; household Jellyfin users; Exportarr and Jellyfin exporter settings; qBittorrent nginx wiring; generic service accounts and firewall ports; all fields of `media-config`, `servarrs-setup`, `download-clients-setup`, and `media-setup.target`; and generated scripts. Never project decrypted values, secret file contents, or encrypted YAML payloads. Implement `capture BASELINE`, `check BASELINE`, `structure`, and `run-clean COMMAND...` subcommands with `set -euo pipefail`, actionable JSON diffs, explicit expected-delta assertions for the later Sawnia user addition and AllDebrid removals, and normalization limited to approved helper store-path changes. `run-clean` must capture stdout and stderr, preserve the wrapped command's exit code, permit only exact environment-owned notices recorded by the cleared preflight, and fail on any warning, the Home Manager mismatch, or new repository-owned stderr. Capture the current dirty-worktree baseline, including the existing `jellyfin_sawnia_password` metadata declaration, and record the evaluated pre-refactor `config.system.build.toplevel` store path in `08-ser8-store-path-before.txt`.
  </action>
  <verify>
    <automated>shellcheck scripts/validation/check-ser8-media-parity.sh &amp;&amp; shfmt -d scripts/validation/check-ser8-media-parity.sh &amp;&amp; ! scripts/validation/check-ser8-media-parity.sh run-clean bash -lc 'exit 7' &amp;&amp; ! scripts/validation/check-ser8-media-parity.sh run-clean bash -lc 'echo "warning: synthetic repository warning" &gt;&amp;2' &amp;&amp; scripts/validation/check-ser8-media-parity.sh capture .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json</automated>
  </verify>
  <acceptance_criteria>
    - The fixture parses as JSON and contains every projection category listed in the action.
    - The fixture contains secret metadata and placeholders but no decrypted value field or plaintext credential content.
    - A same-tree capture versus check is equal after normalization.
    - The pre-refactor ser8 store path is recorded as a single non-secret path for final comparison.
    - `run-clean` preserves command failures and rejects the Home Manager mismatch plus every unclassified warning.
  </acceptance_criteria>
  <done>The current evaluated media contract is durably captured, self-comparison passes, and warning ownership is explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Order the aggregate script and introduce the directory import seam</name>
  <files>hosts/ser8/media/default.nix, hosts/ser8/configuration.nix, hosts/ser8/media.nix</files>
  <read_first>
    - hosts/ser8/media/default.nix
    - hosts/ser8/configuration.nix
    - hosts/ser8/media.nix
    - modules/automation/default.nix
    - scripts/validation/check-ser8-media-parity.sh
    - .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json
  </read_first>
  <action>
Before the first service extraction, rewrite the existing aggregate `media-config.script` as a `lib.mkMerge` list of native ordered definitions in the same `hosts/ser8/media.nix`: prologue and helper source at `lib.mkOrder 100`, Sonarr 200, Radarr 300, Prowlarr 400, NZBGet 500, SABnzbd 600, qBittorrent 700, and completion log 800. Preserve every command byte-for-byte except the module expression boundaries, and keep the existing `before` list unchanged. This guarantees `configure_arr` is defined before every service fragment during all intermediate plans. Create `hosts/ser8/media/default.nix` with the repository SPDX header and an imports-only module shape, import `../media.nix` as the aggregate extraction source, then change the ser8 import in `hosts/ser8/configuration.nix` from `./media.nix` to `./media`. Later plans move already-ordered definitions without changing their priorities, and Plan 05 removes the aggregate import after every active definition has an owner. Stage the new file before flake evaluation so the Git-backed flake includes it.
  </action>
  <verify>
    <automated>nixfmt --check hosts/ser8/media/default.nix hosts/ser8/configuration.nix hosts/ser8/media.nix &amp;&amp; scripts/validation/check-ser8-media-parity.sh check .planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-ser8-media-before.json &amp;&amp; scripts/validation/check-ser8-media-parity.sh run-clean make build-ser8</automated>
  </verify>
  <acceptance_criteria>
    - `hosts/ser8/configuration.nix` imports `./media` exactly once and no longer imports `./media.nix`.
    - `hosts/ser8/media/default.nix` contains only its module argument and imports declaration.
    - The normalized evaluated projection remains equal to the baseline.
    - The evaluated script order is prologue 100, services 200 through 700, and epilogue 800 before Plan 02 begins.
    - `make build-ser8` succeeds without new warnings.
  </acceptance_criteria>
  <done>The directory import path is active and behavior remains equal to the captured baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOPS-backed config to checked-in fixture | Runtime secret references are projected into a repository artifact. |
| Local Nix environment to validation result | Environment warnings can obscure repository-owned regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | projection and baseline fixture | high | mitigate | Select only SOPS names and metadata, never values; make fixture safety an automated gate. |
| T-08-02 | Tampering | normalization and expected deltas | high | mitigate | Normalize only helper store paths and assert Sawnia/AllDebrid deltas by exact attribute names. |
| T-08-03 | Repudiation | warning baseline | medium | mitigate | Persist classified stderr and fail on repository-owned warnings instead of suppressing them. |
| T-08-SC | Tampering | package installs | low | accept | No package-manager install occurs in this plan, so the supply-chain boundary is unchanged. |

## ASVS Level 1 Control Mapping

| ASVS family | Phase control | Verification evidence |
|-------------|---------------|-----------------------|
| V2 Authentication | Preserve hashed-password references, API-key runtime paths, and existing authentication settings. | Non-secret evaluated projection and exact-delta comparison. |
| V4 Access Control | Preserve users, groups, file modes, service hardening, firewall, and namespace boundaries. | Projection assertions plus focused formatter, linter, and build gates. |
| V5 Validation | Use typed NixOS options, structured JSON comparison, strict shell mode, and fail-fast command handling. | Parity runner, Nix evaluation, ShellCheck, statix, and shfmt. |
| V6 Stored Cryptography | Keep sops-nix placeholders and runtime secret paths; never serialize or decrypt secret values. | Fixture-safety checks and unchanged encrypted SOPS sources. |

**ASVS L1 execution gate:** Any critical or high-severity threat with disposition `mitigate` blocks task and plan completion until its cited automated verification passes. No unresolved high-severity threat may advance to the next wave.
</threat_model>

<verification>
Run the parity self-check, formatter, shell linters, structural check, and `make build-ser8`. Do not proceed to Wave 2 while the repository-owned Home Manager release warning remains unresolved outside Phase 8.
</verification>

<success_criteria>
- A non-secret baseline exists and self-comparison passes.
- The warning preflight fails closed on the known repository-owned mismatch.
- ser8 imports the media directory and evaluates to the same normalized media contract.
</success_criteria>

<output>
Create `.planning/phases/08-reorganize-ser8-media-nix-into-per-service-modules/08-01-SUMMARY.md` when done.
</output>
